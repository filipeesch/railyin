# No capability specifications required

This change is a pure implementation correctness fix (two-stage JSON parsing for double-encoded tool inputs in the Anthropic streaming path). It does not add or modify any externally-visible capability. No spec files are required.
