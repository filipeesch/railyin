diff --git a/dist/session.js b/dist/session.js
index 1993aca899468a5ab41170b8d26042e428868a7d..e76f378835decc2af0454962f240fa5a6b68413a 100644
--- a/dist/session.js
+++ b/dist/session.js
@@ -242,13 +242,16 @@
         );
       }
     } else if (event.type === "permission.requested") {
-      const { requestId, permissionRequest, resolvedByHook } = event.data;
+      const { requestId, permissionRequest, promptRequest, resolvedByHook } = event.data;
       if (resolvedByHook) {
         return;
       }
       if (this.permissionHandler) {
         void this._executePermissionAndRespond(requestId, permissionRequest);
       }
+      if (promptRequest && promptRequest.kind === "commands") {
+        void this.rpc.commands.handlePendingCommand({ requestId }).catch(() => {});
+      }
     } else if (event.type === "command.execute") {
       const { requestId, commandName, command, args } = event.data;
       void this._executeCommandAndRespond(requestId, commandName, command, args);
