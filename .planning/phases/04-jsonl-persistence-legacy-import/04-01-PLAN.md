---
phase: 04-jsonl-persistence-legacy-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bun/copilotkit/jsonl-store.ts
  - src/bun/copilotkit/jsonl-store.test.ts
  - src/shared/rpc-types.ts
  - src/bun/handlers/threads.ts
  - src/bun/index.ts
  - src/bun/test/threads-handlers.test.ts
  - e2e/api/copilotkit/threads.test.ts
autonomous: true
requirements:
  - CHAT-08
estimate:
  tokens: 31000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "threads.list returns EVERY JSONL thread — card conversations (kind 'card', name from tasks.title) and standalone sessions (kind 'session', name from chat_sessions.title) — with createdAt/updatedAt; an empty threads dir returns [] (CHAT-08, D-01/D-03)"
    - "The index rebuilds from the log: JsonlStore.list() scans data/threads/ (THREAD_ID_RE-filtered, sorted by mtime desc) with no separate index file; .tmp / .meta.json / non-numeric decoys are skipped, never thrown; missing dir → [] (D-04/D-05)"
    - "threads.list is a RailynAPI RPC method (shared contract + handler + registration), not a raw route — the Phase 5 thread-list UI consumes it (D-02)"
    - "On the real wire, a session created via chatSessions.create + a card created via boards.list/tasks.create, each run once, both appear in threads.list with the correct kind and name after a fresh server restart (index from disk, not memory — Pitfall 3)"
    - "No threads.list query touches conversations.created_at — the table has NO such column (Pitfall 2); timestamps come from tasks.created_at / chat_sessions.created_at / chat_sessions.last_activity_at with file mtime/birthtime fallback; orphan JSONL files (no DB row) still list with kind 'session', name null"
  artifacts:
    - src/shared/rpc-types.ts — ThreadSummary interface + "threads.list" RailynAPI entry
    - src/bun/copilotkit/jsonl-store.ts — public list(): Array<{threadId, mtimeMs, size}> scan
    - src/bun/handlers/threads.ts — threadHandlers(db, store) factory
    - src/bun/index.ts — ...threadHandlers(db, jsonlStore) in allHandlers
    - src/bun/copilotkit/jsonl-store.test.ts — list() unit cases (scan/filter/sort/missing dir/.tmp exclusion/corrupt-entry tolerance)
    - src/bun/test/threads-handlers.test.ts — name/kind/timestamp derivation + orphan-file + empty-store cases
    - e2e/api/copilotkit/threads.test.ts — real-wire listing of session + card threads
  key_links:
    - "store.list() filters readdir entries through THREAD_ID_RE before any path use — the V8 path-traversal gate for the new scan (assertThreadId applies to the other methods; list() never resolves raw filenames)"
    - "The handler joins tasks/chat_sessions keyed on conversations.id — one row per conversation; kind = task_id != null ? 'card' : 'session'"
    - "threads.list returning files from DISK (not runner.listThreads() memory) is the authoritative-index property (D-01, Pitfall 3) — the e2e must assert listing after a FRESH server over a durable dataDir"
    - "conversations.created_at does not exist — any query referencing it fails at runtime (Pitfall 2); the e2e exercises the full join so a bad column can't slip past typecheck"
---

<objective>
**Capability: user-accessible thread index.** `threads.list` — Railyin's own thread-index RPC (CHAT-08, D-01/D-02) — lists every JSONL thread (card conversations AND standalone sessions) with name/kind/timestamps, derived by scanning `data/threads/` (the log IS the index — D-04/D-05) and enriching from the live DB (tasks.title / chat_sessions.title, timestamps from DB → file mtime fallback; `conversations` has NO `created_at` — Pitfall 2). The tracer task wires the thinnest end-to-end slice: store scan → shared contract → handler → composition root → real-wire e2e proving listing works from DISK after a fresh process (the runtime's own `GET /threads` is in-memory-only and returns [] after restart — Pitfall 3; Railyin's RPC is authoritative).

Purpose: deliver ROADMAP success criterion 1 — "User can list and open every thread (card conversations and standalone sessions) through Railyin's own thread-index endpoint" — and CHAT-08.

Output: the `ThreadSummary` contract, `JsonlStore.list()`, the `threads.list` handler + registration, and their unit + real-wire test layer.

*MVP deviation (accepted, Phases 2/3 precedent): the ROADMAP goal is capability-shaped, not a user story; no Vue UI in this phase (deferred to Phase 5).*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jsonl-persistence-legacy-import/04-CONTEXT.md
@.planning/phases/04-jsonl-persistence-legacy-import/04-RESEARCH.md
@.planning/phases/04-jsonl-persistence-legacy-import/04-PATTERNS.md
@src/bun/copilotkit/jsonl-store.ts
@src/bun/copilotkit/jsonl-store.test.ts
@src/bun/handlers/conversations.ts
@src/bun/handlers/chat-sessions.ts
@src/shared/rpc-types.ts
@src/bun/index.ts
@src/bun/db/row-types.ts
@src/bun/test/helpers.ts
@e2e/api/copilotkit/railyin.test.ts
@e2e/api/fixtures/server.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: threads.list end-to-end slice — store scan, contract, handler, registration, real-wire e2e</name>
  <files>
    src/bun/copilotkit/jsonl-store.ts,
    src/shared/rpc-types.ts,
    src/bun/handlers/threads.ts,
    src/bun/index.ts,
    e2e/api/copilotkit/threads.test.ts
  </files>
  <read_first>
    - src/bun/copilotkit/jsonl-store.ts — the class shape: private dataDir (line 31), THREAD_ID_RE (line 23), threadLogPath (lines 26-28), assertThreadId (lines 38-47), the tolerant-read precedent (lines 63-78, skip-don't-crash), the existing "fs" import line (line 18: appendFileSync, existsSync, mkdirSync, readFileSync)
    - src/shared/rpc-types.ts — RailynAPI map entries around line 1036 ("chatSessions.list" is the exact entry format to mirror: params/response object literal), and the response-interface block near MessageType (line 95)
    - src/bun/handlers/conversations.ts — the handler factory pattern (lines 13-14: `export function conversationHandlers(db: Database, ...)` returning an object of "method.name" keys) and the LEFT JOIN enrichment pattern (lines 60-74)
    - src/bun/index.ts — composition root: `const jsonlStore = new JsonlStore(getDataDir())` (line 292), the allHandlers object literal (lines 317-343, esp. chatSessionHandlers at line 339), the RPC router (lines 414-437 — unknown method → 404, handler throw → 500, NO router change needed)
    - e2e/api/copilotkit/railyin.test.ts — the e2e scaffolding to reuse verbatim: startServer({mcpConfig:{}}) (lines 21-25), postJson/parseSseFrames/runInput (lines 34-70), the JSONL-on-disk assertion pattern (test 7), and the typed server.request helper (fixtures/server.ts lines 284-299)
    - 04-RESEARCH.md — §Pattern 1 (file-scan index contract, lines 167-184), §Code Examples (thread listing scan + DB-enriched metadata, lines 286-324), Pitfall 2 (no conversations.created_at), Pitfall 3 (runtime GET /threads in-memory-only)
    - 04-PATTERNS.md — the jsonl-store.ts modification spec (lines 24-61) and the handler/contract/registration analogs (lines 109-174)
  </read_first>
  <behavior>
    - Test 1 (store): a fresh JsonlStore over a temp dir with 3 thread files (2 valid numeric ids, 1 with different mtimes) → list() returns exactly the 2 valid entries, each with the right mtimeMs and size, sorted mtime desc
    - Test 2 (store): decoys — write `7.jsonl.tmp`, `8.meta.json`, `abc.jsonl` beside valid files → list() skips all three without throwing (skip-don't-crash)
    - Test 3 (store): missing threads dir → list() returns []
    - Test 4 (handler): seeded DB with a task (seedProjectAndTask → tasks.title) + a chat session (seedChatSession → chat_sessions.title), thread files for both conversation ids → threadHandlers["threads.list"]() returns both with kind "card"/"session" and the DB titles as name
    - Test 5 (handler): an orphan JSONL file with NO conversations row → still listed, kind "session", name null, mtime-derived timestamps
    - Test 6 (e2e): real server — chatSessions.create + one run → threads.list contains the thread (kind "session"); boards.list → tasks.create + one run → threads.list contains it (kind "card", name = task title); fresh server over the same durable dataDir → the same listing comes back from disk
  </behavior>
  <action>
    1. `src/bun/copilotkit/jsonl-store.ts` — add `readdirSync` and `statSync` to the existing `"fs"` import line. Add a public method `list(): Array<{ threadId: string; mtimeMs: number; size: number }>` per research Pattern 1: resolve the threads dir via join(this.dataDir, "threads"); if the dir is absent, return []; readdirSync it and filter entries that end with ".jsonl" AND whose basename minus the suffix matches THREAD_ID_RE (test name.slice(0, -6)); map survivors via statSync → {threadId, mtimeMs, size}; sort by mtimeMs descending. NEVER interpolate a raw filename into a path (the regex filter runs BEFORE any path use — V8 gate); decoy/odd entries are filtered, never thrown at. Update the file-header doc comment (lines 15-16) to note Phase 4 scope: index rebuild from the log via list().
    2. `src/shared/rpc-types.ts` — add an exported `ThreadSummary` interface (threadId: string; name: string | null; kind: "card" | "session"; createdAt: string; updatedAt: string — ISO strings) in the response-interfaces area near MessageType (line 95). Add the `"threads.list"` entry to the RailynAPI map (next to the chatSessions entries around line 1036) with `params: Record<string, never>` and `response: ThreadSummary[]` — exact format mirror of chatSessions.list.
    3. `src/bun/handlers/threads.ts` (NEW) — export `function threadHandlers(db: Database, store: JsonlStore)` returning `{ "threads.list": async (): Promise<ThreadSummary[]> }` per research §Code Examples: `store.list()` first (empty → return []); then one parameterized join query `SELECT c.id, c.task_id, t.title AS task_title, t.created_at AS task_created, cs.title AS session_title, cs.created_at AS session_created, cs.last_activity_at AS session_activity FROM conversations c LEFT JOIN tasks t ON t.conversation_id = c.id LEFT JOIN chat_sessions cs ON cs.conversation_id = c.id` (all().all()) into a Map keyed by String(id); map each file: kind = row?.task_id != null ? "card" : "session"; name = kind === "card" ? row?.task_title ?? null : (row?.session_title ?? null); createdAt = row?.task_created ?? row?.session_created ?? new Date(f.birthtimeMs ?? f.mtimeMs).toISOString(); updatedAt = row?.session_activity ?? new Date(f.mtimeMs).toISOString(). NEVER reference conversations.created_at (Pitfall 2). Import ThreadSummary from "../../shared/rpc-types.ts" and JsonlStore from "../copilotkit/jsonl-store.ts".
    4. `src/bun/index.ts` — import threadHandlers from "./handlers/threads.ts"; add `...threadHandlers(db, jsonlStore),` to the allHandlers literal (after the chatSessionHandlers line, ~line 339). The router needs no change.
    5. `e2e/api/copilotkit/threads.test.ts` (NEW) — copy the railyin.test.ts scaffolding (startServer, postJson, parseSseFrames, runInput). Suite A: startServer({mcpConfig:{}}): chatSessions.create({title}) → run a text turn on `String(session.conversationId)` → server.request("threads.list", {}) contains an entry with threadId === String(session.conversationId), kind "session", name === title; then boards.list({}) → tasks.create({boardId, projectKey, title, description}) → run on `String(task.conversationId)` → threads.list contains it with kind "card" and name === the task title. Suite B (restart proof, Pitfall 3): mkdtempSync durable dataDir + startServer({dataDir, durableDb: true}) → create session + run → shutdown → startServer again over the SAME dataDir → threads.list still returns the thread (index from disk). Clean up the durable dir in afterAll (caller owns cleanup per the fixture contract).
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/threads.test.ts --timeout 30000 && bun test src/bun/copilotkit/jsonl-store.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test e2e/api/copilotkit/threads.test.ts --timeout 30000` passes: session thread + card thread listed with correct kind/name; fresh-server listing returns the same threads from disk (no watch-mode flags)
    - `bun test src/bun/copilotkit/jsonl-store.test.ts` passes — existing 5 tests unchanged and green
    - `bun run typecheck` — 0 errors (ThreadSummary/threads.list wired through rpc-types, handler, index)
    - No query in threads.ts references `conversations.created_at`
  </acceptance_criteria>
  <done>The threads.list RPC is live end-to-end: the store scans the log dir (decoys skipped), the contract exposes ThreadSummary[], the handler enriches name/kind/timestamps from the DB join (no conversations.created_at), the composition root registers it, and a real server lists session + card threads from disk across a restart.</done>
  <reversibility rating="reversible">Additive RPC method + store method + handler file; removing them restores the prior state (no schema change, no file mutation).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit layer — list() scan cases, crash-tolerance store scenarios, handler enrichment tests</name>
  <files>
    src/bun/copilotkit/jsonl-store.test.ts,
    src/bun/test/threads-handlers.test.ts
  </files>
  <read_first>
    - src/bun/copilotkit/jsonl-store.test.ts — existing structure to extend: makeTempDir (lines 16-19), beforeEach/afterEach (lines 28-35), the traversal-rejection test 3 (lines 63-73) for the assertion style, test 4 tolerant-read (lines 75-87) for the crash-tolerance scenario pattern; writeFileSync is already imported (line 9)
    - src/bun/test/helpers.ts — initDb (lines 10-15), makeTempDir (lines 265-268), seedProjectAndTask (lines 272-293, creates conversations + tasks with title 'Test task'), seedChatSession (lines 409-434, creates a conversation + chat_sessions row with a title), setupTestConfig (lines 311-397, if the handler path needs config env)
    - src/bun/handlers/threads.ts (just created in Task 1) — the exact method to unit-drive: threadHandlers(db, store)["threads.list"]()
    - 04-PATTERNS.md — handler test conventions (lines 193-200): direct factory invocation with initDb + makeTempDir, no HTTP
  </read_first>
  <behavior>
    - Test 1: list() scan — 2 valid files + decoys (.jsonl.tmp, .meta.json, abc.jsonl) → exactly the 2 valid entries returned, sorted mtime desc, correct mtimeMs/size values
    - Test 2: list() missing dir → [] (fresh temp dir with no threads/)
    - Test 3: list() with a corrupt entry name (e.g. "evil.jsonl" dir entry) → skipped, no throw
    - Test 4 (crash tolerance): a thread file with a partial trailing JSON line → list() still returns the thread (index from log) AND read() skips the partial line returning the complete ones (extends the existing test-4 pattern to the index path)
    - Test 5 (handler): seedProjectAndTask + seedChatSession + thread files → threads.list returns kind "card"/"session" with names from tasks.title / chat_sessions.title, createdAt from task_created/session_created, updatedAt from session_activity when present
    - Test 6 (handler): orphan file without a DB row → listed kind "session", name null, mtime-derived timestamps
    - Test 7 (handler): empty store → []
  </behavior>
  <action>
    1. `src/bun/copilotkit/jsonl-store.test.ts` — add a `describe("list()")` block: scan/filter/sort cases (write real files via writeFileSync with explicit mtimes — write then set mtime via utimesSync, or write in sequence and assert relative order), the missing-dir case, a corrupt-entry decoy case, and the crash-tolerance case (write a valid line + a partial trailing line, assert list() still lists the thread id and read() returns only the complete line). Keep the existing tests untouched.
    2. `src/bun/test/threads-handlers.test.ts` (NEW) — following the handlers.test.ts conventions (bun:test, initDb, setupTestConfig for config-dependent paths, makeTempDir for the store): import initDb, makeTempDir, seedProjectAndTask, seedChatSession from helpers; import threadHandlers; import JsonlStore + threadLogPath. For each test: fresh initDb + fresh temp JsonlStore; write thread files via writeFileSync(threadLogPath(tmp.dir, String(convId)), "..." ) for the seeded conversation ids; call `const handlers = threadHandlers(db, new JsonlStore(tmp.dir)); const threads = await handlers["threads.list"]();`. Assert kind/name/timestamps per behaviors 5-7 (seedChatSession sets a title — assert it flows through; check helpers for its exact title string and last_activity_at default). Clean up the temp dir in afterEach.
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/jsonl-store.test.ts && bun test src/bun/test/threads-handlers.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/bun/copilotkit/jsonl-store.test.ts` — all existing + new list() tests pass (no watch-mode flags)
    - `bun test src/bun/test/threads-handlers.test.ts` — all enrichment/orphan/empty cases pass
    - `bun run typecheck` — 0 errors
    - The crash-tolerance case proves both halves of criterion 5 at the unit level: a partial trailing line does not hide the thread from the index (list) and does not break the read
  </acceptance_criteria>
  <done>The threads.list capability has a full unit-test layer: scan tolerance, index-from-log under corruption, and DB-enriched metadata (name/kind/timestamps) with orphan-file and empty-store behavior pinned.</done>
  <reversibility rating="reversible">Test-only additions.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/threads.list | untrusted request enters the RPC router; handler reads the local threads dir + DB and returns metadata (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | JsonlStore.list() / threads.ts | high | mitigate | readdir entries are regex-filtered (THREAD_ID_RE) BEFORE any path use; list() never resolves a raw filename (research Pattern 1; assertThreadId precedent jsonl-store.ts:38-47); decoys skipped, never string-interpolated |
| T-04-02 | Spoofing | POST /api/threads.list cross-origin | low | accept | Read-only listing of local thread metadata (names/timestamps); no mutation, no secrets, no engine execution; matches the existing app-wide RPC posture (research A7) |
| T-04-03 | Tampering | threads.ts DB join | medium | mitigate | Parameterized query with zero interpolated user input — threadIds come from the validated file scan, join keys are fixed columns; no string-built SQL |
| T-04-04 | DoS | corrupt dir entries | low | mitigate | Skip-don't-crash discipline on the scan (research Pattern 1); statSync failures are per-entry (a single broken file cannot 500 the whole listing — wrap per-entry stat in the tolerant pattern if needed) |

No package installs this phase — RESEARCH.md §Package Legitimacy Audit: no `[ASSUMED]`/`[SUS]` names; T-04-SC (supply chain) not applicable.
</threat_model>

<verification>
- `bun test src/bun/copilotkit/jsonl-store.test.ts` — list() scan/tolerance cases + existing 5 tests
- `bun test src/bun/test/threads-handlers.test.ts` — enrichment/orphan/empty handler cases
- `bun test e2e/api/copilotkit/threads.test.ts --timeout 30000` — real-wire session + card listing + restart-from-disk proof
- `bun run typecheck` — clean
</verification>

<success_criteria>
- ROADMAP SC1 met for the listing half: user can list every thread (card + session) through Railyin's own thread-index endpoint (CHAT-08)
- D-01/D-02: RailynAPI RPC method, contract + handler + registration complete; D-03 metadata minimal (DB join, no sidecar)
- D-04/D-05: index rebuilds from the log (list() scan, no separate index file), proven under a partial-line corruption
- The runtime's in-memory GET /threads is NOT the data source (Pitfall 3) — the e2e restart case proves disk-backed listing
</success_criteria>

## Artifacts this plan produces

- **src/bun/copilotkit/jsonl-store.ts** — `list()`: THREAD_ID_RE-filtered readdir scan → `{threadId, mtimeMs, size}[]` sorted mtime desc, missing-dir → [], decoy-tolerant (log IS the index, D-04/D-05)
- **src/shared/rpc-types.ts** — `ThreadSummary` interface + `"threads.list": { params: Record<string, never>; response: ThreadSummary[] }` RailynAPI entry (D-02)
- **src/bun/handlers/threads.ts** — `threadHandlers(db, store)` factory: store scan + parameterized LEFT JOIN (tasks/chat_sessions) → kind/name/createdAt/updatedAt; never queries conversations.created_at (Pitfall 2)
- **src/bun/index.ts** — `...threadHandlers(db, jsonlStore)` registered in allHandlers
- **src/bun/copilotkit/jsonl-store.test.ts** — list() unit block (scan/filter/sort/missing-dir/decoys/partial-line tolerance)
- **src/bun/test/threads-handlers.test.ts** — handler enrichment tests (card/session/orphan/empty)
- **e2e/api/copilotkit/threads.test.ts** — real-wire listing (session + card) + fresh-server restart listing over a durable dataDir
- **Env/config** — none new; **Dependencies** — none new

<output>
Create `.planning/phases/04-jsonl-persistence-legacy-import/04-01-SUMMARY.md` when done.
</output>
