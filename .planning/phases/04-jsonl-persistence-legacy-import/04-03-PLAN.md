---
phase: 04-jsonl-persistence-legacy-import
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - e2e/api/copilotkit/legacy-import.test.ts
  - .planning/phases/04-jsonl-persistence-legacy-import/04-COVERAGE.md
  - .planning/phases/04-jsonl-persistence-legacy-import/04-VALIDATION.md
autonomous: true
requirements:
  - CHAT-08
  - IMPR-01
  - IMPR-02
estimate:
  tokens: 19000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Interrupted/corrupted JSONL writes never lose a thread: a thread file with a partial trailing line still lists through threads.list AND replays its complete lines on connect; a leftover *.jsonl.tmp is invisible to list()/exists() and to a re-import — the log IS the index and tolerant reading is the floor (criterion 5, D-04/D-05)"
    - "A full-phase regression proves both capabilities coexist on the real stack: threads.list + legacyImport.run + runner persistence all green in one suite run"
    - "04-COVERAGE.md records the API-coverage decision: detector output {'detected':false,'signals':[]} — no external API integration (per the plan:pre checkpoint contribution)"
    - "04-VALIDATION.md is signed off: per-task verification map green, Wave 0 checklist complete, nyquist_compliant: true"
  artifacts:
    - e2e/api/copilotkit/legacy-import.test.ts — crash-tolerance e2e (partial-line thread still lists + replays; .tmp residue ignored; re-import safety)
    - .planning/phases/04-jsonl-persistence-legacy-import/04-COVERAGE.md — no-external-API decision record
    - .planning/phases/04-jsonl-persistence-legacy-import/04-VALIDATION.md — completed verification map + sign-off
  key_links:
    - "The tolerant reader (JsonlStore.read skip-malformed-lines, jsonl-store.ts:63-78) + the THREAD_ID_RE-filtered list() are the two halves of 'index rebuilds from the log' — the e2e corrupts a real thread file and proves both at once"
    - "The atomic importLog tmp+rename (04-02) is what keeps the D-07 existence marker honest under crashes — the .tmp-residue e2e case pins that the import path cannot be fooled by its own crash artifacts"
    - "COVERAGE.md mirrors the 02-COVERAGE.md structure (detector record, decision, rationale table)"
---

<objective>
**Capability: crash-tolerance proof and phase seal.** The final plan proves criterion 5 over the real wire — interrupted/corrupted JSONL writes never lose a thread (a partial trailing line still lists and replays, `.tmp` crash artifacts stay invisible, a re-import stays safe) — then closes the phase: full-suite regression green, 04-COVERAGE.md recording the no-external-API decision (per the plan:pre API Coverage Decision Checkpoint; detector output `{"detected":false,"signals":[]}`), and 04-VALIDATION.md signed off.

Purpose: ROADMAP success criterion 5 (crash tolerance) + phase verification contract completion. CHAT-08/IMPR-01/IMPR-02 regression at phase level.

Output: the corruption-resilience e2e cases and the two phase close-out records.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jsonl-persistence-legacy-import/04-CONTEXT.md
@.planning/phases/04-jsonl-persistence-legacy-import/04-RESEARCH.md
@.planning/phases/04-jsonl-persistence-legacy-import/04-PATTERNS.md
@.planning/phases/04-jsonl-persistence-legacy-import/04-VALIDATION.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-VALIDATION.md
@src/bun/copilotkit/jsonl-store.ts
@src/bun/copilotkit/jsonl-store.test.ts
@e2e/api/copilotkit/legacy-import.test.ts
@e2e/api/copilotkit/threads.test.ts
@e2e/api/copilotkit/railyin.test.ts
@e2e/api/fixtures/server.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crash-tolerance e2e — corrupted log still lists + replays, .tmp residue invisible, re-import safe</name>
  <files>
    e2e/api/copilotkit/legacy-import.test.ts
  </files>
  <read_first>
    - 04-RESEARCH.md — Pitfall 5 (partial-append existence lies), §Pattern 1 (list() filter excludes .tmp), Assumption A1 (appendFileSync atomicity claim), Pitfall 6 (replay validation)
    - src/bun/copilotkit/jsonl-store.ts — the tolerant reader (lines 63-78), list() filter (from 04-01), exists() (lines 80-83)
    - e2e/api/copilotkit/legacy-import.test.ts (from 04-02) — the durable dataDir fixture + seeded-import flow to extend; postJson/parseSseFrames helpers
    - e2e/api/copilotkit/railyin.test.ts — the connect-with-history frame assertion pattern (cold connect replay)
    - src/bun/copilotkit/railyin-runner.ts — the cold-replay pipeline (lines 149-190): what a corrupted-tail connect must still satisfy
  </read_first>
  <behavior>
    - Test A (partial trailing line — list + replay): server A imports a seeded thread over a durable dataDir; corrupt the file by appending a partial JSON line (simulating an interrupted runner append, A1); threads.list still returns the thread; a fresh server B over the same dataDir connects → replays the COMPLETE lines (RUN_STARTED first, RUN_FINISHED last), skipping the partial line (tolerant reader)
    - Test B (.tmp residue invisible): write a decoy `{id}.jsonl.tmp` into the threads dir; threads.list skips it; store.exists semantics unchanged — a re-import of an already-imported thread still reports skipped (the final file, not the .tmp, is the marker)
    - Test C (re-import after crash artifact): after the simulated crash (Test A's partial tail), re-running legacyImport.run skips the thread (imported 0) — the crash artifact did not corrupt the marker semantics
  </behavior>
  <action>
    1. `e2e/api/copilotkit/legacy-import.test.ts` — add a "crash tolerance (criterion 5)" describe block reusing the existing durable-dataDir fixture. Test A: after the seed+import flow from 04-02 Task 1, append `'{"type":"RUN_STARTED","threadId":'` (a truncated JSON line) to the thread's .jsonl via appendFileSync; assert threads.list still contains the thread; shutdown; startServer B over the same dataDir → postJson `/api/copilotkit/agent/default/connect` with the imported threadId → parseSseFrames → assert first frame RUN_STARTED and last frame RUN_FINISHED and NO parse errors from the partial tail (the tolerant reader skipped it). Test B: writeFileSync `{id}.jsonl.tmp` decoy next to the thread file; assert threads.list omits it and the imported thread entry is unchanged. Test C: server.request("legacyImport.run", {}) again → {imported: 0, skipped: 1}. Use appendFileSync/writeFileSync from node:fs; cleanup via shutdown + rmSync in afterAll.
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/legacy-import.test.ts --timeout 30000</automated>
  </verify>
  <acceptance_criteria>
    - Tests A/B/C pass in `bun test e2e/api/copilotkit/legacy-import.test.ts --timeout 30000` (no `-x` flag)
    - Test A proves both halves of criterion 5 at once: the corrupted thread still LISTED (index from log) and still REPLAYED (tolerant read) across a restart
    - Test C proves the crash artifact cannot fool the D-07 marker — re-import stays skipped
  </acceptance_criteria>
  <done>Criterion 5 is proven on the real wire: interrupted/corrupted writes never lose a thread — the index rebuilds from the log, the reader tolerates the partial tail, .tmp crash artifacts are invisible, and the idempotency marker stays honest.</done>
  <reversibility rating="reversible">Test-only additions.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full-suite regression, 04-COVERAGE.md, 04-VALIDATION.md sign-off</name>
  <files>
    .planning/phases/04-jsonl-persistence-legacy-import/04-COVERAGE.md,
    .planning/phases/04-jsonl-persistence-legacy-import/04-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md — the exact no-external-API decision-record structure to mirror (detector record, decision, rationale table, outcome)
    - .planning/phases/03-decision-interrupts-resume/03-COVERAGE.md — the Phase-3 example (detector output {"detected":false,"signals":[]}, decision paragraph, rationale table)
    - .planning/phases/04-jsonl-persistence-legacy-import/04-VALIDATION.md — the draft to complete (per-task verification map, Wave 0 checklist, sign-off, nyquist_compliant frontmatter)
    - .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-VALIDATION.md — the completed example (nyquist_compliant: true, wave_0_complete: true)
  </read_first>
  <behavior>
    - Full-suite gate: `bun test src/bun --timeout 20000` + `bun test e2e/api --timeout 30000` + `bun run typecheck` all green in one pass
    - 04-COVERAGE.md exists with the no-external-API decision (detector record + rationale table; no external API integration — bun built-ins + pinned deps only)
    - 04-VALIDATION.md complete: per-task verification map across 04-01..04-03 all green with test counts, Wave 0 checklist fully checked (import.test.ts + list() cases + handler tests + e2e legacy-import created this phase), assumption-delta advisory noted if any assumption fired, nyquist_compliant: true, sign-off approved
  </behavior>
  <action>
    1. Run the full-suite gate command (behavior 1) and record the exact pass counts for the SUMMARY.
    2. Create `04-COVERAGE.md` mirroring 03-COVERAGE.md: detector run record `api-coverage.cjs --json` over 04-RESEARCH.md + 04-CONTEXT.md → `{"detected":false,"signals":[]}`; the decision paragraph ("No external API integration: the phase scans local JSONL files via node:fs, reads frozen SQLite tables via bun:sqlite, and emits AG-UI events consumed by the already-installed pinned packages (@ag-ui/core@0.0.57, @copilotkit/runtime@1.66.4) — no network API, no remote service, no credentials; the only HTTP surface is the app's own loopback origin"); the rationale table (Network API / credentials / third-party endpoints / capability surface — each "none" with the in-process explanation); outcome line.
    3. Complete `04-VALIDATION.md`: fill the per-task verification map (all 5 tasks across 04-01..04-03 with their commands and pass counts), check the Wave 0 checklist (all four Wave 0 gaps from research — import.test.ts, jsonl-store list() cases, handler tests, e2e legacy-import — now exist as phase deliverables), record the open-question resolutions (Q1 → per-user-message grouping; Q2 → DB join only, no sidecar; Q3 → single-shot import with summary; Q4 → all threads returned in v1) and any assumption deltas, set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, approve the sign-off.
  </action>
  <verify>
    <automated>bun test src/bun --timeout 20000 && bun test e2e/api --timeout 30000 && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The full-suite command passes in one run (backend + e2e + typecheck green — record counts in the SUMMARY)
    - 04-COVERAGE.md exists with the detector record + no-external-API decision
    - 04-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`; the per-task map lists all 5 tasks with passing commands
  </acceptance_criteria>
  <done>The phase gate closes: the whole stack (threads.list, legacyImport.run, runner persistence) is green in one suite run, the API-coverage decision is recorded, and the validation strategy is signed off for /gsd-verify-work.</done>
  <reversibility rating="reversible">Documentation records — no production code touched.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh process → durable JSONL + SQLite | the crash-tolerance e2e feeds a corrupted log and .tmp artifacts through threads.list and cold connect — the file system is the untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Tampering | corrupted JSONL log feeding the index/replay | high | mitigate | Tolerant reader skips malformed lines (jsonl-store.ts:63-78); list() regex-filters entries; the e2e proves a partial tail neither hides the thread nor breaks replay (criterion 5) |
| T-04-12 | DoS | .tmp / decoy file accumulation | low | mitigate | list() filters .jsonl.tmp by construction (04-01); re-import skips via the final-file marker — crash artifacts cannot wedge the import (Test C) |
| T-04-13 | Spoofing | phase records misrepresenting coverage | low | mitigate | COVERAGE.md records the deterministic detector output verbatim; VALIDATION.md maps each task to its exact runnable command |

No package installs this phase — RESEARCH.md §Package Legitimacy Audit: no `[ASSUMED]`/`[SUS]` names; T-04-SC (supply chain) not applicable.
</threat_model>

<verification>
- `bun test src/bun --timeout 20000` — full backend suite (store, import, handlers, runner regression)
- `bun test e2e/api --timeout 30000` — full e2e suite (threads.test.ts, legacy-import.test.ts incl. crash tolerance, railyin.test.ts regression, probe)
- `bun run typecheck` — clean
</verification>

<success_criteria>
- ROADMAP SC5 proven: interrupted/corrupted JSONL writes never lose a thread — tolerant read + index rebuild from the log verified over the real wire (D-04/D-05)
- Phase gate green: full backend + e2e + typecheck in one pass
- 04-COVERAGE.md: no-external-API decision recorded per the plan:pre checkpoint
- 04-VALIDATION.md: signed off (nyquist_compliant: true, Wave 0 complete)
</success_criteria>

## Artifacts this plan produces

- **e2e/api/copilotkit/legacy-import.test.ts** — crash-tolerance describe block: partial-tail list+replay across restart (A), .tmp decoy invisibility (B), re-import safety after crash artifacts (C)
- **.planning/phases/04-jsonl-persistence-legacy-import/04-COVERAGE.md** — no-external-API decision record (detector `{"detected":false,"signals":[]}`, rationale table)
- **.planning/phases/04-jsonl-persistence-legacy-import/04-VALIDATION.md** — completed per-task verification map, Wave 0 checklist, open-question resolutions, `nyquist_compliant: true`, sign-off
- **Env/config** — none new; **Dependencies** — none new

<output>
Create `.planning/phases/04-jsonl-persistence-legacy-import/04-03-SUMMARY.md` when done.
</output>
