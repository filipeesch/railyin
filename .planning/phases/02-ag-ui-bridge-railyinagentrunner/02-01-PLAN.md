---
phase: 02-ag-ui-bridge-railyinagentrunner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bun/engine/coordinator.ts
  - src/bun/engine/orchestrator.ts
  - src/bun/engine/execution/chat-executor.ts
  - src/bun/engine/stream/stream-processor.ts
  - src/bun/test/execution-seam.test.ts
  - src/bun/copilotkit/event-bridge.ts
  - src/bun/copilotkit/event-bridge.test.ts
  - src/bun/copilotkit/railyin-agent.ts
  - src/bun/copilotkit/railyin-agent.test.ts
  - src/bun/testing/mock-engine.ts
  - src/bun/index.ts
  - e2e/api/copilotkit/railyin.test.ts
autonomous: true
requirements:
  - BRDG-01
  - BRDG-02
  - BRDG-03
  - RUNR-01
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - A chat turn on threadId = String(conversation.id) streams RUN_STARTED (FIRST, carrying input) → TEXT_MESSAGE_START/CONTENT/END → RUN_FINISHED (LAST) over SSE on a real spawned server (RUNR-01, SC1; D-01/D-03/D-12)
    - Engine `reasoning` deltas route to REASONING_MESSAGE_START/CONTENT/END; `tool_start`/`tool_result` route to the complete TOOL_CALL_START/ARGS/END/RESULT lifecycle with `messageId` present on every RESULT (BRDG-02/03; Pitfalls 5/6)
    - The executeChatTurn seam fires `onEngineEvent` for every raw EngineEvent in exact order and `onRunEnd` exactly at the terminal points (done/error/aborted/decision); behavior is byte-identical when `opts` is absent (BRDG-01; research A1)
    - Every bridge-emitted event zod-parses via `EventSchemas` from `@ag-ui/core`; every run stream ends in exactly one terminal event — no finalizeRunEvents "INCOMPLETE_STREAM" RUN_ERROR (Pitfall 3)
    - `clone()` re-attaches injected deps (Pitfall 1) and `abortRun()` routes to `orchestrator.cancel(executionId)`; the probe gate (`RAILYN_COPILOTKIT_PROBE`) is checked BEFORE the real registration (Pitfall 9, D-12)
    - Phase 1 probe tests (copilotkit.test.ts, sse-text-diff.test.ts) stay green — the probe path still registers ScriptedAgent + base InMemoryAgentRunner, byte-identical (D-12)
  artifacts:
    - src/bun/copilotkit/event-bridge.ts (+ event-bridge.test.ts) — pure EngineEvent → BaseEvent translation, one translation path
    - src/bun/copilotkit/railyin-agent.ts (+ railyin-agent.test.ts) — AbstractAgent subclass: RUN_STARTED-first, clone() override, abortRun() override, terminal emission
    - src/bun/engine/{coordinator.ts, orchestrator.ts, execution/chat-executor.ts, stream/stream-processor.ts} — additive `opts` threading (9th executeChatTurn param)
    - src/bun/test/execution-seam.test.ts — seam contract test through the REAL executor chain
    - src/bun/testing/mock-engine.ts — scripted tool/reasoning/slow/dangling/error scenarios driven by prompt markers
    - src/bun/index.ts — D-12 registration: RailyinAgent + base runner for now (runner swap in 02-02), probe gate first
    - e2e/api/copilotkit/railyin.test.ts — run-path wire tests through RailyinAgent + mock engine
  key_links:
    - onEngineEvent fires at the top of consume()'s for-await loop (stream-processor.ts:184) BEFORE the switch — exact raw ordering, single translation path
    - onRunEnd fires at the terminal points: done case (~415), fatal error (~446, before return), decision_request (~484, before return), abort-in-loop (~201), post-loop abort (~547), catch (~548) — 6 code points, 4 outcomes
    - Agent emits RUN_STARTED WITH `input` — the runner only patches when input is absent, so the persisted user turn matches the wire (State of the Art correction)
    - Probe gate checked BEFORE real registration; probe mode keeps base InMemoryAgentRunner (threadIds like "t1" are non-numeric — the JSONL store would reject them, and probe wire text must stay byte-identical)
    - Agent validates threadId `^\d+$` + conversation existence BEFORE executeChatTurn — first line of path-traversal defense and clear RUN_ERROR for unknown threads
---

<objective>
One chat turn through the AG-UI boundary: `RunAgentInput` → `RailyinAgent.run` → the threaded execution seam → `MockExecutionEngine` → `EngineEvent` → `event-bridge` → AG-UI `BaseEvent` stream (RUN_STARTED first, terminal last) — proven on the real wire. This is the phase's tracer slice: it wires the phase's riskiest bespoke code (translation table + agent lifecycle + the 4-layer executor seam) end-to-end before persistence is added in 02-02.

Purpose: BRDG-01 (exactly one translation path, D-02), BRDG-02 (REASONING_*, D-03), BRDG-03 (complete TOOL_CALL lifecycle, Pitfalls 5/6), RUNR-01 (one boundary for all five engines through EngineRegistry, D-10), D-12 registration swap (probe gate first, Pitfall 9). The runner stays base `InMemoryAgentRunner` in this plan — persistence lands in 02-02.

Deviation from D-02's parenthetical (recorded as assumption-delta): the `markClaudeExecution` hack is NOT physically deleted this phase. The AG-UI bridge is the single chat translation path without touching it (the bridge consumes raw EngineEvents via `onEngineEvent`, never the /ws broadcast machinery). Physical removal would double-broadcast Claude `text_chunk` events to the still-live legacy /ws chat UI — violating IMPR-03 rollback. Deletion moves to Phase 7 cleanup.

Output: the seam across 4 executor files, `src/bun/copilotkit/{event-bridge,railyin-agent}.ts` with co-located unit tests, the mock-engine scripted scenarios, the D-12 composition-root registration, and a real-server e2e file proving the run path.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-CONTEXT.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-RESEARCH.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-PATTERNS.md
@src/bun/engine/coordinator.ts
@src/bun/engine/orchestrator.ts
@src/bun/engine/execution/chat-executor.ts
@src/bun/engine/stream/stream-processor.ts
@src/bun/testing/mock-engine.ts
@src/bun/index.ts
@e2e/api/copilotkit/probe-agent.ts
@e2e/api/copilotkit/copilotkit.test.ts
@e2e/api/fixtures/server.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Thread the onEngineEvent/onRunEnd seam through executeChatTurn — one real chat turn through the executor chain</name>
  <files>
    src/bun/engine/coordinator.ts,
    src/bun/engine/orchestrator.ts,
    src/bun/engine/execution/chat-executor.ts,
    src/bun/engine/stream/stream-processor.ts,
    src/bun/test/execution-seam.test.ts
  </files>
  <read_first>
    - 02-RESEARCH.md §Architecture Patterns Pattern 2 (the seam, lines 226-237), §Code Examples (agent → executor seam, lines 444-453)
    - src/bun/engine/stream/stream-processor.ts — consume() for-await loop top (line 184), the six terminal code points (lines 201, 415, 446-459, 484-497, 531-547, 548-561), runNonNative (lines 124-138); do NOT remove markClaudeExecution (deviation recorded in the objective)
    - src/bun/engine/execution/chat-executor.ts — execute() signature (lines 36-45) and the runNonNative call (line 187); constructor-injected optional-callback precedent `onNewMessage` (line 33, guards at 109-114)
    - src/bun/engine/orchestrator.ts — executeChatTurn pass-through (lines 163-174)
    - src/bun/engine/coordinator.ts — the ExecutionCoordinator interface (line 10)
    - src/bun/test/orchestrator.test.ts — the real-chain test pattern (describe at line 282), and src/bun/test/helpers.ts (initDb :10-61, setupTestConfig :311-397, seedChatSession :409-434, makeTestRegistry :442-446)
  </read_first>
  <behavior>
    - Test 1 (seam order): real Orchestrator → ChatExecutor → StreamProcessor chain with MockExecutionEngine; call executeChatTurn with opts collecting onEngineEvent; every yielded EngineEvent arrives in exact order, including `usage` and `done`
    - Test 2 (byte-identical absent): executeChatTurn WITHOUT opts behaves identically — no callback, no crash, DB statuses unchanged (done → chat_sessions idle, executions completed)
    - Test 3 (terminal outcomes): done → onRunEnd("done"); fatal error event → onRunEnd("error"); abort mid-stream (signal abort) → onRunEnd("aborted"); decision_request event → onRunEnd("decision")
  </behavior>
  <action>
    Add an optional trailing parameter `opts?: { onEngineEvent?: (e: EngineEvent) => void; onRunEnd?: (o: "done" | "error" | "aborted" | "decision") => void }` threaded through four layers, per research A1 (callback tap, NOT a stream-returning method — the callback preserves StreamProcessor's DB dual-write and exact ordering):

    1. `coordinator.ts` line 10: append the `opts?` param to the `executeChatTurn` interface signature (import `EngineEvent` type from `./types.ts` — already imported via line 2's namespace import).
    2. `orchestrator.ts` lines 163-174: append `opts?` to the method signature and pass it as the 9th argument to `this.chatExecutor.execute(...)` — mirror how `attachments`/`engineContent` already flow.
    3. `chat-executor.ts` `execute()` (lines 36-45): append `opts?` to the signature; pass it through at the ONLY call-site change — line 187 `this.streamProcessor.runNonNative(null, conversationId, executionId, engine, execParams, opts)`. Do NOT read `sessionId` (research A3: ignored by the executor).
    4. `stream-processor.ts` `runNonNative` (lines 124-138): append `opts?` param, forward to `consume(...)`. `consume()` (line 144): append `opts?` param. Two guarded call sites:
       - At the TOP of the for-await loop body (line 184), AFTER the abort check, BEFORE the switch: `opts?.onEngineEvent?.(event)` — fires for EVERY raw engine event in exact order; when absent, byte-identical behavior.
       - `onRunEnd` at the six terminal code points, each guarded `opts?.onRunEnd?.(...)`, AFTER the DB status updates at each point: "done" at the end of the done case (before break, ~line 443); "error" at the end of the fatal-error case (before the `return`, ~line 459) and in the catch block (line ~548); "decision" at the end of the decision_request case (before `return`, ~line 496); "aborted" in both abort paths (before `return` at ~line 201 and at ~line 546). Do NOT touch the legacy broadcast machinery, and do NOT remove `markClaudeExecution`/`claudeExecutionIds` (IMPR-03 — the legacy /ws UI is still live; deviation documented in the objective).
    5. `src/bun/test/execution-seam.test.ts` (NEW): follow `orchestrator.test.ts`'s construction pattern (initDb + setupTestConfig + makeTestRegistry + seedChatSession + MockExecutionEngine). Drive the REAL chain — real Orchestrator → real ChatExecutor → real StreamProcessor — and assert the three behaviors above. For the abort test, abort via the returned executionId through `orchestrator.cancel(executionId)` after the first event arrives; for the decision_request test, script the mock engine to yield `{ type: "decision_request", payload: "{}" }` (see Task 3's mock-engine markers — if Task 3's marker infrastructure is not yet merged, yield the events from a local inline scripted engine in this test instead; do not block on task ordering).
  </action>
  <verify>
    <automated>bun test src/bun/test/execution-seam.test.ts -x && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/bun/test/execution-seam.test.ts -x` passes all three behaviors (order, absent-opts, 4 terminal outcomes)
    - `bun run typecheck` passes with the new trailing params (existing test stubs that override runNonNative with fewer params remain valid — TS allows fewer params in overrides)
    - grep confirms the seam call sites: `onEngineEvent` at the loop top and `onRunEnd` at the six terminal points, all `opts?.`-guarded
  </acceptance_criteria>
  <done>The seam is live across the 4 executor files: executeChatTurn accepts the optional opts param, onEngineEvent fires per raw event in order, onRunEnd fires at all terminal points, and the real-chain contract test proves order + absent-opts byte-identity + 4 outcomes.</done>
  <reversibility rating="reversible">Pure additive optional parameter — removing it restores the exact prior signatures; no call site changes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the pure event-bridge translation module with EventSchemas-validated output</name>
  <files>
    src/bun/copilotkit/event-bridge.ts,
    src/bun/copilotkit/event-bridge.test.ts
  </files>
  <read_first>
    - 02-RESEARCH.md §Pattern 2/Pattern 4 (bridge contract, D-09 synthesis), §Common Pitfalls 3/5/6, §Code Examples (event schemas lines 413-420, EngineEvent union lines 422-442)
    - 02-PATTERNS.md §event-bridge.ts (mapping table lines 63-81, toolCallId namespacing pattern from stream-processor.ts:163-165/313-319)
    - src/bun/engine/types.ts — the EngineEvent union (lines 20-50)
    - src/bun/pipeline/stream-event-enricher.test.ts — co-located pure-module test shape (bun:test imports)
  </read_first>
  <behavior>
    - Test 1 (token): token events → TEXT_MESSAGE_START (messageId, role "assistant") then TEXT_MESSAGE_CONTENT {delta} then TEXT_MESSAGE_END, all parsing with EventSchemas from @ag-ui/core
    - Test 2 (reasoning): reasoning events → REASONING_MESSAGE_START {role:"reasoning"} / CONTENT / END (BRDG-02)
    - Test 3 (tools): tool_start → TOOL_CALL_START/ARGS/END; tool_result → TOOL_CALL_RESULT with messageId === `${toolCallId}-result` (Pitfall 5); every emitted event zod-parses via EventSchemas
    - Test 4 (terminal paths): each of done/error/aborted outcomes produces exactly one terminal event (RUN_FINISHED / RUN_ERROR) (Pitfall 3)
    - Test 5 (D-09 synthesis): a run ending with a dangling TOOL_CALL_START gets synthetic TOOL_CALL_END + TOOL_CALL_RESULT {messageId, toolCallId, content:""} BEFORE the terminal
    - Test 6 (namespacing): child/internal tool calls (parentCallId set) get unique `${parentCallId}::${callId}::${seq}` ids — no duplicate toolCallId within a run (Pitfall 6)
  </behavior>
  <action>
    Create `src/bun/copilotkit/event-bridge.ts` — a pure module (no I/O, no constructor-visible deps) exporting, per the research contract:

    - A state factory `createTranslateState(threadId: string, runId: string)` returning `{ threadId, runId, textSeq, reasoningSeq, toolSeq, openToolCallIds: string[] }` — per-run mutable state owned by the agent's run closure (NEVER agent instance fields; the runtime clones per request).
    - `translateEngineEvent(event: EngineEvent, state): BaseEvent[]` — the exhaustive dispatch over the EngineEvent union (types.ts:20-50), mirroring stream-processor.ts's consume() switch 1:1:
      - `token` → TEXT_MESSAGE_START (messageId `${runId}-text-${seq}`, role "assistant") on first token of a block + TEXT_MESSAGE_CONTENT {messageId, delta} per token + TEXT_MESSAGE_END on the next non-token boundary or at finish (group consecutive tokens into one assistant block)
      - `reasoning` → REASONING_MESSAGE_START (messageId `${runId}-reasoning-${seq}`, role "reasoning") + CONTENT per delta + END at boundary (BRDG-02)
      - `tool_start` → TOOL_CALL_START {toolCallId, toolCallName: event.name, parentMessageId?} + TOOL_CALL_ARGS {toolCallId, delta: event.arguments} + TOOL_CALL_END {toolCallId}. toolCallId: `${parentCallId}::${callId}::${++seq}` when `parentCallId` is set (mirror stream-processor.ts:313-319), else `event.callId ?? call_${Date.now()}_${random}` (mirror stream-processor.ts:291). Track the id in `openToolCallIds`.
      - `tool_result` → TOOL_CALL_RESULT {messageId: `${toolCallId}-result`, toolCallId, content: event.result, role: "tool"} — messageId REQUIRED by the zod schema (Pitfall 5). Remove the toolCallId from `openToolCallIds` (matching by namespaced id when parentCallId set, else raw callId).
      - `subagent_start` → TOOL_CALL_START (toolCallName "subagent", namespaced id) + TOOL_CALL_ARGS {delta: JSON.stringify({intent, prompt})}; `subagent_stop` → TOOL_CALL_END + TOOL_CALL_RESULT {messageId: `${toolCallId}-result`, content: ""}
      - `done`/`error`/`usage`/`status`/`task_updated`/`new_message`/`compaction_*`/`ask_user`/`shell_approval`/`decision_request` → [] (ignore: board /ws path is UNCHANGED — no double-broadcast, BRDG-01; the terminal is emitted by the agent via the finish helper)
    - `synthesizeMissingToolResults(state, events: BaseEvent[]): BaseEvent[]` — D-09/A5: for every id still in `state.openToolCallIds`, append TOOL_CALL_END {toolCallId} + TOOL_CALL_RESULT {messageId: `${toolCallId}-result`, toolCallId, content: ""} — call this BEFORE the terminal so the persisted log never contains dangling calls (do NOT rely on finalizeRunEvents for this: it early-returns when a terminal exists).
    - `terminalEvent(threadId, runId, outcome: "done" | "error" | "aborted" | "decision", error?: { message: string; code?: string }): BaseEvent` — RUN_FINISHED {threadId, runId, result: null} for done/aborted/decision; RUN_ERROR {message, code} for error. Exactly ONE terminal per run (Pitfall 3).

    Co-locate `event-bridge.test.ts`: table-driven `bun:test` per family (stream-event-enricher.test.ts shape), and zod-parse EVERY emitted event with `EventSchemas` from `@ag-ui/core` (the Don't Hand-Roll row 4 validation contract). Import types from `@ag-ui/client` (`BaseEvent`) and `@ag-ui/core` (`EventType`); `EngineEvent` from `../engine/types.ts`.
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/event-bridge.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/bun/copilotkit/event-bridge.test.ts -x` passes all mapping families (token/reasoning/tools/subagent), terminal paths, D-09 synthesis, and namespacing tests
    - Every event in every test parses through EventSchemas without zod errors — including TOOL_CALL_RESULT.messageId presence
    - No I/O imports in event-bridge.ts (pure module — grep confirms no fs/path/net imports)
  </acceptance_criteria>
  <done>The pure translation module exists with the full mapping table, D-09 synthesis, namespacing, and exactly-one-terminal guarantee — all EventSchemas-validated by co-located unit tests.</done>
  <reversibility rating="reversible">Additive pure module — deletable without touching any other file.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build RailyinAgent (clone/abortRun/terminal contract), register it in the composition root, and prove the run path on the real wire</name>
  <files>
    src/bun/copilotkit/railyin-agent.ts,
    src/bun/copilotkit/railyin-agent.test.ts,
    src/bun/testing/mock-engine.ts,
    src/bun/index.ts,
    e2e/api/copilotkit/railyin.test.ts
  </files>
  <read_first>
    - e2e/api/copilotkit/probe-agent.ts — the exact AbstractAgent shape to mirror (imports :23-25, run() shape :56-63 with the rxjs nested-version cast, event contract doc :10-21, RUN_STARTED-first :71)
    - 02-RESEARCH.md §Pattern 2 (verified AbstractAgent contract: abstract run(), clone() field list, abortRun() no-op, finalizeRunEvents terminal requirement), §Common Pitfalls 1/3/9
    - 02-PATTERNS.md §railyin-agent.ts (clone/abortRun shape :53-57)
    - src/bun/index.ts — the CopilotRuntime mount (lines 245-277), orchestrator construction + late-binding pattern (lines 228-236)
    - src/bun/testing/mock-engine.ts — the execute() generator + abort-check rhythm (lines 18-37)
    - e2e/api/copilotkit/copilotkit.test.ts — postJson/parseSseFrames/runInput scaffolding (lines 29-56); e2e/api/fixtures/server.ts — startServer options (lines 134-150)
  </read_first>
  <behavior>
    - Test 1 (agent lifecycle): fake coordinator (handlers.test.ts:97 literal shape) driving onEngineEvent [token, reasoning, tool_start, tool_result] then onRunEnd("done"); agent.run(input) emits RUN_STARTED FIRST (with input), mapped events in order, RUN_FINISHED LAST
    - Test 2 (clone): `agent.clone().orchestrator === agent.orchestrator` and db preserved (Pitfall 1)
    - Test 3 (abort): abortRun() calls coordinator.cancel(executionId) with the executionId returned by executeChatTurn
    - Test 4 (terminal guard): stream completes WITHOUT onRunEnd (pause path) → completion-guard appends RUN_FINISHED — no INCOMPLETE_STREAM RUN_ERROR (Pitfall 3)
    - Test 5 (unknown conversation): run on a non-existent conversation id emits RUN_ERROR without calling executeChatTurn
    - Test 6 (e2e): real server run on threadId = String(conversation.id) → 200 SSE, first frame RUN_STARTED with input, last frame RUN_FINISHED
  </behavior>
  <action>
    1. `src/bun/copilotkit/railyin-agent.ts` (NEW): `class RailyinAgent extends AbstractAgent` (imports exactly like probe-agent.ts:23-25: AbstractAgent from "@ag-ui/client", EventType/RunAgentInput/AGUIEvent from "@ag-ui/core", from from "rxjs"; cast the return like probe-agent.ts:62). Constructor: `super({ agentId: "default", description: "Railyin engine bridge agent" })` + injected deps `private readonly db: Database` (bun:sqlite) and `private readonly orchestrator: ExecutionCoordinator`. Override `clone()` (Pitfall 1): `const c = super.clone(); c.db = this.db; c.orchestrator = this.orchestrator; return c;`. Override `abortRun()` (base is a no-op): keep a per-run abort flag + the executionId from executeChatTurn in the run closure; on abortRun, if executionId is set call `this.orchestrator.cancel(executionId)` (interface coordinator.ts:12), else set the flag so the run cancels immediately after executeChatTurn resolves.
       `run(input)` (implements the abstract method — do NOT override runAgent): validate threadId against `/^\d+$/` and check `SELECT 1 FROM conversations WHERE id = ?` (unknown conversation → stream RUN_STARTED {threadId, runId, input} then RUN_ERROR {message, code: "THREAD_NOT_FOUND"} then complete). Extract `content` = the LAST message in `input.messages` with role "user" and type "text" (its `text` field); none found → RUN_STARTED + RUN_ERROR {code: "NO_USER_MESSAGE"}. Then: create a per-run `ReplaySubject(Infinity)` (rxjs), emit RUN_STARTED {threadId, runId, input} FIRST (WITH input — State of the Art: the runner only patches when absent, so the persisted user turn matches the wire), call `this.orchestrator.executeChatTurn(0, conversationId, content, undefined, null, workspaceKey, undefined, undefined, { onEngineEvent, onRunEnd })` — sessionId 0 per research A3; model/mcpTools undefined (the executor resolves conversations.model via EngineRegistry — D-10). `workspaceKey` resolution for THIS plan: `getDefaultWorkspaceKey()` (the task→chat_sessions→default resolver is 02-03 Task 1). onEngineEvent feeds `translateEngineEvent(event, state)` output into the subject; onRunEnd appends `synthesizeMissingToolResults(state, accumulated)` then `terminalEvent(...)` ("done"→done, "error"→error with message, "aborted"→aborted, "decision"→decision — Phase 3 replaces decision semantics) then completes the subject. Completion guard: subscribe to subject completion; if no terminal was emitted (pause paths like ask_user/shell_approval where consume ends without an outcome), append RUN_FINISHED before completing. ALL per-run state (subject, state, abort flag, executionId, accumulated events) lives in the run closure — NEVER instance fields (anti-pattern, research line 256). Return the subject as the Observable with the probe-style cast (`as unknown as ReturnType<AbstractAgent["run"]>`).
    2. `src/bun/copilotkit/railyin-agent.test.ts` (NEW): fake ExecutionCoordinator object literal (handlers.test.ts:97 shape) with `executeChatTurn` capturing the opts and `cancel` recording calls; construct with a real in-memory bun:sqlite DB via helpers.ts initDb + a seeded conversations row (seedChatSession). Cover behaviors 1-5 above. For behavior 1, drive the fake synchronously: `executeChatTurn` calls `opts.onEngineEvent` for the scripted sequence then `opts.onRunEnd("done")`, returning `{ message: {}, executionId: 42 }`.
    3. `src/bun/testing/mock-engine.ts` (MOD): extend `MockExecutionEngine.execute` (lines 18-37) so `params.prompt` containing a sentinel marker yields scripted sequences — keep the existing plain-text behavior, the `copilot/mock-model` listModels contract, and the abort-check rhythm (check `params.signal.aborted` before EVERY yield): `__SCRIPT_TOOLS__` → reasoning ("reasoning about the request") + tool_start {name:"read_file", callId:"call_1", arguments: JSON.stringify({path:"a.txt"})} + tool_result {name:"read_file", callId:"call_1", result:"file contents"} + token + done; `__SCRIPT_DANGLING_TOOL__` → tool_start (no result) + token + done (D-09 wire proof); `__SCRIPT_SLOW__` → token "slow" + delay(2000) + done (concurrent-run test in 02-02); `__SCRIPT_ERROR__` → token + error {message: "scripted failure", fatal: true}.
    4. `src/bun/index.ts` (MOD, D-12): keep the `RAILYN_COPILOTKIT_PROBE === "1"` gate and dynamic probe import FIRST (Pitfall 9); when probe disabled AND `orchestrator` is non-null, construct `const railyinAgent = new RailyinAgent(db, orchestrator)` and use `{ default: railyinAgent }` as the agents map (keep the `type CopilotAgents` cast bridge at lines 268-271). Register `const copilotRuntime = new CopilotRuntime({ agents: copilotAgents })` — NO runner override yet (base InMemoryAgentRunner; 02-02 swaps it in). Preserve the mount branch, `srv.timeout(req, 0)`, and multi-route handler untouched. Probe mode must remain byte-identical (base runner, ScriptedAgent only).
    5. `e2e/api/copilotkit/railyin.test.ts` (NEW): scaffold from copilotkit.test.ts (startServer without copilotkitProbe — the REAL agent registers; postJson/parseSseFrames/runInput helpers, but runInput's messages become `[{ id: "u1", role: "user", type: "text", text: "<content>" }]`). Tests: (a) `server.request("chatSessions.create", {})` → threadId = String(session.conversation_id) → POST run "hello" → 200 text/event-stream; frames[0] RUN_STARTED (assert its input.messages contains the user text); last frame RUN_FINISHED (RUNR-01 on the wire, RUNR-03 mapping); (b) run "__SCRIPT_TOOLS__" → frames include REASONING_MESSAGE_START/CONTENT/END and the full TOOL_CALL_START/ARGS/END/RESULT lifecycle, RESULT.messageId === `${toolCallId}-result` (BRDG-02/03 on the wire); (c) run "__SCRIPT_DANGLING_TOOL__" → a TOOL_CALL_RESULT frame appears before RUN_FINISHED (D-09); (d) run "__SCRIPT_ERROR__" → a RUN_ERROR frame appears and is the terminal frame. All /api/copilotkit/* calls via RAW fetch (AG-UI, not RailynAPI — copy the header comment discipline).
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/railyin-agent.test.ts -x && bun test e2e/api/copilotkit/railyin.test.ts -x && bun test e2e/api/copilotkit/copilotkit.test.ts -x && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Agent unit tests green (lifecycle, clone deps, abort→cancel, completion guard, unknown-conversation RUN_ERROR)
    - `bun test e2e/api/copilotkit/railyin.test.ts -x` green: real-server run with RUN_STARTED-first + input + RUN_FINISHED-last, tool lifecycle, reasoning events, dangling-tool synthesis, RUN_ERROR terminal
    - Phase 1 probe tests still green (`copilotkit.test.ts -x` passes) — probe gate first, base runner in probe mode
    - `bun run typecheck` clean
  </acceptance_criteria>
  <done>The real agent is registered in the composition root behind the probe gate; a chat turn on a real spawned server streams a valid AG-UI run (RUN_STARTED with input → TEXT/REASONING/TOOL lifecycle → terminal) through exactly one translation path, with the mock engine proving tool/reasoning/error/synthesis scenarios on the wire.</done>
  <reversibility rating="costly">D-01/D-12: the bridge-in-agent architecture and the registration swap are research-backed locked decisions; reversing later touches the runner contract and the composition root — flagged, not gated.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loopback client → /api/copilotkit/* | untrusted RunAgentInput JSON crossing into the runtime → runner → RailyinAgent; same-origin localhost, no auth (inherited posture, ASVS V3/V4 n/a) |
| agent → ExecutionCoordinator | internal boundary: the agent derives conversationId/workspaceKey from client-supplied threadId; DB reads only (SELECT), no writes from the agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | threadId → conversation lookup (agent) | high | mitigate | Agent validates `/^\d+$/` + `SELECT 1 FROM conversations WHERE id = ?` BEFORE any use — non-numeric or unknown ids get RUN_ERROR, never a filesystem or executor side effect; unit-tested (behavior 5) and e2e-asserted (V5/V8 first line; the jsonl-store sanitization in 02-02 is the second line) |
| T-02-02 | DoS | concurrent run on one thread | medium | mitigate | Inherited runner lock ("Thread already running" — base InMemoryAgentRunner, research Pitfall 2); unit + e2e assertion (200 + zero frames, NOT 500) lands in 02-02 Task 3; client busy-guard is Phase 5 |
| T-02-03 | Tampering | malformed RunAgentInput flooding the runtime | low | mitigate | Runtime zod parse (RunAgentInputSchema) → 400 before any agent work (Phase 1 test 6, unchanged); no new pre-parse work in this plan |
| T-02-04 | Spoofing | probe ScriptedAgent exposed in `bun run prod` | low | mitigate | RAILYN_COPILOTKIT_PROBE gate checked BEFORE real registration (Pitfall 9); probe mode keeps base runner; prod registers RailyinAgent only when orchestrator exists |
| T-02-05 | (correctness) | clone() dropping injected deps → undefined orchestrator on first run | n/a (correctness, not security) | mitigate | clone() override re-attaching db/orchestrator + unit test `clone().orchestrator === original.orchestrator` (Pitfall 1; documented as non-security per RESEARCH.md Security Domain) |
| T-02-06 | (correctness) | agent stream ending without terminal → finalizeRunEvents appends INCOMPLETE_STREAM RUN_ERROR | n/a (correctness) | mitigate | Agent always emits exactly one terminal (onRunEnd mapping + completion guard); unit-tested terminal paths (Pitfall 3) |

Accepted risks documented for later phases: prompt injection via engine output (bridge treats engine content as data — never re-executed; import-time concern deferred to Phase 4), unbounded JSONL growth (single-user local; Phase 4 hardening).
</threat_model>

<verification>
- `bun test src/bun/test/execution-seam.test.ts -x` — seam contract through the real executor chain (BRDG-01)
- `bun test src/bun/copilotkit/event-bridge.test.ts -x` — translation table, EventSchemas validation, D-09 synthesis (BRDG-01/02/03)
- `bun test src/bun/copilotkit/railyin-agent.test.ts -x` — agent lifecycle, clone, abort, terminal guard (RUNR-01)
- `bun test e2e/api/copilotkit/railyin.test.ts -x` — run path on the real wire through RailyinAgent + mock engine
- `bun test e2e/api/copilotkit/copilotkit.test.ts -x` — Phase 1 probe regression (probe gate + base runner intact)
- `bun run typecheck` — additive params and new modules compile
</verification>

<success_criteria>
- ROADMAP SC1 (partial this plan): a chat turn streams normalized AG-UI events through exactly one translation path — proven on the real wire for token/reasoning/tool/error scenarios
- BRDG-01/02/03 + RUNR-01 unit- and wire-covered; D-09 synthesis proven on the wire (dangling tool → RESULT before RUN_FINISHED)
- Phase 1 probe suite stays green — the D-12 swap is rollback-safe by construction
</success_criteria>

## Artifacts this phase produces

- **src/bun/copilotkit/event-bridge.ts** — `createTranslateState(threadId, runId)`, `translateEngineEvent(event, state): BaseEvent[]` (exhaustive EngineEvent dispatch), `synthesizeMissingToolResults(state, events): BaseEvent[]` (D-09), `terminalEvent(threadId, runId, outcome, error?): BaseEvent`; pure, no I/O
- **src/bun/copilotkit/railyin-agent.ts** — `class RailyinAgent extends AbstractAgent`: constructor(db, orchestrator); `clone()` re-attaching deps; `abortRun()` → orchestrator.cancel; `run(input)` with threadId validation, conversation-existence check, RUN_STARTED-with-input first, per-run ReplaySubject closure, terminal via onRunEnd + completion guard
- **src/bun/copilotkit/event-bridge.test.ts / railyin-agent.test.ts** — co-located vitest suites (EventSchemas validation; fake-coordinator lifecycle tests)
- **src/bun/engine/coordinator.ts, orchestrator.ts, execution/chat-executor.ts, stream/stream-processor.ts** — additive `opts?: { onEngineEvent?, onRunEnd? }` threading; onEngineEvent at consume() loop top; onRunEnd at 6 terminal code points (4 outcomes); byte-identical when absent
- **src/bun/test/execution-seam.test.ts** — real-chain seam contract test
- **src/bun/testing/mock-engine.ts** — scripted scenarios via prompt markers `__SCRIPT_TOOLS__` / `__SCRIPT_DANGLING_TOOL__` / `__SCRIPT_SLOW__` / `__SCRIPT_ERROR__`
- **src/bun/index.ts** — D-12: RailyinAgent registered when probe disabled + orchestrator present; probe gate first; base runner in probe mode
- **e2e/api/copilotkit/railyin.test.ts** — real-server run-path suite (RUN_STARTED-with-input first, TEXT/REASONING/TOOL lifecycle, D-09 synthesis, RUN_ERROR terminal)
- **Env/config** — none new (no env vars, no YAML changes)

<output>
Create `.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-01-SUMMARY.md` when done.
</output>
