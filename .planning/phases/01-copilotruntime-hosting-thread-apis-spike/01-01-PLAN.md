---
phase: 1-copilotruntime-hosting-thread-apis-spike
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - e2e/api/copilotkit/pins.test.ts
autonomous: false
requirements:
  - HOST-03
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - package.json lists all five pinned deps with EXACT versions and no caret: @copilotkit/runtime "1.66.4", @copilotkit/vue "1.66.4", @ag-ui/core "0.0.57", @ag-ui/client "0.0.57", @ag-ui/encoder "0.0.57"
    - @copilotkit/vue sits in `dependencies` (not devDependencies) per D-10
    - bun install, bun run build, and bun run typecheck all pass after the pin install (assumption A1 holds)
    - a human confirmed the two SUS-flagged 1.66.4 packages on npmjs.com BEFORE bun add ran (package legitimacy gate)
    - e2e/api/copilotkit/pins.test.ts passes, asserting the exact pin strings and dependency section
  artifacts:
    - package.json (5 new exact pins)
    - bun.lock (resolved tree: zod@3 nested under copilotkit, @ag-ui/*@0.0.57 exact)
    - e2e/api/copilotkit/pins.test.ts
  key_links:
    - @copilotkit/runtime@1.66.4 → @ag-ui/*@0.0.57 exact dependency chain (D-09: never bump AG-UI independently)
    - zod@3 nests under copilotkit; no project zod@4 import may enter AG-UI code paths (RESEARCH Pitfall 5)
---

<objective>
Pin the exact AG-UI/CopilotKit stack and prove the install is build-clean, with a human-verified package legitimacy gate.

MVP user story (phase goal, derived from ROADMAP §Phase 1 Goal): **As a** Railyin maintainer, **I want to** see the pinned CopilotRuntime stack installed on the project with exact versions and a green build, **so that** the runtime-mount probe in plan 01-02 starts from a verified, drift-free foundation.

Purpose: HOST-03 evidence part 1 — the pins must be exact (D-09), the Vue SDK pinned-but-unconsumed (D-10), and the install must not break the existing app (A1) or entangle zod@4 with the runtime's zod@3 (Pitfall 5).
Output: package.json + bun.lock updated via `bun add --exact`; `e2e/api/copilotkit/pins.test.ts` locking the pins; green build/typecheck.
</objective>

<assumption_delta_decision>
noun="server process" decision="no-change" rationale="'no second server process' in the phase scope is a constraint reaffirming the single-process invariant (HOST-01), not a second instance — the detector fired on the word 'second' and is a FALSE POSITIVE; no pluralization change occurs."
</assumption_delta_decision>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-RESEARCH.md
@.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-PATTERNS.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify the two SUS-flagged CopilotKit packages before install</name>
  <what-built>Nothing built — a mandatory pre-install legitimacy check per the package legitimacy gate. RESEARCH.md §Package Legitimacy Audit flags @copilotkit/runtime@1.66.4 and @copilotkit/vue@1.66.4 as [SUS] on the "too-new" signal alone (published 2026-08-07, one day before research). Both come from the official CopilotKit/CopilotKit repo and have no postinstall scripts (verified via npm view), but the protocol requires human confirmation before any install proceeds. This checkpoint is never auto-approvable (workflow.auto_advance ignored).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@copilotkit/runtime — confirm the package owner/scope is the official CopilotKit org, the 1.66.4 version entry exists, published date is 2026-08-07, and the repository link points to github.com/CopilotKit/CopilotKit. Confirm no install/postinstall scripts.
    2. Open https://www.npmjs.com/package/@copilotkit/vue — same checks: official org, 1.66.4 exists, no suspicious scripts. Note the peer dependency on vue >= 3.3.0 (project has Vue 3).
    3. Cross-check versions match the research table: @copilotkit/runtime@1.66.4, @copilotkit/vue@1.66.4 (D-09 user decision — if the registry disagrees with the pin, STOP and report; do not substitute a different version).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `bun add --exact`, or describe the concern found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the five pinned packages exactly and gate the build</name>
  <files>package.json, bun.lock</files>
  <read_first>
    - package.json (current dependencies; the exact-pin convention at line 28 "@anthropic-ai/claude-agent-sdk": "0.3.204")
    - 01-RESEARCH.md §Standard Stack (installation command, zod@3 nesting expectation, Pitfall 5)
  </read_first>
  <action>
    Run `bun add --exact @copilotkit/runtime@1.66.4 @copilotkit/vue@1.66.4 @ag-ui/core@0.0.57 @ag-ui/client@0.0.57 @ag-ui/encoder@0.0.57` (per D-09/D-10, all five in `dependencies` — @copilotkit/vue is a regular dep pinned but NOT imported anywhere this phase). Never hand-edit package.json for these pins (lockfile drift). After install:
    - Verify package.json records the five exact versions with no caret.
    - Inspect `bun pm ls` output and record the install-time finding in the task summary: confirm @ag-ui/core, @ag-ui/client, @ag-ui/encoder resolve to 0.0.57, zod@3 is nested under @copilotkit/runtime (not hoisted to the project's zod@4), and rxjs 7.8.x is present.
    - Run `bun run build` and `bun run typecheck` — both must pass (assumption A1). If bun install warns about peer/dep conflicts (Pitfall 5 warning sign), do not proceed — report the tree in the summary and stop.
  </action>
  <verify>
    <automated>bun run build && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun pm ls` shows @copilotkit/runtime@1.66.4, @copilotkit/vue@1.66.4, @ag-ui/core@0.0.57, @ag-ui/client@0.0.57, @ag-ui/encoder@0.0.57
    - package.json contains the strings `"@copilotkit/runtime": "1.66.4"`, `"@copilotkit/vue": "1.66.4"`, `"@ag-ui/core": "0.0.57"`, `"@ag-ui/client": "0.0.57"`, `"@ag-ui/encoder": "0.0.57"` (no caret before any version)
    - `bun run build` exits 0 and `bun run typecheck` exits 0
    - No `import` of @copilotkit/vue or @ag-ui anywhere in src/ or e2e/ (pin-only, D-10)
  </acceptance_criteria>
  <done>Five exact pins present in package.json dependencies (no carets); build + typecheck green; install findings (zod@3 nesting, exact AG-UI resolution) recorded in the SUMMARY.</done>
  <reversibility rating="reversible">Version bump; bun remove + re-pin restores the prior tree.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Lock the pins with a unit test</name>
  <files>e2e/api/copilotkit/pins.test.ts</files>
  <read_first>
    - e2e/api/smoke.test.ts (describe/test shape, bun:test imports, no server needed here)
    - e2e/api/mcp-oauth.test.ts (node:fs read pattern for project files)
    - package.json (the five pins just written)
  </read_first>
  <action>
    Create e2e/api/copilotkit/pins.test.ts as a pure unit test (no server, no startServer import). Read package.json via readFileSync(new URL("../../package.json", import.meta.url), "utf-8") — path from e2e/api/copilotkit/ up to the repo root. Assert, using toBe with exact strings (no regex, no caret tolerance): dependencies["@copilotkit/runtime"] === "1.66.4", dependencies["@copilotkit/vue"] === "1.66.4", dependencies["@ag-ui/core"] === "0.0.57", dependencies["@ag-ui/client"] === "0.0.57", dependencies["@ag-ui/encoder"] === "0.0.57". Add one test asserting devDependencies does NOT contain "@copilotkit/vue" (regular dependency per D-10) and one asserting dependencies contains no key matching /^@ag-ui\/.+\.\d+/ style ranges — keep it simple: assert the exact five strings and that "@copilotkit/vue" is absent from devDependencies. This test is the HOST-03 pin evidence and the guard against future bump drift.
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/pins.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File e2e/api/copilotkit/pins.test.ts exists and passes: `bun test e2e/api/copilotkit/pins.test.ts` exits 0 with all assertions green
    - The test asserts the exact five version strings and the absence of @copilotkit/vue from devDependencies
    - No server is spawned by the test (pure unit, no beforeAll/startServer)
  </acceptance_criteria>
  <done>pins.test.ts exists and passes via `bun test e2e/api/copilotkit/pins.test.ts`, asserting the five exact version strings and the absence of @copilotkit/vue from devDependencies.</done>
  <reversibility rating="reversible">Test-only file; removable without production impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project (bun add) | untrusted third-party code entering the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-SC | Tampering | npm installs of @copilotkit/runtime@1.66.4 + @copilotkit/vue@1.66.4 | high | mitigate | blocking-human checkpoint (Task 1) verifying official CopilotKit org, 1.66.4 existence, publish date, no postinstall scripts on npmjs.com; never auto-approvable; RESEARCH.md §Package Legitimacy Audit table is the audit source |
| T-1-02 | Tampering | package.json pins / bun.lock drift | low | mitigate | `bun add --exact` (no hand-editing) + pins.test.ts asserting exact strings; a future casual `bun add pkg` bump would fail the test loudly |

Mitigations reference RESEARCH.md §Package Legitimacy Audit verdicts ([SUS] flagged on version-freshness only; official repo verified; no postinstall scripts).
</threat_model>

<verification>
- `bun test e2e/api/copilotkit/pins.test.ts` — pin assertions (HOST-03)
- `bun run build && bun run typecheck` — install did not break the app (assumption A1)
- `bun pm ls` output recorded in the plan SUMMARY as install-time evidence (zod@3 nesting, exact AG-UI pins)
</verification>

<success_criteria>
- All five deps pinned exactly in package.json `dependencies`; human approved the SUS packages; build + typecheck green; pins.test.ts green.
- The install findings (zod@3 nesting, no peer conflicts, exact @ag-ui resolution) are recorded in the SUMMARY for HOST-03 evidence.
</success_criteria>

## Artifacts this phase produces

- **package.json** — deps added: `@copilotkit/runtime@1.66.4`, `@copilotkit/vue@1.66.4`, `@ag-ui/core@0.0.57`, `@ag-ui/client@0.0.57`, `@ag-ui/encoder@0.0.57` (all regular `dependencies`, exact pins, D-09/D-10)
- **bun.lock** — resolved tree (zod@3 nested under @copilotkit/runtime; @ag-ui/*@0.0.57 exact; rxjs 7.8.x)
- **e2e/api/copilotkit/pins.test.ts** — test module asserting the five exact pin strings and @copilotkit/vue's dependency section (HOST-03 pin evidence)
- **Env var** — none created in this plan (RAILYN_COPILOTKIT_PROBE arrives in 01-02)

<output>
Create `.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-01-SUMMARY.md` when done.
</output>
