---
phase: 07-cleanup-feature-trim
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/mainview/components/ChatSidebar.vue
  - src/mainview/components/ConversationPanel.vue
  - src/mainview/components/ConversationInput.vue
  - src/mainview/components/ChatEditor.vue
  - src/mainview/components/ConversationBody.vue
  - src/mainview/components/MessageBubble.vue
  - src/mainview/components/StreamBlockNode.vue
  - src/mainview/components/SubagentBlock.vue
  - src/mainview/components/ToolCallBlock.vue
  - src/mainview/components/ToolCallGroup.vue
  - src/mainview/components/ReasoningBubble.vue
  - src/mainview/components/TransitionEventCard.vue
  - src/mainview/components/CodeReviewCard.vue
  - src/mainview/components/InlineChipText.vue
  - src/mainview/components/McpToolsPopover.vue
  - src/mainview/components/ContextPopover.vue
  - src/mainview/components/AskUserPrompt.vue
  - src/mainview/components/ShellApprovalPrompt.vue
  - src/mainview/components/DecisionRequest.vue
  - src/shared/stream-tree.ts
  - src/mainview/utils/pairToolMessages.ts
  - src/mainview/utils/buildDisplayItems.ts
  - src/mainview/composables/useTypewriter.ts
  - src/mainview/stores/draft.ts
  - src/mainview/stores/chat.ts
  - src/mainview/stores/conversation.ts
  - src/mainview/stores/task.ts
  - src/mainview/rpc.ts
  - src/mainview/App.vue
  - src/shared/rpc-types.ts
  - src/mainview/stores/chat.test.ts
  - src/mainview/stores/conversation.test.ts
  - src/mainview/stores/task.test.ts
  - src/mainview/stores/dispatch.test.ts
  - src/mainview/stores/draft.test.ts
  - src/mainview/utils/pairToolMessages.test.ts
  - src/mainview/utils/buildDisplayItems.test.ts
  - src/bun/test/stream-tree-scenarios.test.ts
  - e2e/ui/fixtures/mock-ws.ts
  - e2e/ui/fixtures/index.ts
  - e2e/ui/interview-me.spec.ts
  - e2e/ui/stream-reactivity.spec.ts
  - e2e/ui/timeline-pipeline.spec.ts
autonomous: true
requirements: [TRIM-file_diff, TRIM-code_review, TRIM-transition_event, TRIM-usage display, TRIM-ask_user, TRIM-shell_approval]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The dead legacy chat component chain and dead mainview modules no longer exist (grep per file before deletion — RESEARCH Pattern 1 Groups A/C; Group B FileDiff.vue + ReadView.vue MUST STAY — live importers FileChangesRenderer.vue:34-35)
    - The stores expose only their live surfaces: chat.ts keeps sessions CRUD/unread/selectSession/cancel/onChatSessionUpdated; conversation.ts keeps messagesLoading/loadMessages/setActiveConversation/sortMessagesInPlace; task.ts keeps the task.updated queue drain and loses onTaskStreamEvent/compactTask/fetchContextUsage
    - rpc.ts dispatch + App.vue wiring handle only the kept pushes (task.updated, workflow.reloaded, code.ref, chatSession.updated, lsp.install.line); the A2 decision (07-01) is reflected (drop → no stream.error handler)
    - rpc-types.ts no longer exports the custom stream protocol types or the dead push members (StreamEvent, StreamEventType, StreamError, stream.event, stream.error, message.new); git grep of those terms returns zero in src/ and e2e/
    - Playwright tripwire + the full e2e/ui suite stay green after the strips (517/0 baseline)
  artifacts:
    - 19 Group A components + stream-tree.ts + pairToolMessages/buildDisplayItems/useTypewriter/draft + their tests deleted
    - Stores/App.vue/rpc.ts stripped per Pattern 1 Group D + Pattern 5
    - mock-ws.ts keeps push + pushChatSessionUpdated; pushStreamEvent/pushDone/pushSessionDone/pushNewMessage removed
    - New session-status spec assertion: session drawer flips running→idle via the chatSession.updated push with no stream event (VALIDATION.md wave-0 item)
  key_links:
    - Group B protection: FileDiff.vue + ReadView.vue must survive (FileChangesRenderer live imports) — the CONTEXT D-01 list error (Pitfall 1)
    - draft.ts deletion ⇢ chat.ts AND task.ts draftStore strips must land first (both stores import useDraftStore)
    - PushMessage trim ⇢ all consumers stripped in this plan BEFORE the type removal task (mock-ws.ts, fixtures/index.ts:72, interview-me.spec.ts:541, stores, rpc.ts, App.vue)
    - e2e spec header comments mentioning the dead protocol (stream-reactivity.spec.ts:4, timeline-pipeline.spec.ts:9) must be cleaned — they trip the D-07 grep gate
---

<objective>
Delete the verified-dead legacy chat stack (D-01) and strip the live frontend surfaces that served the custom stream protocol, so the user no longer sees any trimmed feature and the protocol types can be removed with zero stragglers.

**As a** user, **I want to** the app to contain no remnants of the old chat UI or the trimmed features (usage ring, transition cards, review cards, ask_user/shell approval prompts, drafts), **so that** the deletion is invisible — the new CopilotKit chat keeps working with no dead code behind it.

Purpose: this is the user-visible half of the deletion. Group A components (19 files) are a dead island (Phase 6 retire evidence), Group C mainview modules serve only them, and the live stores (chat/conversation/task) still carry streaming-layer code that consumes the dying protocol. The plan strips consumers first, then removes the protocol types (StreamEvent/StreamError/PushMessage members) — type-removal LAST so tsc proves every straggler is gone. It also strips the e2e fixture helpers and lands the session-status replacement spec (the new chatSession.updated-driven completion flow from 07-01).
Output: dead stack gone, stores lean, protocol types gone from the shared contract, e2e suite green.
</objective>

## Artifacts this phase produces

- Deleted: 19 Group A components (ChatSidebar, ConversationPanel, ConversationInput, ChatEditor, ConversationBody, MessageBubble, StreamBlockNode, SubagentBlock, ToolCallBlock, ToolCallGroup, ReasoningBubble, TransitionEventCard, CodeReviewCard, InlineChipText, McpToolsPopover, ContextPopover, AskUserPrompt, ShellApprovalPrompt, DecisionRequest) + src/shared/stream-tree.ts, utils/pairToolMessages.ts, utils/buildDisplayItems.ts, composables/useTypewriter.ts, stores/draft.ts + 5 test files
- KEPT: FileDiff.vue, ReadView.vue (live FileChangesRenderer imports), stores/chat.ts + conversation.ts + task.ts stripped to live surfaces
- `src/mainview/rpc.ts` — stream.error/stream.event/message.new registrations + dispatch cases removed
- `src/mainview/App.vue` — stream wiring removed per A2; task.updated/code.ref/chatSession.updated/onWsReconnect kept
- `src/shared/rpc-types.ts` — StreamEventType/StreamEvent/StreamError deleted; PushMessage keeps task.updated, workflow.reloaded, code.ref, chatSession.updated, lsp.install.line
- `e2e/ui/fixtures/mock-ws.ts` — pushStreamEvent/pushDone/pushSessionDone/pushNewMessage removed; push + pushChatSessionUpdated kept
- New session-status replacement spec coverage (chat-session-drawer: running→idle via chatSession.updated, no stream event)

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-cleanup-feature-trim/07-RESEARCH.md
@.planning/phases/07-cleanup-feature-trim/07-PATTERNS.md
@src/mainview/stores/chat.ts
@src/mainview/stores/conversation.ts
@src/mainview/rpc.ts
@src/mainview/App.vue
@e2e/ui/fixtures/mock-ws.ts
</context>

<tasks>

<!-- planner-discipline-allow: StreamEvent -->
<!-- planner-discipline-allow: StreamEventType -->
<!-- planner-discipline-allow: StreamError -->
<!-- planner-discipline-allow: StreamBlock -->
<!-- planner-discipline-allow: stream.event -->
<!-- planner-discipline-allow: stream.error -->
<!-- planner-discipline-allow: message.new -->
<!-- planner-discipline-allow: ConversationPanel -->
<!-- planner-discipline-allow: ConversationInput -->
<!-- planner-discipline-allow: ChatEditor -->
<!-- planner-discipline-allow: MessageBubble -->
<!-- planner-discipline-allow: StreamBlockNode -->
<!-- planner-discipline-allow: ToolCallBlock -->
<!-- planner-discipline-allow: ToolCallGroup -->
<!-- planner-discipline-allow: draftStore -->
<!-- planner-discipline-allow: useDraftStore -->
<!-- planner-discipline-allow: pairToolMessages -->
<!-- planner-discipline-allow: buildDisplayItems -->
<!-- planner-discipline-allow: useTypewriter -->
<!-- planner-discipline-allow: stream-tree -->

<task type="auto" tdd="false">
  <name>Task 1: Dead legacy chat stack deletion (Group A components + Group C mainview modules, D-01)</name>
  <files>src/mainview/components/ChatSidebar.vue, src/mainview/components/ConversationPanel.vue, src/mainview/components/ConversationInput.vue, src/mainview/components/ChatEditor.vue, src/mainview/components/ConversationBody.vue, src/mainview/components/MessageBubble.vue, src/mainview/components/StreamBlockNode.vue, src/mainview/components/SubagentBlock.vue, src/mainview/components/ToolCallBlock.vue, src/mainview/components/ToolCallGroup.vue, src/mainview/components/ReasoningBubble.vue, src/mainview/components/TransitionEventCard.vue, src/mainview/components/CodeReviewCard.vue, src/mainview/components/InlineChipText.vue, src/mainview/components/McpToolsPopover.vue, src/mainview/components/ContextPopover.vue, src/mainview/components/AskUserPrompt.vue, src/mainview/components/ShellApprovalPrompt.vue, src/mainview/components/DecisionRequest.vue, src/shared/stream-tree.ts, src/mainview/utils/pairToolMessages.ts, src/mainview/utils/buildDisplayItems.ts, src/mainview/composables/useTypewriter.ts, src/bun/test/stream-tree-scenarios.test.ts, src/mainview/utils/pairToolMessages.test.ts, src/mainview/utils/buildDisplayItems.test.ts</files>
  <read_first>
    - 07-RESEARCH.md Pattern 1 (the verified deletion inventory — Group A table with per-file importer evidence, Group B keep-list, Group C module evidence)
    - 07-PATTERNS.md "Deletion inventory" section
    - .planning/phases/06-e2e-migration-verification/06-SUMMARY.md (the Phase 6 retire evidence this inventory was verified against)
  </read_first>
  <action>
    Delete the 19 Group A components (ChatSidebar, ConversationPanel, ConversationInput, ChatEditor, ConversationBody, MessageBubble, StreamBlockNode, SubagentBlock, ToolCallBlock, ToolCallGroup, ReasoningBubble, TransitionEventCard, CodeReviewCard, InlineChipText, McpToolsPopover, ContextPopover, AskUserPrompt, ShellApprovalPrompt, DecisionRequest) and the Group C mainview modules (src/shared/stream-tree.ts, utils/pairToolMessages.ts, utils/buildDisplayItems.ts, composables/useTypewriter.ts) with their tests (stream-tree-scenarios.test.ts, pairToolMessages.test.ts, buildDisplayItems.test.ts).

    BEFORE each deletion, re-run the zero-importer grep proof (Pattern 1 snippet): `rg -n "ComponentName" src/mainview --glob '*.vue' --glob '*.ts'` — expect matches ONLY within the dead group or nothing; `rg -ln "stream-tree" src e2e` — expect only the deleted test. Record each proof in the commit message (Phase 6 retire-with-rationale pattern).

    DO NOT DELETE (Group B — CONTEXT D-01 list error, Pitfall 1): src/mainview/components/FileDiff.vue and ReadView.vue — LIVE importers chat/tool-call-renderers/FileChangesRenderer.vue:34-35. They stay untouched.

    Draft.ts is NOT in this task — stores/chat.ts + stores/task.ts still import useDraftStore until Task 2 strips them; draft.ts + draft.test.ts die there.
  </action>
  <acceptance_criteria>
    - All 19 Group A components and the 4 Group C modules + 3 test files are deleted (files absent from disk)
    - FileDiff.vue + ReadView.vue still exist; FileChangesRenderer.vue still imports them
    - `rg -n "ConversationPanel|ConversationInput|ChatEditor|MessageBubble|StreamBlockNode" src/mainview --glob '*.vue' --glob '*.ts'` returns zero
    - `rg -ln "stream-tree|pairToolMessages|buildDisplayItems|useTypewriter" src e2e` returns zero (draft.ts allowed to remain until Task 2)
    - `bun run typecheck` passes (no live importer broke)
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>The dead chat stack is gone with per-file grep proofs; FileDiff/ReadView survive; typecheck is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Live store strips (Group D) + rpc.ts + App.vue wiring strips + draft deletion</name>
  <files>src/mainview/stores/chat.ts, src/mainview/stores/conversation.ts, src/mainview/stores/task.ts, src/mainview/stores/draft.ts, src/mainview/rpc.ts, src/mainview/App.vue, src/mainview/stores/chat.test.ts, src/mainview/stores/conversation.test.ts, src/mainview/stores/task.test.ts, src/mainview/stores/dispatch.test.ts, src/mainview/stores/draft.test.ts</files>
  <read_first>
    - src/mainview/stores/chat.ts (KEEP onChatSessionUpdated :43-70 incl. the running→idle drain guard :62-69 which now fires from the NEW backend push; STRIP onChatStreamEvent :72-92, onChatNewMessage :94-109, queue machinery :20-22/:242-301, suppressDrainIds, useDraftStore :9,:14,:224, sendMessage's message.new comment :190-191, StreamEvent import :6)
    - src/mainview/stores/conversation.ts (KEEP messagesLoading/loadMessages :139-174, setActiveConversation :118-130, sortMessagesInPlace :114-116, loadOlderMessages :176-193; STRIP StreamBlock/ConversationStreamState :29-46, streamStates/contextUsageByConversation :99-111, fetchContextUsage :229-236, onStreamError :238-251, onStreamEvent :253-408, onNewMessage :410-426, removeScopedLiveBlocks :55-90, tryParseJson/extractToolResultText :6-27, stream-state pruning in loadMessages/refreshLatestPage, refreshLatestPage if unreferenced after)
    - src/mainview/stores/task.ts (STRIP onTaskStreamEvent :450-460, compactTask :359-363, fetchContextUsage :262-266 + callers :198,:228 + exports; KEEP the task.updated queue drain :231-233; also remove the useDraftStore import :10,:15 and the draftStore.clear call :330)
    - src/mainview/rpc.ts (STRIP onStreamError/onStreamEventMessage/onNewMessage exports :53-56 + fields :41-44 + ws dispatch cases :91-92,:94; KEEP task.updated/workflow.reloaded/code.ref/chatSession.updated/lsp.install.line :93,:95-102 + onWsReconnect + install-line listeners)
    - src/mainview/App.vue (STRIP per the 07-01 A2 decision: DROP → remove onStreamError handler :49-60, onStreamEventMessage block :62-66, onNewMessage block :72-75, and their imports :19; KEEP onTaskUpdated/onCodeRef/onChatSessionUpdated/onWsReconnect wiring :68-81)
  </read_first>
  <action>
    Strip chat.ts: remove useDraftStore import + draftStore field + the archiveSession draftStore.clear (:224); remove sessionQueues/suppressDrainIds/enqueueMessage/dequeueMessage/startEdit/confirmEdit/cancelEdit/takeQueue/drainSessionQueue and their exports; remove onChatStreamEvent + onChatNewMessage entirely; in onChatSessionUpdated, the drain guard (:62-69) references removed functions — the KEEP semantics are: mark-unread on idle/waiting_user (unchanged) and the running→idle transition simply updates status (queue draining is gone with the queue). Remove the sendMessage comment about the message.new push. selectSession: remove the fetchContextUsage call (:157). Remove the StreamEvent import; keep QueuedMessage/QueueState imports ONLY if still referenced (they should not be — verify and drop).

    Strip conversation.ts: delete the local StreamBlock/ConversationStreamState interfaces, streamStates/contextUsageByConversation state, removeScopedLiveBlocks + tryParseJson + extractToolResultText helpers, fetchContextUsage, onStreamError, onStreamEvent, onNewMessage, and the stream-state pruning inside loadMessages/refreshLatestPage; delete refreshLatestPage if it becomes unreferenced; drop the StreamError/StreamEvent/StreamEventType imports (:4). Keep the messages list + loading gate + sort + setActive + loadOlder (frozen reads stay — conversations.getMessages RPC survives).

    Strip task.ts: remove onTaskStreamEvent, compactTask, fetchContextUsage + their callers/exports; remove useDraftStore import + draftStore field + the :330 clear. KEEP the task.updated push handler + queue drain (:231-233) — the live board path.

    Delete src/mainview/stores/draft.ts + draft.test.ts (grep useDraftStore importers first: expect exactly chat.ts + task.ts, both stripped in this task).

    rpc.ts: remove the three push registrations + fields + dispatch cases per the read_first list; keep api(), onWorkflowReloaded, onCodeRef, onChatSessionUpdated, onWsReconnect, onInstallLine, connectWs, decisions/notes helpers.

    App.vue: per the A2 outcome (07-01 Task 3 checkpoint): DROP → remove the onStreamError handler, onStreamEventMessage block, onNewMessage block + imports. KEEP toastForActivity/onTaskUpdated/onCodeRef/onChatSessionUpdated/onWsReconnect + boot sequence.

    Rework the store tests to mirror the strips: chat.test.ts (delete queue/live-block/stream-push coverage — keep sessions CRUD + onChatSessionUpdated + selectSession), conversation.test.ts (delete the makeStreamEvent helpers + live-block suite; keep loading gate + sort + frozen-read behavior), task.test.ts (delete onTaskStreamEvent/compact/contextUsage coverage), dispatch.test.ts (delete onStreamEvent/onTaskStreamEvent dispatch coverage; keep the kept-push dispatch). These are reworks, not wholesale deletions — the stores' kept surfaces still need tests.
  </action>
  <acceptance_criteria>
    - chat.ts/conversation.ts/task.ts compile with no references to: useDraftStore, sessionQueues, suppressDrainIds, enqueueMessage, dequeueMessage, startEdit, confirmEdit, cancelEdit, takeQueue, drainSessionQueue, onChatStreamEvent, onChatNewMessage, onStreamEvent, onStreamError, onNewMessage, fetchContextUsage, compactTask, onTaskStreamEvent, StreamBlock, ConversationStreamState, refreshLatestPage (if unreferenced)
    - draft.ts + draft.test.ts deleted; `rg -n "draftStore|useDraftStore|stores/draft" src/mainview --glob '*.ts' --glob '*.vue'` returns zero
    - rpc.ts has no onStreamError/onStreamEventMessage/onNewMessage exports or dispatch cases; App.vue has no stream-error/stream-event/new-message wiring (per A2)
    - `bun test src/mainview/stores --timeout 20000` passes (reworked suites) and `bun run typecheck` is clean
  </acceptance_criteria>
  <verify>
    <automated>bun test src/mainview/stores --timeout 20000</automated>
  </verify>
  <done>The stores/rpc/App.vue expose only live surfaces, draft is deleted, and the frontend unit suites are green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Protocol type removal (PushMessage/StreamEvent/StreamError) + e2e fixture strips + session-status replacement spec</name>
  <files>src/shared/rpc-types.ts, e2e/ui/fixtures/mock-ws.ts, e2e/ui/fixtures/index.ts, e2e/ui/interview-me.spec.ts, e2e/ui/stream-reactivity.spec.ts, e2e/ui/timeline-pipeline.spec.ts, e2e/ui/chat-session-drawer.spec.ts</files>
  <read_first>
    - src/shared/rpc-types.ts (StreamEventType :619-631, StreamEvent :633-645, StreamError :647-652, PushMessage :1196-1204)
    - e2e/ui/fixtures/mock-ws.ts (pushStreamEvent :58-61, pushDone :63-78, pushSessionDone :80-95, pushNewMessage :102-105, pushChatSessionUpdated :97-100, header comment :9-15)
    - e2e/ui/fixtures/index.ts (:72 getStreamEvents mock)
    - e2e/ui/interview-me.spec.ts (:541 StreamEvent-typed pushStreamEvent call)
    - e2e/ui/chat-session-drawer.spec.ts (the status-flip coverage — pushChatSessionUpdated pattern at :262-270,:390-410)
  </read_first>
  <action>
    e2e fixture strips: mock-ws.ts — delete pushStreamEvent, pushDone, pushSessionDone, pushNewMessage and the StreamEvent import; KEEP push (:49-56) + pushChatSessionUpdated (:97-100) (the session-status replacement fixture helper) + disconnect/nextMessage/install; update the header comment (:9-15) to drop the deleted-helper references. fixtures/index.ts — remove the getStreamEvents mock (:72) and any StreamEvent-typed fixture helpers (grep the file for the protocol terms). interview-me.spec.ts — remove the pushStreamEvent call at :541 (the spec's interrupt coverage uses the canonical mock-agui fixtures; if the removed line was load-bearing for a test, rework that test against the interrupt payload fixtures instead). Clean the protocol references in the header comments of stream-reactivity.spec.ts (:4) and timeline-pipeline.spec.ts (:9) — these trip the D-07 git grep gate even as comments.

    Type removal (LAST — every consumer is stripped): remove StreamEventType, StreamEvent, and StreamError from src/shared/rpc-types.ts; remove the stream.event, stream.error, and message.new members from PushMessage (:1197-1198,:1200) — KEEP task.updated, workflow.reloaded, code.ref, chatSession.updated, lsp.install.line. Run `bun run typecheck` and fix any straggler (a missed consumer surfaces here — that is the point of the ordering).

    Session-status replacement spec (VALIDATION.md wave-0 item): add/extend coverage in e2e/ui/chat-session-drawer.spec.ts asserting a session drawer's status flips running → idle via the chatSession.updated push with NO stream event involved (the ws.pushChatSessionUpdated pattern at :262-270 is the template; mock-agui drives the chat, the WS push drives the sidebar). If an existing test already covers the flip (chat-sidebar.spec.ts CS status-dot tests), add a focused assertion documenting the no-stream-event contract instead of duplicating.

    Wave gate: `bun run build` + `bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts` tripwire + `bun test e2e/api --timeout 30000` + `bun run typecheck`; then the full `bunx playwright test e2e/ui` suite (517/0 baseline must hold).
  </action>
  <acceptance_criteria>
    - rpc-types.ts has no StreamEventType/StreamEvent/StreamError and PushMessage has no stream.event/stream.error/message.new members
    - mock-ws.ts exposes only push + pushChatSessionUpdated (plus install/disconnect/nextMessage); `rg -n "pushStreamEvent|pushDone|pushSessionDone|pushNewMessage" e2e` returns zero
    - `git grep -n "StreamEvent\|StreamEventType\|StreamError\|stream\.event\|stream\.error\|message\.new\|stream-tree" src e2e` returns zero
    - `bun run typecheck` clean; `bunx playwright test e2e/ui --timeout 30000` — hmm, run via the project script: `bun run test:e2e` after `bun run build` — full suite green (517 pass / 8 skip / 0 fail baseline holds)
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>The protocol types are gone from the shared contract, e2e fixtures match, the grep gate terms are zero across src and e2e, and the full Playwright suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /ws push channel → stores | only kept push types cross this boundary after the strip |
| rpc.ts dispatch → store handlers | dispatch cases removed for the dead pushes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-20 | Tampering | Dead-component deletion (wrong-file risk) | high | mitigate | Per-file zero-importer grep proofs BEFORE each deletion (Phase 6 retire-with-rationale pattern); Group B FileDiff/ReadView explicitly protected (Pitfall 1 — CONTEXT list error); typecheck gate after deletion |
| T-07-21 | Tampering | Zombie protocol types in rpc-types | medium | mitigate | Consumer-strips-first ordering; protocol type removal LAST in this plan; git grep gate (zero across src + e2e) enforces completeness |
| T-07-22 | Spoofing | chatSession.updated push handling | low | accept | Same-origin /ws, local single-user app — unchanged trust from today's kept pushes |
| T-07-23 | DoS | Store queue machinery remnants | low | mitigate | Queue/draft machinery deleted with its only consumers (dead ConversationInput); no live caller remains |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package operations this phase — no supply-chain surface |
</threat_model>

<verification>
- Per-task: frontend unit suites + typecheck (verify fields) — no `-x` flag anywhere
- Task 3 closes the wave: build + tripwire + full e2e/ui (517/0 baseline) + e2e/api + typecheck
- Grep gates: dead-component terms, protocol terms, fixture helper terms all zero
</verification>

<success_criteria>
- 19 dead components + 4 dead modules + draft + protocol types deleted with grep proofs
- Stores/rpc/App.vue expose only live surfaces; draft gone; fixture helpers match the kept pushes
- Protocol grep terms zero across src and e2e; full Playwright suite green (517/0)
</success_criteria>

<output>
Create `.planning/phases/07-cleanup-feature-trim/07-03-SUMMARY.md` when done
</output>
