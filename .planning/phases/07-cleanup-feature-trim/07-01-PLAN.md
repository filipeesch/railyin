---
phase: 07-cleanup-feature-trim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bun/engine/types.ts
  - src/bun/engine/stream/stream-processor.ts
  - src/bun/engine/coordinator.ts
  - src/bun/engine/orchestrator.ts
  - src/bun/engine/stream/raw-message-buffer.ts
  - src/bun/conversation/conv-message-buffer.ts
  - src/bun/pipeline/write-buffer.ts
  - src/bun/server/stream-processor.ts
  - src/bun/pipeline/stream-event-enricher.ts
  - src/bun/db/stream-events.ts
  - src/bun/conversation/messages.ts
  - src/bun/engine/pi/compaction-coordinator.ts
  - src/bun/conversation/context.ts
  - src/bun/server/notifications.ts
  - src/bun/index.ts
  - src/shared/rpc-types.ts
  - src/bun/handlers/tasks.ts
  - src/bun/handlers/chat-sessions.ts
  - src/bun/handlers/engine.ts
  - src/bun/engine/execution/chat-executor.ts
  - src/bun/engine/execution/human-turn-executor.ts
  - src/bun/engine/execution/retry-executor.ts
  - src/bun/engine/execution/code-review-executor.ts
  - src/bun/engine/execution/transition-executor.ts
  - src/bun/jobs/retention-job.ts
  - src/bun/test/execution-seam.test.ts
  - src/bun/test/stream-processor.test.ts
  - src/bun/test/orchestrator.test.ts
  - src/bun/test/server/shutdown.test.ts
  - src/bun/test/server/notifications.test.ts
  - src/bun/test/chat-executor.test.ts
  - src/bun/test/human-turn-executor.test.ts
  - src/bun/test/retry-executor.test.ts
  - src/bun/test/transition-executor.test.ts
  - src/bun/test/code-review-executor.test.ts
  - src/bun/test/executor-test-helpers.ts
  - src/bun/test/conv-message-buffer.test.ts
  - src/bun/test/raw-message-buffer.test.ts
  - src/bun/test/write-buffer.test.ts
  - src/bun/test/stream-event-enricher.test.ts
  - src/bun/test/server/stream-processor.test.ts
  - src/bun/test/stream-pipeline-scenarios.test.ts
  - src/bun/test/engine-handlers.test.ts
  - e2e/api/smoke.test.ts
autonomous: false
requirements: [TRIM-shell_approval, TRIM-ask_user, TRIM-status/status_chunk, TRIM-usage display, TRIM-compaction_summary, TRIM-file_diff]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A run (task or session) completes with the DB lifecycle triad updating exactly as before: tasks.execution_state / chat_sessions.status / executions.status transitions preserved on running, done, error, cancelled, waiting_user paths
    - No code path writes conversation_messages, stream_events, or model_raw_messages during runs (grep gate: the three INSERT statements absent from src/bun outside tests and migrations)
    - The session sidebar flips running → idle on run completion via the chatSession.updated push (consume() fires onSessionStatusChange; notifications.notifyChatSessionUpdated already exists — only the call-site is new)
    - sendMessage RPCs (tasks.sendMessage, chatSessions.sendMessage, code-review submit, retry) return { executionId } without a message object; every consumer updated (tsc clean)
    - markClaudeExecution double-broadcast hack is gone (D-03); the AG-UI bridge tap remains the single translation path
    - The RPC response contract + smoke test surface are green on the new contract (e2e/api/smoke.test.ts passes after rework)
  artifacts:
    - src/bun/engine/stream/stream-processor.ts rewritten: no ConvMessageBuffer, no onStreamEvent, no rawBuffer, no markClaudeExecution, no trimmed cases, no _emitFileDiffFromWrittenFiles/_flushAccumulators/_appendPromptMessage/_pauseExecution; DB triad + onEngineEvent tap + finally block intact; opts.onSessionStatusChange wired in coordinator.ts ChatTurnOpts
    - Deleted modules: raw-message-buffer.ts, conv-message-buffer.ts, server/stream-processor.ts, stream-event-enricher.ts, db/stream-events.ts, write-buffer.ts (WaitFn relocated), conversation/messages.ts
    - orchestrator.ts: setOnStreamEvent, markClaudeExecution, respondShellApprovalByExecution, compactTask, compactConversation, onNewMessage field, onRawMessageEnqueued ctor arg all removed; new onSessionStatusChange ctor arg present
    - index.ts: all StreamEventProcessor wiring gone; notifyNewMessage unbind gone; session-status push bound to the orchestrator callback
    - e2e/api/smoke.test.ts reworked: no conversation_messages-based assistant assertions; execution-status polling + frozen-table proof
  key_links:
    - consume() done/error/abort/catch/decision paths → onSessionStatusChange → orchestrator → notifier.notifyChatSessionUpdated (Pitfall 2: without this the session drawer sticks on running)
    - opts.onEngineEvent tap ordering in consume() (BRDG-01) must stay FIRST in the event loop — the AG-UI bridge is the single translation path
    - RPC response contract: executor return shapes → handler return shapes → RailynAPI types → smoke.test.ts assertions
    - engine/types.ts EngineEvent trim → tsc-enumerated stragglers in engine emitters (fixed in 07-02, same wave)
---

<objective>
Surgical rewrite of the execution engine's write paths so runs stop writing to the frozen SQLite chat tables (D-05), while the board/session lifecycle, the AG-UI bridge feed, and the session-status push keep working (D-04, D-03).

**As a** user running a task or chat session, **I want to** the run to complete with statuses updating exactly as before, **so that** the frozen-table guarantee (zero new writes) holds with no visible regression — the session drawer still flips to idle, the board still shows execution states, and failures still surface.

Purpose: consume() in src/bun/engine/stream/stream-processor.ts is the LIVE execution state machine for ALL runs (task, session, and AG-UI). This plan strips its legacy write paths (ConvMessageBuffer → conversation_messages, onStreamEvent → stream_events + /ws push, rawBuffer → model_raw_messages), removes the trimmed EngineEvent cases, deletes the markClaudeExecution hack (D-03), adds the mandatory session-status replacement push, excises executor-level appendMessage writes, and reworks the RPC response contract + smoke suite. Type removals land FIRST so tsc enumerates every straggler (loud failure, shared-contract discipline per AGENTS.md).
Output: a green write-excised execution path proven by reworked unit + smoke tests; companion module deletions; the 07-01 wave gate green.

Planner decisions recorded (the agent's Discretion): (1) D-05 scope = Option B (broad): ALL conversation_messages writes stop — appendMessage (conversation/messages.ts:13-18) is a 4th writer and dies; sendMessage responses become { executionId } without the message field; smoke tests poll execution status instead of getMessages. Rationale: success criterion 2 says "ZERO new writes" — a carve-out would leave a permanent INSERT in a frozen table and a grep-gate exception. (2) model_raw_messages writes stop with the rest (research OQ3 recommendation; table stays frozen; retention job keeps pruning old rows). (3) The session-status replacement fires onSessionStatusChange wherever consume() writes chat_sessions status AND in the decision_request case (which today also needs a chat_sessions waiting_user write for taskId==null to preserve the sidebar state the message.new push used to provide — verify against stores/chat.ts:94-109 behavior before/after).
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup-feature-trim/07-RESEARCH.md
@.planning/phases/07-cleanup-feature-trim/07-PATTERNS.md
@.planning/phases/06-e2e-migration-verification/06-SUMMARY.md
@src/bun/engine/stream/stream-processor.ts
@src/bun/engine/orchestrator.ts
@src/bun/engine/coordinator.ts
@src/bun/index.ts
@src/bun/server/notifications.ts
@e2e/api/smoke.test.ts
</context>

<tasks>

<!-- planner-discipline-allow: INSERT INTO conversation_messages -->
<!-- planner-discipline-allow: INSERT INTO stream_events -->
<!-- planner-discipline-allow: INSERT INTO model_raw_messages -->
<!-- planner-discipline-allow: markClaudeExecution -->
<!-- planner-discipline-allow: StreamEvent -->
<!-- planner-discipline-allow: StreamEventType -->
<!-- planner-discipline-allow: stream.event -->
<!-- planner-discipline-allow: OnStreamEvent -->
<!-- planner-discipline-allow: OnNewMessage -->
<!-- planner-discipline-allow: EngineResumeInput -->
<!-- planner-discipline-allow: shell_approval -->
<!-- planner-discipline-allow: ask_user -->
<!-- planner-discipline-allow: compaction_start -->
<!-- planner-discipline-allow: compaction_done -->
<!-- planner-discipline-allow: new_message -->
<!-- planner-discipline-allow: writtenFiles -->
<!-- planner-discipline-allow: input_tokens -->

<task type="tracer" tdd="false">
  <name>Task 1: EngineEvent trim + consume() surgical rewrite + session-status callback (the zero-write execution state machine)</name>
  <files>src/bun/engine/types.ts, src/bun/engine/stream/stream-processor.ts, src/bun/engine/coordinator.ts, src/bun/test/execution-seam.test.ts, src/bun/test/stream-processor.test.ts</files>
  <read_first>
    - src/bun/engine/stream/stream-processor.ts (full 711-line read — every remove/keep decision below is line-anchored)
    - src/bun/engine/types.ts lines 20-62 (EngineEvent / OnToken / OnError / OnTaskUpdated / OnNewMessage / OnStreamEvent / EngineResumeInput / ExecutionParams.onRawModelMessage)
    - src/bun/engine/coordinator.ts (ChatTurnOpts + ExecutionCoordinator)
    - src/bun/test/execution-seam.test.ts (the seam contract tests that must be reworked, not deleted)
  </read_first>
  <action>
    Remove the trimmed members from the EngineEvent union in engine/types.ts: ask_user (:39), shell_approval (:41), status (:42), usage (:43), new_message (:45), compaction_start (:46), compaction_done (:47). KEEP token, reasoning, tool_start, tool_result, subagent_start, subagent_stop, decision_request, task_updated, done, error. Delete the EngineResumeInput type (:60-62) entirely (the only resume channel becomes decision_request via AG-UI forwardedProps). Delete the OnStreamEvent (:58) and OnNewMessage (:57) type aliases (OnError :55 and OnTaskUpdated :56 stay). Also remove the `onRawModelMessage` field from ExecutionParams (it feeds makePersistCallback, which dies — the model_raw_messages write). Do NOT remove `writtenFiles` from tool_result yet (07-02 owns that — zero consumers post-rewrite; removing it here would overlap 07-02's file set). After the trim, run `bun run typecheck` and RECORD the straggler file list — the remaining errors must be confined to: engine emitters (claude adapter/engine/events, opencode engine/adapter/event-translator, copilot/events, cursor/translate-events, copilotkit/event-bridge), orchestrator.ts (setOnStreamEvent/markClaudeExecution/respondShellApprovalByExecution — removed in Task 2), index.ts (wiring — Task 2), and stores (07-03). Fix NOTHING outside this task's file set; the wave gate tolerates the enumerated red surface until 07-02 lands.

    Rewrite consume() in engine/stream/stream-processor.ts per the surgical strip (Pattern 2). REMOVE: the ConvMessageBuffer instantiation (:153) and every convBuffer.enqueue/flush/onNewMessage call across all cases; every this.onStreamEvent?.(...) call (the enumerated sites at :202, :217, :226, :237, :244, :262, :272, :289, :295, :328, :341, :376, :403-418, :426, :431, :449, :467, :502, :507, :519, :572, :588, :638, :642, :695-707); the whole case blocks for status (:242-248), usage (:397-421 — INCLUDING the executions input_tokens/output_tokens write: ContextEstimator falls back to its message-count slow path when tokens are null, and the provider-level token accounting in ai/anthropic.ts stays engine-internal), shell_approval (:477-486), ask_user (:488-492), compaction_start (:525-529), compaction_done (:531-539), new_message (:546-549); the private helpers _flushAccumulators (:627-644), _appendPromptMessage (:646-654), _pauseExecution (:656-668), _emitFileDiffFromWrittenFiles (:670-710); the childToolSeq / childLiveBlockIdByCall / childCallKey live-block dedup machinery (:158-167, :321-327, :370-375) — it only served the dead live protocol; the rawMessageSeq map + HIGH_FREQ_RAW_EVENT_TYPES (:25-29, :45) + makePersistCallback (:105-121); the rawBuffer ctor arg (:54) and its import (:15-16); the claudeExecutionIds set (:50), markClaudeExecution/clearClaudeExecution (:67-75), setOnStreamEvent (:63-65), and the claudeExecutionIds skip logic at the token (:223-227) and reasoning (:234-238) cases (D-03 — the raw-delta broadcast path dies in Task 2); the onNewMessage ctor arg (:58) and the new_message case. KEEP: the DB lifecycle triad — the run-start updates (:176-184), the done case DB updates (:439-447) minus the convBuffer/onStreamEvent lines, the fatal-error case DB updates + onError + abort + onRunEnd (:454-470), the non-fatal error path minus convBuffer (:471-474), the abort paths (:187-205, :557-574) minus convBuffer/onStreamEvent lines, the decision_request case (:494-523) minus convBuffer/onStreamEvent lines — and for taskId == null ADD a chat_sessions waiting_user write + onSessionStatusChange call there (preserves the sidebar state the message.new push used to deliver; verify the pre-trim behavior at stores/chat.ts:94-109 first), the task_updated case (:541-544), the opts.onEngineEvent tap (:210 — MUST stay the first action in the loop, BRDG-01), opts.onRunEnd calls, onToken/onError/onTaskUpdated calls, the token/reasoning accumulators + hadOutput flag, and the ENTIRE finally block (:590-621) minus clearClaudeExecution/rawMessageSeq (keep the pending_messages drain + deferred transition + final task push).

    Add the session-status callback: extend ChatTurnOpts in engine/coordinator.ts with `onSessionStatusChange?: (conversationId: number) => void`. In consume(), fire `opts?.onSessionStatusChange?.(conversationId)` at every site that writes chat_sessions status (run-start 'running' for taskId==null is NOT needed — only completion paths): done case (after the idle write), fatal error case, both abort paths, the catch block, and the decision_request case (after the new waiting_user write). Add the same optional callback to the ExecutionCoordinator interface's executeChatTurn/executeHumanTurn signatures (coordinator.ts) so the orchestrator can inject a default (Task 2 wires it).

    Rework the seam tests (execution-seam.test.ts): the usage-event ordering assertion (test 1 asserts usage flows to onEngineEvent — the fake now must not emit usage; re-assert ordering with token/reasoning/tool events only), the ask_user pause-path tests (4d WR-02: re-drive with decision_request instead — ask_user no longer exists), and any raw-buffer/onNewMessage assertions. Update src/bun/test/stream-processor.test.ts (the engine state-machine tests) the same way: delete StreamEvent-shaped expectations, keep the DB-triad + onRunEnd + cancel assertions, and ADD a test asserting onSessionStatusChange fires on the done path (taskId==null session run) and the decision path.
  </action>
  <acceptance_criteria>
    - engine/types.ts: the EngineEvent union contains no trimmed members; EngineResumeInput, OnStreamEvent, OnNewMessage gone; ExecutionParams has no onRawModelMessage field
    - consume() contains no ConvMessageBuffer references, no onStreamEvent calls, no markClaudeExecution references, no rawBuffer references, and no cases for status/usage/shell_approval/ask_user/compaction_start/compaction_done/new_message
    - consume() still updates tasks.execution_state / chat_sessions.status / executions.status on every lifecycle path (run-start, done, fatal error, abort, catch, decision) and still calls opts.onEngineEvent before any other per-event action, and the finally block still drains pending_messages + deferred transitions
    - coordinator.ts ChatTurnOpts has onSessionStatusChange; consume() fires it on done/error/abort/catch/decision paths
    - `bun test src/bun/test/execution-seam.test.ts src/bun/test/stream-processor.test.ts --timeout 20000` passes (reworked suites)
    - `bun run typecheck` errors, if any, are confined to the enumerated 07-02 files (engine emitters, event-bridge) and the Task-2 wiring files (orchestrator.ts, index.ts)
  </acceptance_criteria>
  <verify>
    <automated>bun test src/bun/test/execution-seam.test.ts src/bun/test/stream-processor.test.ts --timeout 20000</automated>
  </verify>
  <done>consume() runs with zero writes to the three frozen tables, the session-status callback is wired into the terminal paths, the seam tests are green, and the remaining tsc errors are the enumerated 07-02/07-03 stragglers.</done>
  <reversibility rating="costly">The largest single edit of the phase (711-line state machine strip); git-recoverable but a careful executor must keep the DB triad + finally block intact — no migration, no data change, so not one-way.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Writer-module deletions + orchestrator/index/notifications wiring + session-status push binding</name>
  <files>src/bun/engine/stream/raw-message-buffer.ts, src/bun/conversation/conv-message-buffer.ts, src/bun/pipeline/write-buffer.ts, src/bun/server/stream-processor.ts, src/bun/pipeline/stream-event-enricher.ts, src/bun/db/stream-events.ts, src/bun/engine/orchestrator.ts, src/bun/index.ts, src/bun/server/notifications.ts, src/bun/jobs/retention-job.ts, src/bun/test/orchestrator.test.ts, src/bun/test/server/shutdown.test.ts, src/bun/test/server/notifications.test.ts, src/bun/test/conv-message-buffer.test.ts, src/bun/test/raw-message-buffer.test.ts, src/bun/test/write-buffer.test.ts, src/bun/test/stream-event-enricher.test.ts, src/bun/test/server/stream-processor.test.ts, src/bun/test/stream-pipeline-scenarios.test.ts</files>
  <read_first>
    - src/bun/index.ts lines 53-68 (imports), 138-146 (channel/notifier/streamProc), 225-244 (engine factories + orchestrator wiring), 341-343 (handler map), 468 (auto-archive job)
    - src/bun/engine/orchestrator.ts lines 54-133 (ctor + executor instantiation), 180-182 (markClaudeExecution), 270-355 (respondShellApprovalByExecution + compactTask + compactConversation)
    - src/bun/server/notifications.ts (whole 35-line file — notifyChatSessionUpdated at :28-30 KEEPS, notifyNewMessage :20-22 dies)
    - src/bun/jobs/retention-job.ts (WaitFn type import at :2)
    - src/bun/pipeline/write-buffer.ts (WaitFn export)
  </read_first>
  <action>
    Delete the writer modules and their test files: src/bun/engine/stream/raw-message-buffer.ts + raw-message-buffer.test.ts, src/bun/conversation/conv-message-buffer.ts + conv-message-buffer.test.ts, src/bun/server/stream-processor.ts + src/bun/test/server/stream-processor.test.ts, src/bun/pipeline/stream-event-enricher.ts + stream-event-enricher.test.ts, src/bun/db/stream-events.ts, src/bun/test/stream-pipeline-scenarios.test.ts. Delete src/bun/pipeline/write-buffer.ts + write-buffer.test.ts and relocate the WaitFn type into src/bun/jobs/retention-job.ts (inline or a local export — retention-job.ts:2 is its only production importer; grep `write-buffer` afterwards and expect zero matches in src/ outside deleted test references).

    Orchestrator (src/bun/engine/orchestrator.ts): remove the onRawMessageEnqueued ctor arg (:81) + createRawMessageBuffer import/usage + rawBuffer.start (:42-43, :92-93); remove setOnStreamEvent (:70-72) and markClaudeExecution (:180-182); remove the onNewMessage field (:68, :89) and its StreamProcessor ctor argument (:98); remove respondShellApprovalByExecution (:272-316 — the shell_approval trim; engine.resume with that resume input dies with EngineResumeInput) and compactTask/compactConversation (:318-355 — the manual compaction RPC path; note: engine.compact + cross-engine-context.ts:62 compaction-on-switch STAY live, do not touch the ExecutionEngine.compact interface); pass onTaskUpdated only where still required by executor ctors (code-review-executor keeps it; chat-executor loses onNewMessage at :132). ADD a new optional ctor arg `onSessionStatusChange?: (conversationId: number) => void` and merge a default into the opts passed to chatExecutor.execute and humanTurnExecutor.execute: `opts.onSessionStatusChange ?? this.sessionStatusCb` so the callback fires even when AG-UI callers don't provide one.

    index.ts: remove the StreamEventProcessor import (:57), construction (:142), the 7th Orchestrator ctor arg streamProc.onRawMessageEnqueued (:235), setOnStreamEvent wiring (:239), setMarkClaudeExecution wiring (:241), and streamProc.start() (:244). Remove onNewMessage from the EngineFactory type (:146) and from the engine factory signatures (:149-167 — the ENGINE CLASSES' ctor args are 07-02's scope; only the factory call sites in index.ts change here) and drop notifier.notifyNewMessage.bind from the factory wiring (:229). Bind the session-status push: pass `(conversationId: number) => { const row = db.query(chat_sessions-by-conversation_id); if (row) notifier.notifyChatSessionUpdated(mapSession(row)); }` as the orchestrator's new onSessionStatusChange arg — reuse the existing session mapping used by chatSessionHandlers (handlers/chat-sessions.ts) rather than hand-rolling a mapper.

    notifications.ts: delete notifyNewMessage (:20-22) and its ConversationMessage import. KEEP onError (:7-14) untouched in this plan — its fate is the Task-3 A2 checkpoint decision (drop the push vs new type; if the decision is DROP, the onError body becomes a no-op in Task 3).

    Tests: orchestrator.test.ts — remove StreamEvent-dependent scenarios + markClaudeExecution/rawBuffer/compact/respondShellApproval coverage; shutdown.test.ts — remove the markClaudeExecution stub (:20); notifications.test.ts — remove notifyNewMessage coverage, add notifyChatSessionUpdated coverage if missing.
  </action>
  <acceptance_criteria>
    - The six writer modules and their test files are deleted; `rg -n "write-buffer" src/bun --glob '!**/test/**'` returns zero; retention-job.ts compiles with the relocated WaitFn
    - orchestrator.ts has no references to onRawMessageEnqueued, createRawMessageBuffer, setOnStreamEvent, markClaudeExecution, respondShellApprovalByExecution, compactTask, compactConversation, or onNewMessage; the new onSessionStatusChange ctor arg exists and is merged into executeChatTurn/executeHumanTurn opts
    - index.ts has no references to StreamEventProcessor; the EngineFactory and factories take no onNewMessage; the session-status wrapper is bound to the orchestrator arg
    - notifications.ts has no notifyNewMessage
    - `bun test src/bun/test/orchestrator.test.ts src/bun/test/server/shutdown.test.ts src/bun/test/server/notifications.test.ts --timeout 20000` passes
    - `rg -n "INSERT INTO conversation_messages|INSERT INTO stream_events|INSERT INTO model_raw_messages" src/bun --glob '!**/test/**' --glob '!**/migrations/**'` returns zero (the only remaining writer, appendMessage in conversation/messages.ts, dies in Task 3)
  </acceptance_criteria>
  <verify>
    <automated>bun test src/bun/test/orchestrator.test.ts src/bun/test/server/shutdown.test.ts src/bun/test/server/notifications.test.ts --timeout 20000</automated>
  </verify>
  <done>All six writer modules + their tests are gone, orchestrator/index wiring is trimmed, the session-status push reaches notifier.notifyChatSessionUpdated from run-end paths, and the frozen-table INSERT grep gate is clean except for the Task-3 appendMessage straggler.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Executor write excision + RPC response contract + smoke rework + failure-toast decision (A2)</name>
  <files>src/bun/conversation/messages.ts, src/bun/engine/pi/compaction-coordinator.ts, src/bun/conversation/context.ts, src/bun/engine/execution/chat-executor.ts, src/bun/engine/execution/human-turn-executor.ts, src/bun/engine/execution/retry-executor.ts, src/bun/engine/execution/code-review-executor.ts, src/bun/engine/execution/transition-executor.ts, src/bun/handlers/tasks.ts, src/bun/handlers/chat-sessions.ts, src/bun/handlers/engine.ts, src/shared/rpc-types.ts, src/bun/test/chat-executor.test.ts, src/bun/test/human-turn-executor.test.ts, src/bun/test/retry-executor.test.ts, src/bun/test/transition-executor.test.ts, src/bun/test/code-review-executor.test.ts, src/bun/test/executor-test-helpers.ts, src/bun/test/engine-handlers.test.ts, e2e/api/smoke.test.ts</files>
  <read_first>
    - src/bun/conversation/messages.ts (appendMessage :4-19 — the 4th conversation_messages writer)
    - src/bun/engine/pi/compaction-coordinator.ts (DefaultMessageAppender :24-29 uses appendMessage)
    - src/bun/engine/execution/{chat-executor.ts, human-turn-executor.ts, retry-executor.ts, code-review-executor.ts, transition-executor.ts} (appendMessage call sites + {message, executionId} response builders: chat-executor :50,:108,:119,:194; human-turn-executor :69,:88,:188,:213,:295; retry-executor :67; code-review-executor :146-147,:208; transition-executor :76,:97)
    - src/bun/handlers/tasks.ts + src/bun/handlers/chat-sessions.ts (sendMessage/code-review handler response relay)
    - e2e/api/smoke.test.ts lines 180-403 (the assertions that break)
    - src/bun/engine/stream/stream-processor.ts Task-1 output (the new consume() — this task's edits must not re-introduce writes)
  </read_first>
  <action>
    Delete src/bun/conversation/messages.ts (Option B — zero conversation_messages writes; grep its importers first and expect exactly: chat-executor, human-turn-executor, retry-executor, code-review-executor, transition-executor, pi/compaction-coordinator). Fix every compile straggler per tsc: pi/compaction-coordinator.ts — DefaultMessageAppender.appendCompactionSummary becomes a no-op with a comment (the compaction_summary message type dies; the pi engine's background compaction itself stays live — only the persisted summary row goes away); conversation/context.ts — if its compact helpers (compactMessages/compactConversation used by cursor/engine.ts:134 + review.test.ts) call appendMessage, excise those writes in place (the compaction behavior stays; the summary row no longer persists). Keep context-usage.ts's resolveContextWindow untouched here (07-04 trims estimateConversationContextUsage).

    Executors: remove every appendMessage call; change the return contract from `{ message: ConversationMessage; executionId: number }` to `{ executionId: number }` in chat-executor.ts (sendMessage paths), human-turn-executor.ts, retry-executor.ts, code-review-executor.ts (keep its execution flow + orchestrator.executeCodeReview wiring — Pitfall 4: the executor itself is LIVE via CodeReviewOverlay submit), and transition-executor.ts (keep the executor — column transitions + on_enter_prompt are live board behavior; only the transition_event writes die). Remove the now-unused ConversationMessage imports and the conversation_messages-backed messageId plumbing. Update the RailynAPI response types in src/shared/rpc-types.ts for tasks.sendMessage, chatSessions.sendMessage, chatSessions.submitDecisions (if it returns a message), tasks.retry, and the code-review submit RPC to `{ executionId: number }` — run tsc and fix every consumer (frontend stores call these RPCs but ignore the message field — verify via tsc that no `.message` access remains).

    Handlers: remove the executions.respondShellApproval entry from handlers/engine.ts (its orchestrator method died in Task 2; setShellAutoApprove STAYS — the A3 replacement channel) and rework engine-handlers.test.ts accordingly; update tasks.ts + chat-sessions.ts handlers to relay the new { executionId } response.

    Smoke rework (e2e/api/smoke.test.ts): the ~8 breaking assertions (:212 sent.message.role, :215-222 assistant waitFor via getMessages, :221 user-message content, :257,:281 sent.message.role, :263-269 assistant-count, :287-291 assistant-count + content, :324 sent.messageId, :327-335 assistant waitFor + user content, :375-380,:396-401 assistant content) are rewritten to: (1) assert the new response shape ({ executionId } — no message field), (2) poll execution/task/session status instead of conversation_messages (the existing getTask/getSession waitFor helpers at :224-227,:337-341 are the pattern — wait for executionState 'completed' / session status 'idle'), (3) add a frozen-table proof per conversation test: after the run, conversations.getMessages returns no NEW rows (length unchanged from before sendMessage — the tables receive zero writes). The waitFor helper (:13-29) and the task-alias equivalence assertions (:198-202) stay.

    Then resolve A2 at the checkpoint below, and execute its outcome: if DROP (recommended) — notifications.ts onError becomes a no-op with a comment, and App.vue's stream.error toast wiring is stripped in 07-03 (the config-error sentinel branch is dead code today: rpc.ts has no config.error dispatch case); if KEEP — introduce a new push type + rpc.ts dispatch + App.vue handler per the checkpoint choice.
  </action>
  <acceptance_criteria>
    - conversation/messages.ts deleted; pi/compaction-coordinator DefaultMessageAppender is a no-op; no executor contains an appendMessage call
    - All sendMessage-family RailynAPI responses are `{ executionId: number }`; tsc finds zero `.message` consumers on those responses
    - CodeReviewExecutor and TransitionExecutor still execute (review submit + column transitions + on_enter_prompt flows untouched)
    - `bun test src/bun/engine/execution --timeout 20000` passes (chat/human-turn/retry/transition/code-review executor suites reworked) and `bun test src/bun/test/engine-handlers.test.ts --timeout 20000` passes
    - `bun test e2e/api/smoke.test.ts --timeout 30000` passes with the new contract + frozen-table proofs
    - `rg -n "INSERT INTO conversation_messages|INSERT INTO stream_events|INSERT INTO model_raw_messages" src/bun --glob '!**/test/**' --glob '!**/migrations/**'` returns zero (Task 2 gate now fully clean)
  </acceptance_criteria>
  <verify>
    <automated>bun test e2e/api/smoke.test.ts --timeout 30000</automated>
  </verify>
  <done>The executor layer writes nothing to frozen tables, the RPC contract is { executionId }, the smoke suite is green with frozen-table proofs, and the A2 failure-toast decision is recorded and executed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>A2 — what replaces the "Execution failed" toast (currently fired from the /ws stream.error push at App.vue:58-61)?</decision>
  <context>The custom stream.error push dies with the protocol (PushMessage trim in 07-03). Chat failures already surface via the AG-UI RUN_ERROR event in the chat surface and the board execution_state='failed' badge. The toast only ever fired for taskId != null errors. The config-error sentinel branch (App.vue:51-55) is dead code — rpc.ts's WS dispatch has no config.error case. Research recommendation (OQ1/A2): drop the toast; revisit in v2.</context>
  <options>
    <option id="option-a">
      <name>Drop the toast (recommended)</name>
      <pros>Smallest diff; RUN_ERROR + failed execution_state already cover failure UX; one less custom protocol surface</pros>
      <cons>Background task failures (non-chat) lose the transient banner — user notices via the board badge instead</cons>
    </option>
    <option id="option-b">
      <name>Keep the toast via a new push type</name>
      <pros>Preserves today's exact UX for background failures</pros>
      <cons>New push type + rpc.ts dispatch case + App.vue handler + fixture updates — contradicts the zero-custom-protocol success criterion; the push must be added to PushMessage before the 07-03 trim</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or describe an alternative</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine adapters → consume() | engine-emitted events cross into the execution state machine (trimmed members removed this plan) |
| consume() → DB (frozen tables) | the write paths being excised — the D-05 guarantee |
| consume() → /ws push channel | session-status push (new) vs the dying stream pushes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | conversation_messages / stream_events / model_raw_messages writes | high | mitigate | Writer modules deleted (conv-message-buffer, raw-message-buffer, db/stream-events, messages.ts); INSERT grep gate (zero outside tests/migrations) enforced at Task 2/3 acceptance and the D-07 gate; retention-job stays prune-only |
| T-07-02 | Tampering | Frozen-table integrity via retained helpers | medium | mitigate | appendMessage whole-module deletion (Option B); pi DefaultMessageAppender no-op; context.ts compact helpers excise writes — tsc enumerates every straggler (type-removal-first ordering) |
| T-07-03 | Spoofing | chatSession.updated push source | low | accept | Same-origin /ws, local single-user app — unchanged trust from today's task.updated pushes |
| T-07-04 | Information disclosure | executions.input_tokens read path | low | accept | The usage write stops; ContextEstimator degrades to its message-count slow path (verified fallback at context-estimator.ts:20-47); single-user local app |
| T-07-05 | Tampering | stream.error / message.new zombie push paths | low | mitigate | PushMessage members removed (07-03); notifications.onError resolved by the A2 decision checkpoint (drop → no-op); no half-alive protocol surfaces |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package operations in this phase — no supply-chain surface (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Per-task: targeted unit suites (listed in each task's verify) — no `-x` flag anywhere (bun 1.4.0)
- Wave 1 gate (run after 07-02 lands): `bun run build && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts` tripwire + `bun test e2e/api --timeout 30000` + `bun run typecheck` + `bun test src/bun --timeout 20000` (record the new count — it legitimately drops as pipeline tests are deleted)
- INSERT grep gate: `rg -n "INSERT INTO conversation_messages|INSERT INTO stream_events|INSERT INTO model_raw_messages" src/bun --glob '!**/test/**' --glob '!**/migrations/**'` → zero
- Frozen-table integrity: no migration files touched; tables never dropped (D-04 permanent constraint)
</verification>

<success_criteria>
- consume() + executors write zero rows to conversation_messages / stream_events / model_raw_messages (grep gate clean)
- Session sidebar flips running → idle via the chatSession.updated push on completion (onSessionStatusChange wired end-to-end)
- markClaudeExecution gone (D-03); onEngineEvent remains the single translation path
- sendMessage-family RPCs return { executionId }; smoke.test.ts green with frozen-table proofs
- A2 decision recorded and executed; wave gate green
</success_criteria>

<output>
Create `.planning/phases/07-cleanup-feature-trim/07-01-SUMMARY.md` when done
</output>
