---
phase: 07-cleanup-feature-trim
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bun/engine/claude/adapter.ts
  - src/bun/engine/claude/engine.ts
  - src/bun/engine/claude/events.ts
  - src/bun/engine/claude/bash-permission-gate.ts
  - src/bun/engine/claude/file-state-cache.ts
  - src/bun/engine/opencode/engine.ts
  - src/bun/engine/opencode/adapter.ts
  - src/bun/engine/opencode/event-translator.ts
  - src/bun/engine/copilot/events.ts
  - src/bun/engine/copilot/session.ts
  - src/bun/engine/cursor/translate-events.ts
  - src/bun/engine/execution/execution-params-builder.ts
  - src/bun/copilotkit/event-bridge.ts
  - src/bun/test/claude-adapter.test.ts
  - src/bun/test/claude-events.test.ts
  - src/bun/test/claude-file-diff-integration.test.ts
  - src/bun/test/claude-rpc-scenarios.test.ts
  - src/bun/test/copilot-events.test.ts
  - src/bun/test/copilot-rpc-scenarios.test.ts
  - src/bun/test/bash-permission-gate.test.ts
  - src/bun/test/file-state-cache.test.ts
  - src/bun/test/approved-commands.test.ts
  - src/bun/test/execution-params-builder.test.ts
  - src/bun/test/cursor/rpc-scenarios.test.ts
autonomous: false
requirements: [TRIM-shell_approval, TRIM-ask_user, TRIM-status/status_chunk, TRIM-usage display, TRIM-compaction_summary, TRIM-file_diff]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - No engine adapter emits a trimmed EngineEvent member (ask_user, shell_approval, status, usage, compaction_start, compaction_done, new_message) — tsc is the enumerator (EngineEvent union trimmed in 07-01 Task 1)
    - The bash-permission-gate and file-state-cache modules no longer exist; claude runs with zero interactive shell gate (posture per A3 decision) and zero file-state-diff computation
    - opencode's shell permission path never hangs invisibly: waitForResume is gone, replaced by the A3-chosen posture (recommended: shellAutoApprove auto-approve)
    - writtenFiles is removed from EngineEvent.tool_result and FileChangesRenderer still renders diffs (it derives from tool ARGS via buildDiffPayloadsFromArgs — arg-derived, unaffected)
    - The event-bridge drop-list is simplified to the kept events; decision_request still returns [] (interrupts flow through the registry — do NOT "fix" it)
  artifacts:
    - Deleted: src/bun/engine/claude/bash-permission-gate.ts, src/bun/engine/claude/file-state-cache.ts + their stub tests
    - claude adapter/engine/events.ts: no gate wiring, no FileStateCache instantiation/param, no computeFileDiff feed, no writtenFiles population, no ask_user/shell_approval/status/usage emitters
    - opencode engine/adapter/event-translator.ts: no waitForResume, no ask_user/status/usage emitters, shell posture per A3
    - copilot/events.ts, cursor/translate-events.ts: no trimmed emitters; copilot/session.ts: no onRawModelMessage call sites
    - execution-params-builder.ts: no onRawModelMessage in ExecutionParams construction
    - event-bridge.ts: drop-list reduced to kept events
  key_links:
    - engine/types.ts trim (07-01) → tsc error list = THIS plan's work items (claude/opencode/copilot/cursor + bridge)
    - opencode engine.ts:128-136 waitForResume → A3 checkpoint decision (auto-approve vs deny) — the shell_approval trim's security-relevant behavior change
    - writtenFiles removal → claude/events.ts:200-217 + tools.ts:137-139 + event-bridge consumers all compile-fail until excised; FileChangesRenderer must keep working (arg-derived)
    - A3 decision → whether tasks.setShellAutoApprove RPCs (KEPT) remain the only shell-permission channel
---

<objective>
Trim the trimmed-feature emitters from the five engine adapters and delete the two whole-module gates, so the engine layer compiles clean against the trimmed EngineEvent union (07-01 Task 1) and shell commands never hang invisibly (Pitfall 7).

**As a** user running a task with the claude or opencode engine, **I want to** the run to proceed without invisible approval hangs and without stale diff/usage machinery, **so that** the shell_approval/ask_user/status/usage/compaction trim removes the dead channels entirely (research: today shell_approval is an invisible hang — the bridge already drops it at event-bridge.ts:280-289 and no UI can answer it).

Purpose: engine-side completion of the trim. The EngineEvent union members were removed in 07-01; tsc now enumerates every emitter. This plan fixes claude (gate + cache + file-diff feed), opencode (waitForResume shell posture — human-gated decision), copilot/cursor (status/usage/ask_user emitters), and the event-bridge drop-list. Also removes the ExecutionParams onRawModelMessage plumbing (the model_raw_messages write's engine-side surface, completing 07-01 Task 1's type removal).
Output: engine layer compiles clean; the 07-01+07-02 wave gate green (build + tripwire + e2e/api + typecheck + src/bun).
</objective>

## Artifacts this phase produces

- Deleted modules: `src/bun/engine/claude/bash-permission-gate.ts`, `src/bun/engine/claude/file-state-cache.ts` + their stub tests
- `src/bun/engine/types.ts` — writtenFiles field removed from EngineEvent.tool_result
- `src/bun/engine/claude/{adapter,engine,events}.ts` — gate wiring, FileStateCache feed, computeFileDiff/writtenFiles emission, shell_approval/ask_user/status/usage emitters all removed
- `src/bun/engine/opencode/{engine,adapter,event-translator}.ts` — waitForResume removed; shell posture per A3 (recommended: shellState.shellAutoApprove auto-approve); ask_user emitters removed
- `src/bun/engine/copilot/events.ts`, `src/bun/engine/cursor/translate-events.ts` — trimmed emitters removed
- `src/bun/engine/execution/execution-params-builder.ts` + adapters — onRawModelMessage plumbing removed
- Engine class ctors (copilot/claude/opencode/cursor/pi) — onNewMessage ctor arg removed
- `src/bun/copilotkit/event-bridge.ts` — drop-list reduced to kept events; decision_request registry path untouched
- A3 checkpoint decision record (opencode shell posture)

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-cleanup-feature-trim/07-RESEARCH.md
@.planning/phases/07-cleanup-feature-trim/07-PATTERNS.md
@src/bun/engine/types.ts
@src/bun/copilotkit/event-bridge.ts
</context>

<tasks>

<!-- planner-discipline-allow: shell_approval -->
<!-- planner-discipline-allow: ask_user -->
<!-- planner-discipline-allow: compaction_start -->
<!-- planner-discipline-allow: compaction_done -->
<!-- planner-discipline-allow: new_message -->
<!-- planner-discipline-allow: writtenFiles -->
<!-- planner-discipline-allow: FileStateCache -->
<!-- planner-discipline-allow: BashPermissionGate -->
<!-- planner-discipline-allow: status_chunk -->
<!-- planner-discipline-allow: waitForResume -->

<task type="auto" tdd="false">
  <name>Task 1: Claude trim — gate + cache modules deleted, emitters and file-diff feed excised, writtenFiles field removed</name>
  <files>src/bun/engine/claude/bash-permission-gate.ts, src/bun/engine/claude/file-state-cache.ts, src/bun/engine/claude/adapter.ts, src/bun/engine/claude/engine.ts, src/bun/engine/claude/events.ts, src/bun/engine/types.ts, src/bun/test/bash-permission-gate.test.ts, src/bun/test/file-state-cache.test.ts, src/bun/test/claude-adapter.test.ts, src/bun/test/claude-events.test.ts, src/bun/test/claude-file-diff-integration.test.ts, src/bun/test/claude-rpc-scenarios.test.ts</files>
  <read_first>
    - src/bun/engine/claude/adapter.ts (BashPermissionGate wiring :10,:201-205, emitter :378; FileStateCache param :5,:49,:465; onRawModelMessage call sites)
    - src/bun/engine/claude/engine.ts (DefaultFileStateCache instantiation :6,:45,:107-128)
    - src/bun/engine/claude/events.ts (computeFileDiff feed :200-217,:260-315, writtenFiles population :125-ish, ask_user/status/usage emitters)
    - src/bun/engine/types.ts lines 26-38 (tool_result incl. writtenFiles :37)
    - src/bun/engine/claude/bash-permission-gate.ts + file-state-cache.ts (whole-module reads before deletion)
    - src/bun/test/claude-file-diff-integration.test.ts (writtenFiles/file_diff coverage to delete or rework)
  </read_first>
  <action>
    Delete src/bun/engine/claude/bash-permission-gate.ts + src/bun/test/bash-permission-gate.test.ts and src/bun/engine/claude/file-state-cache.ts + src/bun/test/file-state-cache.test.ts (grep their importers first: claude adapter/engine/events + orchestrator type-only refs — the orchestrator refs died in 07-01 Task 2; expect zero live importers after this task).

    Remove the writtenFiles field from the tool_result member of the EngineEvent union in src/bun/engine/types.ts (:37) — tsc then enumerates every producer/consumer: claude/events.ts (computeWrittenFiles + FileStateCache feed :200-217), tools.ts (:137-139, fix via tsc), and the event-bridge consumer. Remove the FileDiffPayload import if it becomes unused. FileChangesRenderer is NOT affected (it derives diffs from tool ARGS via buildDiffPayloadsFromArgs at FileChangesRenderer.vue:67 — arg-derived, never used writtenFiles).

    claude/adapter.ts: remove the BashPermissionGate construction + wiring (:10,:201-205) and the shell_approval emitter (:378); remove the FileStateCache param (:5,:49,:465); remove any onRawModelMessage call site (raw-delta persistence — the model_raw_messages write dies; the /ws text_chunk broadcast that onEnqueue served died in 07-01 Task 2). claude/engine.ts: remove DefaultFileStateCache instantiation (:6,:45,:107-128). claude/events.ts: remove computeFileDiff/writtenFiles emission (:200-217,:260-315) and any ask_user/status/usage emitters — if a shallow no-output fallback at :281-284 exists, it can stay only if it emits nothing trimmed (prefer removal — the live renderer is arg-derived, so either is safe; pick removal for a clean grep gate).

    Tests: delete/rework the file-diff + gate coverage in claude-file-diff-integration.test.ts, claude-adapter.test.ts, claude-events.test.ts, claude-rpc-scenarios.test.ts (StreamEvent- and shell-approval-dependent scenarios die; the RPC scenarios for kept events stay).
  </action>
  <acceptance_criteria>
    - bash-permission-gate.ts and file-state-cache.ts (and their tests) deleted; `rg -n "BashPermissionGate|FileStateCache" src/bun --glob '!**/test/**'` returns zero
    - EngineEvent.tool_result has no writtenFiles field; `rg -n "writtenFiles" src/bun --glob '!**/test/**'` returns zero
    - claude adapter/engine/events compile with no references to the gate, the cache, or trimmed EngineEvent members
    - `bun test src/bun/test/claude-adapter.test.ts src/bun/test/claude-events.test.ts src/bun/test/claude-file-diff-integration.test.ts src/bun/test/claude-rpc-scenarios.test.ts --timeout 20000` passes
  </acceptance_criteria>
  <verify>
    <automated>bun test src/bun/test/claude-adapter.test.ts src/bun/test/claude-events.test.ts src/bun/test/claude-file-diff-integration.test.ts --timeout 20000</automated>
  </verify>
  <done>The claude engine stack compiles with zero gate/cache/writtenFiles/trimmed-emitter references and its suites are green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: opencode shell-posture replacement (A3) + copilot/cursor emitter trims + event-bridge drop-list</name>
  <files>src/bun/engine/opencode/engine.ts, src/bun/engine/opencode/adapter.ts, src/bun/engine/opencode/event-translator.ts, src/bun/engine/copilot/events.ts, src/bun/engine/cursor/translate-events.ts, src/bun/copilotkit/event-bridge.ts, src/bun/test/approved-commands.test.ts, src/bun/test/copilot-events.test.ts, src/bun/test/copilot-rpc-scenarios.test.ts, src/bun/test/cursor/rpc-scenarios.test.ts</files>
  <read_first>
    - src/bun/engine/opencode/engine.ts (waitForResume :128-136, the shellState.shellAutoApprove path :158-165, ask_user emitters)
    - src/bun/engine/opencode/adapter.ts + event-translator.ts (ask_user/status/usage emitters)
    - src/bun/engine/copilot/events.ts + src/bun/engine/cursor/translate-events.ts (status/usage/ask_user emitters)
    - src/bun/copilotkit/event-bridge.ts (drop-list :280-289)
    - src/bun/test/approved-commands.test.ts (shell-approval behavior coverage)
  </read_first>
  <action>
    Resolve the A3 checkpoint below, then implement its outcome in opencode/engine.ts: remove waitForResume (:128-136) and the shell_approval emitter; under auto-approve (recommended), route permission requests through the existing shellState.shellAutoApprove path (the one that today auto-approves when configured — at :158-165 area) so commands execute without pausing; under deny, route through a rejection that returns a permission-denied tool error instead of pausing. NEVER leave a wait that no UI can answer (the pre-trim invisible hang). Remove ask_user emitters from opencode adapter/event-translator.

    copilot/events.ts + cursor/translate-events.ts: remove the status/usage/ask_user/compaction emitters (tsc lists them — fix every one). copilot-rpc-scenarios.test.ts + cursor/rpc-scenarios.test.ts: delete the trimmed-event scenarios (e.g. cursor §6.3.10 usage-persists-input_tokens — the executions token write died in 07-01 Task 1) and the shell-approval coverage in approved-commands.test.ts.

    copilotkit/event-bridge.ts: simplify the drop-list (:280-289) — remove the cases for usage/status/compaction_start/compaction_done/ask_user/shell_approval (the events no longer exist); keep task_updated, new_message handling as-is IF the engine still emits them (they die in Task 3's ctor-arg removal — if tsc says new_message is gone from the union, remove its bridge case too), and keep the decision_request case returning [] (:288) UNCHANGED — interrupts flow through the module-level registry; do not "fix" it.
  </action>
  <acceptance_criteria>
    - opencode/engine.ts has no waitForResume and no shell_approval emission; the A3-chosen posture is implemented (no invisible hang — commands auto-approve or deny deterministically)
    - copilot/events.ts + cursor/translate-events.ts emit no trimmed members
    - event-bridge.ts drop-list contains only events that still exist in the EngineEvent union; decision_request still returns []
    - `bun test src/bun/test/approved-commands.test.ts src/bun/test/copilot-events.test.ts src/bun/test/copilot-rpc-scenarios.test.ts src/bun/test/cursor/rpc-scenarios.test.ts --timeout 20000` passes
  </acceptance_criteria>
  <verify>
    <automated>bun test src/bun/test/copilot-rpc-scenarios.test.ts src/bun/test/cursor/rpc-scenarios.test.ts --timeout 20000</automated>
  </verify>
  <done>All non-claude engines compile against the trimmed union, opencode's shell posture is deterministic per A3, and the bridge drop-list matches the kept events.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: onRawModelMessage plumbing removal + engine onNewMessage ctor-arg removal + tsc-clean wave</name>
  <files>src/bun/engine/execution/execution-params-builder.ts, src/bun/engine/copilot/session.ts, src/bun/engine/opencode/engine.ts, src/bun/engine/cursor/engine.ts, src/bun/engine/copilot/engine.ts, src/bun/engine/claude/engine.ts, src/bun/engine/pi/pi-engine-factory.ts, src/bun/test/execution-params-builder.test.ts, src/bun/test/claude-adapter.test.ts</files>
  <read_first>
    - src/bun/engine/execution/execution-params-builder.ts (onRawModelMessage population — makePersistCallback's consumer)
    - src/bun/engine/stream/stream-processor.ts Task-1 output (makePersistCallback is gone; the params builder must not reference it)
    - Engine class ctor signatures: copilot/engine.ts, claude/engine.ts, opencode/engine.ts, cursor/engine.ts, pi/pi-engine-factory.ts (the onNewMessage ctor arg that index.ts stopped passing in 07-01 Task 2)
  </read_first>
  <action>
    execution-params-builder.ts: remove the onRawModelMessage field from the ExecutionParams it builds (the type field died in 07-01 Task 1) and the makePersistCallback call that populated it. Then remove every adapter call site that invoked params.onRawModelMessage: copilot/session.ts (the raw-delta → broadcast path — the broadcast side died in 07-01 Task 2, so the call sites die now), claude/adapter.ts (fix per tsc), and any other engine surface tsc enumerates. Rework execution-params-builder.test.ts accordingly.

    Remove the onNewMessage ctor arg from the engine classes: CopilotEngine, ClaudeEngine, OpenCodeEngine, CursorEngine, and the pi engine factory (createPiEngine) — index.ts stopped passing it in 07-01 Task 2; the classes still declare it. Also remove the `new_message` emission wherever an engine adapter still emits it (tsc enumerates). Keep the onTaskUpdated ctor arg everywhere (task.updated push stays live).

    FINAL wave check: run `bun run typecheck` — it must be CLEAN (all stragglers from 07-01 Task 1's trim are resolved: emitters, bridge, params builder, engine ctors). Then run the wave-1 gate: `bun run build` + the tripwire specs + `bun test e2e/api --timeout 30000` + `bun test src/bun --timeout 20000` (record the new src/bun count — pipeline-test deletions legitimately lower the 2396 baseline). If any Playwright spec or api test regressed, fix in this task before closing the wave.
  </action>
  <acceptance_criteria>
    - `rg -n "onRawModelMessage" src/bun` returns zero; execution-params-builder and all adapters compile
    - No engine class ctor takes onNewMessage; `rg -n "onNewMessage" src/bun/engine --glob '!**/test/**'` returns zero (the store-side onNewMessage is 07-03's scope)
    - `bun run typecheck` is CLEAN (exit 0) — the entire engine layer compiles against the trimmed union
    - Wave gate green: `bun run build` succeeds; `bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts` passes; `bun test e2e/api --timeout 30000` passes; `bun test src/bun --timeout 20000` passes (new count recorded)
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>The engine layer is fully clean against the trimmed contract and the wave-1 gate is green end-to-end.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>A3 — after the shell_approval trim, what is the opencode engine's posture for shell permission requests?</decision>
  <context>Removing waitForResume eliminates the only interactive shell gate for opencode (and the claude gate module is deleted in Task 1). Today a shell_approval is an invisible hang — the AG-UI bridge drops it (event-bridge.ts:280-289) and no UI can answer. The project posture (PROJECT.md:76): decision_request is the ONLY human-in-the-loop channel. Research recommendation (OQ2/A3): auto-approve via the existing shellState.shellAutoApprove path. This is a security-relevant behavior change — engines execute shell commands without an interactive gate.</context>
  <options>
    <option id="option-a">
      <name>Auto-approve (recommended)</name>
      <pros>Matches the trim decision; commands flow through the existing shellAutoApprove path; no hang; tasks.setShellAutoApprove RPCs (KEPT) still allow per-run control</pros>
      <cons>Shell commands execute without per-command consent — acceptable for a local single-user app running trusted engines (existing posture for the other four engines)</cons>
    </option>
    <option id="option-b">
      <name>Deny-by-default (permission errors)</name>
      <pros>Conservative: no shell command runs unless the engine's own config approves it</pros>
      <cons>Breaks shell tool workflows that today rely on approval; requires an always-deny config knob; more code in a deletion phase</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or describe an alternative</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine adapters → EngineEvent stream | trimmed members removed — the engine layer's only output channel to consume()/bridge |
| opencode engine → shell execution | the A3 posture decision — security-relevant behavior change |
| engine → ExecutionParams | onRawModelMessage plumbing removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Elevation of Privilege | opencode shell execution after approval-trim | medium | mitigate | A3 blocking human checkpoint decides auto-approve vs deny BEFORE the waitForResume removal lands; no invisible-hang state ever survives; local single-user app, engines are trusted local agents (project posture: decision_request is the only interactive HITL) |
| T-07-11 | Tampering | claude shell gate removal | medium | mitigate | BashPermissionGate deleted with the shell_approval channel; claude executes shell tools unapproved — same trusted-local-agent posture; documented in the A3 decision record |
| T-07-12 | Information disclosure | file-state-cache / writtenFiles diff surface | low | mitigate | FileStateCache + writtenFiles removed; FileChangesRenderer stays arg-derived (no sensitive computed diffs retained beyond tool args) |
| T-07-13 | Tampering | zombie trimmed emitters (half-alive protocol) | low | mitigate | EngineEvent union trim (07-01) makes every leftover emitter a compile error — tsc-clean wave check in Task 3 |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package operations this phase — no supply-chain surface |
</threat_model>

<verification>
- Per-task: targeted engine suites (verify fields) — no `-x` flag anywhere
- Task 3 closes the wave: clean typecheck + build + tripwire + e2e/api + src/bun (new count recorded)
- Grep gates: BashPermissionGate/FileStateCache/writtenFiles/onRawModelMessage/onNewMessage(engine) all zero in src/bun non-test
</verification>

<success_criteria>
- Engine layer compiles clean against the trimmed EngineEvent union (typecheck exit 0)
- No engine emits trimmed events; gates and file-state modules deleted; opencode shell posture deterministic per A3
- event-bridge drop-list matches the kept events; decision_request registry path untouched
- Wave-1 gate green (build + tripwire + e2e/api + src/bun)
</success_criteria>

<output>
Create `.planning/phases/07-cleanup-feature-trim/07-02-SUMMARY.md` when done
</output>
