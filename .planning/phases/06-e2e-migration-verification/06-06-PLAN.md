---
phase: 06-e2e-migration-verification
plan: 06
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - e2e/ui/autocomplete.spec.ts
  - e2e/ui/cursor.spec.ts
  - e2e/ui/task-drawer.spec.ts
  - e2e/ui/extended-chat.spec.ts
autonomous: true
requirements:
  - VERF-02
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "autocomplete.spec.ts (34 tests) is migrated per the PATTERNS map (D-01): the ~12 slash/editor-behavior tests (AC-1/2/3/10/11/12/16/21/22/25/29/30) → the C-3 copilot-slash-menu pattern + chat-input textarea editor assertions, and the ~22 CodeMirror chip/#/@/LSP + attachment tests retire in-file with recorded rationale (chips removed with the legacy editor; attachments out of scope CONT-01)"
    - "cursor.spec.ts (7 tests) migrates the 3 render intents (CU-2.1/3.1/4.1) onto model-agnostic agui scripts (S-1 streaming / C-4 interrupt per intent) and retires CU-1.1/1.2 + picker tests in-file (model picker removed with the legacy input)"
    - "task-drawer.spec.ts (10 tests) migrates MSG-1 (send without reopen) → S-1 and TD-5/6 (latest message + ordered history) → S-2 + registerHistory, retires TD-2 (toolbar chrome), TD-3 (attachment chip), TD-7 (transition cards) in-file with rationale, and keeps TD-1/4/8 green byte-identical"
    - "extended-chat.spec.ts (19 red) migrates P-12/13/14 (stop/cancel) → C-1 (slow script + stop-btn + chat-stopped + agui.stopRequests) and the remaining streaming intents → S-1, and retires P-15, Q-16..20 (model selector), R-20..25+23 (compaction — trimmed), S-1..3 (legacy decision_request_prompt ws flow — covered by C-4/C-5) in-file with rationale"
    - "Each migrated file passes ALONE (Pitfall 4/9); the tripwire (chat-copilotkit + board + board-ws-updates) stays green; green non-chat specs untouched (D-03); the canonical spec never edited (Pitfall 8). After this plan the red surface is ZERO — only the D-05 gate (06-07) remains"
  artifacts:
    - e2e/ui/autocomplete.spec.ts — slash/editor tests migrated; chip tests retired
    - e2e/ui/cursor.spec.ts — render intents migrated; picker tests retired
    - e2e/ui/task-drawer.spec.ts — MSG-1/TD-5/6 migrated; TD-2/3/7 retired
    - e2e/ui/extended-chat.spec.ts — stop/streaming migrated; selector/compaction/ws-flow tests retired
  key_links:
    - "autocomplete anchors: .cm-tooltip-autocomplete slash menu, .cm-content chips — map per PATTERNS.md:200; C-3 pattern at chat-copilotkit.spec.ts:288-310"
    - "cursor anchors: decision_request_prompt seed 89, ws.pushStreamEvent 162-164, CU-4 interview UI 218-230 — map per PATTERNS.md:205"
    - "task-drawer anchors: .task-chat-view .conv-body scroll asserts 66-74, conv-body__tail 107-112, [data-index] order 110-112, msg--user 256-257 — map per PATTERNS.md:206"
    - "extended-chat anchors: ws.pushStreamEvent textChunk 40/70/125/198/393, decision_request_prompt ws flow S-1..3 413-518 — map per PATTERNS.md:197"
---

<objective>
**Capability: the input/editor surface specs pass on the mock foundation, closing the migration wave with ZERO red files (VERF-02, D-01/D-02).** Migrate autocomplete.spec.ts (slash commands + editor behavior), cursor.spec.ts (render intents), task-drawer.spec.ts (send + history ordering), and extended-chat.spec.ts (stop/cancel + streaming) onto the agui fixture patterns, retiring their removed-feature tests in-file.

Purpose: these are the last four migrate files — autocomplete is the largest retire/migrate split (~22 retired of 34), extended-chat carries the stop-family intents (Pitfall 5 territory) plus the compaction/selector retires. Green here means the entire red surface is cleared and only the D-05 full gate (06-07) remains.

Output: four green migrated spec files; the suite's red surface reaches ZERO before the gate.

*MVP deviation (accepted, Phases 2/3/4/5 precedent): capability-shaped goal; this plan executes the D-01 migration list incrementally (D-02).*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e2e-migration-verification/06-RESEARCH.md (Pattern 1; Pitfalls 3, 5, 8, 9; baseline table rows 80, 85, 88, 89, 93, 97)
@.planning/phases/06-e2e-migration-verification/06-PATTERNS.md (per-file maps 195, 197, 200, 205, 206; operation pattern 3 — slash menu; pattern 5 — C-3)

# Migration template (FROZEN — read-only):
@e2e/ui/chat-copilotkit.spec.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate autocomplete.spec.ts (slash menu + editor behavior; retire CodeMirror chip tests)</name>
  <files>e2e/ui/autocomplete.spec.ts</files>
  <read_first>
    e2e/ui/autocomplete.spec.ts (whole file — 34 tests; AC-1/2/3/10/11/12/16/21/22/25/29/30 vs AC-4..9/13..15/17..20/23/24/26..28/31..34)
    e2e/ui/chat-copilotkit.spec.ts (C-3 288-310 — the copilot-slash-menu pattern with api.handle("engine.listCommands"))
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md autocomplete map row (line 200)
  </read_first>
  <acceptance_criteria>
    - The ~12 slash/editor tests migrate to the C-3 pattern: api.handle("engine.listCommands", ...) → type "/" → [data-testid="copilot-slash-menu"] → [role="option"] click → input value assertion on [data-testid="chat-input"] textarea; editor-behavior tests assert the textarea (focus, typing, Enter submit) instead of .cm-content.
    - The ~22 CodeMirror chip tests (AC-4..9, 13..15, 17..20, 23, 24, 26..28, 31..34 — #/@/LSP chips, .cm-tooltip-autocomplete extras, attachments) retire in-file with one-line rationale each (chips removed with ChatEditor; attachments out of scope CONT-01).
    - Zero .cm-content / .cm-tooltip-autocomplete / chip selectors remain.
    - Verify: file passes ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Migrate per the PATTERNS map (line 200). For each slash test, follow the C-3 pattern exactly (api.handle("engine.listCommands", () => [{ name: "fake-cmd", description: "d" }]); type "/" via chatTextarea; assert slashMenu visible; click the [role="option"] with hasText "/fake-cmd"; assert input value). Retire the chip/attachment tests with recorded rationale. Keep the file name.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/autocomplete.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    autocomplete green (slash/editor intents migrated, chip tests retired with rationale); zero legacy editor selectors; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite + in-file retires; git revert restores.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate cursor.spec.ts (render intents) + task-drawer.spec.ts (send + history ordering)</name>
  <files>e2e/ui/cursor.spec.ts, e2e/ui/task-drawer.spec.ts</files>
  <read_first>
    e2e/ui/cursor.spec.ts (whole file — 7 tests; CU-2.1/3.1/4.1 render intents, CU-1.1/1.2 + picker tests, decision_request_prompt seed 89, ws.pushStreamEvent 162-164, CU-4 interview UI 218-230)
    e2e/ui/task-drawer.spec.ts (whole file — 10 tests; MSG-1 send-without-reopen, TD-5/6 latest+ordered history, TD-2 toolbar chrome, TD-3 attachment chip, TD-7 transition cards, TD-1/4/8 green)
    e2e/ui/chat-copilotkit.spec.ts (S-1 63-83, S-2 85-110, C-4 312-385)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md cursor + task-drawer map rows (lines 205-206)
  </read_first>
  <acceptance_criteria>
    - cursor.spec.ts: CU-2.1/3.1/4.1 (render intents) migrated to model-agnostic agui scripts (S-1 for streaming intents; C-4 interrupt pattern for the CU-4 interview intent — decision-card asserts instead of the legacy interview UI); CU-1.1/1.2 + picker tests retire in-file (model picker removed with the legacy input); zero ws.pushStreamEvent / decision_request_prompt constructs remain.
    - task-drawer.spec.ts: MSG-1 → S-1 (submitChatMessage streams via /run, no reopen); TD-5/6 → S-2 + registerHistory (latest message visible, ordered list via nth-asserts on copilot-chat-view content); TD-2/TD-3/TD-7 retire in-file with rationale (toolbar chrome — .task-drawer__actions, attachment chip — removed, transition cards — trimmed); TD-1/4/8 stay byte-identical.
    - Verify: both files pass ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Migrate cursor.spec.ts per the PATTERNS map (line 205): the three render intents keep their rendering intent but assert the generic surface (quick script for streaming, interrupt pattern for the decision intent — model-agnostic per the map). Delete the picker tests with rationale.

    Migrate task-drawer.spec.ts per the map (line 206): MSG-1 and TD-5/6 onto S-1/S-2 with registerHistory (TD-5/6 need the Task-1 history knob — ordered alternating user/assistant history asserted by message order inside copilot-chat-view). Delete TD-2/TD-3/TD-7 with rationale. Leave TD-1/4/8 untouched. Keep both file names.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/cursor.spec.ts e2e/ui/task-drawer.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    cursor + task-drawer green with intents migrated and removed-feature tests retired; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrites; git revert restores.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate extended-chat.spec.ts (stop/cancel + streaming; retire selectors/compaction/ws-flow)</name>
  <files>e2e/ui/extended-chat.spec.ts</files>
  <read_first>
    e2e/ui/extended-chat.spec.ts (whole file — 521 lines; ws.pushStreamEvent textChunk 40/70/125/198/393, decision_request_prompt ws flow S-1..3 413-518, P-12/13/14 stop tests, Q-16..20 model selector, R-20..25+23 compaction, P-15)
    e2e/ui/chat-copilotkit.spec.ts (S-1 63-83, C-1 237-265)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md extended-chat map row (line 197)
  </read_first>
  <acceptance_criteria>
    - P-12/13/14 (stop/cancel) → C-1 pattern: agui.script = "slow", stop-btn click, chat-stopped "Stopped" label, agui.stopRequests contains the threadId; remaining streaming intents → S-1 quick-script pattern.
    - In-file retires with rationale: P-15, Q-16..20 (model selector — removed), R-20..25+23 (compaction — trimmed feature, compaction_summary in trim list), S-1..3 (legacy decision_request_prompt ws flow — covered by C-4/C-5 interrupt pattern).
    - Zero ws.pushStreamEvent / decision_request_prompt / .pi-stop-circle / .input-model-select constructs remain.
    - Verify: file passes ALONE (Pitfall 5: stop tests assert agui.stopRequests, not timing), then tripwire.
  </acceptance_criteria>
  <action>
    Migrate per the PATTERNS map (line 197): stop tests → C-1 pattern exactly (agui.script = "slow" BEFORE submitChatMessage; assert stop-btn visible; click; assert chat-stopped "Stopped"; assert agui.stopRequests contains String(conversationId)); streaming tests → S-1. Delete the retire list with recorded rationale (S-1..3's decision intents are covered by the canonical C-4/C-5 — note that coverage in the rationale). Keep the file name.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/extended-chat.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    extended-chat green (stop + streaming migrated, ~16 tests retired with rationale); zero legacy constructs; tripwire green; red surface ZERO.
  </done>
  <reversibility rating="reversible">Spec rewrite + in-file retires; git revert restores.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 06-06)

- `e2e/ui/autocomplete.spec.ts` — ~12 slash/editor tests on copilot-slash-menu; ~22 chip tests retired
- `e2e/ui/cursor.spec.ts` — 3 render intents model-agnostic; picker tests retired
- `e2e/ui/task-drawer.spec.ts` — MSG-1/TD-5/6 migrated (registerHistory); TD-2/3/7 retired
- `e2e/ui/extended-chat.spec.ts` — stop family on C-1; selector/compaction/ws-flow retired
- In-file retire rationale records → SUMMARY

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated spec → agui fixture | Slash-menu RPCs (engine.listCommands) + AG-UI traffic all mocked via page.route |
| slash-menu input → chat-input textarea | CopilotChatInput value assertions — client DOM only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-23 | Tampering | Stop tests flaky under fullyParallel (slow-script 3s hold) | medium | mitigate | Assert agui.stopRequests (deterministic) not timing (Pitfall 5); stop tests in one file (extended-chat) reduce concurrent slow-script load; CI retries:2 absorbs residual |
| T-06-24 | Tampering | Over-retirement in the large autocomplete split (22 tests) | medium | mitigate | Each retired test tagged with its removed construct (.cm-tooltip-autocomplete chips / # / @ / LSP / attachments) — executor re-greps the selector in src/mainview before deleting (Pitfall 2); rationale in SUMMARY |
| T-06-25 | Tampering | Slash-menu intent drift (engine.listCommands contract) | medium | mitigate | C-3 pattern copied exactly (api.handle + hasText "/fake-cmd" + input value assert); the RPC contract is fixed by rpc-types.ts |
| T-06-26 | Tampering | TD-5/6 ordered-history asserts misreading CopilotChat message order | medium | mitigate | Order asserted via registerHistory + nth-message locators inside copilot-chat-view (PATTERNS S-2 + knob); file run alone catches ordering misses |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Per task: migrated file(s) alone after `bun run build` + tripwire.
- Wave merge: tripwire + `bun test e2e/ui/fixtures/mock-agui.test.ts` + `bun run typecheck` + `npx playwright test e2e/ui --list | wc -l` (file count sanity — 53 minus 11 retires = 42 spec files).
</verification>

<success_criteria>
- autocomplete, cursor, task-drawer, extended-chat green on the agui fixture; the 25-file red surface is ZERO; the full-suite gate (06-07) is the only remaining task.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-migration-verification/06-06-SUMMARY.md` when done
</output>
