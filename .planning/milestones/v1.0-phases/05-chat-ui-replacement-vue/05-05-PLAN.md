---
phase: 05-chat-ui-replacement-vue
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified:
  - src/mainview/components/chat/ChatThreadSidebar.vue
  - src/mainview/views/BoardView.vue
  - src/mainview/components/SessionChatView.vue
  - src/mainview/components/chat/RailyinChat.vue
  - e2e/ui/chat-copilotkit.spec.ts
autonomous: true
requirements:
  - CHAT-02
  - UI-01
  - UI-04
  - IMPR-03
estimate:
  tokens: 30000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ChatThreadSidebar.vue replaces ChatSidebar.vue in BoardView (same v-if/close contract, toggle button + badge + activeChatSessionId watch stay): resizable 160-400px (default 220, persisted under chat-sidebar-width), session rows with status dot + unread dot + relative time + rename/archive (chatSessions.* endpoints, still live), New Session action (data-testid thread-new), Legacy Import action (data-testid legacy-import-btn), empty state 'No sessions yet' + 'Start a new session to begin.' with the New session button active (UI-SPEC thread-list rows + Copywriting Contract) (UI-01, D-02)"
    - "Sidebar data decision (RESEARCH Open Question 1/A8, planner decision): v1 lists SESSIONS from chatSessions.list via the existing chatStore (status/unread/rename/archive all work, zero backend change); card threads stay opened from the board (as today); threads.list consumption for card threads in the sidebar is v2 sidebar work (CHAT-13 territory) — ThreadSummary has no status/unread/taskId fields (rpc-types.ts:111-121) and no open path exists without a backend change"
    - "Legacy Import action wiring: calls legacyImport.run (IMPR-01 endpoint), spinner on the action button while running, completion toast, idempotent re-run shows 'Already imported — no duplicates' info toast, failure toast with error detail and the action remains re-runnable (UI-SPEC legacy-import-action rows, IMPR-02)"
    - "SessionChatView Chat tab renders RailyinChat (threadId = String(session.conversationId), commandsScope = { workspaceKey: session.workspaceKey }) in place of ConversationBody + ConversationInput; Decisions/Notes tabs, header chrome, archive/rename actions, and the scv-loading pattern stay (D-02, D-03); the defineExpose scroll methods become safe no-ops"
    - "Markdown + code blocks render at parity with the old editor for coding output (CHAT-02): the ConversationBody.vue:608-627 + MessageBubble.vue:316-350 parity rules (p, h1-h4, ul/ol, li, code, pre, blockquote, table, hr, a — 1.6 line-height, block margins) are ported into RailyinChat's non-scoped .railyn-chat style block; pre blocks get max-height + internal scroll; long messages wrap, no bubble-column overflow (UI-SPEC chat-message-stream long-text/overflow rows)"
    - "Board /ws reactivity keeps working: task.updated, code.ref, lsp, chatSession.updated push handlers (rpc.ts + App.vue) untouched; board-ws-updates.spec.ts stays green (UI-04)"
    - "IMPR-03: the old chat stack stays ALIVE — no legacy chat component/store/composable file is modified or deleted this phase, and no new chat component imports legacy chat components (RailyinChat imports CopilotKit + 05-02 chat components only); the grep/typecheck gates in the phase gate prove it"
    - "CSS override depth (unresolved UI-SPEC assumption): RailyinChat.vue is the single override surface; parity overrides confined to its non-scoped style block scoped under .railyn-chat; visual diff against the legacy editor is the held-out check (backstop)"
  artifacts:
    - src/mainview/components/chat/ChatThreadSidebar.vue
    - src/mainview/views/BoardView.vue — sidebar swap
    - src/mainview/components/SessionChatView.vue — Chat-tab swap
    - src/mainview/components/chat/RailyinChat.vue — markdown parity CSS
    - e2e/ui/chat-copilotkit.spec.ts — sidebar scenarios
  key_links:
    - "BoardView swap site: lines 210-213 (<ChatSidebar v-if=chatSidebarOpen @close=...>) + import at 268 — toggle + activeChatSessionId watch (745) unchanged"
    - "SessionChatView swap site: Chat-tab branch lines 74-128 (ConversationBody 75-85 + ConversationInput 101-128) — Decisions/Notes tabs 87-98 stay"
    - "Legacy import RPC: legacyImport.run (rpc-types.ts:1116-1119) + threads.list (1109-1112) — the sidebar's only backend surface"
  backstop:
    - statement: "stopped response shows 'Stopped' label"
      verification: backstop
    - statement: "failed tool call shows error state"
      verification: backstop
---

<objective>
**Capability: the swap completes — sidebar, session chat, markdown parity — and the phase proves itself at the gate.** Replace `ChatSidebar.vue` with the new `ChatThreadSidebar.vue` in BoardView, swap the SessionChatView Chat tab onto RailyinChat, port the markdown/code parity CSS into the single override surface, and close the phase with the UI-04 / IMPR-03 / CHAT-02 gates: /ws reactivity green, legacy stack provably alive, and the held-out visual backstops (Stopped label, failed tool card, markdown parity) confirmed.

Purpose: deliver the phase's last success criteria — "board chat fully powered by CopilotKit … while board /ws reactivity keeps working and the old chat stack code survives for rollback" — and produce the evidence Phase 6 (E2E migration) builds on.

Output: ChatThreadSidebar, the session swap, parity CSS, and the phase gate evidence.

*MVP deviation (accepted, Phases 2/3/4 precedent): the ROADMAP goal is capability-shaped, not a user story; this plan completes the swap and gates the phase.*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chat-ui-replacement-vue/05-PATTERNS.md (lines 107-150 ChatThreadSidebar port; 292-311 BoardView/TaskChatView/SessionChatView swaps; 353-376 shared CSS patterns)
@.planning/phases/05-chat-ui-replacement-vue/05-RESEARCH.md (Pitfall 6 sidebar enrichment; Open Question 1; Pattern 5)

# Port/swap sources (read-only):
@src/mainview/components/ChatSidebar.vue
@src/mainview/views/BoardView.vue (swap site 210-213 + import 268)
@src/mainview/components/SessionChatView.vue (swap site 74-128)
@src/mainview/components/ConversationBody.vue (parity CSS 608-627)
@src/mainview/components/MessageBubble.vue (parity CSS 316-350)
@src/mainview/stores/chat.ts (sessions, hasUnread, archive/rename — read-only, stays alive)
@src/mainview/components/chat/RailyinChat.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ChatThreadSidebar port + BoardView swap + sidebar scenarios</name>
  <files>
    src/mainview/components/chat/ChatThreadSidebar.vue
    src/mainview/views/BoardView.vue
    e2e/ui/chat-copilotkit.spec.ts
  </files>
  <read_first>
    src/mainview/components/ChatSidebar.vue (entire file — rows, status/unread dots, rename/archive, resize, defineExpose)
    src/mainview/views/BoardView.vue (swap site 210-213, import 268, toggle 85-97, activeChatSessionId watch 745)
    src/mainview/stores/chat.ts (sessions getter, hasUnread, rename/archive actions — read-only)
    src/mainview/rpc.ts (legacyImport.run + threads.list signatures via api())
    src/shared/rpc-types.ts (ThreadSummary 111-121, ImportSummary 127-133)
  </read_first>
  <acceptance_criteria>
    - ChatThreadSidebar.vue exists under src/mainview/components/chat/: ports ChatSidebar's structure — resizable width (160-400, default 220, STORAGE_KEY "chat-sidebar-width", defineExpose({ sidebarWidth })), session rows (status dot classes, unread dot, relative time, hover rename/archive via chatSessions.*), New Session button (data-testid thread-new), and adds the Legacy Import action (data-testid legacy-import-btn) with in-progress spinner, completion toast, idempotent info toast, and failure toast (legacyImport.run).
    - Data source per the planner decision (Research Open Question 1/A8): chatSessions.list via useChatStore — sessions only in v1; status/unread dots are session-only; card threads are NOT listed (no open path without a backend change).
    - Empty state: "No sessions yet" / "Start a new session to begin." with the New session button active (Copywriting Contract).
    - BoardView.vue: <ChatSidebar> (210-213) replaced by <ChatThreadSidebar> with the same v-if="chatSidebarOpen" + @close contract; import updated (268); toggle button + badge + activeChatSessionId watch untouched; ChatSidebar.vue left on disk unmodified (D-10).
    - Spec scenarios in chat-copilotkit.spec.ts: sidebar lists sessions from chatSessions.list mock (thread-item-{id} rows); New Session creates (chatSessions.create capture); Legacy Import button triggers legacyImport.run capture + toast; empty list renders the empty-state copy.
  </acceptance_criteria>
  <action>
    Create ChatThreadSidebar.vue as a port of ChatSidebar.vue per the acceptance criteria — copy the verified resize pattern (PATTERNS.md lines 124-147), the row structure (status-dot--{status} classes, title ellipsis + title tooltip, unread dot, rename/archive actions), and the empty-state copy from the Copywriting Contract. Add the Legacy Import action row wired to `api("legacyImport.run")` (verify the exact method signature in src/shared/rpc-types.ts first) with the toast states from the acceptance criteria (success toast on completion, "Already imported — no duplicates" info toast on idempotent re-run, error toast with detail on failure).

    Swap the mount in BoardView.vue at lines 210-213 + import at 268 (same v-if/close contract). Leave ChatSidebar.vue untouched on disk.

    Add the sidebar scenarios to chat-copilotkit.spec.ts using api.handle for chatSessions.list / legacyImport.run and the test ids from the acceptance criteria.

    No fenced code blocks: implement per the verified identifiers above.
  </action>
  <verify>
    <automated>bun run build && npx playwright test e2e/ui/chat-copilotkit.spec.ts</automated>
    <human-check>Sidebar visually: width resize handle works, status/unread dots match the old sidebar, dark mode renders.</human-check>
  </verify>
  <done>
    ChatThreadSidebar live in BoardView with all actions + scenarios green; ChatSidebar.vue untouched on disk.
  </done>
  <reversibility rating="reversible">Template swap in BoardView; legacy sidebar file intact — git revert restores it (D-10).</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SessionChatView swap + markdown parity CSS + phase gate (CHAT-02, UI-04, IMPR-03)</name>
  <files>
    src/mainview/components/SessionChatView.vue
    src/mainview/components/chat/RailyinChat.vue
  </files>
  <read_first>
    src/mainview/components/SessionChatView.vue (Chat-tab branch 74-128, scv-loading 56-59, defineExpose 365-370)
    src/mainview/components/ConversationBody.vue (parity CSS rules 608-627)
    src/mainview/components/MessageBubble.vue (parity CSS rules 316-350)
    src/mainview/components/chat/RailyinChat.vue (non-scoped style block — the single override surface)
    src/mainview/rpc.ts (WS push dispatch — must remain untouched)
  </read_first>
  <acceptance_criteria>
    - SessionChatView Chat tab renders `<RailyinChat :thread-id="String(session.conversationId)" :title="session.title" :commands-scope="{ workspaceKey: session.workspaceKey }" />` in place of ConversationBody + ConversationInput (v-if branch 74-128); Decisions/Notes tabs (87-98), header, archive/rename, scv-loading for the pre-connect phase, and defineExpose-as-safe-no-op stay; dead imports/handlers removed from THIS view only.
    - Markdown parity CSS ported into RailyinChat's non-scoped style block, scoped under .railyn-chat: the ConversationBody.vue:608-627 rules (p, h1-h4, ul/ol, li, code, pre with max-height + internal scroll) + MessageBubble.vue:316-350 rules (blockquote, table, hr, a) — token-driven colors only (--p-*), dark-mode block included; no hardcoded layout beyond the ported rules.
    - Phase gate (all green): `bun run build && npx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board-ws-updates.spec.ts e2e/ui/board.spec.ts` + `bun test src/bun --timeout 20000` (backend regression) + `bun run typecheck`.
    - IMPR-03 gates (automated): (a) `git diff --name-only <phase-base>..HEAD` shows NO legacy chat files modified — src/mainview/components/{ChatSidebar,ConversationBody,ConversationInput,ConversationPanel,ChatEditor,MessageBubble,ToolCallBlock,ToolCallGroup,SubagentBlock,ReasoningBubble,DecisionRequest,FileDiff,ReadView}.vue, src/mainview/stores/{conversation,chat}.ts, src/mainview/rpc.ts; (b) `rg "components/(ChatSidebar|ConversationBody|ConversationInput|MessageBubble|ToolCallBlock|SubagentBlock|DecisionRequest)" src/mainview/components/chat/` returns no imports of legacy chat components by the new chat code.
    - Held-out visual backstops (end-of-phase human verify, UI-SPEC): (a) "Stopped" label renders in the right spot on a stopped partial response; (b) failed tool calls show the red error state, never a spinner; (c) markdown/code visual parity with the old editor (CHAT-02).
  </acceptance_criteria>
  <action>
    Swap the SessionChatView Chat tab per the acceptance criteria (template-branch swap; keep scv-loading for the pre-connect phase or hand off to RailyinChat's isConnecting state — pick one and keep the Decisions/Notes tabs intact). Remove only the dead chat imports/handlers from SessionChatView; do not modify any legacy component.

    Port the parity CSS into RailyinChat.vue's non-scoped style block (single override surface per D-01 + the unresolved css-override-depth assumption): copy the rule set from ConversationBody.vue:608-627 + MessageBubble.vue:316-350, re-scoped under .railyn-chat with --p-* tokens (exact CSS port, not restyled). Add the dark-mode block per the shared pattern (html.dark-mode .railyn-chat …).

    Run the full phase gate from the acceptance criteria. If any non-chat legacy spec outside the listed regression set fails, note it in the summary (Phase 6 owns chat-surface spec migration, VERF-02) — but board-ws-updates.spec.ts + board.spec.ts must be green (UI-04).

    Record the IMPR-03 evidence (gate outputs) and the held-out backstop confirmations in the summary for the phase gate reviewer.
  </action>
  <verify>
    <automated>bun run build && npx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board-ws-updates.spec.ts e2e/ui/board.spec.ts && bun test src/bun --timeout 20000 && bun run typecheck</automated>
    <human-check>Held-out UI-SPEC backstops: (1) stop a running response — "Stopped" label appears on the partial message; (2) trigger a failed tool call — red error state renders, no spinner; (3) compare rendered markdown/code blocks against the legacy editor — visual parity (CHAT-02).</human-check>
  </verify>
  <done>
    Session chat on RailyinChat; parity CSS ported; phase gate green; IMPR-03 evidence recorded; backstops confirmed.
  </done>
  <reversibility rating="reversible">All swaps are template-branch + git-revertable; legacy code intact by design (D-10).</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 05-05)

- `src/mainview/components/chat/ChatThreadSidebar.vue` — resizable session sidebar with New Session + Legacy Import + empty state (thread-new / legacy-import-btn test ids)
- `src/mainview/views/BoardView.vue` — ChatThreadSidebar mount
- `src/mainview/components/SessionChatView.vue` — Chat-tab swap to RailyinChat
- `src/mainview/components/chat/RailyinChat.vue` — markdown/code parity CSS (single override surface)
- `e2e/ui/chat-copilotkit.spec.ts` — sidebar scenarios
- Phase-gate evidence: UI-04 regression (board-ws-updates), IMPR-03 grep gates, CHAT-02 visual backstop

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session list → sidebar | Server-derived session data (titles/status) rendered as text rows — no HTML |
| legacyImport.run → sidebar | User-triggered RPC with server-side validation (Phase 4 IMPR-01/02) |
| markdown content → DOM | Assistant/tool markdown rendered via StreamMarkdown (component-based) + the ported marked pipeline for delegate/decision content (same profile as legacy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | XSS via markdown parity CSS surface | medium | mitigate | Parity overrides are CSS-only (no new v-html); assistant stream stays on StreamMarkdown (no v-html, verified); ported renderers unchanged from 05-02 mitigations |
| T-05-02 | Tampering | Legacy-import RPC misuse | low | accept | Server-side idempotent import over frozen tables (Phase 4 IMPR-01/02); same-origin single user; RPC schema-validated |
| T-05-03 | Spoofing | Cross-origin runtime use | low | accept | Existing same-origin 403 gate on the runtime mount (Phase 1, WR-03) |
| T-05-04 | Tampering | styles.css preflight leak regressions | low | mitigate | Import-once + layout verification already done (05-03); parity CSS confined to .railyn-chat root |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs; pins locked by pins.test.ts |
</threat_model>

<verification>
- Phase gate (this task): new chat suite + board-ws-updates.spec.ts + board.spec.ts + backend suite + typecheck + IMPR-03 grep gates + held-out visual backstops (human-check).
- Legacy chat-surface specs remain red-by-design (their UI is swapped) — Phase 6 (VERF-02) owns migrating them; record the failing set in the summary for the Phase 6 plan.
</verification>

<success_criteria>
- All three board chat surfaces run on CopilotKit; markdown parity ported; /ws regression green; legacy stack provably alive (IMPR-03); backstops confirmed.
</success_criteria>

<output>
Create `.planning/phases/05-chat-ui-replacement-vue/05-05-SUMMARY.md` when done
</output>
