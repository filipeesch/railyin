---
phase: 05-chat-ui-replacement-vue
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/mainview/App.vue
  - src/mainview/main.ts
  - src/mainview/components/chat/RailyinChat.vue
  - src/mainview/components/TaskChatView.vue
  - e2e/ui/chat-copilotkit.spec.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-07
  - UI-01
estimate:
  tokens: 36000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CopilotKitProvider is mounted at the app root (App.vue) with runtime-url='/api/copilotkit' and NO publicApiKey/licenseToken (local-first, verified provider props); all existing /ws push registrations (onStreamError/onStreamEventMessage/onTaskUpdated/onNewMessage/onCodeRef/onChatSessionUpdated) stay live (UI-04)"
    - "@copilotkit/vue/styles.css is imported exactly once (main.ts) and the board layout is verified after the import (RESEARCH Pitfall 4 — 67KB Tailwind v4 with @layer base preflight can leak)"
    - "RailyinChat.vue is the single CopilotChat surface (D-01): CopilotChat with :thread-id (String(conversationId) — auto connect + JSONL replay per CHAT-07), :welcome-screen=false, :input-tools-menu (from useCommandsCache via toToolsMenu, CHAT-06), the #input slot with CopilotChatInput, useDefaultRenderTool() for generic tools (D-04), useInterrupt + hasInterrupt, stop → agent.abortRun(); all CopilotKit CSS overrides live in its non-scoped style block"
    - "The TaskChatView Chat tab renders RailyinChat (threadId = String(task.conversationId), commandsScope = { taskId }); ConversationBody + ConversationInput are no longer rendered there; ChangedFilesPanel/TodoPanel/header/toolbar chrome stay (D-02); defineExpose keeps its scroll methods as safe no-ops (ConversationDrawer still calls scheduleScrollToBottomIfAuto on drawer open)"
    - "Agent responses stream token-by-token in the board chat via AG-UI text events over /api/copilotkit (CHAT-01) — the new e2e spec proves a submitted message renders the assistant stream text"
    - "Reopening the task drawer shows full history: the second open triggers POST /agent/default/connect (captured by MockAgui) and the replayed messages render (CHAT-07)"
    - "A never-run thread renders the chat empty state 'No messages yet' + 'Send a message to start, or type / to browse commands.' with the input enabled (UI-SPEC chat-message-stream empty row + Copywriting Contract)"
    - "Thread connect/replay shows a centered ProgressSpinner until first MESSAGES_SNAPSHOT; no layout reflow while streaming (UI-SPEC loading row)"
    - "RUN_ERROR renders an inline error row in the message list + PrimeVue error toast ('Execution failed: {error}'); the input re-enables (UI-SPEC error row + Copywriting Contract)"
    - "Chat input placeholder 'Send a message… (Shift+Enter for newline)' preserved; multiline growth to ~5 lines then internal scroll; draft text preserved on send failure (UI-SPEC chat-input empty/error/long-text rows)"
    - "Unresolved UI-SPEC assumption (queue-behavior): the legacy queue-messages affordance is dropped with the old stack; the input is disabled while running (queue-dropped v1 behavior)"
    - "Never-run threads replay empty (RUNR-06) and unknown thread ids are rejected server-side with THREAD_NOT_FOUND — RailyinChat always passes a numeric conversation id string (railyin-agent.ts:223-224)"
  artifacts:
    - src/mainview/components/chat/RailyinChat.vue — thin CopilotChat wrapper (provider consumption, slots, hooks, CSS surface)
    - src/mainview/App.vue — CopilotKitProvider mount
    - src/mainview/main.ts — styles.css import
    - src/mainview/components/TaskChatView.vue — Chat-tab template swap
    - e2e/ui/chat-copilotkit.spec.ts — streaming + history + empty/error state specs
  key_links:
    - "threadId prop → internal connectAgent → POST /agent/default/connect → MockAgui connect replay → MESSAGES_SNAPSHOT — the CHAT-07 chain the tracer spec proves"
    - "The #input slot props (modelValue, isRunning, inputToolsMenu, onUpdateModelValue, onSubmitMessage, onStop) are the only correct wiring surface for CopilotChatInput — verified CopilotChatInputSlotProps"
    - "Aborted and done runs are byte-identical on the wire (RUN_FINISHED with no outcome) — the 'Stopped' label (05-04) must be client state, never derived from wire events (RESEARCH Pattern 5)"
  backstop:
    - statement: "stopped response shows 'Stopped' label"
      verification: backstop
---

<objective>
**Capability: one board chat surface fully on CopilotKit.** The tracer slice wires the thinnest end-to-end path through every layer the phase touches: MockAgui connect replay (test layer, from 05-01) → CopilotKitProvider mount (root) → styles.css import (bootstrap) → `RailyinChat.vue` — the single thin wrapper owning CopilotChat + slots + hooks + CSS (D-01) — → the TaskChatView Chat-tab swap (view) → the first new-chat e2e specs proving token streaming (CHAT-01) and history-on-reopen (CHAT-07). Board chrome and /ws handlers stay untouched (D-02, UI-04).

Purpose: prove the CopilotKit surface end-to-end on the agent's best early-context tokens — the mount, the thread wiring, the input slot, the streaming render, and the fixture replay all in one commit. If the architecture is wrong (e.g. slot prop mismatch, connect contract drift), it fails here, after one commit, not after ten layers.

Output: RailyinChat.vue core, provider + CSS mount, TaskChatView swap, and the first chat-copilotkit specs.

*MVP deviation (accepted, Phases 2/3/4 precedent): the ROADMAP goal is capability-shaped, not a user story; the tracer is the phase's first user-facing slice.*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chat-ui-replacement-vue/05-RESEARCH.md (Patterns 1-4, Code Examples 283-346, Pitfalls 2/4/5)
@.planning/phases/05-chat-ui-replacement-vue/05-PATTERNS.md (lines 34-105 RailyinChat; 292-306 App.vue/main.ts/TaskChatView)

# Verified integration points:
@src/mainview/App.vue
@src/mainview/main.ts
@src/mainview/components/TaskChatView.vue
@src/mainview/components/ConversationDrawer.vue (non-scoped CSS override pattern, lines 143-177)
@e2e/ui/chat-copilotkit.spec.ts (created here)
@node_modules/@copilotkit/vue/dist/v2/components/chat/CopilotChat.vue.d.ts (props/slots/emits)
@node_modules/@copilotkit/vue/dist/v2/components/chat/types.d.ts (CopilotChatInputSlotProps, ToolsMenuItem)
@node_modules/@ag-ui/client/dist/index.d.mts (AbstractAgent: isRunning, connectAgent, abortRun)
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1 (TRACER): RailyinChat core end-to-end — provider, wrapper, TaskChatView swap, streaming + history specs</name>
  <files>
    src/mainview/App.vue
    src/mainview/main.ts
    src/mainview/components/chat/RailyinChat.vue
    src/mainview/components/TaskChatView.vue
    e2e/ui/chat-copilotkit.spec.ts
  </files>
  <read_first>
    node_modules/@copilotkit/vue/dist/v2/components/chat/CopilotChat.vue.d.ts (props: threadId, inputToolsMenu, welcomeScreen, hasExplicitThreadId, isConnecting; emits: stop; slot types)
    node_modules/@copilotkit/vue/dist/v2/components/chat/types.d.ts (CopilotChatInputSlotProps, ToolsMenuItem)
    node_modules/@copilotkit/vue/dist/v2/hooks/use-agent.d.ts + use-default-render-tool.d.ts + use-interrupt.d.ts
    src/mainview/App.vue (WS push registrations that must stay — lines 44-99)
    src/mainview/components/TaskChatView.vue (Chat-tab branch lines 118-177, defineExpose 542-545)
    e2e/ui/fixtures/helpers.ts (openTaskDrawer, sendMessage signatures)
    e2e/ui/chat.spec.ts (existing spec conventions — api.handle + ws.push orchestration)
  </read_first>
  <acceptance_criteria>
    - App.vue wraps `<RouterView />` in `<CopilotKitProvider runtime-url="/api/copilotkit">` (import from `@copilotkit/vue/v2`); all onStreamError/onStreamEventMessage/onTaskUpdated/onNewMessage/onCodeRef/onChatSessionUpdated registrations remain (UI-04).
    - main.ts imports `@copilotkit/vue/styles.css` once; the board view renders without layout regressions after the import (manual spot-check + the legacy board spec run in the wave gate).
    - RailyinChat.vue (src/mainview/components/chat/RailyinChat.vue) — the single CopilotKit surface per D-01: props `threadId: string`, `title?: string`, `commandsScope?: { taskId?: number; workspaceKey?: string }`; template hosts CopilotChat with :thread-id, :input-tools-menu (computed via toToolsMenu over getCommandsRef(taskId)/getCommandsRefForWorkspace(workspaceKey) from useCommandsCache — 05-02), :welcome-screen="false", class "railyn-chat", @stop handler; #input slot rendering CopilotChatInput bound to the slot props (modelValue/isRunning/inputToolsMenu/onUpdateModelValue/onSubmitMessage/onStop) with data-testid="chat-input" and a stop button data-testid="stop-btn" shown when isRunning; script setup calls useDefaultRenderTool() once (default card for all non-domain tools — D-04), useInterrupt() (hasInterrupt → :disabled on the input), useAgent({ agentId: "default" }); onStop sets stopRequested and calls agent.value?.abortRun(); a non-scoped style block starts the .railyn-chat layout CSS (flex column, height 100%).
    - TaskChatView.vue: the Chat-tab branch (v-if="activeTab === 'chat' && task") renders `<RailyinChat :thread-id="String(task.conversationId)" :title="task.title" :commands-scope="{ taskId: task.id }" />` in place of ConversationBody + ConversationInput; ChangedFilesPanel + TodoPanel stay inside the branch; the ConversationBody/ConversationInput imports, conversationStore usage, and the send/enqueue/edit handlers dead after the swap are removed from this file (git history preserves them — D-10); defineExpose keeps scrollToBottom/scheduleScrollToBottomIfAuto as safe no-ops so ConversationDrawer.onAfterShow (drawer:70-72) never throws.
    - e2e/ui/chat-copilotkit.spec.ts (new): uses the agui fixture + api.handle for RPC orchestration + openTaskDrawer helper. Spec 1 (streaming): open the task drawer, type into data-testid="chat-input", submit, assert the assistant stream text from buildQuickRunEvents ("hello") renders. Spec 2 (history): send a message, close the drawer, reopen — assert the prior assistant text still renders and MockAgui captured a POST to /agent/default/connect for the task's threadId.
  </acceptance_criteria>
  <action>
    Wire the tracer slice in this order, committing each vertical seam as it works:

    1. App.vue + main.ts: mount CopilotKitProvider around RouterView (import { CopilotKitProvider } from "@copilotkit/vue/v2"); add the styles.css import to main.ts beside the existing CSS imports. Run the app once (bun run dev) and spot-check the board layout after the 67KB stylesheet import (RESEARCH Pitfall 4 — if @layer base preflight leaks, record what shifted; the mitigation is the .railyn-chat-scoped overrides in 05-05).
    2. RailyinChat.vue: build the wrapper per the acceptance criteria, following the RESEARCH code example (RESEARCH.md lines 297-346) and PATTERNS.md lines 48-103. Verify every prop/slot/hook name against the installed type definitions in read_first — do not guess. The #input slot MUST be present from day one: it carries data-testid="chat-input" and the stop button (UI-SPEC test ids); it is also where :disabled="hasInterrupt" lands.
    3. TaskChatView.vue: template-branch swap (Open Question 2 recommendation — v-if swap, not mount-and-hide). Keep the header/toolbar/tabs chrome and the ChangedFilesPanel/TodoPanel. Remove dead imports/handlers from THIS view only — no legacy file under src/mainview/components/ is otherwise touched (D-10).
    4. e2e/ui/chat-copilotkit.spec.ts: write Spec 1 (streaming) and Spec 2 (history) using the agui fixture (from 05-01), api.handle for baseline RPC responses (copy the baseline set from fixtures/index.ts — they auto-apply), and openTaskDrawer. Do not touch legacy specs.

    The single end-to-end proof is the spec run in verify — a real user can now stream a chat in the task drawer through the new stack.
  </action>
  <verify>
    <automated>bun run build && npx playwright test e2e/ui/chat-copilotkit.spec.ts</automated>
    <human-check>Open the task drawer in bun run dev and confirm: message streams, markdown/code renders (basic), no board layout regression from the styles.css import.</human-check>
  </verify>
  <done>
    Streaming spec + history spec green in chat-copilotkit.spec.ts; task drawer chats through CopilotChat with threadId wiring; board chrome untouched.
  </done>
  <reversibility rating="reversible">Template-branch swap: legacy files untouched on disk; reverting the view + wrapper returns the old UI (D-10 rollback path).</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Chat states — loading, empty, error (refine the proven slice)</name>
  <files>
    src/mainview/components/chat/RailyinChat.vue
    e2e/ui/chat-copilotkit.spec.ts
  </files>
  <read_first>
    node_modules/@copilotkit/vue/dist/v2/components/chat/CopilotChatView.vue.d.ts (isConnecting, autoScroll props)
    node_modules/@ag-ui/client/dist/index.d.mts (AgentSubscriber — onMessagesSnapshot, onRunFinalized)
    src/mainview/components/SessionChatView.vue (scv-loading ProgressSpinner pattern lines 56-59)
    src/mainview/App.vue (toast usage pattern — useToast + toast.add for "Execution failed")
  </read_first>
  <acceptance_criteria>
    - Loading: while the thread connect/replay is in flight (isConnecting), RailyinChat renders a centered ProgressSpinner (legacy .scv-loading pattern) instead of an empty flash; the welcome screen never flashes (welcome-screen=false + explicit threadId/hasExplicitThreadId — Pitfall 2).
    - Empty: after connect resolves with zero messages (never-run thread — MockAgui returns the empty SSE body per RUNR-06), RailyinChat renders the UI-SPEC copy — heading "No messages yet" + body "Send a message to start, or type / to browse commands." — with the input enabled.
    - Error: a RUN_ERROR terminal (MockAgui script "error" — a run whose SSE body ends with RUN_ERROR carrying an error message) renders an inline error row in the message area AND a PrimeVue error toast "Execution failed: {error}" (matching legacy onStreamError behavior, App.vue:54-57); the input re-enables afterward.
    - Specs: chat-copilotkit.spec.ts gains empty-state (never-run thread shows the copy) and error-state (RUN_ERROR → inline row + toast + input re-enabled) scenarios.
  </acceptance_criteria>
  <action>
    Extend RailyinChat.vue with the three states, keyed off the verified hooks/props: isConnecting (CopilotChat prop — pass through or read from the agent subscription), a local message-count/empty computed (agent.subscribe({ onMessagesSnapshot }) — count messages), and a run-error surface (subscribe onRunFinalized / RUN_ERROR detection per the installed AgentSubscriber types; mirror App.vue's toast pattern via useToast, "Execution failed: {error}" with life 6000).

    Add the MockAgui "error" script variant: extend the /run dispatch in e2e/ui/fixtures/mock-agui.ts with a builder emitting RUN_STARTED → TEXT_MESSAGE_START/CONTENT/END ("hello") → RUN_ERROR (message "simulated failure") — reuse EventEncoder + patchRunStartedInput. Select the variant via a new `agui.script` property (default "quick").

    Write the two new spec scenarios; keep them in the same spec file's describe structure. Do not weaken the Task 1 specs.

    No fenced code blocks: implement per the verified identifiers above.
  </action>
  <verify>
    <automated>bun run build && npx playwright test e2e/ui/chat-copilotkit.spec.ts</automated>
  </verify>
  <done>
    Loading/empty/error states render per the UI-SPEC rows; all four chat-copilotkit scenarios green; typecheck passes.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces (plan 05-03)

- `src/mainview/components/chat/RailyinChat.vue` — the single CopilotChat surface (props threadId/title/commandsScope; #input slot with chat-input/stop-btn test ids; useDefaultRenderTool; useInterrupt; stop→abortRun; loading/empty/error states; non-scoped .railyn-chat CSS block)
- `src/mainview/App.vue` — CopilotKitProvider mount
- `src/mainview/main.ts` — @copilotkit/vue/styles.css import
- `src/mainview/components/TaskChatView.vue` — Chat-tab swap to RailyinChat
- `e2e/ui/chat-copilotkit.spec.ts` — streaming, history-reopen, empty-state, error-state scenarios
- `e2e/ui/fixtures/mock-agui.ts` — "error" script variant

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → DOM | Assistant text from /api/copilotkit streams into StreamMarkdown (component-based rendering — verified no v-html) |
| client → /api/copilotkit | The browser posts run/connect/stop to the app's own origin (same-origin gate enforced server-side, WR-03) |
| assistant content → toast | Error messages from RUN_ERROR render in a PrimeVue toast — text interpolation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | XSS via streamed assistant markdown | medium | mitigate | CopilotChat renders through StreamMarkdown (component AST, no v-html — verified in streamdown-vue dist); RailyinChat adds no v-html for assistant content; markdown parity CSS only (05-05) |
| T-05-02 | Spoofing | Unauthorized runtime use | low | accept | Existing control: same-origin 403 gate on the runtime mount (Phase 1, WR-03, verified in e2e/api); provider takes no keys |
| T-05-03 | Tampering | styles.css preflight leak (layout tampering) | low | mitigate | Import once in main.ts; verify board layout after import (Pitfall 4); overrides confined to the .railyn-chat root |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs this phase; pins covered by pins.test.ts |
</threat_model>

<verification>
- Wave gate: `bun run build && npx playwright test e2e/ui/chat-copilotkit.spec.ts` + `bun test src/mainview --timeout 20000` + `bun run typecheck`.
- Expected-soon-red: legacy chat-surface specs (chat.spec.ts, extended-chat, conversation-*, queue-messages, tool-rendering, delegate-rendering, interview-me) break as the swapped UI stops rendering the old stack — they are Phase 6 migration fodder (VERF-02); do NOT chase them green this phase. The 05-01 legacy proof ran BEFORE the swap and stays valid as the pre-swap baseline.
</verification>

<success_criteria>
- Task drawer chat streams through CopilotKit with history replay, empty/loading/error states, all proven by the new spec suite.
</success_criteria>

<output>
Create `.planning/phases/05-chat-ui-replacement-vue/05-03-SUMMARY.md` when done
</output>
