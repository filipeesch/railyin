---
phase: 05-chat-ui-replacement-vue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/ui/fixtures/mock-agui.ts
  - e2e/ui/fixtures/mock-agui.test.ts
  - e2e/ui/fixtures/index.ts
  - vitest.config.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-07
estimate:
  tokens: 16000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "MockAgui handles POST /api/copilotkit/agent/:agentId/connect with an EventEncoder-framed SSE replay body (RUN_STARTED + historic events + MESSAGES_SNAPSHOT + RUN_FINISHED; empty 200 SSE for never-run threads per RUNR-06) so the Phase 5 chat specs can assert CHAT-07 history replay (05-01, VALIDATION.md Wave 0)"
    - "MockAgui handles POST /api/copilotkit/agent/:agentId/stop/:threadId with { success: true } so the CHAT-04 stop specs can assert the client's abortRun→/stop round-trip (05-01)"
    - "The agui fixture is registered in e2e/ui/fixtures/index.ts and installable alongside ApiMock (route.fallback() hands /api/copilotkit/* to MockAgui, mock-api.ts:95-98); installing it does not break the legacy specs that run before the swap (chat.spec.ts stays green in this wave)"
    - "buildConnectReplaySseBody is unit-tested: replay shape contains the canonical event sequence and a never-run thread yields an empty SSE body (05-01 unit gate)"
  artifacts:
    - e2e/ui/fixtures/mock-agui.ts — /connect + /stop routes + buildConnectReplaySseBody
    - e2e/ui/fixtures/mock-agui.test.ts — builder unit tests
    - e2e/ui/fixtures/index.ts — agui auto-use fixture
    - vitest.config.ts — include extended to e2e/ui/fixtures/**/*.test.ts
  key_links:
    - "buildConnectReplaySseBody reuses EventEncoder framing + MOCK_AGUI_SSE_HEADERS + buildQuickRunEvents (probe-agent.ts:36-44) — never hand-rolls frames (mock-agui.ts header contract)"
    - "MockAgui.install() dispatch order: run/connect/stop/info branches BEFORE the 404 fallthrough"
    - "Agui fixture must not change behavior for legacy specs: /api/copilotkit/** is only ever called by the new chat surface; legacy specs that never issue those requests are unaffected"
---

<objective>
**Capability: the E2E fixture foundation for the new chat suite.** Extend `MockAgui` (the byte-validated AG-UI SSE fixture, `e2e/ui/fixtures/mock-agui.ts`) with the two routes the CopilotChat client needs on mount and stop: `POST /agent/:agentId/connect` (JSONL history replay per RUNR-05/RUNR-06 — drives CHAT-07) and `POST /agent/:agentId/stop/:threadId` (drives CHAT-04), plus a unit-tested `buildConnectReplaySseBody` builder, and register the fixture in `e2e/ui/fixtures/index.ts` so the new specs can consume it.

Purpose: this is the Wave 0 scaffold every Phase 5 chat spec depends on (VALIDATION.md Wave 0 Requirements). Without it, `CopilotChat`'s mount-time connect (threadId prop → auto `connectAgent`) 404s and the new specs cannot run. Legacy specs stay untouched (they never call `/api/copilotkit/*` — the old chat stack never used it).

Output: MockAgui routes + builder + unit tests + fixture wiring.

*MVP deviation (accepted, Phases 2/3/4 precedent): the ROADMAP goal is capability-shaped, not a user story; this plan is the test-infrastructure prerequisite (Wave 0) for the phase's tracer slice.*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Only what the plan touches:
@e2e/ui/fixtures/mock-agui.ts
@e2e/ui/fixtures/mock-api.ts
@e2e/ui/fixtures/index.ts
@e2e/api/copilotkit/probe-agent.ts
@vitest.config.ts
@.planning/phases/05-chat-ui-replacement-vue/05-PATTERNS.md  (lines 312-341 — MockAgui extension + fixture wiring patterns)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MockAgui /connect + /stop routes with unit-tested replay builder</name>
  <files>
    e2e/ui/fixtures/mock-agui.ts
    e2e/ui/fixtures/mock-agui.test.ts
    vitest.config.ts
  </files>
  <read_first>
    e2e/ui/fixtures/mock-agui.ts (entire file — install() dispatch, EventEncoder, MOCK_AGUI_SSE_HEADERS, patchRunStartedInput)
    e2e/api/copilotkit/probe-agent.ts (buildQuickRunEvents + ScriptedAgent — the canonical event source)
    vitest.config.ts (include pattern — must be extended for the fixture test)
  </read_first>
  <acceptance_criteria>
    - `e2e/ui/fixtures/mock-agui.ts` handles `POST /api/copilotkit/agent/{agentId}/connect` BEFORE the 404 fallthrough: parses the threadId from the URL path, fulfills with `MOCK_AGUI_SSE_HEADERS` and a body from `buildConnectReplaySseBody(threadId)`; a never-run/unknown threadId yields an empty SSE body (RUNR-06).
    - The connect replay body mirrors the runner replay shape: RUN_STARTED + the quick event sequence (reusing `buildQuickRunEvents`) + MESSAGES_SNAPSHOT + RUN_FINISHED, all `EventEncoder`-framed via the same patch/encode path as `buildQuickRunSseBody` — never hand-rolled frames.
    - `POST /api/copilotkit/agent/{agentId}/stop/{threadId}` fulfills `{ success: true }` as JSON.
    - `e2e/ui/fixtures/mock-agui.test.ts` unit-tests `buildConnectReplaySseBody`: (a) decoded frames contain RUN_STARTED then MESSAGES_SNAPSHOT then RUN_FINISHED in order; (b) empty-body path for unknown threads returns an empty string; (c) frames decode to valid AGUIEvent JSON.
    - `vitest.config.ts` test.include extended to `["src/mainview/**/*.test.ts", "e2e/ui/fixtures/**/*.test.ts"]` so `bun test e2e/ui/fixtures/mock-agui.test.ts` runs.
  </acceptance_criteria>
  <action>
    Extend `MockAgui.install()` in e2e/ui/fixtures/mock-agui.ts with two dispatch branches inserted between the /run branch and the /info branch (dispatch order: run → connect → stop → info → 404).

    Connect branch: match `POST` + regex `^/api/copilotkit/agent/[^/]+/connect$`; extract threadId from the path; fulfill with `MOCK_AGUI_SSE_HEADERS["content-type"]`, `cache-control: MOCK_AGUI_SSE_HEADERS["cache-control"]`, and body `buildConnectReplaySseBody(threadId)`.

    Add exported `buildConnectReplaySseBody(threadId: string): string` next to `buildQuickRunSseBody`: if the threadId is not a known thread id (fixture-side registry: a `Set<string>` of threadIds the spec pre-registered via a new `MockAgui.registerThread(threadId)` method), return empty string (never-run thread per RUNR-06); otherwise reuse `buildQuickRunEvents(threadId, runId)` with a stable runId (e.g. `"replay-run"`), append a `MESSAGES_SNAPSHOT` event listing the quick run's messages (messageId "m1", role "assistant", content "hello" — matching buildQuickRunEvents), and end with RUN_FINISHED; encode every frame with the same `encoder.encode(patchRunStartedInput(event, parsedInput))` path used in `buildQuickRunSseBody` (construct a minimal RunAgentInput for the patch: `{ threadId, runId, messages: [], forwardedProps: { script: "quick" } }` parsed through `RunAgentInputSchema.parse`). Keep all framing through `EventEncoder` and the existing `MOCK_AGUI_SSE_HEADERS` — never raw strings.

    Stop branch: match `POST` + regex `^/api/copilotkit/agent/[^/]+/stop/[^/]+$`; fulfill `{ success: true }` as application/json.

    Update the file-header usage comment: MockAgui is now wired into the fixture layer this phase (no longer "Phase 6 consumes it").

    Create e2e/ui/fixtures/mock-agui.test.ts — pure node tests (no @playwright/test imports) importing `buildConnectReplaySseBody` and `EventEncoder`: decode the produced SSE body frame-by-frame (split on "\n\n", strip "data: " prefixes) and assert the event-type sequence RUN_STARTED → … → MESSAGES_SNAPSHOT → RUN_FINISHED, that MESSAGES_SNAPSHOT's payload references the replayed "hello" message, and that an unregistered threadId produces an empty string. Add a `registerThread` test proving registration flips the empty→replay path.

    Extend `vitest.config.ts` test.include as specified in acceptance criteria.
  </action>
  <verify>
    <automated>bun test e2e/ui/fixtures/mock-agui.test.ts --timeout 20000 && bun run typecheck</automated>
  </verify>
  <done>
    Connect and stop routes live in MockAgui with unit-tested builder; the fixture test suite passes; typecheck green.
  </done>
  <reversibility rating="reversible">Test-fixture-only change; removing the routes restores the pre-phase fixture (404 fallthrough) with no app impact.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the agui fixture into fixtures/index.ts with legacy-spec safety proof</name>
  <files>
    e2e/ui/fixtures/index.ts
    e2e/ui/fixtures/mock-agui.ts
  </files>
  <read_first>
    e2e/ui/fixtures/index.ts (entire file — Fixtures type + base.extend pattern)
    e2e/ui/fixtures/mock-api.ts (route.fallback() /api/copilotkit hand-off, lines 91-98)
    e2e/ui/chat.spec.ts (first 40 lines — the legacy spec used as the regression proof)
  </read_first>
  <acceptance_criteria>
    - `agui: MockAgui` is added to the `Fixtures` type and registered as an auto-use fixture in `base.extend<Fixtures>` following the `ws` fixture pattern (install before `use(agui)`).
    - Install-order safety holds: `ApiMock` hands `/api/copilotkit/*` to `route.fallback()` (mock-api.ts:95-98), so agui's `/api/copilotkit/**` route wins regardless of install order.
    - Legacy proof: `bun run build && npx playwright test e2e/ui/chat.spec.ts` passes with the auto-agui fixture installed (legacy specs never issue /api/copilotkit requests; behavior-neutral).
    - No legacy spec file is modified by this task (git status shows only the fixtures changed).
  </acceptance_criteria>
  <action>
    In e2e/ui/fixtures/index.ts: import `MockAgui` from "./mock-agui"; add `agui: MockAgui` to the `Fixtures` type; add an auto-use fixture entry (same shape as the `ws` fixture: instantiate, `await agui.install()`, `await use(agui)`) BEFORE the `api` fixture entry (agui.install() is safe before/after api.install() because ApiMock's route.fallback() defers /api/copilotkit/* to the agui route — verify against mock-api.ts:91-98).

    Do NOT modify mock-agui.ts in this task unless the install-order proof requires it (it should not). Do NOT touch any legacy spec.

    Then run the legacy regression proof: `bun run build && npx playwright test e2e/ui/chat.spec.ts`. If any legacy spec fails due to the agui route interception, report the failure in the summary rather than adapting the legacy spec — interception must be behavior-neutral for specs that never call /api/copilotkit.
  </action>
  <verify>
    <automated>bun run build && npx playwright test e2e/ui/chat.spec.ts</automated>
  </verify>
  <done>
    agui auto-fixture wired; legacy chat.spec.ts passes unchanged; git diff touches fixtures only.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces (plan 05-01)

- `MockAgui.connect` route — `POST /agent/:agentId/connect` SSE replay (CHAT-07 mock)
- `MockAgui.stop` route — `POST /agent/:agentId/stop/:threadId` → `{ success: true }` (CHAT-04 mock)
- `buildConnectReplaySseBody(threadId)` — exported builder (replay / empty-body per RUNR-06)
- `MockAgui.registerThread(threadId)` — fixture-side thread registry
- `agui` auto-use fixture in `Fixtures` (e2e/ui/fixtures/index.ts)
- `e2e/ui/fixtures/mock-agui.test.ts` — builder unit tests
- `vitest.config.ts` include extension

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MockAgui ↔ client | Test fixture only — never ships to production; the mock's SSE bodies must stay byte-consistent with the real runtime wire (the text-diff test in e2e/api guards this) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | MockAgui connect replay frames | low | mitigate | Reuse `EventEncoder` + `MOCK_AGUI_SSE_HEADERS` + `buildQuickRunEvents` — the single-source-of-truth framing the fixture already mandates; the mock can never drift from the real wire format |
| T-05-02 | Spoofing | fixture route interception | low | accept | Test-only `page.route()` interception on localhost preview; no production surface; legacy specs proven unaffected by Task 2's regression run |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs this phase (RESEARCH §Package Legitimacy Audit — no [ASSUMED]/[SUS] additions; pin-lock test e2e/api/copilotkit/pins.test.ts covers the pinned stack) |
</threat_model>

<verification>
- Wave gate: `bun test src/mainview --timeout 20000` (frontend unit suite stays green) + `bun run build && npx playwright test e2e/ui/chat.spec.ts` (legacy chat spec stays green with the agui fixture installed).
- The new chat spec suite (05-03 onward) consumes `agui` from the fixtures.
</verification>

<success_criteria>
- MockAgui answers connect and stop; the replay builder is unit-tested; the agui fixture is wired; legacy specs are provably unaffected (chat.spec.ts green in this wave).
</success_criteria>

<output>
Create `.planning/phases/05-chat-ui-replacement-vue/05-01-SUMMARY.md` when done
</output>
