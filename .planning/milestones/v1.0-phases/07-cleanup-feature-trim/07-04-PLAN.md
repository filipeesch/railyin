---
phase: 07-cleanup-feature-trim
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - src/shared/rpc-types.ts
  - src/bun/handlers/conversations.ts
  - src/bun/handlers/tasks.ts
  - src/bun/handlers/chat-sessions.ts
  - src/bun/context-usage.ts
  - src/bun/test/handlers.test.ts
  - src/bun/test/context-estimator.test.ts
  - src/bun/test/cross-engine-context.test.ts
autonomous: true
requirements: [TRIM-file_diff, TRIM-code_review, TRIM-transition_event, TRIM-status/status_chunk, TRIM-usage display, TRIM-compaction_summary]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The seven trim RPCs are removed from RailynAPI and their handlers: conversations.getStreamEvents, conversations.contextUsage, tasks.contextUsage, tasks.compact, chatSessions.compact, tasks.getFileDiff, executions.respondShellApproval (the last one's orchestrator method died in 07-01)
    - MessageType contains only live members: user, assistant, system, tool_call, tool_result, decision_request_prompt, reasoning — transition_event, ask_user_prompt, file_diff, compaction_summary, code_review, status are gone
    - context-usage.ts keeps resolveContextWindow (live in tasks.ts:291,:405 + conversations.ts:82); only estimateConversationContextUsage dies
    - ContextEstimator + cross-engine-context (compaction-on-switch) stay live and tested; engine.compact interface untouched
    - tsc is clean and the trim grep terms return zero in src/ (type-removal-first ordering — the 07-03 store strips removed the frontend callers)
  artifacts:
    - rpc-types.ts: trimmed RailynAPI entries + trimmed MessageType
    - handlers: conversations.ts (getStreamEvents/contextUsage), tasks.ts (contextUsage/compact/getFileDiff), chat-sessions.ts (compact) — entry removals
    - context-usage.ts: estimateConversationContextUsage gone, resolveContextWindow intact
    - handlers.test.ts reworked (context-usage fallback coverage :995 area dies with the RPC)
  key_links:
    - RailynAPI type removal FIRST → tsc enumerates every handler/store straggler (the loud-failure strategy; store callers were stripped in 07-03)
    - tasks.setShellAutoApprove + chatSessions.setShellAutoApprove RPCs MUST STAY (the A3 replacement channel from 07-02)
    - conversations.getMessages + chatSessions.getMessages KEEP working (frozen reads + SessionChatView gate + smoke surface — research OQ4)
    - MessageType trim ⇢ any remaining producer of those types (consume()/executors were excised in 07-01; tsc proves no straggler)
---

<objective>
Remove the trimmed features' backend RPC surfaces (D-02): the seven trim RPCs, the six trimmed MessageType members, and the usage-display helper that only served them — keeping the frozen reads (getMessages), the shell-auto-approve channel, and the live compaction-on-switch path intact.

**As a** user, **I want to** the app's RPC surface to contain no endpoints for trimmed features (context usage, compaction, file-diff fetching, shell approval), **so that** the custom protocol and its feature trims are fully discharged on the backend — with nothing callable that the UI can no longer serve.

Purpose: the backend half of the trim. 07-03 stripped the frontend callers (task.ts store's fetchContextUsage/compactTask); this plan removes the type declarations and handler entries. Type-removal-first makes tsc the enumerator. Context-usage.ts is a partial death: resolveContextWindow stays (live in tasks.ts + conversations.ts model-context resolution), estimateConversationContextUsage dies with its RPC. ContextEstimator, cross-engine-context, and engine.compact are untouched live features.
Output: trimmed RailynAPI + MessageType, clean handlers, tsc green, trim grep terms zero in src/.
</objective>

## Artifacts this phase produces

- `src/shared/rpc-types.ts` — MessageType trimmed to user/assistant/system/tool_call/tool_result/decision_request_prompt/reasoning; RailynAPI entries removed: conversations.getStreamEvents, conversations.contextUsage, tasks.contextUsage, tasks.compact, tasks.getFileDiff, executions.respondShellApproval, chatSessions.compact (setShellAutoApprove entries KEPT)
- `src/bun/handlers/{conversations,tasks,chat-sessions}.ts` — trim handler entries removed; getMessages + code-review submit flows untouched
- `src/bun/context-usage.ts` — estimateConversationContextUsage deleted; resolveContextWindow kept (live in tasks.ts + conversations.ts)
- KEPT live: ContextEstimator, cross-engine-context compaction-on-switch, ExecutionEngine.compact interface
- Reworked: handlers.test.ts (context-usage coverage removed); context-estimator + cross-engine-context suites verified green

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-cleanup-feature-trim/07-RESEARCH.md
@.planning/phases/07-cleanup-feature-trim/07-PATTERNS.md
@src/shared/rpc-types.ts
@src/bun/context-usage.ts
</context>

<tasks>

<!-- planner-discipline-allow: getStreamEvents -->
<!-- planner-discipline-allow: contextUsage -->
<!-- planner-discipline-allow: getFileDiff -->
<!-- planner-discipline-allow: respondShellApproval -->
<!-- planner-discipline-allow: compaction_summary -->
<!-- planner-discipline-allow: transition_event -->
<!-- planner-discipline-allow: ask_user_prompt -->
<!-- planner-discipline-allow: code_review -->
<!-- planner-discipline-allow: file_diff -->
<!-- planner-discipline-allow: status_chunk -->
<!-- planner-discipline-allow: estimateConversationContextUsage -->
<!-- planner-discipline-allow: PersistedStreamEvent -->

<task type="auto" tdd="false">
  <name>Task 1: RailynAPI + MessageType trim in rpc-types.ts (type-removal-first)</name>
  <files>src/shared/rpc-types.ts</files>
  <read_first>
    - src/shared/rpc-types.ts: MessageType (:95-108), the RailynAPI entries at :781-788 (conversations.getStreamEvents + conversations.contextUsage), :817-826 (tasks.contextUsage + tasks.compact), :871-874 (tasks.getFileDiff), :957-968 (tasks.setShellAutoApprove KEEP / executions.respondShellApproval / chatSessions.setShellAutoApprove KEEP), :1103-1106 (chatSessions.compact), :1116-1119 (legacyImport.run — untouched here, 07-05 gates it)
  </read_first>
  <action>
    Remove the six MessageType members: transition_event (:101), ask_user_prompt (:102), file_diff (:104), compaction_summary (:106), code_review (:107), status (:108). KEEP decision_request_prompt (:103 — the live HITL message type; the decision flow still writes it via consume()'s decision_request case... note: consume() no longer persists chat messages at all (07-01), so verify whether any live code still PRODUCES a decision_request_prompt row — if none does, keep the type member anyway: it documents the frozen-table schema the importer maps (import.ts TRIMMED_TYPES references it) and frozen reads may return it. The member is a type for frozen rows, not a writer).

    Remove the seven RailynAPI entries: conversations.getStreamEvents (:781-784 — also drop its PersistedStreamEvent type import reference; db/stream-events.ts died in 07-01), conversations.contextUsage (:785-788), tasks.contextUsage (:817-820), tasks.compact (:823-826), tasks.getFileDiff (:871-874), executions.respondShellApproval (:961-964), chatSessions.compact (:1103-1106).

    KEEP: tasks.setShellAutoApprove (:957-960) and chatSessions.setShellAutoApprove (:965-968) — the A3 replacement channel; conversations.getMessages, chatSessions.getMessages, tasks.sendMessage/chatSessions.sendMessage (now { executionId } per 07-01), legacyImport.run (07-05 gates it).

    Run `bun run typecheck` and RECORD the straggler list — the errors must be confined to: handlers (conversations.ts, tasks.ts, chat-sessions.ts — Task 2's file set) and context-usage.ts (:36-42 estimateConversationContextUsage — Task 2). Fix nothing here; the wave is green when Task 2 lands.
  </action>
  <acceptance_criteria>
    - MessageType has exactly: user, assistant, system, tool_call, tool_result, decision_request_prompt, reasoning
    - RailynAPI has no getStreamEvents/contextUsage/tasks.compact/chatSessions.compact/getFileDiff/respondShellApproval entries; setShellAutoApprove entries still present
    - `bun run typecheck` errors, if any, are confined to the enumerated Task-2 files (handlers/conversations.ts, handlers/tasks.ts, handlers/chat-sessions.ts, context-usage.ts)
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>The shared contract no longer declares any trim RPC or trimmed message type; remaining tsc errors are the enumerated handler stragglers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Handler entry removals + context-usage partial trim + test reworks</name>
  <files>src/bun/handlers/conversations.ts, src/bun/handlers/tasks.ts, src/bun/handlers/chat-sessions.ts, src/bun/context-usage.ts, src/bun/test/handlers.test.ts, src/bun/test/context-estimator.test.ts, src/bun/test/cross-engine-context.test.ts</files>
  <read_first>
    - src/bun/handlers/conversations.ts (getStreamEvents handler + contextUsage handler + the resolveContextWindow usage at :82 — KEEP)
    - src/bun/handlers/tasks.ts (contextUsage/compact/getFileDiff handlers; resolveContextWindow usages at :291,:405 — KEEP)
    - src/bun/handlers/chat-sessions.ts (compact handler; getMessages KEEP)
    - src/bun/context-usage.ts (resolveContextWindow :7-34 KEEP; estimateConversationContextUsage :36-42 dies)
    - src/bun/test/handlers.test.ts (the tasks.contextUsage — resolveContextWindow coverage at :894-918 and the :995 fallback)
  </read_first>
  <action>
    Remove the handler entries that Task 1's type removal enumerated (the handler maps are Record-shaped; tsc points at each one): conversations.ts — getStreamEvents + contextUsage entries; tasks.ts — contextUsage + compact + getFileDiff entries; chat-sessions.ts — compact entry. Remove now-unused imports (e.g. the db/stream-events read helper that died in 07-01 if still imported). Do NOT touch the getMessages/getFileDiff-adjacent code beyond the entry removal — conversations.getMessages stays (frozen reads + SessionChatView gate), and the code-review submit flow (tasks.sendMessage with _type "code_review" → orchestrator.executeCodeReview → CodeReviewExecutor) is LIVE and untouched.

    context-usage.ts: delete estimateConversationContextUsage (:36-42) and its ConversationMessage/DB dependencies; keep resolveContextWindow (:7-34) verbatim (its importers tasks.ts + conversations.ts still resolve the model context window — the model-context feature, not the usage display). If estimateConversationContextUsage was the only user of an imported type, drop the import.

    Tests: handlers.test.ts — delete the tasks.contextUsage-driven resolveContextWindow coverage (:894-918) and the :995-area fallback coverage; keep the remaining handler tests (getMessages etc.). context-estimator.test.ts + cross-engine-context.test.ts — RUN them unmodified; they must stay green (ContextEstimator + compaction-on-switch are live). If any of their scenarios referenced a removed RPC, excise only that scenario.
  </action>
  <acceptance_criteria>
    - `rg -n "getStreamEvents|contextUsage|tasks\.compact|chatSessions\.compact|getFileDiff|respondShellApproval" src/bun/handlers src/bun/context-usage.ts src/shared/rpc-types.ts` returns zero
    - `rg -n "estimateConversationContextUsage" src` returns zero; resolveContextWindow still exported and used by tasks.ts + conversations.ts
    - `rg -n "compaction_summary|transition_event|ask_user_prompt|code_review|file_diff|\"status\"" src/bun --glob '!**/test/**' --glob '!**/migrations/**'` — expected results: zero for the trimmed members (compaction_summary may appear in migrations + importer TRIMMED_TYPES — those are frozen-read/documentation references, allowlisted)
    - `bun run typecheck` is CLEAN; `bun test src/bun/test/handlers.test.ts src/bun/test/context-estimator.test.ts src/bun/test/cross-engine-context.test.ts --timeout 20000` passes
  </acceptance_criteria>
  <verify>
    <automated>bun test src/bun/test/handlers.test.ts --timeout 20000</automated>
  </verify>
  <done>The trim RPCs and message types have zero references in production code, resolveContextWindow survives, and the live context/compaction paths are green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Trim grep gate + tripwire + wave gate</name>
  <files>src/bun/test/handlers.test.ts</files>
  <read_first>
    - 07-RESEARCH.md Pattern 3 (the gate sequence) and the D-07 gate terms
  </read_first>
  <action>
    Run the trim completeness gate: `git grep -n "getStreamEvents\|contextUsage\|respondShellApproval\|getFileDiff" src e2e` → expect zero. Then run the wave-3 gate: `bun run build` && `bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts` && `bun test e2e/api --timeout 30000` && `bun run typecheck` && `bun test src/bun --timeout 20000`. If any leg regressed (e.g. a spec that mocked a trimmed RPC), fix the straggler in this task — the 07-03 fixture layer already dropped the trimmed mocks, so a failure here indicates a missed caller.
  </action>
  <acceptance_criteria>
    - `git grep -n "getStreamEvents\|contextUsage\|respondShellApproval\|getFileDiff" src e2e` returns zero
    - Wave-3 gate green: build succeeds, tripwire specs pass, e2e/api passes, typecheck clean, src/bun passes (new count recorded)
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>All trim RPC terms are gone from the repo and the wave-3 gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC router → handlers | removed entries are unreachable — the surface shrinks |
| handlers → orchestrator | respondShellApproval/compact paths gone (methods died in 07-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-30 | Tampering | Zombie trim RPCs callable after trim | medium | mitigate | Type removal first → tsc enumerates every handler; Task 3 git grep gate (zero across src + e2e) is the enforcement |
| T-07-31 | Elevation of Privilege | respondShellApproval re-exposure | low | mitigate | Orchestrator method died in 07-01; handler entry + RailynAPI type removed here; setShellAutoApprove stays as the only shell channel (per A3) |
| T-07-32 | Information disclosure | getMessages frozen reads | low | accept | Intentional (research OQ4: keep the RPC for old-history display + smoke surface); read-only, no new surface |
| T-07-33 | Tampering | MessageType members used by hidden writers | low | mitigate | All producers excised in 07-01 (consume + executors); tsc + trim grep gate prove zero references in src/bun non-test |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package operations this phase — no supply-chain surface |
</threat_model>

<verification>
- Per-task: typecheck + targeted handler/context tests (verify fields) — no `-x` flag anywhere
- Task 3 closes the wave: build + tripwire + e2e/api + typecheck + src/bun
- Trim grep gates: zero for the seven RPC terms + six message types in production code
</verification>

<success_criteria>
- RailynAPI + MessageType trimmed; handlers cleaned; tsc clean
- resolveContextWindow + ContextEstimator + cross-engine-context + engine.compact live and green
- Trim grep terms zero; wave-3 gate green
</success_criteria>

<output>
Create `.planning/phases/07-cleanup-feature-trim/07-04-SUMMARY.md` when done
</output>
