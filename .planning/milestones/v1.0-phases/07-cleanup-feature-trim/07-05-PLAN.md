---
phase: 07-cleanup-feature-trim
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - src/bun/index.ts
  - src/bun/handlers/legacy-import.ts
  - src/shared/rpc-types.ts
  - src/mainview/components/chat/ChatThreadSidebar.vue
  - e2e/api/fixtures/server.ts
  - e2e/api/copilotkit/legacy-import.test.ts
  - e2e/ui/chat-copilotkit.spec.ts
  - e2e/ui/fixtures/index.ts
  - .planning/phases/07-cleanup-feature-trim/07-VALIDATION.md
autonomous: false
requirements: [TRIM-file_diff, TRIM-code_review, TRIM-transition_event, TRIM-status/status_chunk, TRIM-usage display, TRIM-compaction_summary, TRIM-ask_user, TRIM-shell_approval]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - legacyImport.run is registered ONLY when RAILYN_LEGACY_IMPORT=1 (server-side env gate at registration — the RAILYN_COPILOTKIT_PROBE precedent); without the flag the RPC is absent (404), with it the importer works exactly as before (idempotent, reads-only over frozen tables)
    - A legacyImport.enabled RPC (unconditional, returns { enabled: boolean }) drives the ChatThreadSidebar import-button visibility — the button is hidden when disabled (no 404-driven UI)
    - The import module (src/bun/copilotkit/import.ts) and its reads are untouched — frozen-table reads are permanent (D-04)
    - The D-07 post-deletion gate is green end-to-end: tripwire, git grep zero for protocol + dead-module terms, build, full Playwright 517/0, e2e/api (82 → new count), src/bun (2396 → new count, recorded), typecheck, mock-agui 23/0
    - All four phase success criteria hold: no trimmed features visible, zero new writes to frozen tables, zero protocol references + green suites, import retired behind the flag
  artifacts:
    - index.ts: legacyImportEnabled const + gated legacyImport.run registration + legacyImport.enabled registration
    - rpc-types.ts: legacyImport.enabled entry added next to legacyImport.run
    - ChatThreadSidebar.vue: import button visibility bound to legacyImport.enabled
    - e2e/api/fixtures/server.ts: legacyImport option in StartServerOptions (extraEnv RAILYN_LEGACY_IMPORT=1)
    - legacy-import.test.ts: all spawns pass the flag option
    - chat-copilotkit.spec.ts L-3 reworked: button hidden by default; visible + flow tested with the enabled mock
    - 07-VALIDATION.md closed (nyquist_compliant: true); 07-COVERAGE.md created (API-coverage decision record)
  key_links:
    - index.ts registration gate ⇢ legacy-import.test.ts spawn flags (without them the suite 404s) ⇢ ChatThreadSidebar button visibility ⇢ chat-copilotkit L-3 spec mocks
    - D-07 gate leg 2 (git grep) ⇢ every earlier plan's deletions — the phase-level completion proof
    - e2e/api count: the smoke rework (07-01) + legacy-import flag must leave the suite green at the new count
---

<objective>
Retire the legacy import behind the RAILYN_LEGACY_IMPORT flag (D-06), then run the full D-07 post-deletion gate and close out the phase's verification artifacts (VALIDATION + API-coverage decision).

**As a** user who already imported their legacy history, **I want to** the import button to disappear from the UI and the RPC to be unreachable, **so that** the legacy import path is retired by default while remaining available for any future migration (the module and its frozen-table reads stay).

Purpose: D-06 + D-07 + close-out. The flag uses the existing env-gate pattern (RAILYN_COPILOTKIT_PROBE at index.ts:262) with a type-safe visibility channel (legacyImport.enabled RPC — the recommended Pattern 4 option; build-time env and 404-driven UI are rejected). Then the 8-leg D-07 gate (tripwire → git grep zero → build → full Playwright → e2e/api → src/bun → typecheck → mock-agui) proves the four success criteria, and the phase's verification contract closes (VALIDATION.md nyquist sign-off + COVERAGE.md API-coverage declaration: detector {"detected":false,"signals":[]} — no external API integration, zero packages).
Output: import retired behind the flag, gate evidence recorded, phase validation closed.
</objective>

## Artifacts this phase produces

- `src/bun/index.ts` — legacyImportEnabled env gate + gated legacyImport.run registration + unconditional legacyImport.enabled RPC
- `src/shared/rpc-types.ts` — legacyImport.enabled entry beside legacyImport.run
- `src/mainview/components/chat/ChatThreadSidebar.vue` — import button visibility bound to legacyImport.enabled
- `e2e/api/fixtures/server.ts` — StartServerOptions.legacyImport option (extraEnv RAILYN_LEGACY_IMPORT=1)
- `e2e/api/copilotkit/legacy-import.test.ts` — all 5 spawns flagged; `e2e/ui/chat-copilotkit.spec.ts` L-3 reworked (hidden-by-default + enabled flow)
- `e2e/ui/fixtures/index.ts` — legacyImport.enabled api mock
- 07-VALIDATION.md closed (nyquist_compliant: true); 07-COVERAGE.md created (detector JSON verbatim + no-external-API declaration + assumption-delta table)
- D-07 gate evidence: 8-leg results + new e2e/api + src/bun counts

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cleanup-feature-trim/07-RESEARCH.md
@.planning/phases/07-cleanup-feature-trim/07-PATTERNS.md
@.planning/phases/07-cleanup-feature-trim/07-VALIDATION.md
@.planning/phases/06-e2e-migration-verification/06-SUMMARY.md
@src/bun/index.ts
@src/bun/handlers/legacy-import.ts
@src/mainview/components/chat/ChatThreadSidebar.vue
@e2e/api/fixtures/server.ts
</context>

<tasks>

<!-- planner-discipline-allow: RAILYN_LEGACY_IMPORT -->
<!-- planner-discipline-allow: legacyImport.run -->
<!-- planner-discipline-allow: legacyImport.enabled -->
<!-- planner-discipline-allow: StreamEvent -->
<!-- planner-discipline-allow: stream.event -->
<!-- planner-discipline-allow: stream.error -->
<!-- planner-discipline-allow: message.new -->
<!-- planner-discipline-allow: stream-tree -->
<!-- planner-discipline-allow: ConversationPanel -->
<!-- planner-discipline-allow: ConversationInput -->
<!-- planner-discipline-allow: ChatEditor -->
<!-- planner-discipline-allow: MessageBubble -->
<!-- planner-discipline-allow: StreamBlockNode -->

<task type="auto" tdd="false">
  <name>Task 1: Legacy import retirement behind the flag (D-06)</name>
  <files>src/bun/index.ts, src/bun/handlers/legacy-import.ts, src/shared/rpc-types.ts, src/mainview/components/chat/ChatThreadSidebar.vue, e2e/api/fixtures/server.ts, e2e/api/copilotkit/legacy-import.test.ts, e2e/ui/chat-copilotkit.spec.ts, e2e/ui/fixtures/index.ts</files>
  <read_first>
    - src/bun/index.ts (:262-267 the RAILYN_COPILOTKIT_PROBE env-gate precedent; :343 legacyImportHandlers registration)
    - src/bun/handlers/legacy-import.ts (the handler module shape — gate INSIDE or at registration, your choice, but the RPC must be absent when disabled, not erroring)
    - src/mainview/components/chat/ChatThreadSidebar.vue (import button :38-45 + legacyImport.run call :281 + toast flow :288-300)
    - src/shared/rpc-types.ts (:1116-1119 legacyImport.run entry — add legacyImport.enabled beside it)
    - e2e/api/fixtures/server.ts (StartServerOptions :23-58 + the copilotkitProbe extraEnv precedent :184-186)
    - e2e/api/copilotkit/legacy-import.test.ts (5 spawn sites :93,:143,:188,:250,:284)
    - e2e/ui/chat-copilotkit.spec.ts (L-3 :429-455 — the import-button spec)
    - e2e/ui/fixtures/index.ts (the api mock map — add legacyImport.enabled)
  </read_first>
  <action>
    Server gate: in index.ts, mirror the probe precedent — `const legacyImportEnabled = process.env.RAILYN_LEGACY_IMPORT === "1";` — and gate the legacyImportHandlers registration at :343 on it. Register an UNCONDITIONAL tiny `legacyImport.enabled` RPC (reads the same const; response { enabled: boolean }) so the frontend has a type-safe visibility channel. Add the matching RailynAPI entry in rpc-types.ts next to legacyImport.run (:1116-1119 — which stays declared; it is simply unregistered when the flag is off).

    Frontend: ChatThreadSidebar.vue — fetch legacyImport.enabled once (on mount or alongside loadSessions) and hide the import button (:38-45) when disabled (v-if on the enabled ref; keep the importing/loading state machinery for the enabled case). Do NOT drive visibility from a 404 or the toast flow (:288-300 stays for the enabled case).

    Test infrastructure: e2e/api/fixtures/server.ts — add a `legacyImport?: boolean` option to StartServerOptions mirroring copilotkitProbe (:33-38), setting extraEnv.RAILYN_LEGACY_IMPORT = "1" when true (:184-186 precedent). legacy-import.test.ts — add the option to all 5 spawns (:93,:143,:188,:250,:284).

    UI spec: chat-copilotkit.spec.ts L-3 (:429-455) — the button is now hidden by default, so the spec must: (a) assert the button is NOT visible when the fixture's legacyImport.enabled mock returns false (default), and (b) assert the full import flow (capture legacyImport.run, success/idempotent/failure toasts) with the enabled mock returning true + a visible button. e2e/ui/fixtures/index.ts — add legacyImport.enabled to the api mock map (default false; the L-3 test overrides via api.returns).
  </action>
  <acceptance_criteria>
    - `bun run typecheck` clean; `bun run build` succeeds
    - Without the flag: `legacyImport.run` returns 404 over the wire (spawn a server via the fixture withOUT the option and request it) and `legacyImport.enabled` returns { enabled: false }
    - With the flag: `bun test e2e/api/copilotkit/legacy-import.test.ts --timeout 30000` passes (all 5 spawns flagged)
    - ChatThreadSidebar hides the button when enabled=false; L-3 passes both branches (hidden-by-default + enabled flow)
    - `rg -n "RAILYN_LEGACY_IMPORT" src e2e` shows only the gate, the RPC registration, the fixture option, and the test spawns
  </acceptance_criteria>
  <verify>
    <automated>bun test e2e/api/copilotkit/legacy-import.test.ts --timeout 30000</automated>
  </verify>
  <done>The import is retired behind the flag end-to-end (server gate + enabled RPC + hidden button + flagged test spawns + reworked UI spec).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: D-07 post-deletion gate — full 8-leg sequence</name>
  <files>src/bun/index.ts, e2e/api/fixtures/server.ts</files>
  <read_first>
    - 07-RESEARCH.md Pattern 3 (the gate sequence verbatim)
    - 06-SUMMARY.md (the baseline evidence: 517/8/0 Playwright, 82 e2e/api, 2396 src/bun, mock-agui 23)
  </read_first>
  <action>
    Run the complete D-07 gate in ONE chained command per leg, starting with `rm -rf test-results playwright-report` (pre-gate hygiene per 06-SUMMARY):

    1. Tripwire: `bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts`
    2. grep gate: `git grep -n "StreamEvent\|stream.event\|stream.error\|message.new\|stream-tree\|StreamEventType" src e2e` → expect ZERO; then `git grep -n "ConversationPanel\|ConversationInput\|ChatEditor\|MessageBubble\|StreamBlockNode" src/mainview` → expect ZERO
    3. `bun run build`
    4. `bunx playwright test e2e/ui` → 517 pass / 8 skip / 0 fail (42 spec files)
    5. `bun test e2e/api --timeout 30000` → record the new count (82 baseline; the smoke rework + flag may change it)
    6. `bun test src/bun --timeout 20000` → record the new count (2396 baseline; pipeline-test deletions LOWER it — this is expected, record not regress)
    7. `bun run typecheck` → clean
    8. `bun test e2e/ui/fixtures/mock-agui.test.ts` → 23 pass

    No leg skippable. If any leg fails, fix the cause IN THIS TASK (the failure is a missed reference or a test not yet reworked — the fix belongs to the phase, not the next gate run). Record all leg results + the two new counts in the summary. The 8 Playwright skips (interview-me A6-gap) and 2 src/bun skips are pre-existing intentional — flag them in the summary for the phase-gate reviewer per 06-SUMMARY.
  </action>
  <acceptance_criteria>
    - Leg 2 grep gates both return zero
    - Leg 4: 517 passed / 8 skipped / 0 failed / 0 did-not-run
    - Legs 5-8: e2e/api, src/bun, typecheck, mock-agui all green with new counts recorded in the summary
    - All 8 legs executed in order; results documented
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>The full D-07 gate is green end-to-end with evidence recorded — all four phase success criteria hold.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase close-out — VALIDATION sign-off + COVERAGE decision record</name>
  <files>.planning/phases/07-cleanup-feature-trim/07-VALIDATION.md</files>
  <read_first>
    - 07-VALIDATION.md (the per-task verification map — fill every row with its result; the nyquist sign-off checklist at the bottom)
    - 06-SUMMARY.md (the 06-COVERAGE.md close-out precedent)
  </read_first>
  <action>
    Close 07-VALIDATION.md: fill the per-task verification map (every TBD row → result + command), mark Wave 0 requirements complete (baseline captured in 06-SUMMARY; inventory verified across 07-01..07-05), complete the sign-off checklist (all tasks have automated verify; no watch-mode flags; feedback latency < 17min held; sampling continuity held), and set `nyquist_compliant: true`, `status: closed`, `wave_0_complete: true` in the frontmatter.

    Create .planning/phases/07-cleanup-feature-trim/07-COVERAGE.md recording the API-coverage decision: re-run the detector — `node "$HOME/.config/opencode/gsd-core/bin/lib/api-coverage.cjs" --json 07-RESEARCH.md 07-CONTEXT.md` — record the verbatim JSON output (planning-time run: {"detected":false,"signals":[],...}), and declare: **No external API integration** — zero packages added/removed, zero API keys, zero external hosts; all UI traffic mocked via page.route; e2e/api is the single real-server layer. Coverage matrix not required (06 precedent).

    Assumption-delta check (advisory): walk 07-RESEARCH assumptions A1-A5 against the executed plans — A1 (session-status push required) and A2/A3 (checkpoint decisions) were confirmed/decided; record each as CONFIRMED/DECIDED in the COVERAGE file. If anything fired differently than assumed, record the delta with its resolution. Schema Gate: NOT triggered (zero migrations — frozen-table constraint held throughout; verify `git log --oneline -20 -- src/bun/db/migrations` shows no new migration in this phase).
  </action>
  <acceptance_criteria>
    - 07-VALIDATION.md frontmatter: nyquist_compliant: true, status: closed, wave_0_complete: true; per-task map fully populated
    - 07-COVERAGE.md exists with the detector JSON verbatim + the no-external-API declaration + assumption-delta table
    - `git log --oneline -20 -- src/bun/db/migrations` shows no phase-7 migration (frozen tables untouched)
  </acceptance_criteria>
  <verify>
    <automated>node "$HOME/.config/opencode/gsd-core/bin/lib/api-coverage.cjs" --json .planning/phases/07-cleanup-feature-trim/07-RESEARCH.md .planning/phases/07-cleanup-feature-trim/07-CONTEXT.md</automated>
  </verify>
  <done>The validation contract is closed, the API-coverage decision is recorded, and the assumption log is reconciled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC router → legacyImport.run | gated registration — absent when the flag is off |
| browser → legacyImport.enabled | unconditional boolean read — no privileged data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-40 | Tampering | legacyImport.run exposure when flag off | low | mitigate | Registration-time env gate (RAILYN_LEGACY_IMPORT=1); absent RPC = 404, never an error-driven UI path (Pattern 4 recommendation; 404-driven UI explicitly rejected) |
| T-07-41 | Information disclosure | legacyImport.enabled probe | low | accept | Returns a boolean derived from an env var — no sensitive data; local single-user app |
| T-07-42 | Tampering | Incomplete deletion (gate evasion) | high | mitigate | 8-leg D-07 gate is the phase proof: git grep zero for protocol + dead-component terms; every earlier plan's grep gates chain into it |
| T-07-43 | Tampering | Frozen-table migration drift | high | mitigate | Zero-migration rule verified in close-out (git log check); D-04 permanent constraint |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package operations this phase — no supply-chain surface |
</threat_model>

<verification>
- Task 1: flagged-spawn legacy-import suite + typecheck + build (no `-x` flag anywhere)
- Task 2: the full 8-leg D-07 gate with recorded evidence and new counts
- Task 3: VALIDATION frontmatter + COVERAGE.md existence + migration-log check
</verification>

<success_criteria>
- Legacy import retired behind the flag end-to-end (server gate, enabled RPC, hidden button, flagged spawns, reworked L-3)
- D-07 gate green on all 8 legs with counts recorded
- VALIDATION closed, COVERAGE decision recorded, assumption deltas reconciled, zero migrations
</success_criteria>

<output>
Create `.planning/phases/07-cleanup-feature-trim/07-05-SUMMARY.md` when done
</output>
