---
phase: 06-e2e-migration-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - e2e/ui/chat.spec.ts
  - e2e/ui/delegate-rendering.spec.ts
  - e2e/ui/conversation-body.spec.ts
autonomous: true
requirements:
  - VERF-02
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "chat.spec.ts (12 tests) is migrated onto the agui fixture per the PATTERNS map (D-01): M-1 → S-1 streaming pattern, M-3/4 ws.pushStreamEvent flows → /run script streams, N-6 → C-1 stop pattern (stop-btn + chat-stopped + agui.stopRequests), O-9/11 → S-2 registerThread history replay, O-10 → S-2 + registerHistory ordered history, N-5/7 keep the [data-task-id] exec-* task-card assertions (board surface, untouched), N-9's editor-enabled half migrates to chatTextarea-enabled-while-running and its queue-button half retires in-file with rationale"
    - "delegate-rendering.spec.ts (5 tests) is migrated to the toolcall script family (T-2 pattern — tool-card-tc-sub DelegateSummaryRenderer with intent 'Write the auth module' + markdown result), the legacy makeDelegateMessages seed and .delegate-divider/.msg--assistant-digest/.tc selectors are deleted, and test.describe.configure({ mode: 'serial' }) is DROPPED (Pitfall 4 — route clobbering is gone with per-test agui fixtures); the file runs green with default workers"
    - "conversation-body.spec.ts migrates CB-1/CB-1b → C-2 reasoning script ([data-message-id=r1] card) and CB-3 → T-2 toolcall script (tool-card-*), and retires CB-2 (virtualization — PERF-01 deferred, full-history replay is v1) and CB-4 (transition cards — trimmed feature) in-file with recorded rationale"
    - "Each migrated file passes ALONE after its migration (Pitfall 4/9 — never trust an aggregate run), the regression tripwire (chat-copilotkit + board + board-ws-updates) stays green throughout (D-03, Pitfall 8 — the canonical spec is never edited), and no green non-chat spec is touched"
  artifacts:
    - e2e/ui/chat.spec.ts — migrated (12/12 green)
    - e2e/ui/delegate-rendering.spec.ts — migrated (5/5 green, serial mode dropped)
    - e2e/ui/conversation-body.spec.ts — migrated (3/3 green after in-file retires)
  key_links:
    - "chat.spec legacy anchors: sendMessage helper (CodeMirror .cm-content), .msg--user M-1 (40-57), .msg__bubble.streaming M-2 (59-78), ws.pushStreamEvent M-3/4 (80-121), .pi-stop-circle N-6 (150-169), queue-btn N-9 (200-232), .conversation-inner .msg order O-10 (258-279) — each maps to a canonical-spec pattern (PATTERNS.md:195)"
    - "delegate-rendering serial mode at line 18 + makeDelegateMessages seed 27-120 — both deleted; T-2 canonical copy at chat-copilotkit.spec.ts:182-213"
    - "conversation-body anchors: ws.pushStreamEvent reasoning/text (72-73), .rb/.rb__content CB-1/1b (75-109), .conv-body .tc CB-3 (133-149), .transition-card/.msg--prompt CB-4 (151-179) — map per PATTERNS.md:199"
---

<objective>
**Capability: the three core streaming-surface specs pass on the mock foundation (VERF-02, D-01/D-02).** Migrate chat.spec.ts (12 tests — the largest legacy streaming spec), delegate-rendering.spec.ts (5 tests — tool-card family + serial-mode removal), and conversation-body.spec.ts (reasoning + tool groups + in-file retires) onto the agui fixture patterns, each keeping its test intent while asserting the new RailyinChat DOM.

Purpose: these are the streaming-core files the remaining migrations build confidence from — chat.spec is the oldest and most-referenced chat spec, delegate-rendering proves the tool-call rendering family, conversation-body proves the reasoning surface. Green here first validates the migration pattern at scale (not just the 3-test tracer) before the larger files in 06-04..06-06.

Output: three green migrated spec files with zero legacy chat selectors; the suite's red surface shrinks from 13 migrate files to 10.

*MVP deviation (accepted, Phases 2/3/4/5 precedent): capability-shaped goal; this plan executes the D-01 migration list incrementally (D-02).*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e2e-migration-verification/06-RESEARCH.md (Pattern 1 migration map 183-231; Pitfalls 3, 4, 8, 9)
@.planning/phases/06-e2e-migration-verification/06-PATTERNS.md (per-file migration maps 193-207 rows for chat.spec / delegate-rendering / conversation-body; operation patterns 1-8; legacy→new selector table 308-322)

# Migration template (FROZEN — read-only):
@e2e/ui/chat-copilotkit.spec.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate chat.spec.ts (12 tests — streaming, stop, history, editor-state)</name>
  <files>e2e/ui/chat.spec.ts</files>
  <read_first>
    e2e/ui/chat.spec.ts (whole file — suites M/N/O; the sendMessage helper; the legacy anchors listed in the plan key_links)
    e2e/ui/chat-copilotkit.spec.ts (S-1 63-83, S-2 85-110, C-1 237-265 — the exact patterns to copy)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md chat.spec map row (line 195)
    e2e/ui/fixtures/helpers.ts (the extracted chat helpers from 06-01)
  </read_first>
  <acceptance_criteria>
    - All 12 tests green on the agui fixture: M-1 streams via submitChatMessage + /run (S-1 pattern — assert user text + assistant 'hello' in [data-testid="copilot-chat-view"]); M-3/M-4 use agui scripts instead of ws.pushStreamEvent; N-6 uses agui.script = "slow" + stop-btn + chat-stopped + agui.stopRequests assertion (C-1); O-9/O-11 use registerThread + collectConnectRequests (S-2); O-10 uses registerHistory for ordered multi-message replay; N-5/N-7 keep the [data-task-id] exec-* task-card class assertions (board surface — untouched by the chat swap).
    - N-9's editor-enabled-while-running half asserts chatTextarea(page) enabled during a slow run; its queue-button half is retired in-file with rationale (queue UI removed — research Open Question 5).
    - Zero legacy selectors in the file: .msg--user, .msg--assistant, .msg__bubble.streaming, .conversation-inner .msg, .pi-stop-circle, queue-btn, .cm-content.
    - Verify: file passes ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Rewrite per the PATTERNS map (line 195) using the 06-01 helpers (chatTextarea / submitChatMessage / collectConnectRequests) imported from ./fixtures. Map each legacy anchor exactly: M-1 → S-1; M-3/4 → agui script streams; N-6 → C-1 (agui.script = "slow" BEFORE submitChatMessage; assert stopRequests contains String(conversationId)); N-9 → split (editor-half migrates, queue-half retires); O-9/11 → S-2; O-10 → registerHistory with the 4-message alternating array from PATTERNS (u1/a1/u2/a2) and assert nth-message order inside copilot-chat-view.

    Preserve the exec-* task-card assertions (N-5/N-7) exactly as-is (they assert the task-card execution surface, which is board-side and untouched). Keep the file name. No fenced code blocks — implement per the cited patterns.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/chat.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    chat.spec 12/12 green on the agui fixture; zero legacy chat selectors; N-9 queue half retired with rationale; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite on the proven fixture pattern; git revert restores the legacy spec.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate delegate-rendering.spec.ts (5 tests — delegate tool cards, drop serial mode)</name>
  <files>e2e/ui/delegate-rendering.spec.ts</files>
  <read_first>
    e2e/ui/delegate-rendering.spec.ts (whole file — serial mode line 18, makeDelegateMessages seed 27-120, .delegate-divider S-D1 124-134, .delegate-divider__label S-D2 136-145, .msg--assistant digest S-D3 147-156, .tc count S-D4/S-D5 158-217)
    e2e/ui/chat-copilotkit.spec.ts (T-2 182-213 — the toolcall-script domain-card pattern)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md delegate-rendering map row (line 198)
  </read_first>
  <acceptance_criteria>
    - All 5 tests assert the DelegateSummaryRenderer via [data-testid="tool-card-tc-sub"] (agui.script = "toolcall"): the card header contains the intent text, expanding via locator("button").first() reveals the markdown result — per T-2.
    - test.describe.configure({ mode: "serial" }) is deleted (Pitfall 4: per-test agui fixtures make parallel safe — proven by running the file with default workers).
    - The makeDelegateMessages seed (27-120) and all legacy selectors (.delegate-divider, .delegate-divider__label, .msg--assistant, .tc) are deleted.
    - Verify: file passes ALONE with default (non-serial, parallel) workers — the run must show all 5 tests executed, zero "did not run".
  </acceptance_criteria>
  <action>
    Rewrite the 5 tests onto the T-2 pattern: set agui.script = "toolcall" before submitChatMessage, assert tool-card-tc-sub contains the intent ("Write the auth module") and the result markdown after expand. Drop serial mode entirely. Delete the seed builders. Keep the file name. If the delegate intent text differs from the canonical fixture, verify against buildToolCallRunEvents in e2e/api/copilotkit/probe-agent.ts (the canonical event source — don't hand-roll new fixture sequences).
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/delegate-rendering.spec.ts</automated>
  </verify>
  <done>
    delegate-rendering 5/5 green in parallel mode (no serial configure, no did-not-run); legacy seed and selectors gone.
  </done>
  <reversibility rating="reversible">Spec rewrite; git revert restores.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate conversation-body.spec.ts (reasoning + tool groups; retire virtualization + transition cards in-file)</name>
  <files>e2e/ui/conversation-body.spec.ts</files>
  <read_first>
    e2e/ui/conversation-body.spec.ts (whole file — CB-1/CB-1b reasoning 68-109, CB-3 tool groups 133-149, CB-4 transition cards 151-179, CB-2 virtualization)
    e2e/ui/chat-copilotkit.spec.ts (C-2 267-286 reasoning card; T-2 182-213 tool cards)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md conversation-body map row (line 199)
  </read_first>
  <acceptance_criteria>
    - CB-1/CB-1b migrated to the C-2 reasoning pattern: agui.script = "reasoning", assert [data-message-id="r1"] collapsed (Thinking…|Thought for), expand via first button, assert summary text.
    - CB-3 migrated to the T-2 toolcall pattern: agui.script = "toolcall", assert tool-card-* group rendering.
    - CB-2 (virtualization — PERF-01 deferred, full-history replay is v1 behavior) and CB-4 (transition cards — trimmed feature, transition_event in the trim list) are retired in-file with one-line rationale each, recorded in the SUMMARY.
    - Zero .rb / .rb__content / .conv-body .tc / .transition-card / .msg--prompt / .msg__bubble.streaming selectors remain.
    - Verify: file passes ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Migrate per the PATTERNS map (line 199) with the 06-01 helpers. CB-1/CB-1b → C-2 pattern; CB-3 → T-2 pattern; delete CB-2/CB-4 tests with a comment-block rationale (or note in the commit message per the SUMMARY record). Keep the file name.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/conversation-body.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    conversation-body green (CB-1/CB-1b/CB-3 migrated, CB-2/CB-4 retired with rationale); zero legacy selectors; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite; in-file retires restorable via git.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 06-03)

- `e2e/ui/chat.spec.ts` — migrated: streaming via /run (S-1), stop via slow-script + stop-btn (C-1), history via registerThread/registerHistory (S-2); exec-* task-card asserts kept; N-9 queue half retired
- `e2e/ui/delegate-rendering.spec.ts` — migrated to tool-card-tc-sub; serial mode dropped; seed deleted
- `e2e/ui/conversation-body.spec.ts` — reasoning (C-2) + tool groups (T-2); CB-2/CB-4 retired in-file
- In-file retire rationale records (N-9 queue half, CB-2, CB-4) → SUMMARY

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated spec → agui fixture | All chat traffic flows through page.route /api/copilotkit/*; assertions target client-rendered DOM only |
| spec → task-card surface | exec-* class assertions on [data-task-id] cards — board-side surface, untouched by the chat swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Tampering | chat.spec rewrite breaking exec-* task-card asserts | medium | mitigate | N-5/N-7 assertions copied byte-identical (board surface — D-03); verify runs include board specs via tripwire |
| T-06-11 | Spoofing | Spec reaching a real server via unhandled route | low | mitigate | Auto-use fixtures install page.route for /api/** (ApiMock 501-loud) + /api/copilotkit/** (MockAgui) before page.goto — unchanged contract (D-04) |
| T-06-12 | Tampering | Serial-mode cascade hiding failures in delegate-rendering | medium | mitigate | Serial mode dropped + file run ALONE with default workers (Pitfall 4); verify asserts zero did-not-run |
| T-06-13 | Tampering | In-file retires removing live-feature coverage (over-retirement) | medium | mitigate | Each in-file retire carries one-line rationale (removed feature / config-moved / covered-by-new-spec); executor re-greps the selector before deleting the test (Pitfall 2); recorded in SUMMARY |
| T-06-14 | Tampering | Canonical spec (chat-copilotkit) drift | low | mitigate | FROZEN (Pitfall 8): no migration task edits it; tripwire after every task |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Per task: the migrated file alone (`bunx playwright test e2e/ui/<file>.spec.ts` after `bun run build`) + tripwire.
- Wave merge (this plan): tripwire + `bun test e2e/ui/fixtures/mock-agui.test.ts` + `bun run typecheck`.
- Red-surface check: `npx playwright test e2e/ui --list | wc -l` reflects only deletions so far (06-02) and no new files.
</verification>

<success_criteria>
- chat.spec 12/12, delegate-rendering 5/5 (parallel), conversation-body green — all on the agui fixture with zero legacy chat selectors; the suite's red surface shrinks from 13 migrate files to 10.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-migration-verification/06-03-SUMMARY.md` when done
</output>
