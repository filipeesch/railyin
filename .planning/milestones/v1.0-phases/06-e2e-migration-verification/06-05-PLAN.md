---
phase: 06-e2e-migration-verification
plan: 05
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - e2e/ui/chat-session-drawer.spec.ts
  - e2e/ui/interview-me.spec.ts
autonomous: true
requirements:
  - VERF-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "chat-session-drawer.spec.ts (45 tests) is migrated per the PATTERNS map (D-01): the 19 red session-chat tests (B/C/D-3/E-1/E-4/J-1/L-1/A-4) → S-1/S-2/C-1 patterns with session variants of the chat helpers on .session-chat-view, E-4 scroll → S-2 + registerHistory + the CopilotChat scroll container, and the 7 in-file retires land with rationale (A-6/G-1..3/H-2 model selector — removed; D-6 submitDecisions — covered by C-4 resume payload; K-1/K-2 file chips — removed); the 19 already-green tests stay byte-identical"
    - "interview-me.spec.ts (29 tests) migrates the 23 red tests onto the C-4/C-5 interrupt pattern (agui.script = 'interrupt' → decision-card + .di__option rows → flip to 'quick' before decision-submit → resume-payload assertions on agui.lastRunInput.resume); notes/recordAsDecisions intents map to DecisionInterrupt's .di__textarea--notes + recordAsDecisions checkbox (verified DecisionInterrupt.vue:37-122); submitDecisions RPC assertions become resume-payload assertions; the 6 green Decisions-tab tests (T-F/G, T-H/H2, T-I/I2) stay byte-identical"
    - "Per A6: if a migrated interview-me intent needs a decision-card surface DecisionInterrupt does not have (e.g. checkbox-type questions), the executor records the gap in the SUMMARY instead of hand-rolling a fixture/renderer extension — a gap in the RENDERER is a product decision for the phase-gate reviewer, not a fixture workaround"
    - "Each migrated file passes ALONE (Pitfall 4/9); the tripwire (chat-copilotkit + board + board-ws-updates) stays green throughout; green non-chat specs untouched (D-03); canonical spec never edited (Pitfall 8)"
  artifacts:
    - e2e/ui/chat-session-drawer.spec.ts — 19 migrated + 7 in-file retires; 19 green kept (45 total)
    - e2e/ui/interview-me.spec.ts — 23 migrated; 6 green kept
  key_links:
    - "chat-session-drawer anchors: typeInSessionEditor helper stays byte-identical (helpers.ts:45-54 — green specs depend on it; session chat variants of chatTextarea/submitChatMessage are NEW inline helpers on .session-chat-view), .session-chat-view .conv-body (394-399), status_chunk (419-433), scroll (607-626)"
    - "interview-me anchors: decision_request_prompt message seeds (42, 302), T-J streaming flow (405+), submitDecisions asserts → agui.lastRunInput.resume (C-4 pattern at chat-copilotkit.spec.ts:312-385)"
    - "DecisionInterrupt surface to map against: decision-card testid, .di__option rows, Other freetext, .di__textarea--notes, recordAsDecisions checkbox, decision-submit (DecisionInterrupt.vue:37-122)"
---

<objective>
**Capability: the session-chat and decision-interrupt surface specs pass on the mock foundation (VERF-02, D-01/D-02).** Migrate chat-session-drawer.spec.ts (19 red of 45 — session drawer chat on the new RailyinChat surface) and interview-me.spec.ts (23 red of 29 — decision-request intents on the canonical interrupt pattern).

Purpose: these two files carry the two remaining surface families — the session drawer (session-scoped chat variants) and the interrupt card UX (the human-in-the-loop channel, CHAT-09). They also hold the largest in-file retire sets tied to removed chrome (model selector in the session drawer, submitDecisions RPC flow).

Output: two green migrated spec files; the suite's red surface shrinks from 7 migrate files to 5.

*MVP deviation (accepted, Phases 2/3/4/5 precedent): capability-shaped goal; this plan executes the D-01 migration list incrementally (D-02).*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e2e-migration-verification/06-RESEARCH.md (Pattern 1; Assumption A6 (decision-card DOM); baseline table rows 81, 91; Pitfalls 3, 8, 9)
@.planning/phases/06-e2e-migration-verification/06-PATTERNS.md (per-file maps 196, 201; operation pattern 7 — decision interrupt)

# Migration template (FROZEN — read-only):
@e2e/ui/chat-copilotkit.spec.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate chat-session-drawer.spec.ts (19 migrate + 7 in-file retire; 19 green stay)</name>
  <files>e2e/ui/chat-session-drawer.spec.ts</files>
  <read_first>
    e2e/ui/chat-session-drawer.spec.ts (whole file — identify the 19 red vs 19 green; .session-chat-view .conv-body 394-399, status_chunk 419-433, scroll 607-626, model-selector tests A-6/G-1..3/H-2, D-6 submitDecisions, K-1/K-2 file chips)
    e2e/ui/chat-copilotkit.spec.ts (S-1 63-83, S-2 85-110, C-1 237-265)
    e2e/ui/fixtures/helpers.ts (typeInSessionEditor 45-54 — MUST stay byte-identical; openSessionDrawer 38-42)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md chat-session-drawer map row (line 196)
  </read_first>
  <acceptance_criteria>
    - The 19 red tests migrate: B/C/D-3/E-1/E-4/J-1/L-1/A-4 → S-1/S-2/C-1 patterns scoped to .session-chat-view (new INLINE session variants of chatTextarea/submitChatMessage — the shared helpers stay task-drawer-scoped; openSessionDrawer flow preserved); E-4 scroll → S-2 registerHistory + CopilotChat scroll container.
    - The 7 in-file retires land with one-line rationale each: A-6, G-1..3, H-2 (in-session model selector — removed, .input-model-select only in dead ConversationInput), D-6 (submitDecisions RPC flow — covered by C-4 resume payload), K-1/K-2 (file chips — removed).
    - The 19 already-green tests stay byte-identical; typeInSessionEditor and openSessionDrawer helpers untouched (Pitfall 3 — green session specs depend on them).
    - Verify: file passes ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Migrate per the PATTERNS map (line 196). For the session-chat surface, define inline chatTextareaSession/submitChatMessageSession locators targeting the RailyinChat instance inside .session-chat-view (the drawer's session chat renders the same copilot-chat-view testid — confirm the locator scope against SessionChatView.vue). Delete the 7 retire tests with recorded rationale. Leave the 19 green tests untouched. Keep the file name.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/chat-session-drawer.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    chat-session-drawer fully green (19 migrated + 19 kept, 7 retired with rationale); session helpers additive only; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite + in-file retires; git revert restores.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate interview-me.spec.ts (23 tests — decision interrupts on the canonical C-4/C-5 pattern)</name>
  <files>e2e/ui/interview-me.spec.ts</files>
  <read_first>
    e2e/ui/interview-me.spec.ts (whole file — decision_request_prompt message seeds 42/302, T-A..Q red tests, T-J streaming flow 405+, the 6 green Decisions-tab tests T-F/G, T-H/H2, T-I/I2)
    e2e/ui/chat-copilotkit.spec.ts (C-4/C-5 312-385 — interrupt + resume-payload pattern)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md interview-me map row (line 201)
    src/mainview/components/chat/DecisionInterrupt.vue lines 37-122 (the .di__option / Other freetext / .di__textarea--notes / recordAsDecisions checkbox / decision-submit surface)
  </read_first>
  <acceptance_criteria>
    - The 23 red tests migrate to the C-4/C-5 pattern: agui.script = "interrupt" before submitChatMessage → assert [data-testid="decision-card"] content → select .di__option rows → agui.script = "quick" before clicking decision-submit → assert agui.lastRunInput.resume carries interruptId "decision-interrupt-1" + non-empty payload.answers.
    - Notes/recordAsDecisions intents assert DecisionInterrupt's .di__textarea--notes and the recordAsDecisions checkbox (DecisionInterrupt.vue:37-122); submitDecisions RPC assertions become resume-payload assertions.
    - The 6 green Decisions-tab tests (T-F/G, T-H/H2, T-I/I2) stay byte-identical.
    - Per A6: if a test needs a decision surface DecisionInterrupt lacks (e.g. checkbox-type questions), the test is marked skipped-with-gap-note and the gap recorded in the SUMMARY — never a fixture/renderer workaround without the phase-gate reviewer.
    - Verify: file passes ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Migrate per the PATTERNS map (line 201) + operation pattern 7. For each red test: replace the decision_request_prompt ws seed with agui.script = "interrupt"; assert the decision-card + options; flip to "quick" before submit; assert the resume payload via expect.poll on agui.lastRunInput.resume (the C-4 pattern's exact assertions). Map notes/recordAsDecisions to the verified DecisionInterrupt surface. Keep the 6 green tests untouched. Keep the file name.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/interview-me.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    interview-me green (23 migrated, 6 kept); resume-payload asserts in place; any A6 gap recorded; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite; git revert restores.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 06-05)

- `e2e/ui/chat-session-drawer.spec.ts` — 19 tests migrated to session-scoped agui patterns; 7 in-file retires (model selector, submitDecisions, file chips); 19 green kept
- `e2e/ui/interview-me.spec.ts` — 23 tests on the interrupt pattern (decision-card, resume payload); 6 green kept
- In-file retire rationale records → SUMMARY

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated spec → agui fixture | Interrupt/stream traffic via page.route /api/copilotkit/* only |
| decision-card content → DOM | Fixture-authored interrupt questions render via DecisionInterrupt.vue — the same component the real runtime drives |
| resume payload → agui.lastRunInput | The /run request body after decision submit — client-authored, captured by the fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-19 | Tampering | Session-chat helpers clobbering shared helpers (typeInSessionEditor) | medium | mitigate | Session variants are NEW inline helpers scoped to .session-chat-view; typeInSessionEditor/openSessionDrawer byte-identical (Pitfall 3); green session specs in the tripwire set prove it |
| T-06-20 | Tampering | Interrupt tests asserting fixture-invented DOM (A6 gap) | medium | mitigate | Assertions map against verified DecisionInterrupt.vue:37-122 surface; gaps recorded as skipped-with-note, never fixture workarounds (A6) |
| T-06-21 | Tampering | submitDecisions RPC coverage lost in migration | medium | mitigate | Intent preserved as resume-payload assertions on agui.lastRunInput (C-4 pattern); the RPC flow is covered by the real-wire decision-cycle e2e in e2e/api (Phase 3 VERF-01) |
| T-06-22 | Tampering | Green Decisions-tab tests disturbed | medium | mitigate | T-F/G, T-H/H2, T-I/I2 kept byte-identical; file run alone catches drift |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Per task: migrated file alone after `bun run build` + tripwire.
- Wave merge: tripwire + `bun test e2e/ui/fixtures/mock-agui.test.ts` + `bun run typecheck`.
</verification>

<success_criteria>
- chat-session-drawer (45/45) and interview-me (29/29) green on the agui fixture; red surface shrinks from 7 migrate files to 5.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-migration-verification/06-05-SUMMARY.md` when done
</output>
