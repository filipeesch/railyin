---
phase: 06-e2e-migration-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/ui/queue-messages.spec.ts (deleted)
  - e2e/ui/model-persistence.spec.ts (deleted)
  - e2e/ui/reasoning-mode-select.spec.ts (deleted)
  - e2e/ui/sampling-preset-select.spec.ts (deleted)
  - e2e/ui/model-picker-multi-engine.spec.ts (deleted)
  - e2e/ui/attachment-history.spec.ts (deleted)
  - e2e/ui/mcp-tools.spec.ts (deleted)
  - e2e/ui/conversation-pagination.spec.ts (deleted)
  - e2e/ui/compact-button.spec.ts (deleted)
  - e2e/ui/transition-card-legacy.spec.ts (deleted)
  - e2e/ui/conversation-draft.spec.ts (deleted)
  - e2e/ui/code-server.spec.ts (in-file retire CS-D-1..5)
autonomous: false
requirements:
  - VERF-02
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The 11 whole-file retires (queue-messages 25, model-persistence 10, reasoning-mode-select 4, sampling-preset-select 10, model-picker-multi-engine 5, attachment-history 3, mcp-tools 34, conversation-pagination 10, compact-button 3, transition-card-legacy 2, conversation-draft 7 ≈ 113 tests) are each justified by the Pattern-2 grep proof that the tested feature exists ONLY in dead legacy components (e.g. queue-btn / .input-model-select / .ctx-popover / .inline-chip-text__chip--file / .chat-editor__chip / .msg--prompt / load-older sentinel), and each batch is confirmed by a blocking human checkpoint before deletion (RESEARCH Q1 — irreversible; D-01 retire-with-rationale discretion)"
    - "Retirement is recorded, not silent (Pitfall 7): per-file one-line rationale (what it tested → where the feature went: removed / config-moved / covered-by-new-spec) lives in this plan + the Phase 6 SUMMARY, and the git commit messages carry the rationale"
    - "code-server.spec.ts CS-D-1..5 (CodeRef chips — .attachment-chip .ln__* only in dead ConversationInput.vue) retire in-file; the 10 green CS-A/B/C tests stay untouched and green"
    - "Post-retire state is clean (D-03): no deleted file matches npx playwright test e2e/ui --list; every green non-chat spec stays untouched; the regression tripwire (chat-copilotkit + board + board-ws-updates) stays green; bun run typecheck clean"
  artifacts:
    - 11 deleted spec files (git history is the audit trail)
    - e2e/ui/code-server.spec.ts — 5 in-file test deletions, 10 green tests kept
    - Retire rationale table in this plan + Phase 6 SUMMARY
  key_links:
    - "Pattern-2 grep evidence per retired file (rg over entire src/mainview/ proving the feature exists only in dead legacy components; rg proving no live importer) — re-run by the executor BEFORE each checkpoint, not assumed from RESEARCH"
    - "e2e/api suite keeps real-server /api/copilotkit/* coverage — the retired specs' mock-only surface has no other coverage loss (RESEARCH Security Domain, last row)"
---

<objective>
**Capability: the removed-feature spec surface is retired with code-verified rationale and human approval, so the migrated suite contains only specs for live features (VERF-02).** Delete the 11 whole-file retires (specs whose subjects — queue UI, in-chat model/reasoning/sampling selectors, MCP popover, pagination, compaction ring, transition cards, draft persistence — were verified removed from the live UI in Phase 5) behind two blocking human checkpoints, and retire the 5 CodeRef-chip tests in-file from code-server.spec.ts.

Purpose: retirement is first-class Phase 6 scope (RESEARCH Q1/Q2) — these ~113 tests cannot be migrated because their features do not exist in the new surface, and re-adding removed UI for test compatibility is explicitly forbidden (Don't Hand-Roll: no queue/send-button testid shims). The D-01 discretion clause requires each retire be "recorded with rationale"; the checkpoints give the irreversible deletion a human nod.

Output: a suite whose red surface is exactly the 13 migrate files, ready for the 06-03..06-06 migration wave.

*MVP deviation (accepted, Phases 2/3/4/5 precedent): the ROADMAP goal is capability-shaped, not a user story; this plan executes the verified retire list with per-batch human gates.*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e2e-migration-verification/06-RESEARCH.md (baseline table rows 78-105 — per-file retire classification; Pattern 2 Retire-with-Rationale 232-242; Pitfalls 2, 7; Open Questions 1-2)
@.planning/phases/06-e2e-migration-verification/06-PATTERNS.md (per-file retire rationale table 331-348; retire process rules 348)
@.planning/research/FEATURES.md (anti-features/trim list — the authoritative removed-feature evidence)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Retire batch A — queue + model/reasoning/sampling selector specs (5 files, blocking human gate)</name>
  <read_first>
    .planning/phases/06-e2e-migration-verification/06-RESEARCH.md baseline table rows 82-84, 91, 101 (per-file retire classification + grep proof references) + Pattern 2 (232-242, the verification rule)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md retire rationale table (331-348)
    .planning/research/FEATURES.md (the authoritative removed-feature evidence)
  </read_first>
  <what-built>
    Retire-batch A prepared for deletion, each file justified by the Pattern-2 grep evidence (feature exists ONLY in dead legacy components, no live importer) re-run by the executor this session:

    | File (tests) | Subject → fate | Grep proof |
    |---|---|---|
    | queue-messages.spec.ts (25) | Queue UI + drain → removed (UI-SPEC:140 "no queue affordance") | queue-btn / queue-chips only in dead ConversationInput.vue |
    | model-persistence.spec.ts (10) | In-chat model selector persistence → removed | .input-model-select only at ConversationInput.vue:175 |
    | reasoning-mode-select.spec.ts (4) | Per-model reasoning-effort selector → removed; thinkingFormat now engines.yaml config (AGENTS.md); CHAT-05 covered by chat-copilotkit C-2 | ConversationInput.vue:290 only |
    | sampling-preset-select.spec.ts (10) | Preset selector → removed; presets now per-model in engines.yaml (AGENTS.md) | input-model-settings-select only in dead ConversationInput |
    | model-picker-multi-engine.spec.ts (5) | Engine-grouped model picker → removed with the legacy input; ManageModelsModal is a different surface | picker markup only in dead ConversationInput |

    The executor has run: rg -n "{selector}" src/mainview/ (→ only dead ConversationInput.vue) and rg -n "ConversationInput" src/mainview/ --include="*.vue" -l (→ no live importer) for each file, and will delete the 5 files upon approval, committing with a message that names each file + one-line rationale (Pitfall 7).
  </what-built>
  <how-to-verify>
    1. For each of the 5 files, confirm the executor's grep output shows the tested selector exists ONLY in dead legacy components (ConversationInput.vue / ChatEditor / MessageBubble) and no live view imports them. Spot-check with: rg -n "queue-btn|input-model-select|model-select__value|input-model-settings-select|model-picker" src/mainview/
    2. Confirm the 5 files are the exact retired list above (research baseline table rows 82-84, 91, 101).
    3. Decide: approve the deletion (the features are gone from the live UI; Phase 7 owns any re-trim debate), or reject any file whose feature you want back (then it must be re-implemented as new work, not migrated).
    4. After approval the executor deletes the 5 files + commits; verify with: npx playwright test e2e/ui --list | rg "queue-messages|model-persistence|reasoning-mode-select|sampling-preset-select|model-picker-multi-engine" → no matches.
  </how-to-verify>
  <resume-signal>Type "approved" (deletes batch A) or list the file(s) to keep/restore.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Retire batch B — content/trim specs (6 files, blocking human gate)</name>
  <read_first>
    .planning/phases/06-e2e-migration-verification/06-RESEARCH.md baseline table rows 87-90, 96, 100, 102 + Pattern 2 (232-242)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md retire rationale table (331-348)
    .planning/research/FEATURES.md (trim list: compaction_summary, transition_event, attachments out of scope)
  </read_first>
  <what-built>
    Retire-batch B prepared for deletion, same Pattern-2 grep evidence re-run by the executor:

    | File (tests) | Subject → fate | Grep proof |
    |---|---|---|
    | attachment-history.spec.ts (3) | [#ref|label] file-chip rendering → removed (CONT-01 attachments out of scope) | .inline-chip-text__chip--file only in dead MessageBubble/InlineChipText |
    | mcp-tools.spec.ts (34) | MCP server popover UI → removed; MCP tool calls in chat covered by default-card T-1 pattern | V-1..33 popover only in dead ConversationInput |
    | conversation-pagination.spec.ts (10) | Load-older sentinel pagination → removed; full-history replay is v1 (PERF-01 deferred) | load-older sentinel only in dead ConversationBody |
    | compact-button.spec.ts (3) | Context ring + manual compact → removed (trim; compaction_summary in trimmed list) | .ctx-popover / context-ring-btn only in dead ConversationInput |
    | transition-card-legacy.spec.ts (2) | .msg--prompt / transition_event conversation rendering → removed (trim) | .msg--prompt / .transition-card only in dead ConversationBody |
    | conversation-draft.spec.ts (7) | CodeMirror draft persistence → removed (no draft/initialValue props in CopilotChatInput.vue.d.ts — verified) | .chat-editor__chip + draft store only in dead ChatEditor |

    The executor has re-run the greps (e.g. rg -n "inline-chip-text__chip--file|ctx-popover|msg--prompt|load-older|transition-card|draft" src/mainview/ confined to dead components) and will delete the 6 files upon approval with rationale-bearing commit messages.
  </what-built>
  <how-to-verify>
    1. Spot-check the grep evidence per file (rg over src/mainview/ for the selectors in the table — each must resolve to dead legacy components only).
    2. Confirm the 6 files match the research baseline table rows 87-90, 96, 100, 102 (attachment-history, mcp-tools, conversation-pagination, compact-button, transition-card-legacy, conversation-draft).
    3. Decide: approve, or keep/reimplement any subject (e.g. if MCP popover or pagination must come back — that is new feature work, Phase 7 territory, not migration).
    4. After approval: executor deletes + commits; verify: npx playwright test e2e/ui --list | rg "attachment-history|mcp-tools|conversation-pagination|compact-button|transition-card-legacy|conversation-draft" → no matches.
  </how-to-verify>
  <resume-signal>Type "approved" (deletes batch B) or list the file(s) to keep/restore.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Retire CodeRef-chip tests in-file from code-server.spec.ts (CS-D-1..5)</name>
  <files>e2e/ui/code-server.spec.ts</files>
  <read_first>
    e2e/ui/code-server.spec.ts (344 lines — identify CS-D-1..5 vs the 10 green CS-A/B/C tests; the attachment-chip assertions)
    .planning/phases/06-e2e-migration-verification/06-RESEARCH.md row 24 of the baseline table (CS-D-1..5 retire rationale)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md retire rationale table row for code-server (line 346)
  </read_first>
  <acceptance_criteria>
    - CS-D-1..5 deleted from e2e/ui/code-server.spec.ts (CodeRef chips in the input area — .attachment-chip .ln__* exists only in dead ConversationInput.vue); rationale recorded in the SUMMARY.
    - The 10 green CS-A/B/C tests (code.ref WS dispatch + overlay flow) remain byte-identical and pass.
    - Verify: bunx playwright test e2e/ui/code-server.spec.ts → 10/10 green (was 10/15 with 5 red).
  </acceptance_criteria>
  <action>
    Grep-verify first (Pitfall 2): rg -n "attachment-chip|ln__" src/mainview/ must resolve to dead ConversationInput.vue only; rg -n "ConversationInput" src/mainview/ --include="*.vue" -l must show no live importer. Then delete the 5 CS-D tests from the file, leaving CS-A/B/C untouched. Do not touch the code.ref WS dispatch or overlay assertions (the live surface — CS-A/B/C prove UI-04/ws coverage). Commit with the rationale in the message (Pitfall 7).
  </action>
  <verify>
    <automated>bunx playwright test e2e/ui/code-server.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts && bun run typecheck</automated>
  </verify>
  <done>
    code-server.spec.ts 10/10 green with CS-D-1..5 gone; tripwire + typecheck green; retire rationale recorded.
  </done>
  <reversibility rating="costly">In-file test deletion; git diff restores it, but the coverage-intent decision is recorded for the human in the SUMMARY.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 06-02)

- 11 deleted spec files (queue-messages, model-persistence, reasoning-mode-select, sampling-preset-select, model-picker-multi-engine, attachment-history, mcp-tools, conversation-pagination, compact-button, transition-card-legacy, conversation-draft) — ~113 tests retired with rationale
- `e2e/ui/code-server.spec.ts` — CS-D-1..5 removed, 10 green tests kept
- Retire rationale table (this plan) + Phase 6 SUMMARY record + rationale-bearing commit messages

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retire decision → spec deletion | Human-approved, grep-verified deletion — irreversible at the test level (git history survives) |
| retired mock surface → coverage | The mock protocol surface the retired specs exercised (queue RPCs, selector classes) has no other UI consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering (process) | Whole-file retire deletion hiding a live-endpoint regression | medium | mitigate | Pattern-2 grep verification re-run by the executor before every deletion (rg over entire src/mainview/ — Pitfall 2); blocking human checkpoint per batch; e2e/api suite keeps real-server /api/copilotkit/* coverage (RESEARCH Security Domain) |
| T-06-07 | Tampering (process) | Unrecorded retire (Pitfall 7) | medium | mitigate | Per-file rationale table in plan + SUMMARY + commit messages; reviewer can distinguish trim-retire from accidental delete |
| T-06-08 | Tampering | Over-retirement of a live feature (e.g. selectors present in a non-chat modal) | medium | mitigate | Grep covers the whole src/mainview/ tree, not just chat components (Pitfall 2); the 28 green files are untouched by this plan (D-03) |
| T-06-09 | Spoofing | Deleted specs' mocked RPCs silently removed while live consumers exist | low | mitigate | RPC-method check: if a retired spec's RPC (e.g. queue.*, mcp.getStatus) still has a live consumer, the retire is wrong — executor verifies via rg of the RPC name in src/mainview before deletion |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- After each approved batch: `npx playwright test e2e/ui --list` no longer contains the deleted files; commit shows exactly the intended deletions.
- After Task 3: code-server.spec.ts 10/10 + tripwire + typecheck.
- End of plan: red surface is exactly the 13 migrate files (research baseline), nothing else disturbed (D-03).
</verification>

<success_criteria>
- 11 whole-file retires + 5 in-file retires executed with human approval and recorded rationale; suite red surface reduced from 25 files to the 13 migrate files; green files untouched.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-migration-verification/06-02-SUMMARY.md` when done
</output>
