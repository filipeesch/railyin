---
phase: 06-e2e-migration-verification
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - e2e/ui/tool-rendering.spec.ts
  - e2e/ui/stream-reactivity.spec.ts
  - e2e/ui/timeline-pipeline.spec.ts
autonomous: true
requirements:
  - VERF-02
estimate:
  tokens: 33000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "tool-rendering.spec.ts (13 tests) is migrated onto the toolcall script family (T-1/T-2/T-3 patterns): S-25 rawDiff → tool-card-tc-write (FileChangesRenderer dispatch — verified FileChangesRenderer.vue:15), S-26/S-31 subagent → tool-card-tc-sub, S-27 stale-running → T-3 registerThread replay-completed (pi-check-circle present, no spinner, no 'Running…'), S-29..33 cursor-family → model-agnostic quick script; legacy writtenFiles seeds and .tc/.fdiff__body selectors are gone"
    - "stream-reactivity.spec.ts migrates the 13 live intents (A-1..3, C-1/2, E-1..7 autoscroll, F-1, G-1/2) onto agui scripts; autoscroll assertions move from .conv-body to the CopilotChat scroll container (.railyn-chat); G writtenFiles stats assert via tool-card-tc-write +N/−N; B-2 (data-stream-version) and F-2 (status_chunk) retire in-file with rationale; B-1/D-1 green tests stay untouched"
    - "timeline-pipeline.spec.ts migrates the streaming intents (T-28/30/31/33/35 → quick script, T-29/32 → reasoning script C-2) and retires T-34/36 (status_chunk — trimmed feature) plus compaction/transition-event tests in-file with rationale"
    - "Each migrated file passes ALONE (Pitfall 4/9); the tripwire (chat-copilotkit + board + board-ws-updates) stays green throughout; no green non-chat spec touched (D-03); the canonical spec is never edited (Pitfall 8)"
  artifacts:
    - e2e/ui/tool-rendering.spec.ts — migrated (13/13 green)
    - e2e/ui/stream-reactivity.spec.ts — migrated (~13 green, B-2/F-2 retired in-file)
    - e2e/ui/timeline-pipeline.spec.ts — migrated (streaming intents green, status_chunk/compaction retired)
  key_links:
    - "tool-rendering legacy anchors: writtenFiles seeds 78-104/269-282/345/521/568, .tc cards, .fdiff__body — mapped per PATTERNS.md:204"
    - "stream-reactivity legacy anchors: ws.pushStreamEvent textChunk floods 71/372-383/429-470/507-538/627/712/773/824/847-861, toolCallEvent 85/112, .conv-body scroll asserts — mapped per PATTERNS.md:203"
    - "timeline-pipeline legacy anchors: ws.pushStreamEvent mkEvent text/reasoning 55/65/76/89-91/102-103/132/157/163, status_chunk T-34/36 117-149 — mapped per PATTERNS.md:202"
---

<objective>
**Capability: the tool-card and stream-reactivity surface specs pass on the mock foundation (VERF-02, D-01/D-02).** Migrate tool-rendering.spec.ts (13 tests — the full tool-card rendering family including rawDiff, subagent, stale-state), stream-reactivity.spec.ts (stream flooding + autoscroll + writtenFiles stats), and timeline-pipeline.spec.ts (streaming + reasoning intents) onto the agui fixture patterns.

Purpose: these three files carry the richest streaming assertions (event floods, scroll behavior, tool-card state machines) — they validate the agui fixture's fidelity under load and finish the tool/stream third of the migration wave.

Output: three green migrated spec files; the suite's red surface shrinks from 10 migrate files to 7.

*MVP deviation (accepted, Phases 2/3/4/5 precedent): capability-shaped goal; this plan executes the D-01 migration list incrementally (D-02).*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e2e-migration-verification/06-RESEARCH.md (Pattern 1; Pitfalls 3, 5, 8, 9; baseline table rows 93-95)
@.planning/phases/06-e2e-migration-verification/06-PATTERNS.md (per-file maps 202-204; operation patterns 1-8; selector table 308-322)

# Migration template (FROZEN — read-only):
@e2e/ui/chat-copilotkit.spec.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate tool-rendering.spec.ts (13 tests — full tool-card family)</name>
  <files>e2e/ui/tool-rendering.spec.ts</files>
  <read_first>
    e2e/ui/tool-rendering.spec.ts (whole file — writtenFiles seeds 78-104/269-282/345/521/568, .tc cards, .fdiff__body, subagent S-26/31, stale S-27, cursor-family S-29..33)
    e2e/ui/chat-copilotkit.spec.ts (T-1 159-180 default card, T-2 182-213 domain cards, T-3 215-231 replay-completed)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md tool-rendering map row (line 204)
    src/mainview/components/chat/tool-call-renderers/FileChangesRenderer.vue (line 15 — the +N/−N dispatch the S-25 assert targets)
  </read_first>
  <acceptance_criteria>
    - All 13 tests green via agui.script = "toolcall": generic cards → [data-testid="copilot-tool-render"] (T-1); shell/file/delegate domains → tool-card-tc-bash / tool-card-tc-write / tool-card-tc-sub (T-2); S-25 rawDiff asserts tool-card-tc-write showing the file path + change stats (FileChangesRenderer dispatch); S-26/S-31 subagent asserts tool-card-tc-sub intent + markdown result; S-27 stale asserts the T-3 replay-completed state (registerThread before open; .pi-check-circle present, no .pi-spinner/.pi-spin, no "Running…").
    - S-29..33 cursor-family tests become model-agnostic quick-script assertions (no engine-specific mocks — the cursor engine no longer drives chat rendering; D-01 intent kept, assertions normalized to the generic surface).
    - Zero .tc / .fdiff__body / writtenFiles-seed constructs remain.
    - Verify: file passes ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Rewrite per the PATTERNS map (line 204) with the 06-01 helpers. Map each legacy test to its canonical pattern (T-1/T-2/T-3). For S-25's rawDiff intent, assert the FileChangesRenderer surface via tool-card-tc-write (file path + +N/−N stat chips). For S-27's stale-state intent, use the registerThread replay path (T-3). For the cursor-family tests, use the generic quick script (agui.script = "quick") — the rendering intent is model-agnostic. No fenced code blocks — implement per the cited patterns.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/tool-rendering.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    tool-rendering 13/13 green on the toolcall fixture; zero legacy selectors; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite on the proven fixture pattern; git revert restores.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate stream-reactivity.spec.ts (floods, autoscroll, writtenFiles stats; retire data-stream-version + status_chunk)</name>
  <files>e2e/ui/stream-reactivity.spec.ts</files>
  <read_first>
    e2e/ui/stream-reactivity.spec.ts (whole file — textChunk flood sites 71/372-383/429-470/507-538/627/712/773/824/847-861, toolCallEvent 85/112, .conv-body scroll asserts, G writtenFiles stats, B-2 data-stream-version, F-2 status_chunk; B-1/D-1 green tests)
    e2e/ui/chat-copilotkit.spec.ts (S-1 streaming 63-83, T-2 writtenFiles domain card 182-213)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md stream-reactivity map row (line 203)
  </read_first>
  <acceptance_criteria>
    - The 13 live intents migrate: A-1..3, C-1/2, E-1..7 (autoscroll), F-1, G-1/2 → agui script streams (quick for text floods, toolcall for tool events); autoscroll assertions target the CopilotChat scroll container (.railyn-chat) instead of .conv-body; G writtenFiles stats assert via tool-card-tc-write +N/−N chips.
    - B-2 (data-stream-version) and F-2 (status_chunk — trimmed feature) retire in-file with rationale; B-1/D-1 (already green) stay byte-identical.
    - Zero ws.pushStreamEvent calls and zero .conv-body / .msg__bubble.streaming selectors remain.
    - Verify: file passes ALONE (flood-heavy — watch for timeouts; raise per-assertion timeouts only where the S-1 pattern already does), then tripwire.
  </acceptance_criteria>
  <action>
    Rewrite per the PATTERNS map (line 203) with the 06-01 helpers. Text-flood intents become quick-script streams (submitChatMessage then assert progressive content in copilot-chat-view — the fixture streams the full sequence; multi-message floods use registerHistory where the intent is ordered history rather than live push). Autoscroll intents assert scroll position on .railyn-chat (the CopilotChat container — verify the actual scrollable element in RailyinChat.vue before asserting). Keep B-1/D-1 byte-identical. Retire B-2/F-2 with recorded rationale.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/stream-reactivity.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    stream-reactivity green with live intents migrated and B-2/F-2 retired; zero legacy stream-mocking constructs; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite; git revert restores.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate timeline-pipeline.spec.ts (streaming + reasoning intents; retire status_chunk/compaction)</name>
  <files>e2e/ui/timeline-pipeline.spec.ts</files>
  <read_first>
    e2e/ui/timeline-pipeline.spec.ts (whole file — mkEvent text/reasoning builders 55/65/76/89-91/102-103/132/157/163, status_chunk T-34/36 117-149, compaction/transition events)
    e2e/ui/chat-copilotkit.spec.ts (S-1 63-83, C-2 267-286)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md timeline-pipeline map row (line 202)
  </read_first>
  <acceptance_criteria>
    - T-28/30/31/33/35 (text streaming intents) → S-1 quick-script pattern; T-29/32 (reasoning intents) → C-2 reasoning-script pattern ([data-message-id="r1"]).
    - T-34/36 (status_chunk — trimmed feature, status/status_chunk in the trim list) and any compaction/transition-event tests retire in-file with rationale.
    - Zero ws.pushStreamEvent / mkEvent constructs and zero .msg__bubble.streaming / .rb selectors remain.
    - Verify: file passes ALONE, then tripwire.
  </acceptance_criteria>
  <action>
    Rewrite per the PATTERNS map (line 202) with the 06-01 helpers: map the mkEvent text intents to the quick script and the reasoning intents to the reasoning script; delete the status_chunk and compaction/transition tests with recorded rationale. The mkEvent helper and StreamEvent-typed builders are deleted with the file's legacy machinery.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/timeline-pipeline.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    timeline-pipeline green with streaming/reasoning intents migrated and trimmed-feature tests retired; tripwire green.
  </done>
  <reversibility rating="reversible">Spec rewrite; git revert restores.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 06-04)

- `e2e/ui/tool-rendering.spec.ts` — 13/13 on toolcall fixture (default/domain/replay-completed cards; cursor family model-agnostic)
- `e2e/ui/stream-reactivity.spec.ts` — live intents on agui scripts + .railyn-chat autoscroll; B-2/F-2 retired
- `e2e/ui/timeline-pipeline.spec.ts` — streaming/reasoning intents migrated; status_chunk/compaction retired
- In-file retire rationale records → SUMMARY

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated spec → agui fixture | Flood/scroll/tool assertions consume fixture-generated SSE only; no real server |
| spec → CopilotChat scroll container | Autoscroll assertions on .railyn-chat — client DOM, not server state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Tampering | Autoscroll asserts on the wrong scroll element (Pitfall — .conv-body gone) | medium | mitigate | Executor verifies the actual scrollable container in RailyinChat.vue before asserting (PATTERNS row 203); file run alone catches element misses |
| T-06-16 | Tampering | slow-script parallel flakiness in stop-adjacent asserts | low | mitigate | Stop scenarios assert agui.stopRequests (deterministic) not timing (Pitfall 5); no stop scenarios in this plan's files — noted for 06-06 |
| T-06-17 | Tampering | In-file retires (B-2, F-2, T-34/36, compaction) removing live coverage | medium | mitigate | One-line rationale per retire (trimmed features — status_chunk/compaction in FEATURES.md trim list); executor re-grep before delete; SUMMARY record |
| T-06-18 | Tampering | Tool-state regressions masked by stale writtenFiles seeds | medium | mitigate | Seeds deleted; assertions derive from the canonical buildToolCallRunEvents fixture (probe-agent.ts — never hand-rolled) |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Per task: migrated file alone after `bun run build` + tripwire.
- Wave merge: tripwire + `bun test e2e/ui/fixtures/mock-agui.test.ts` + `bun run typecheck`.
</verification>

<success_criteria>
- tool-rendering 13/13, stream-reactivity green (live intents), timeline-pipeline green — zero legacy selectors/mocking constructs; red surface shrinks from 10 migrate files to 7.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-migration-verification/06-04-SUMMARY.md` when done
</output>
