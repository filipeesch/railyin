---
phase: 06-e2e-migration-verification
plan: 07
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-05
  - 06-06
files_modified:
  - .planning/phases/06-e2e-migration-verification/06-COVERAGE.md (created)
  - .planning/phases/06-e2e-migration-verification/06-VALIDATION.md (closed)
  - .planning/phases/06-e2e-migration-verification/06-SUMMARY.md (created)
autonomous: true
requirements:
  - VERF-02
  - VERF-03
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The D-05 full gate runs GREEN on the new stack (success criteria 2+3): bun run build; the full Playwright suite (all spec files — 42 after the 11 retires — npx/bunx playwright test e2e/ui, zero failures, zero did-not-run); bun test e2e/api --timeout 30000 (82 pass); bun test src/bun --timeout 20000 (2396 pass / 2 skip — D-06 bridge/runner suites stay green); bun run typecheck clean; bun test e2e/ui/fixtures/mock-agui.test.ts (23+ pass — the extended fixture self-tests)"
    - "Gate hygiene (RESEARCH Runtime State Inventory): stale red artifacts are cleared (rm -rf test-results playwright-report) BEFORE the final gate run so the gate evidence reflects the green run; dist/ is rebuilt by the gate itself"
    - "The API-coverage decision is recorded (plan:pre contribution): 06-COVERAGE.md exists with the verbatim detector output {'detected':false,'signals':[]} and the reasoned 'No external API integration' declaration — this phase adds zero packages, zero API keys, zero external hosts (all traffic mocked in page.route; e2e/api is the single real-server layer)"
    - "06-VALIDATION.md is closed (nyquist_compliant: true, per-task map statuses green, sign-off) and the Phase 6 SUMMARY records: the D-05 gate evidence with exact counts, the full retire rationale table (11 whole-file + ~40 in-file), the migration delta (13 files migrated), and any A6/A1 assumption deltas that fired"
  artifacts:
    - .planning/phases/06-e2e-migration-verification/06-COVERAGE.md
    - .planning/phases/06-e2e-migration-verification/06-SUMMARY.md
    - .planning/phases/06-e2e-migration-verification/06-VALIDATION.md (closed)
  key_links:
    - "D-05 gate sequence (RESEARCH Pattern 4): build → full Playwright → e2e/api → src/bun → typecheck → mock-agui self-tests — ordered; the Playwright full run is the last-longest leg (~17 min at 6 workers)"
    - "Count anchors: 53 spec files on disk at phase start (A1 — ROADMAP '55' = count drift) → 42 after the 11 whole-file retires (06-02) → all green"
    - "COVERAGE detector output (run at planning time): {\"detected\":false,\"signals\":[]}"
---

<objective>
**Capability: the entire automated surface is green on the new stack and the phase closes with full evidence (VERF-02 + VERF-03, D-05/D-06).** Run the ordered D-05 gate — build, full Playwright suite, backend smoke (e2e/api), bridge/runner units (src/bun), typecheck, fixture self-tests — all green; record the API-coverage decision (no external API integration); close 06-VALIDATION.md; write the Phase 6 SUMMARY with gate evidence + the complete retire/migrate rationale tables.

Purpose: this is the phase's success-criterion gate — "the entire automated test surface is green on the new stack before any cleanup". It is a final-task gate per D-05, not remediation work: every precondition (e2e/api 82, src/bun 2396, typecheck, build) was verified green at planning time, and the migration wave (06-01..06-06) clears the red surface before this plan runs.

Output: the green full-suite gate evidence, 06-COVERAGE.md, closed 06-VALIDATION.md, and 06-SUMMARY.md.

*MVP deviation (accepted, Phases 2/3/4/5 precedent): capability-shaped goal; this plan executes the D-05 verification gate and the phase close-out.*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e2e-migration-verification/06-RESEARCH.md (Pattern 4 — the ordered gate sequence 260-272; Assumption A1 — 53 vs 55 spec count; Runtime State Inventory 287-299; verified baseline 68-117)
@.planning/phases/06-e2e-migration-verification/06-VALIDATION.md (the contract being closed — per-task verification map, Wave 0, sign-off checklist)
@.planning/phases/06-e2e-migration-verification/06-02-PLAN.md (retire rationale table — the SUMMARY's retire record source)
@.planning/phases/05-chat-ui-replacement-vue/05-COVERAGE.md (the COVERAGE.md format precedent)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: D-05 full-suite gate — all suites green on the new stack</name>
  <files>e2e/ui/** (read-only — gate evidence), .planning/phases/06-e2e-migration-verification/06-SUMMARY.md (gate evidence recorded)</files>
  <read_first>
    .planning/phases/06-e2e-migration-verification/06-RESEARCH.md Pattern 4 (lines 260-272 — the exact ordered sequence) + verified baseline table (68-117 — the count anchors)
    playwright.config.ts (testDir/testMatch/workers/retries — do not edit)
  </read_first>
  <acceptance_criteria>
    - Pre-gate hygiene: stale artifacts cleared (rm -rf test-results playwright-report) so gate artifacts reflect the green run (RESEARCH Runtime State Inventory).
    - Regression tripwire first: bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts — green (the canonical + board sets must never have been disturbed).
    - Full gate sequence, each leg green:
      1. bun run build
      2. bunx playwright test e2e/ui — full suite, ALL spec files (42 after retires), 0 failures, 0 did-not-run
      3. bun test e2e/api --timeout 30000 — 82 pass / 0 fail
      4. bun test src/bun --timeout 20000 — 2396 pass / 2 skip / 0 fail (D-06)
      5. bun run typecheck — clean
      6. bun test e2e/ui/fixtures/mock-agui.test.ts — 23+ pass (extended fixture)
    - The full-suite summary line shows zero failures; if ANY leg fails, the executor fixes the cause (it will be a migration defect, NOT a new gate — the gate is a final task per D-05, research preconditions were all green).
  </acceptance_criteria>
  <action>
    Run the D-05 sequence in order per Pattern 4. Record the exact output of every leg (pass counts, duration) in 06-SUMMARY.md as the gate evidence block. If a leg fails, triage and fix the migrated spec/fixture (the failing file is always a 06-01..06-06 artifact — its fix is in-scope; do not edit green specs or the canonical spec), then re-run the full sequence. Do not skip any leg; do not declare the gate green on a partial run.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui && bun test e2e/api --timeout 30000 && bun test src/bun --timeout 20000 && bun run typecheck && bun test e2e/ui/fixtures/mock-agui.test.ts</automated>
  </verify>
  <done>
    All six gate legs green with the expected counts recorded (Playwright 0 failures / 0 did-not-run; e2e/api 82; src/bun 2396 pass / 2 skip; typecheck clean; mock-agui 23+); gate evidence written to 06-SUMMARY.md.
  </done>
  <reversibility rating="reversible">Gate is evidence-gathering + fix-on-failure; all fixes are spec/fixture-level and git-revertable.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Close-out — 06-COVERAGE.md, 06-VALIDATION.md, Phase 6 SUMMARY</name>
  <files>
    .planning/phases/06-e2e-migration-verification/06-COVERAGE.md (created)
    .planning/phases/06-e2e-migration-verification/06-VALIDATION.md (closed)
    .planning/phases/06-e2e-migration-verification/06-SUMMARY.md (created)
  </files>
  <read_first>
    .planning/phases/05-chat-ui-replacement-vue/05-COVERAGE.md (format precedent — detector output verbatim + reasoned declaration)
    .planning/phases/06-e2e-migration-verification/06-VALIDATION.md (the frontmatter + sign-off checklist to close)
    .planning/phases/06-e2e-migration-verification/06-02-PLAN.md (retire rationale table source)
    .planning/phases/06-e2e-migration-verification/06-01..06-06-SUMMARY.md (per-plan evidence to aggregate)
  </read_first>
  <acceptance_criteria>
    - 06-COVERAGE.md exists with the verbatim detector output {"detected":false,"signals":[]} (re-run the detector at close-out: node <gsd-core>/bin/lib/api-coverage.cjs --json 06-RESEARCH.md 06-CONTEXT.md) and the reasoned "No external API integration" declaration: zero packages added, zero API keys, zero external hosts; all traffic mocked via page.route; e2e/api is the single real-server layer (per 05-COVERAGE.md precedent).
    - 06-VALIDATION.md closed: nyquist_compliant: true; per-task verification map rows all green (retire grep checks, migrated-file runs, gate); sign-off checklist complete.
    - 06-SUMMARY.md created with: gate evidence block (exact counts), the full retire rationale table (11 whole-file + in-file retires by file), the migration delta (13 files migrated, helper/knob additions), assumption deltas that fired (A1 spec-count drift confirmed; A6 decision-card surface gaps if any), and next-phase readiness (Phase 7 cleanup scope — red surface zero, IMPR-03 rollback window closes).
  </acceptance_criteria>
  <action>
    Create 06-COVERAGE.md per the 05-COVERAGE.md precedent (detector output verbatim + declaration + rationale table). Update 06-VALIDATION.md frontmatter (nyquist_compliant: true) and mark every row in the per-task verification map green. Create 06-SUMMARY.md aggregating the six plan summaries + the gate evidence + the retire/migrate records. If any assumption delta fired during execution (A1, A6, Open Question 3 test-ID scheme), record the resolution.
  </action>
  <verify>
    <automated>node "$(dirname $(which gsd-tools 2>/dev/null || echo /Users/filipe.esch/.config/opencode/gsd-core/bin/gsd-tools.cjs))/lib/api-coverage.cjs" --json .planning/phases/06-e2e-migration-verification/06-RESEARCH.md .planning/phases/06-e2e-migration-verification/06-CONTEXT.md && rg -n "detected.:false" .planning/phases/06-e2e-migration-verification/06-COVERAGE.md && rg -n "nyquist_compliant: true" .planning/phases/06-e2e-migration-verification/06-VALIDATION.md</automated>
  </verify>
  <done>
    06-COVERAGE.md, closed 06-VALIDATION.md, and 06-SUMMARY.md all exist with the acceptance-criteria content; detector output recorded verbatim.
  </done>
  <reversibility rating="reversible">Documentation artifacts; all content is evidence-recorded.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 06-07)

- Green full-suite gate evidence (recorded in 06-SUMMARY.md): build + 42-file Playwright suite + e2e/api 82 + src/bun 2396 + typecheck + mock-agui 23+
- `.planning/phases/06-e2e-migration-verification/06-COVERAGE.md` — API-coverage decision record (detected:false, no external API)
- `.planning/phases/06-e2e-migration-verification/06-SUMMARY.md` — gate evidence + retire/migrate records + assumption deltas
- `.planning/phases/06-e2e-migration-verification/06-VALIDATION.md` — closed (nyquist_compliant: true)

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate evidence → phase close | The D-05 verdict is only as trustworthy as the recorded run — no partial/skipped legs |
| retired/migrated records → Phase 7 | The SUMMARY's retire rationale is Phase 7's input for the trim-deletion decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-27 | Tampering | Gate declared green on a partial/skipped leg | medium | mitigate | Task 1 verify chains all six legs in one command; SUMMARY records each leg's exact counts; no leg skippable by instruction |
| T-06-28 | Tampering | Stale red artifacts misattributed to the gate run | low | mitigate | test-results/playwright-report cleared before the final run (RESEARCH Runtime State Inventory) |
| T-06-29 | Spoofing | A green spec silently reaching a real server (mock-boundary leak) surfacing only at gate scale | medium | mitigate | Auto-use fixtures install page.route for /api/** (501-loud) + /api/copilotkit/** before any goto; the tripwire runs first as the canary |
| T-06-30 | Tampering | COVERAGE/VALIDATION closed without evidence | low | mitigate | Automated verify greps the verbatim detector output + nyquist flag in the closed files |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs (RESEARCH §Package Legitimacy Audit); if the executor is tempted to add a package, that is a don't-hand-roll violation — the pinned stack covers all gate tooling |
</threat_model>

<verification>
- Task 1: the six-leg chained command (automated; the Playwright leg is the long pole at ~17 min).
- Task 2: detector re-run + content greps (automated).
- Phase close: all 3 success criteria met — (1) Playwright passes against the mock foundation, (2) all existing specs green alongside new chat/board specs, (3) backend smoke + bridge/runner suites green on the new stack.
</verification>

<success_criteria>
- The D-05 gate is green end-to-end with recorded evidence; the API-coverage decision is recorded (no external API integration); VALIDATION.md is closed; Phase 6 SUMMARY exists with the full retire/migrate audit trail — Phase 7 (cleanup) can proceed against a provably green stack.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-migration-verification/06-07-SUMMARY.md` when done
</output>
