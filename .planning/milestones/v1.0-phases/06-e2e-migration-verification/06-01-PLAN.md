---
phase: 06-e2e-migration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/ui/fixtures/mock-agui.ts
  - e2e/ui/fixtures/mock-agui.test.ts
  - e2e/ui/fixtures/helpers.ts
  - e2e/ui/fixtures/index.ts
  - e2e/ui/conversation-stream-state.spec.ts
autonomous: true
requirements:
  - VERF-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MockAgui serves a configurable multi-message history replay per thread (D-04 Pattern 3): a new optional historyMessages knob on buildConnectReplaySseBody replaces the hardcoded single 'hello' snapshot, and a new per-instance registerHistory(threadId, messages) method stores per-thread history next to knownThreadIds (WR-05 parity — never module-level); the default behavior (snapshot [{id:'m1', role:'assistant', content:'hello'}]) stays byte-identical when the knob is omitted (backward compat)"
    - "The fixture extension is wire-valid by construction and by test (Pitfall 6): every frame still goes through EventEncoder + patchRunStartedInput (mock-agui.ts:35, 56-61); mock-agui.test.ts gains ≥4 new cases (provided messages in order, default when omitted, per-instance isolation, snapshot before the single terminal RUN_FINISHED) and the full file runs green via bun test e2e/ui/fixtures/mock-agui.test.ts"
    - "Chat-surface helpers chatTextarea / submitChatMessage / collectConnectRequests are extracted VERBATIM from chat-copilotkit.spec.ts:31-59 into e2e/ui/fixtures/helpers.ts and re-exported from e2e/ui/fixtures/index.ts; every legacy helper (openTaskDrawer, sendMessage, openSidebar, openSessionDrawer, typeInSessionEditor, openSessionNotesTab) stays byte-identical (Pitfall 3); chat-copilotkit.spec.ts is NOT edited (Pitfall 8 — frozen migration template)"
    - "The first migrated spec lands (D-01/D-02 incremental pattern proven end-to-end): conversation-stream-state.spec.ts is rewritten onto the agui fixture (SS-1/SS-2 use the threadId-switch pattern — per-thread registerHistory/registerThread + streaming via /run; SS-3 stays) with zero legacy selectors (.msg--user / .msg--assistant / .msg__bubble.streaming) and zero ws.pushStreamEvent / conversations.getMessages stream mocking; the file runs green alone"
    - "The regression tripwire stays green after every fixture change (Pitfall 9): chat-copilotkit.spec.ts + board.spec.ts + board-ws-updates.spec.ts all pass, and bun run typecheck is clean"
  artifacts:
    - e2e/ui/fixtures/mock-agui.ts — historyMessages knob + registerHistory
    - e2e/ui/fixtures/mock-agui.test.ts — new builder cases (≥4)
    - e2e/ui/fixtures/helpers.ts — chatTextarea, submitChatMessage, collectConnectRequests
    - e2e/ui/fixtures/index.ts — re-exports
    - e2e/ui/conversation-stream-state.spec.ts — migrated (3/3 green)
  key_links:
    - "buildConnectReplaySseBody snapshot site (mock-agui.ts:334-347): the hardcoded ternary becomes historyMessages ?? default — the ONLY builder change (PATTERNS.md:211-263)"
    - "connect route branch (mock-agui.ts:479-498): passes the per-thread history through; /run branch, registerThread, stopRequests, script dispatch untouched"
    - "index.ts export line (144-145): the three new helpers join the existing export list — migrated specs import from ./fixtures"
    - "conversation-stream-state SS-1/SS-2 (spec lines 31-88): ws.pushStreamEvent + getMessages stubs replaced by agui scripts + registerHistory per threadId"
---

<objective>
**Capability: the mock foundation is extended and the migration pattern is proven end-to-end on the first spec (VERF-02 foundation).** Extend MockAgui with the configurable multi-message history replay knob (D-04 Pattern 3) plus its self-tests, extract the chat-surface helpers (chatTextarea / submitChatMessage / collectConnectRequests) from the canonical spec into the shared fixture layer, and migrate the smallest spec — conversation-stream-state (3 tests) — onto the agui fixture as the tracer that proves the whole loop: fixture knob → helper → migrated spec green.

Purpose: every later migration plan (06-03..06-06) consumes exactly these two fixture assets — the history knob (needed by chat.spec O-10, chat-session-drawer CD-E-1/E-4, task-drawer TD-5/6, stream-reactivity C-1/C-2/E-7) and the shared chat helpers. Building and proving them first de-risks the 13-file migration wave.

Output: the extended mock-agui.ts + mock-agui.test.ts, the extended helpers.ts + index.ts, and the green migrated conversation-stream-state.spec.ts.

*MVP deviation (accepted, Phases 2/3/4/5 precedent): the ROADMAP goal is capability-shaped, not a user story; this plan builds the phase's foundation slice and proves the migration pattern.*
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e2e-migration-verification/06-RESEARCH.md (Pattern 3 — multi-message history knob; Pitfalls 3, 4, 6, 8, 9; A5 per-instance isolation)
@.planning/phases/06-e2e-migration-verification/06-PATTERNS.md (MockAgui extension section 211-263; helpers extraction 265-277; green-file protection 350-356)

# Migration template (FROZEN — read-only this plan):
@e2e/ui/chat-copilotkit.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — MockAgui multi-message history knob (registerHistory) + self-tests</name>
  <files>e2e/ui/fixtures/mock-agui.ts, e2e/ui/fixtures/mock-agui.test.ts</files>
  <read_first>
    e2e/ui/fixtures/mock-agui.ts (buildConnectReplaySseBody 297-361 — the hardcoded snapshot ternary at 334-347; MockAgui class 363-406 — knownThreadIds registry pattern; connect route branch 479-498 — where the knob threads through)
    e2e/ui/fixtures/mock-agui.test.ts (decodeFrames/typesOf helpers 22-34; describe buildConnectReplaySseBody 36-99 — the pattern for the new cases)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md lines 211-263 (the exact extension spec)
  </read_first>
  <behavior>
    - Test 1: buildConnectReplaySseBody("t-h", "quick", known, historyMessages=[{id:"u1",role:"user",content:"Round 1"},{id:"a1",role:"assistant",content:"Reply 1"},{id:"u2",role:"user",content:"Round 2"},{id:"a2",role:"assistant",content:"Reply 2"}]) → the MESSAGES_SNAPSHOT frame's messages array equals the provided array in order
    - Test 2: buildConnectReplaySseBody("t-h", "quick", known) with no historyMessages → snapshot equals [{id:"m1",role:"assistant",content:"hello"}] (backward compat — byte-identical body)
    - Test 3: per-instance registerHistory isolation — history registered on MockAgui instance A never replays through instance B's registry (WR-05 parity)
    - Test 4: with a history knob, the type sequence keeps MESSAGES_SNAPSHOT strictly before the single terminal RUN_FINISHED (last frame)
  </behavior>
  <action>
    RED first: add the four mock-agui.test.ts cases above to the existing describe buildConnectReplaySseBody block (mirroring the patterns at lines 36-99) and run bun test e2e/ui/fixtures/mock-agui.test.ts — the new cases must fail before any fixture change.

    GREEN: extend buildConnectReplaySseBody (mock-agui.ts:297-361) with an optional historyMessages parameter of type Array<{ id: string; role: string; content?: string }> that, when provided, REPLACES the snapshot ternary at lines 334-347 (default = today's behavior). Add registerHistory(threadId, messages) to the MockAgui class (363-406) storing per-thread history in a NEW per-instance map beside knownThreadIds, and thread the map through the connect route branch (479-498) into buildConnectReplaySseBody.

    Constraints (Pitfall 3 + Don't Hand-Roll): do NOT touch the /run branch, registerThread, stopRequests, script dispatch, or any EventEncoder usage — every frame must still go through encoder.encode(patchRunStartedInput(...)) (mock-agui.ts:35, 56-61). The historic event sequence stays the quick sequence; only the snapshot grows. Per-instance state only (A5/WR-05) — never module-level maps.
  </action>
  <verify>
    <automated>bun test e2e/ui/fixtures/mock-agui.test.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    mock-agui.test.ts runs green with the 4 new cases; the default connect replay body is unchanged (backward-compat test passes); tripwire green; commit with the fixture + test change together (Pitfall 6).
  </done>
  <reversibility rating="reversible">Additive knob on a fixture; default path byte-identical; git revert restores the previous builder.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract chat-surface helpers into fixtures (chatTextarea, submitChatMessage, collectConnectRequests)</name>
  <files>e2e/ui/fixtures/helpers.ts, e2e/ui/fixtures/index.ts</files>
  <read_first>
    e2e/ui/chat-copilotkit.spec.ts lines 31-59 (the verbatim extraction source — chatTextarea, collectConnectRequests, submitChatMessage)
    e2e/ui/fixtures/helpers.ts (file conventions — import type { Page } from "@playwright/test", JSDoc per export; the legacy helpers at 12-60 that MUST stay byte-identical)
    e2e/ui/fixtures/index.ts line 144-145 (the export line to extend)
  </read_first>
  <acceptance_criteria>
    - helpers.ts gains exactly three new exports — chatTextarea(page), submitChatMessage(page, text), collectConnectRequests(page) — copied verbatim from chat-copilotkit.spec.ts:31-59 (same bodies, same JSDoc, no re-implementation); all six legacy helpers byte-identical.
    - index.ts line 144-145 exports the three new helpers alongside the existing six.
    - chat-copilotkit.spec.ts is NOT modified (frozen — Pitfall 8; its inline helper copies stay in place so the canonical spec keeps working untouched).
    - Verify: bun run typecheck clean + tripwire green.
  </acceptance_criteria>
  <action>
    Append the three helpers to e2e/ui/fixtures/helpers.ts following the file's existing conventions (import type { Page } from "@playwright/test"; JSDoc comment above each export; no new imports beyond what the extraction needs — note collectConnectRequests needs only Page, submitChatMessage composes chatTextarea). Extend the re-export list at index.ts line 144-145.

    Do NOT touch openTaskDrawer, sendMessage, openSidebar, openSessionDrawer, typeInSessionEditor, openSessionNotesTab (green specs depend on them — Pitfall 3). Do NOT modify chat-copilotkit.spec.ts (Pitfall 8). No behavior changes: extraction is copy-paste-verbatim.
  </action>
  <verify>
    <automated>bun run typecheck && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    Three new helpers exported from ./fixtures; typecheck clean; tripwire green; no legacy helper or canonical spec touched.
  </done>
  <reversibility rating="reversible">Additive exports only; nothing consumed yet by other files.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate conversation-stream-state.spec.ts — the tracer spec on the agui fixture</name>
  <files>e2e/ui/conversation-stream-state.spec.ts</files>
  <read_first>
    e2e/ui/conversation-stream-state.spec.ts (whole file — 3 tests, 111 lines)
    e2e/ui/chat-copilotkit.spec.ts (S-1 lines 63-83 streaming pattern; S-2 lines 85-110 history-replay pattern)
    .planning/phases/06-e2e-migration-verification/06-PATTERNS.md per-file map row for conversation-stream-state (line 207) + Shared Patterns table 293-306
    e2e/ui/fixtures/index.ts (agui fixture usage — test({ page, api, agui }))
  </read_first>
  <acceptance_criteria>
    - SS-1 migrates to the threadId-switch pattern: two tasks with distinct conversationIds, per-thread agui registration (registerThread or registerHistory per threadId), streaming via submitChatMessage + /run (S-1 pattern); assertion that task B's chat never shows task A's content uses the new surface ([data-testid="copilot-chat-view"] content) — no .msg--user / .msg--assistant / .msg__bubble.streaming selectors anywhere in the file.
    - SS-2 keeps its session-switch flow (openSessionDrawer) but streams via the agui fixture instead of ws.pushStreamEvent; on return to task A, the prior content is asserted via copilot-chat-view (registerHistory for task A's thread, or S-2 registerThread replay).
    - SS-3 stays green (background-task isolation — same selector rewrite only).
    - The legacy taskTextChunk helper (lines 14-28), the StreamEvent import, and all conversations.getMessages stream stubs are deleted.
    - Verify: the file runs green ALONE (Pitfall 4: never trust an aggregate run for the first migration), then the tripwire.
  </acceptance_criteria>
  <action>
    Rewrite the file per the PATTERNS.md map (line 207): SS-1/SS-2 → the S-1 streaming pattern with two threadIds + per-thread registerHistory (use the Task-1 knob — task A registered with a small alternating history so the content asserted on reopen is fixture-driven, never legacy-ws-driven); SS-3 → same streaming pattern with a background thread. Keep the file name (git history + ROADMAP counts survive). Use the helpers from ./fixtures (chatTextarea/submitChatMessage/collectConnectRequests — Task 2) instead of re-declaring them.

    Every migrated assertion targets the new surface testids: copilot-chat-view, chat-input, [data-message-id] rows. The intent of each test (stream isolation across drawers/sessions) is preserved verbatim in spirit (D-01: keep test intent, assert new DOM).

    After the rewrite, run the file alone; then the tripwire. If SS-2's session-switch needs a session variant of the chat helpers on .session-chat-view, mirror the canonical pattern (chatTextarea scoped to the session chat view) INLINE in this spec rather than changing shared helpers.
  </action>
  <verify>
    <automated>bun run build && bunx playwright test e2e/ui/conversation-stream-state.spec.ts && bunx playwright test e2e/ui/chat-copilotkit.spec.ts e2e/ui/board.spec.ts e2e/ui/board-ws-updates.spec.ts</automated>
  </verify>
  <done>
    conversation-stream-state 3/3 green on the agui fixture; zero legacy chat selectors and zero ws.pushStreamEvent uses in the file; tripwire green; migration pattern proven end-to-end.
  </done>
  <reversibility rating="reversible">Spec-file rewrite on a migrated fixture; git diff/revert restores the legacy spec if the pattern misproves.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces (plan 06-01)

- `e2e/ui/fixtures/mock-agui.ts` — `historyMessages` knob on `buildConnectReplaySseBody` + `MockAgui.registerHistory(threadId, messages)` (per-instance map; default behavior byte-identical)
- `e2e/ui/fixtures/mock-agui.test.ts` — ≥4 new builder cases (order, default, isolation, terminal position)
- `e2e/ui/fixtures/helpers.ts` — `chatTextarea(page)`, `submitChatMessage(page, text)`, `collectConnectRequests(page)` (verbatim extraction)
- `e2e/ui/fixtures/index.ts` — re-exports of the three new helpers
- `e2e/ui/conversation-stream-state.spec.ts` — migrated (SS-1/SS-2 threadId-switch + registerHistory, SS-3 kept), 3/3 green

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock-agui.ts builder → migrated specs | Fixture-generated SSE frames consumed by CopilotChat — the boundary where framing drift silently drops events |
| helpers.ts → all specs | Shared helper surface — edits here affect the 28 green files that import the legacy helpers |
| registered history data → connect replay | Test-authored message content flows into MESSAGES_SNAPSHOT frames rendered by the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | buildConnectReplaySseBody history knob | medium | mitigate | Knob only replaces the snapshot array; every frame still framed by EventEncoder + patchRunStartedInput (mock-agui.ts:35, 56-61); never hand-rolled strings; backward-compat self-test proves the default body unchanged |
| T-06-02 | Tampering | mock-agui.test.ts coverage gap | medium | mitigate | Pitfall 6 gate: no builder change without a self-test case — the 4 new cases ship in the same commit as the knob |
| T-06-03 | Tampering | helpers.ts shared surface | medium | mitigate | ADD-only extraction; six legacy helpers byte-identical (Pitfall 3); tripwire (chat-copilotkit + board + board-ws-updates) after the edit; chat-copilotkit.spec.ts frozen (Pitfall 8) |
| T-06-04 | Spoofing | migrated spec reaching a real server | low | mitigate | agui fixture routes /api/copilotkit/** via page.route (fixtures/index.ts 52-56); ApiMock route.fallback contract unchanged; no real-server paths (D-04, AGENTS.md) |
| T-06-05 | Tampering | per-instance state leakage (WR-05) | medium | mitigate | registerHistory map is per-MockAgui-instance like knownThreadIds; isolation self-test case covers cross-instance leakage |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs this phase (RESEARCH §Package Legitimacy Audit — no packages); pins locked by pins.test.ts |
</threat_model>

<verification>
- Per-task: mock-agui self-tests (Task 1), typecheck + tripwire (Task 2), migrated file alone + tripwire (Task 3) — all automated, all < 5 min.
- Per-wave (this plan is Wave 1): tripwire + `bun test e2e/ui/fixtures/mock-agui.test.ts` + `bun run typecheck`.
- Baseline evidence recorded in RESEARCH.md: 19 mock-agui tests before → 23+ after.
</verification>

<success_criteria>
- The multi-message history knob is live, wire-valid, self-tested, and backward-compatible; the shared chat helpers exist; conversation-stream-state.spec.ts (the tracer spec) is green on the agui fixture — the migration pattern is proven and the 13-file migration wave (06-03..06-06) has its foundation.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-migration-verification/06-01-SUMMARY.md` when done
</output>
