---
phase: 1-copilotruntime-hosting-thread-apis-spike
plan: 3
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - e2e/ui/fixtures/mock-agui.ts
  - e2e/ui/fixtures/mock-api.ts
  - e2e/api/copilotkit/sse-text-diff.test.ts
  - .planning/PROJECT.md
autonomous: true
requirements:
  - HOST-01
  - HOST-03
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - ApiMock no longer 501s /api/copilotkit/* — the route handler skips the prefix via route.fallback() so the mock-agui route can answer (D-07; Playwright route conflict resolved)
    - MockAgui (e2e/ui/fixtures/mock-agui.ts) fulfills /api/copilotkit/agent/:agentId/run and /info with EventEncoder-framed SSE bodies (text/event-stream, data: {json}\n\n) — the fixture can never drift from the wire format
    - e2e/api/copilotkit/sse-text-diff.test.ts passes: fixture-emitted data: lines are byte-identical to the real server's captured SSE text for the same scenario (D-07; success criterion 5)
    - .planning/PROJECT.md records the HOST-03 evidence: fetch-native decision (superseding the hono assumption), exact pins, the actual /info JSON + GET /threads response, and the idleTimeout configuration (server.timeout(req, 0) + global 30)
  artifacts:
    - e2e/ui/fixtures/mock-agui.ts (MockAgui class — install(), SSE body builders)
    - e2e/ui/fixtures/mock-api.ts (route.fallback() skip + header doc)
    - e2e/api/copilotkit/sse-text-diff.test.ts
    - .planning/PROJECT.md (evidence edits)
  key_links:
    - EventEncoder from @ag-ui/encoder is the single framing source for the fixture (Don't Hand-Roll)
    - buildQuickRunEvents from e2e/api/copilotkit/probe-agent.ts is the shared canonical event source for agent, fixture, and diff test
    - PROJECT.md Key Decisions table ↔ RESEARCH.md evidence (HOST-03 success criterion 4)
---

<objective>
Build the mock runtime fixtures that will become the Phase 6 E2E foundation, validate them byte-for-byte against the real server, and record the phase's evidence in PROJECT.md.

MVP slice (thin end-to-end): the SAME scripted scenario → (a) real server SSE text and (b) fixture-emitted SSE text → byte-identical diff proves the fixture mirrors the wire.

Purpose: D-07 (fixture foundation validated against the real server, success criterion 5), HOST-03 (fetch-native decision + evidence recorded in PROJECT.md, success criterion 4). The fixture is built now so Phase 6 consumes a proven foundation instead of a hand-rolled approximation.
Output: MockAgui fixture class, the ApiMock route-conflict fix, the text-diff validation test, and the PROJECT.md evidence record.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-RESEARCH.md
@.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-PATTERNS.md
@e2e/ui/fixtures/mock-api.ts
@e2e/ui/fixtures/mock-ws.ts
@e2e/api/copilotkit/probe-agent.ts
@e2e/api/copilotkit/copilotkit.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build MockAgui and resolve the ApiMock route conflict</name>
  <files>
    e2e/ui/fixtures/mock-agui.ts,
    e2e/ui/fixtures/mock-api.ts
  </files>
  <read_first>
    - e2e/ui/fixtures/mock-ws.ts — the streaming-mock shape to mirror (install()/constructor, queue semantics, lines 20-56)
    - e2e/ui/fixtures/mock-api.ts — the route dispatcher to fix (lines 79-117; the 501 default at 88-92) and its header comment (lines 1-14)
    - 01-RESEARCH.md §Code Examples §SSE framing (EventEncoder verbatim format), §Pattern 3, §Don't Hand-Roll
    - e2e/api/copilotkit/probe-agent.ts — the canonical buildQuickRunEvents export to reuse
  </read_first>
  <behavior>
    - Unit-style check (no browser): MockAgui's SSE body builder for the quick scenario, fed the same event objects the real agent emits, produces frames matching `data: {json}\n\n` with no event:/id: fields — asserted in Task 2's diff test against real captures
    - ApiMock.install() route handler: a /api/copilotkit/ URL is handed to the next registered route via route.fallback() instead of 501
  </behavior>
  <action>
    Per D-07 and PATTERNS.md resolution A (preferred): create e2e/ui/fixtures/mock-agui.ts exporting `class MockAgui` modeled on WsMock's constructor(page)/install() shape. Its install() registers `page.route("/api/copilotkit/**", ...)`. The route handler: for POST /agent/:agentId/run — parse the JSON body, read forwardedProps.script (default "quick"), build the SSE body by importing EventEncoder from "@ag-ui/encoder" and encoding each event from the canonical event builder (import buildQuickRunEvents from "../../api/copilotkit/probe-agent.ts"; for silence scripts encode the same quick sequence — the diff test only exercises quick mode); fulfill with route.fulfill({ status: 200, contentType: "text/event-stream", headers: { "cache-control": "no-cache" }, body: frames.join("") }). For GET /info — fulfill 200 JSON with { agents: { default: { id: "default", description: "Spike probe agent" } }, mode: "sse" }. For any other /api/copilotkit/** path — fulfill 404 { error: "Not found" }. Do NOT wire MockAgui into e2e/ui/fixtures/index.ts — Phase 6 consumes it; this phase only builds + validates the class (install() shape identical from day one, per PATTERNS.md).

    Edit e2e/ui/fixtures/mock-api.ts: in the install() route callback (before the method lookup), when `url.pathname.startsWith("/api/copilotkit/")` call `await route.fallback(); return;` — Playwright hands the request to the previously registered route (mock-agui's). Update the header comment (lines 1-14) to state /api/copilotkit/* is owned by mock-agui.ts, not ApiMock. The loud-501 behavior for every other unhandled /api/* method stays.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - e2e/ui/fixtures/mock-agui.ts exports MockAgui with constructor(page), async install(), and internal SSE body builders that use EventEncoder (no string-template framing)
    - e2e/ui/fixtures/mock-api.ts skips /api/copilotkit/ paths via route.fallback() before the 501 lookup; non-copilotkit unhandled paths still return 501
    - `bun run typecheck` passes
    - No import of @copilotkit/vue anywhere (D-10 still holds)
  </acceptance_criteria>
  <done>MockAgui exists with constructor(page)/install()/EventEncoder-built SSE bodies; ApiMock skips /api/copilotkit/* via route.fallback() while keeping 501 for other unhandled paths; typecheck passes.</done>
  <reversibility rating="reversible">Test-fixture files; no production impact.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate the fixture against the real server by SSE text diff</name>
  <files>e2e/api/copilotkit/sse-text-diff.test.ts</files>
  <read_first>
    - 01-RESEARCH.md §Pattern 3 (fixture validation by SSE text diff) and §Code Examples §SSE framing + §Raw SSE probe
    - e2e/api/smoke.test.ts — server lifecycle skeleton (beforeAll/afterAll)
    - e2e/api/copilotkit/probe-agent.ts — the canonical event builder
    - e2e/ui/fixtures/mock-agui.ts — the fixture under validation
  </read_first>
  <behavior>
    - Test: run the identical quick scenario against (a) the real server (startServer({ copilotkitProbe: true }) + raw fetch) capturing the raw SSE body text, and (b) the MockAgui SSE body builder; assert the data: lines are byte-identical (toEqual on the split frames arrays)
  </behavior>
  <action>
    Create e2e/api/copilotkit/sse-text-diff.test.ts per research Pattern 3. beforeAll: startServer({ copilotkitProbe: true }). Test: (1) REAL side — raw fetch POST /api/copilotkit/agent/default/run with body { threadId: "diff-t1", runId: "diff-r1", tools: [], context: [], forwardedProps: { script: "quick" }, state: [], messages: [] } and accept text/event-stream; capture `await res.text()`; assert status 200 and content-type text/event-stream. (2) FIXTURE side — build the same scenario's SSE text through the MockAgui body builder (import EventEncoder from "@ag-ui/encoder" and encode buildQuickRunEvents("diff-t1", "diff-r1") — same canonical events, same order; the fixture path must NOT re-type the event literals, it reuses the shared builder). (3) Assert: split both texts on "\n\n", filter(Boolean), and expect the fixture frames array to strictly equal the real frames array (byte-identical data: lines — catches framing drift like event:/id: fields, key order, or double newlines). Also assert both responses share the response headers content-type text/event-stream and cache-control no-cache.
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/sse-text-diff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test e2e/api/copilotkit/sse-text-diff.test.ts` passes: fixture frames strictly equal real captured frames for the quick scenario
    - The test derives fixture frames from EventEncoder + buildQuickRunEvents (shared canonical source), not from re-typed literals
    - The real-side capture is regenerated in-test (no committed capture file to go stale)
  </acceptance_criteria>
  <done>sse-text-diff.test.ts passes: fixture data: lines strictly equal the real server's captured SSE text for the quick scenario, with both sides' content-type and cache-control headers asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Record the HOST-03 evidence in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md — Key Decisions table (lines 67-82, especially the hono row at line 80), Context bullets (lines 48-57, especially line 54), footer (line 102)
    - 01-RESEARCH.md §State of the Art + §Summary (the evidence to record: fetch-native decision, exact pins, /info + /threads responses, idleTimeout config)
    - e2e/api/copilotkit/copilotkit.test.ts + 01-02-SUMMARY.md — the actual captured /info JSON and GET /threads response bodies to quote
  </read_first>
  <action>
    Edit .planning/PROJECT.md with three targeted edits (Edit tool, scoped — never rewrite the whole file). (1) Key Decisions table: change the row "CopilotRuntime mounted in Bun.serve via hono handler" (line 80) to record the fetch-native outcome: "CopilotRuntime mounted in Bun.serve via fetch-native createCopilotRuntimeHandler (@copilotkit/runtime/v2)" with rationale "zero framework deps, Bun-native, reversible one-line swap to hono later (D-01); evidence: Phase 1 spike" and outcome "Decided in Phase 1 — see footer evidence". (2) Context bullet at line 54: replace the hono claim with the corrected statement that the runtime v2 exports fetch-native + hono handlers and the fetch-native handler was chosen for the single-process Bun.serve mount (D-01 supersedes the earlier hono assumption). (3) Footer: bump the Last updated line to today's date and append a dated "Phase 1 spike evidence (HOST-03)" subsection quoting: the exact pins (@ag-ui/core|client|encoder@0.0.57, @copilotkit/runtime|vue@1.66.4), the idleTimeout configuration (global idleTimeout: 30 kept; per-request srv.timeout(req, 0) on /api/copilotkit/* — verified >32s silence survival), the stop route shape POST /agent/:agentId/stop/:threadId, the 1.66.4 thread-endpoint finding (self-hosted /info advertises local thread endpoints; GET /threads serves {threads, nextCursor: null} via runner.listThreads(); mutations remain 422 without Intelligence — supersedes the "Intelligence-only" claim), and the actual /info JSON + GET /threads response bodies copied verbatim from the probe test output (D-08 Phase 4 contract evidence). Also correct the E2E context bullet (line 57) to state the mock-agui fixture foundation is validated against the real server (D-07).
  </action>
  <verify>
    <automated>grep -v '^#' .planning/PROJECT.md | grep -c "fetch-native" && grep -v '^#' .planning/PROJECT.md | grep -c "server.timeout(req, 0)" && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md contains the strings "fetch-native", "server.timeout(req, 0)", "1.66.4", and "0.0.57" outside comment lines (positive greps, each count >= 1)
    - The Key Decisions hono row now reads as decided-with-evidence (fetch-native), not pending
    - The footer evidence subsection quotes the actual /info JSON and GET /threads response captured by the probe tests
    - No other sections of PROJECT.md were altered (scoped edits only)
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions hono row flipped to the decided fetch-native outcome, Context bullet corrected, and a dated footer evidence subsection quoting the actual /info JSON + GET /threads responses, pins, idleTimeout config, and stop-route shape (HOST-03).</done>
  <reversibility rating="reversible">Documentation edit; revertible via git.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI test suite → fixture layer | mock fixtures must not silently diverge from the real wire protocol (drift = false-green tests in Phase 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-07 | Integrity | ApiMock 501-swallowing /api/copilotkit/* in UI tests | medium | mitigate | route.fallback() skip in mock-api.ts install() (Task 1) + header doc declaring mock-agui ownership; keeps the loud-501 fail-fast for everything else |
| T-1-08 | Tampering | fixture wire-format drift (framing, key order, missing headers) | medium | mitigate | EventEncoder as the single framing source (never string templates) + sse-text-diff.test.ts asserting byte-identical data: lines vs the real server (Task 2) — the mechanical drift catch Pattern 3 prescribes |
| T-1-09 | Spoofing | mock /info advertising capabilities the real runtime lacks (misleading Phase 5/6 consumers) | low | mitigate | MockAgui's /info mirrors the real /info shape captured in plan 01-02 (agents.default + mode "sse"); any capability additions in Phase 6 must be re-validated against the real server before use |

No installs in this plan — no package-legitimacy entries needed.
</threat_model>

<verification>
- `bun test e2e/api/copilotkit/sse-text-diff.test.ts` — fixture validation (D-07, success criterion 5)
- `bun test e2e/api --timeout 30000` — full API suite regression (mount + fixtures coexist)
- grep assertions on .planning/PROJECT.md — HOST-03 evidence recorded
</verification>

<success_criteria>
- Success criterion 5 met: mock fixtures validated byte-for-byte against the real server and usable as the Phase 6 E2E foundation.
- Success criterion 4 met: exact pins + fetch-native decision recorded with evidence (the actual /info + /threads JSON, idleTimeout config, stop-route shape) in PROJECT.md.
</success_criteria>

## Artifacts this phase produces

- **e2e/ui/fixtures/mock-agui.ts** — `class MockAgui` (constructor(page), `install()` via page.route("/api/copilotkit/**"), run/info/404 fulfill handlers; SSE bodies built with `EventEncoder` from `@ag-ui/encoder` + `buildQuickRunEvents` from e2e/api/copilotkit/probe-agent.ts; NOT wired into fixtures/index.ts — Phase 6 consumes it)
- **e2e/ui/fixtures/mock-api.ts** — edit: `/api/copilotkit/` paths skipped via `route.fallback()` before the method lookup; header comment declares mock-agui ownership of the prefix
- **e2e/api/copilotkit/sse-text-diff.test.ts** — integration test: real-server capture (startServer({ copilotkitProbe: true }) + raw fetch) vs fixture-built SSE text, byte-identical data: lines + header assertions (D-07 validation)
- **.planning/PROJECT.md** — edits: Key Decisions fetch-native row, corrected Context bullet, dated footer "Phase 1 spike evidence (HOST-03)" subsection (pins, idleTimeout config, stop route, thread-endpoint finding, verbatim /info + /threads JSON)

<output>
Create `.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-03-SUMMARY.md` when done.
</output>
