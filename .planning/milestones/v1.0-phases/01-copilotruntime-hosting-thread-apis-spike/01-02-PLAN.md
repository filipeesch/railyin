---
phase: 1-copilotruntime-hosting-thread-apis-spike
plan: 2
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/bun/index.ts
  - e2e/api/fixtures/server.ts
  - e2e/api/copilotkit/probe-agent.ts
  - e2e/api/copilotkit/copilotkit.test.ts
autonomous: true
requirements:
  - HOST-01
  - HOST-02
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - GET /api/copilotkit/info returns 200 JSON advertising agents.default and mode "sse" from the SAME Bun.serve origin (HOST-01; success criterion 1)
    - POST /api/copilotkit/agent/default/run returns 200 with Content-Type text/event-stream and the first SSE frame is RUN_STARTED (run round-trip; D-05)
    - POST /api/copilotkit/agent/default/stop/{threadId} returns {stopped:true} while a run is active (threadId in path per verified route)
    - A run scripted with silenceMs > 30 (the global idleTimeout) survives the silence — events arrive after and RUN_FINISHED closes the stream (HOST-02; success criterion 3)
    - connect to a never-run thread returns 200 SSE with zero events — the empty snapshot contract (D-06)
    - No Access-Control-Allow-Origin header on run responses (D-03 same-origin reconfirmed)
    - The copilotkit prefix is dispatched BEFORE the /api/ RPC router; unknown /api/copilotkit/* subpaths return the runtime's own 404 {error:"Not found"}
    - The ScriptedAgent is registered ONLY under RAILYN_COPILOTKIT_PROBE=1 — `bun run prod` never exposes the fake agent
  artifacts:
    - src/bun/index.ts (copilotkit prefix branch + srv.timeout(req, 0) + env-gated CopilotRuntime wiring)
    - e2e/api/fixtures/server.ts (copilotkitProbe option → RAILYN_COPILOTKIT_PROBE=1 extraEnv)
    - e2e/api/copilotkit/probe-agent.ts (ScriptedAgent extends AbstractAgent + canonical event builder)
    - e2e/api/copilotkit/copilotkit.test.ts (info/run/stop/connect/silence/400/threads assertions)
  key_links:
    - server.timeout(req, 0) applied per-request on /api/copilotkit/* before delegating to createCopilotRuntimeHandler (HOST-02; global idleTimeout: 30 stays)
    - ScriptedAgent emits RUN_STARTED as its FIRST event — the runtime does not synthesize it and client verifyEvents rejects streams that start otherwise (Pitfall 2)
    - Mount order: prefix branch sits between the /api/mcp/oauth/callback check and the POST /api/ RPC router (Pitfall 3)
---

<objective>
Mount the CopilotRuntime inside the existing Bun.serve fetch handler and prove the run/connect/stop round-trip over SSE against a deterministic scripted agent — the phase's tracer slice.

MVP slice (thin end-to-end): one real HTTP client → one Bun.serve origin → createCopilotRuntimeHandler → InMemoryAgentRunner → ScriptedAgent → SSE frames back to the client, verified at the wire level.

Purpose: HOST-01 (single-process mount, D-01/D-02/D-03/D-05), HOST-02 (idle-timeout mitigation, D-04), D-06 (empty-snapshot contract), D-08 evidence capture (thread routes) — all with the probe code kept out of production paths (CONTEXT.md discretion: e2e/, env-gated).
Output: a live /api/copilotkit/* mount in the composition root, a reusable probe agent, and an integration test file proving the round-trip.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-RESEARCH.md
@.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-PATTERNS.md
@src/bun/index.ts
@e2e/api/fixtures/server.ts
@e2e/api/smoke.test.ts
@e2e/api/mcp-oauth.test.ts
@src/bun/testing/mock-engine.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Mount CopilotRuntime in Bun.serve and prove the run/stop round-trip over SSE — one path only</name>
  <files>
    src/bun/index.ts,
    e2e/api/fixtures/server.ts,
    e2e/api/copilotkit/probe-agent.ts,
    e2e/api/copilotkit/copilotkit.test.ts
  </files>
  <read_first>
    - src/bun/index.ts — the fetch handler prefix-dispatch pattern (lines 286-341), the env-gated mock-engine injection (lines 186-189), engine factory map area (lines 137-162) for handler-construction placement
    - e2e/api/fixtures/server.ts — StartServerOptions + extraEnv wiring (lines 22-32, 133-140), spawn env block (lines 142-168)
    - src/bun/testing/mock-engine.ts — the deterministic async-generator producer + delay() pacing to mirror (lines 10-37)
    - e2e/api/smoke.test.ts — beforeAll/afterAll startServer pattern (lines 8-9, 45-53), waitFor helper (lines 13-29)
    - e2e/api/mcp-oauth.test.ts — raw fetch against baseUrl for non-RPC endpoints (lines 196-205)
    - 01-RESEARCH.md §Code Examples (Mounting, ScriptedAgent, Raw SSE probe, Stop route), §Common Pitfalls 1-4
  </read_first>
  <behavior>
    - Test 1 (probe, real server): startServer({ copilotkitProbe: true }); GET /api/copilotkit/info → 200, body JSON has agents.default and mode "sse"
    - Test 2 (probe, real server): POST run with a quick script → 200, content-type text/event-stream, first frame parses to { type: "RUN_STARTED" }, last frame is RUN_FINISHED
    - Test 3 (probe, real server): POST run with silence script, then POST stop/{threadId} while streaming → { stopped: true }
  </behavior>
  <action>
    Per D-01/D-02, mount the fetch-native handler — NEVER the hono handler (decision locked; PROJECT.md's hono assumption is superseded). Three coordinated edits + two new files:

    1. src/bun/index.ts: add imports `CopilotRuntime` and `createCopilotRuntimeHandler` from "@copilotkit/runtime/v2" as TOP-LEVEL imports (they are used unconditionally by the mount). Do NOT top-level-import `scriptedAgent` from the e2e/ tree — an unconditional static import would pull e2e/ modules into the production module graph even when the probe is disabled (benign per T-1-03, but keeps prod graph clean). Instead load it dynamically behind the env gate: `const { scriptedAgent } = await import("../../e2e/api/copilotkit/probe-agent.ts")` executed only when `process.env.RAILYN_COPILOTKIT_PROBE === "1"` (top-level await is supported in Bun's ESM loader). Then construct `const copilotRuntime = new CopilotRuntime({ agents: process.env.RAILYN_COPILOTKIT_PROBE === "1" ? { default: scriptedAgent } : {} })` (env gate per the RAILYN_TEST_EXECUTION_ENGINE pattern at lines 186-189 — `bun run prod` must never register the fake agent and must never load the e2e/ module) and `const copilotHandler = createCopilotRuntimeHandler({ runtime: copilotRuntime, basePath: "/api/copilotkit", mode: "multi-route" })` — NO cors option (D-03 same-origin). In the fetch handler, insert between the /api/mcp/oauth/callback check (line 304) and the POST /api/ RPC router (line 306): a branch matching `url.pathname.startsWith("/api/copilotkit/")` that calls `srv.timeout(req, 0)` then `return copilotHandler(req)`. Do NOT wrap this branch in the RPC try/catch (it would JSON-encode and corrupt SSE) and do NOT let the RPC router see these paths (Pitfall 3). Keep the global `idleTimeout: 30` (Pitfall 1 — the per-request override is the mitigation).

    2. e2e/api/fixtures/server.ts: extend StartServerOptions with `copilotkitProbe?: boolean`; in startServer, when set, add `extraEnv.RAILYN_COPILOTKIT_PROBE = "1"` following the mcpConfig → RAILYN_DATA_DIR pattern (lines 133-140). No other spawn changes.

    3. e2e/api/copilotkit/probe-agent.ts (NEW): `class ScriptedAgent extends AbstractAgent` from "@ag-ui/client", constructed with super({ agentId: "default", description: "Spike probe agent" }). Its `run({ threadId, runId, forwardedProps })` async generator: read script from forwardedProps (default "quick", optional "silence" with silenceMs number); yield RUN_STARTED FIRST (Pitfall 2 — the runtime does not synthesize it), then TEXT_MESSAGE_START (messageId "m1", role "assistant"), TEXT_MESSAGE_CONTENT (delta "hello"), TEXT_MESSAGE_END, then for silence mode await a delay(silenceMs) (copy the delay() helper shape from mock-engine.ts lines 10-12), then RUN_FINISHED with result null. Also export a canonical event builder `buildQuickRunEvents(threadId, runId)` returning the exact event objects in the same order the agent emits — plan 01-03's fixture text-diff reuses it as the single source of event truth.

    4. e2e/api/copilotkit/copilotkit.test.ts (NEW): import { describe, test, expect, beforeAll, afterAll } from "bun:test"; beforeAll startServer({ copilotkitProbe: true }) (timeout 20_000), afterAll shutdown. All /api/copilotkit/* calls use RAW fetch against server.baseUrl (the endpoint is AG-UI, NOT in RailynAPI — document that boundary in a header comment). Test A: GET /api/copilotkit/info → 200, content-type application/json, body.agents.default defined and body.mode === "sse". Test B: POST /api/copilotkit/agent/default/run with headers content-type application/json + accept text/event-stream and body { threadId: "t1", runId: "r1", tools: [], context: [], forwardedProps: { script: "quick" }, state: [], messages: [] } → 200; assert content-type text/event-stream; assert response has NO access-control-allow-origin header (D-03 reconfirm); split body on "\n\n" filter(Boolean); assert frames[0] after slicing the "data: " prefix parses to an object with type "RUN_STARTED"; assert the final frame type is "RUN_FINISHED". Test C: POST run with forwardedProps { script: "silence", silenceMs: 5000 } on a fresh threadId "t2" (do not await the body — fire the request, wait ~800ms for the run to start), then POST /api/copilotkit/agent/default/stop/t2 (threadId in PATH per the verified multi-route stop route, Pitfall 4) → 200 with body.stopped === true; then await the run response text.
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/copilotkit.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test e2e/api/copilotkit/copilotkit.test.ts` passes all three tests (info / run SSE round-trip / stop {stopped:true}) against a real spawned server
    - `bun run typecheck` passes with the new imports and StartServerOptions field
    - src/bun/index.ts shows the copilotkit branch BEFORE the `if (req.method === "POST" && url.pathname.startsWith("/api/"))` router, calling srv.timeout(req, 0) on those requests only
    - The ScriptedAgent registration is wrapped in the RAILYN_COPILOTKIT_PROBE === "1" check; with the flag unset the agents map is empty
    - No CORS options passed to createCopilotRuntimeHandler; no access-control-allow-origin header observed in test B
  </acceptance_criteria>
  <done>The runtime is mounted in src/bun/index.ts under /api/copilotkit/* with srv.timeout(req, 0), env-gated ScriptedAgent registration, the server.ts copilotkitProbe seam, and a passing probe test proving info + run SSE round-trip + stop {stopped:true} against a real spawned server; typecheck green.</done>
  <reversibility rating="reversible">D-01 is reversible by construction — swapping to createCopilotHonoHandler later is a one-line change in the composition root; the mount branch and env gate are plain additive code.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove silence survival, empty connect, validation 400, and thread-route evidence</name>
  <files>e2e/api/copilotkit/copilotkit.test.ts</files>
  <read_first>
    - 01-RESEARCH.md §Code Examples (Raw SSE probe, Stop route), §State of the Art (thread endpoints, 1.66.4), Pitfall 6 (process-local thread store)
    - e2e/api/copilotkit/copilotkit.test.ts (the file created in Task 1 — extend, do not restructure)
    - e2e/api/smoke.test.ts waitFor helper (lines 13-29) for post-silence observation
  </read_first>
  <behavior>
    - Test 4: HOST-02 — run with forwardedProps { script: "silence", silenceMs: 32000 } (exceeds the global idleTimeout 30): the stream must still deliver TEXT_MESSAGE_CONTENT and RUN_FINISHED after the silence (per-test timeout 60_000; bun:test third argument)
    - Test 5: D-06 — POST /api/copilotkit/agent/default/connect on a never-run threadId → 200 text/event-stream, body contains zero data: frames (empty completion, no error)
    - Test 6: malformed RunAgentInput (invalid JSON body) → 400 with error field "Invalid request body"
    - Test 7: unknown subpath GET /api/copilotkit/nope → 404 { error: "Not found" } (runtime's own 404, not the RPC router's)
  </behavior>
  <action>
    Extend copilotkit.test.ts with four more tests using the same raw-fetch pattern:

    Test 4 (HOST-02, D-04): POST run on fresh threadId "t3" with forwardedProps { script: "silence", silenceMs: 32000 }. Give the test a 60_000ms per-test timeout via bun:test's third argument (the file-level `bun test --timeout 30000` default would kill a >30s test). NOTE (accepted latency): this test's automated feedback inherently exceeds the 30s Nyquist latency guideline — the >30s silence IS the HOST-02 evidence, because the silence must exceed the global idleTimeout of 30s to prove `server.timeout(req, 0)` overrides it. The 60_000ms per-test timeout is the planned accommodation; record this accepted-latency note in the plan SUMMARY. Read the full body text; assert it contains a frame with TEXT_MESSAGE_CONTENT (delta "hello") AND a final RUN_FINISHED frame — i.e., the stream survived a silence longer than the 30s global idleTimeout and closed cleanly. If the stream dies mid-silence, the mitigation failed: report and stop (Pitfall 1) rather than papering over.

    Test 5 (D-06): POST /api/copilotkit/agent/default/connect on threadId "never-run-1" with accept text/event-stream → 200, content-type text/event-stream, body split on "\n\n" filter(Boolean) yields an EMPTY frames array (Pitfall 6 — ReplaySubject completes empty; this is the verified contract, not a bug).

    Test 6 (T-1-05): POST run with body "not-json{{{" → 400, parsed body has error === "Invalid request body" (runtime zod parse, verified).

    Test 7 (T-1-04): GET /api/copilotkit/not-a-route → 404 with parsed body error === "Not found" (proves the runtime owns the prefix — the RPC router would return "Unknown method: ...").

    Test 8 (D-08 evidence, Phase 4 contract): AFTER test B's run on "t1" (order matters — the thread store is process-local, Pitfall 6): GET /api/copilotkit/threads → 200, body has a threads array and nextCursor === null; assert some thread entry has id "t1". Then GET /api/copilotkit/threads/events/t1 → record the actual status and body in the test output (expected 200 per 1.66.4 source; if it differs, that IS the recorded evidence — do not assert a specific shape, just log status + first 200 chars).
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit --timeout 30000</automated>
  </verify>
  <acceptance_criteria>
    - `bun test e2e/api/copilotkit --timeout 30000` passes with tests 4-8 green (silence survival, empty connect, 400, runtime 404, /threads listing)
    - Test 4's run survived >32s of agent silence and still delivered RUN_FINISHED — the server.timeout(req, 0) override is empirically proven (HOST-02, assumption A2)
    - Test 5's connect response had zero data: frames on a never-run thread (D-06)
    - Test 8 recorded the actual /api/copilotkit/threads and /threads/events/:threadId responses in the test output (verbatim evidence for the Phase 4 contract)
  </acceptance_criteria>
  <done>All four expansion tests (silence survival >32s with RUN_FINISHED after, empty connect with zero frames, 400 on malformed body, runtime 404 on unknown subpath) plus the /threads evidence test pass under `bun test e2e/api/copilotkit --timeout 30000`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loopback client → /api/copilotkit/* | untrusted HTTP input (RunAgentInput JSON) crossing into the runtime mount; same origin as the existing app, no CORS, no auth (inherited localhost posture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Spoofing | ScriptedAgent registered in prod (`bun run prod`) | low | mitigate | env gate RAILYN_COPILOTKIT_PROBE === "1" wraps the agents map; the probe test starts the server with the flag via startServer({ copilotkitProbe }) and Task 1 acceptance checks the unset-flag path |
| T-1-04 | Tampering | prefix confusion — RPC router swallowing /api/copilotkit/* or runtime claiming RPC paths | low | mitigate | mount branch dispatched BEFORE the POST /api/ router in src/bun/index.ts; probe test 7 asserts unknown copilotkit subpaths return the runtime's own 404 {error:"Not found"}, not an RPC 404 |
| T-1-05 | Tampering / DoS | malformed RunAgentInput flooding the runtime | low | mitigate | runtime zod parse (RunAgentInputSchema) → 400 before any agent work; probe test 6 asserts the 400 path; no unbounded work occurs pre-parse (RESEARCH §Security Domain V4/V5) |
| T-1-06 | DoS | long-lived SSE streams pinning connections | low | accept | local single-user loopback app; streams are agent-scoped and bounded by scripted scripts; server.timeout(req, 0) on copilotkit paths is the deliberate HOST-02 tradeoff, kept off all other paths |

Accepted risks are documented for Phase 2: the real-engine bridge must add the same origin checks the existing launch.run endpoint uses (RESEARCH §Security Domain — "Unauthorized local clients" row).
</threat_model>

<verification>
- `bun test e2e/api/copilotkit/copilotkit.test.ts` — tracer round-trip (HOST-01)
- `bun test e2e/api/copilotkit --timeout 30000` — silence survival (HOST-02), D-06, 400, 404, /threads (D-08)
- `bun run typecheck` — mount + fixture seam compile
- <human-check>Dev boot check (HOST-01 success criterion 1): run `bun run dev --port=3001`, confirm no second listener appears, and open http://127.0.0.1:3001/api/copilotkit/info in a browser — expect the JSON agent info. This check happens at end-of-phase human verification.</human-check>
</verification>

<success_criteria>
- Success criteria 1, 2, 3 from ROADMAP §Phase 1: single-origin mount, run/connect/stop round-trip over SSE, and >30s silence survival are all proven by green automated tests against a real spawned server.
- D-08 evidence captured: actual /info, /threads, and /threads/events/:threadId behavior recorded in test output for the Phase 4 contract.
</success_criteria>

## Artifacts this phase produces

- **src/bun/index.ts** — edits: top-level imports `CopilotRuntime` + `createCopilotRuntimeHandler` from `@copilotkit/runtime/v2`; `scriptedAgent` loaded via dynamic `await import("../../e2e/api/copilotkit/probe-agent.ts")` inside the RAILYN_COPILOTKIT_PROBE gate only (prod module graph never pulls e2e/); `copilotRuntime` (CopilotRuntime instance, agents map env-gated); `copilotHandler` (createCopilotRuntimeHandler, basePath `/api/copilotkit`, mode multi-route, no cors); fetch-handler branch for `/api/copilotkit/` calling `srv.timeout(req, 0)` before delegating (HOST-01/02)
- **e2e/api/fixtures/server.ts** — edit: `StartServerOptions.copilotkitProbe?: boolean` → `extraEnv.RAILYN_COPILOTKIT_PROBE = "1"`
- **e2e/api/copilotkit/probe-agent.ts** — `class ScriptedAgent extends AbstractAgent` (agentId "default"; run() emits RUN_STARTED → TEXT_MESSAGE_START/CONTENT/END → optional silence via forwardedProps.silenceMs → RUN_FINISHED); `buildQuickRunEvents(threadId, runId)` canonical event builder (shared with 01-03 fixtures)
- **e2e/api/copilotkit/copilotkit.test.ts** — integration tests: /info (agents.default, mode "sse"), run SSE round-trip (RUN_STARTED first frame), stop during silence-mode run ({stopped:true}), HOST-02 32s-silence survival (60s per-test timeout), D-06 empty connect, 400 malformed body, runtime 404, /threads + /threads/events/:threadId evidence capture
- **Env var** — `RAILYN_COPILOTKIT_PROBE` (set to "1" by the test fixture; gates ScriptedAgent registration in the composition root; unset in `bun run prod`)
- **Config keys** — none (no YAML/config changes; basePath constant `/api/copilotkit` lives in the composition root)

<output>
Create `.planning/phases/01-copilotruntime-hosting-thread-apis-spike/01-02-SUMMARY.md` when done.
</output>
