---
phase: 03-decision-interrupts-resume
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - e2e/api/copilotkit/railyin.test.ts
  - src/bun/copilotkit/interrupt-registry.ts
  - src/bun/copilotkit/interrupt-registry.test.ts
  - src/bun/copilotkit/railyin-runner.test.ts
  - src/bun/copilotkit/railyin-agent.ts
  - src/bun/index.ts
  - .planning/phases/03-decision-interrupts-resume/03-COVERAGE.md
  - .planning/phases/03-decision-interrupts-resume/03-VALIDATION.md
autonomous: true
requirements:
  - RUNR-08
  - CHAT-09
  - UI-03
  - VERF-01
estimate:
  tokens: 34000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - On the REAL wire, a `__SCRIPT_DECISION__` run ends with RUN_FINISHED `outcome.type === "interrupt"` as the LAST frame (normal terminal, never RUN_ERROR — D-03); a plain run while pending → RUN_ERROR THREAD_BUSY (D-04); a resume run with a translated payload streams continuation frames and ends RUN_FINISHED; the resume RUN_STARTED with input.resume[] is persisted to JSONL (RUNR-08, CHAT-09 SC1/SC2/SC3)
    - D-05 violations fail over the wire: unknown interruptId → RUN_ERROR INVALID_INTERRUPT; `forwardedProps.command.resume` alone does nothing (D-01, Pitfall 6)
    - A cancelled resume over the wire completes plainly and the thread is usable afterward (A4)
    - Replay preserves interrupt terminals and resume[] entries: the runner cold path re-emits a log containing an interrupt-outcome run and a resume run verbatim across per-run boundaries (D-08, RUNR-08, Pitfall 7)
    - Post-restart resume works: the module-level registry lazily rebuilds from the JSONL tail + the `waiting_user` executions row (A2/Open Question 1 — rebuild, not reject), and the agent's resume branch falls back to `ensureOpen(threadId, this.db)` when `get(threadId)` returns null on a fresh process — so a decision paused before a restart can still be answered on a fresh server (old-stack parity)
    - The phase gate closes green: full backend + e2e suites + typecheck; 03-COVERAGE.md records the no-external-API decision and the UI-03 rendering split (Phase 5); 03-VALIDATION.md signed off (nyquist_compliant: true)
  artifacts:
    - e2e/api/copilotkit/railyin.test.ts — tests 11+ : interrupt run, block-while-pending, resume with translated payload, INVALID_INTERRUPT, cancel, forwardedProps inert, JSONL assertions, restart-replay-with-pending-decision
    - src/bun/copilotkit/interrupt-registry.ts — `configure({ store })` + `ensureOpen(threadId, db)` lazy rebuild (last interrupt terminal in the thread log + waiting_user executions row; seq counter continuity)
    - src/bun/copilotkit/railyin-agent.ts — resume branch cold-path fallback: `get(threadId)` → null ⇒ `ensureOpen(threadId, this.db)` (hot path and the D-04 hasOpen block stay on `get()`)
    - src/bun/index.ts — registry configure call in the non-probe path (after jsonlStore creation)
    - src/bun/copilotkit/interrupt-registry.test.ts / railyin-runner.test.ts — rebuild and replay-shape tests
    - 03-COVERAGE.md / 03-VALIDATION.md — API-coverage decision record + validation sign-off
  key_links:
    - The runner's cold-replay pipeline (finalizeRunEvents early-return + compactEvents) already preserves outcome.interrupt and RUN_STARTED.input.resume[] — verified empirically (research D-08); the tests pin the shapes, no runner code change
    - The rebuild correlates the JSONL interrupt terminal with the DB `waiting_user` executions row — the row is the durable truth that survives restarts (research Architecture Responsibility Map)
    - registry.configure must be gated to the non-probe path in index.ts (probe threadIds are non-numeric — the store would reject them)
    - The agent's resume branch keeps `get()` for the hot path and calls `ensureOpen(threadId, this.db)` ONLY when the registry returns null (fresh process) — this fallback is what makes e2e test 17 and the post-restart-resume truth pass; without it the 03-02 branch rejects INVALID_INTERRUPT on a fresh boot
    - Restart-replay e2e reuses the mkdtempSync two-server dataDir contract from test 10
---

<objective>
The **real-wire proof and resilience** of the decision cycle: full fake-engine contract tests over a spawned server (interrupt-outcome run, block-while-pending, resume with translated payload, D-05 rejections, cancel, forwardedProps inert — D-01/Pitfall 6), replay shapes pinning that interrupted runs and resume runs persist and re-emit verbatim (D-08, Pitfall 7), and the **post-restart resume** path: the module-level registry lazily rebuilds from the thread's JSONL tail + the `waiting_user` executions row (A2/Open Question 1 — rebuild, not reject) so a decision paused before a restart remains answerable, matching old-stack parity. The plan closes with the phase gate: full suite green, 03-COVERAGE.md recording the no-external-API decision (per the API-coverage checkpoint contribution) plus the UI-03 coverage split (Phase 3 delivers the event + payload contract; card rendering is Phase 5), and 03-VALIDATION.md signed off.

Purpose: RUNR-08 + CHAT-09 SC1/SC2/SC3 end-to-end on the real wire, VERF-01 (contract tests for events, interrupt outcome, resume, replay — D-09), UI-03 (contract handoff documented).

Output: the e2e decision-cycle suite, the registry rebuild capability, replay shape pins, and the phase close-out records.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decision-interrupts-resume/03-CONTEXT.md
@.planning/phases/03-decision-interrupts-resume/03-RESEARCH.md
@.planning/phases/03-decision-interrupts-resume/03-PATTERNS.md
@e2e/api/copilotkit/railyin.test.ts
@src/bun/copilotkit/interrupt-registry.ts
@src/bun/copilotkit/railyin-runner.ts
@src/bun/copilotkit/railyin-runner.test.ts
@src/bun/copilotkit/jsonl-store.ts
@src/bun/index.ts
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: e2e decision cycle on the real wire — interrupt run, block, resume, invalid, cancel, forwardedProps inert</name>
  <files>
    e2e/api/copilotkit/railyin.test.ts
  </files>
  <read_first>
    - e2e/api/copilotkit/railyin.test.ts — postJson (lines 34-40), parseSseFrames (lines 52-57), runInput (lines 60-70), the startServer fixture (lines 21-25), and the JSONL assertion pattern (test 7, lines 226-244)
    - 03-RESEARCH.md §Code Examples (RunAgentInput.resume[] schema lines 342-345; interrupt detection + resume shape lines 347-359), §Pattern 4 (Phase A/B marker semantics), §A4 (cancelled = dismissal)
    - 03-RESEARCH.md validation map CHAT-09 rows — the e2e behaviors this task proves
  </read_first>
  <behavior>
    - Test 11 (interrupt run, D-03): run "__SCRIPT_DECISION__" → LAST frame is RUN_FINISHED with outcome.type === "interrupt", interrupts[0].reason === "decision_request", interrupts[0].id matches /^decision-\d+-\d+$/, message === "mock context", metadata present; no RUN_ERROR; the interrupt frame is the final frame (run genuinely paused)
    - Test 12 (block-while-pending, D-04): same thread, plain run (no resume[]) → RUN_ERROR THREAD_BUSY as the last frame
    - Test 13 (resume, CHAT-09 SC2): run with resume[] `[{ interruptId, status: "resolved", payload: { decision: "approved", answers: [{ question: "Choose __DECISION_OPTION__", answer: "A", weight: "medium" }] } }]` → continuation frames include TEXT_MESSAGE_CONTENT "Decision received, continuing." and end RUN_FINISHED; the JSONL log on disk contains the resume run's RUN_STARTED with input.resume[0].interruptId
    - Test 14 (INVALID_INTERRUPT): resume with interruptId "decision-999999-1" → RUN_ERROR INVALID_INTERRUPT last frame
    - Test 15 (cancel, A4): resume status "cancelled" → plain RUN_FINISHED with no continuation text; a subsequent plain run on the same thread succeeds (thread not wedged)
    - Test 16 (forwardedProps inert, D-01/Pitfall 6): while pending, run with forwardedProps `{ command: { resume: "x" } }` and NO resume[] → RUN_ERROR THREAD_BUSY (the agent never reads the legacy channel)
  </behavior>
  <action>
    1. `e2e/api/copilotkit/railyin.test.ts` — add a "decision cycle (RUNR-08, CHAT-09)" describe block. Extend the runInput helper (or add resumeInput()) with the schema-valid `resume` field: `{ threadId, runId, tools: [], context: [], forwardedProps: {}, state: [], messages: [...history...], resume: [{ interruptId, status, payload }] }` — resume runs carry the ORIGINAL user message in history (the run's user turn is the decision text, NOT a new prompt). Each test creates its OWN chat session (existing pattern) and is self-contained: tests that need an open interrupt (12, 13, 15, 16) first run "__SCRIPT_DECISION__" on their thread and read the interruptId from the last frame. Reuse the existing postJson/parseSseFrames/startServer scaffolding unchanged. Assert behaviors 11-16.
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/railyin.test.ts --timeout 30000</automated>
  </verify>
  <acceptance_criteria>
    - `bun test e2e/api/copilotkit/railyin.test.ts` passes tests 1-10 (existing) AND 11-16 (new decision-cycle suite)
    - Test 13's JSONL assertion passes: the resume run's RUN_STARTED with input.resume is on disk (readFileSync on `join(server.dataDir, "threads", `${threadId}.jsonl`)`)
    - Test 16 proves the legacy channel is inert — forwardedProps.command.resume never resumes anything
    - No `-x` flag in any test command (bun 1.4.0 constraint)
  </acceptance_criteria>
  <done>The full decision cycle is proven on the real wire: interrupt-outcome terminal, block-while-pending, translated resume with continuation, D-05 rejection, cancel, and the legacy channel's inertness — with persistence of the resume input asserted on disk.</done>
  <reversibility rating="reversible">Test-only additions to the e2e suite.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay resilience + post-restart resume — registry lazy rebuild (A2)</name>
  <files>
    src/bun/copilotkit/interrupt-registry.ts,
    src/bun/copilotkit/interrupt-registry.test.ts,
    src/bun/copilotkit/railyin-runner.test.ts,
    src/bun/copilotkit/railyin-agent.ts,
    src/bun/index.ts,
    e2e/api/copilotkit/railyin.test.ts
  </files>
  <read_first>
    - 03-RESEARCH.md §D-08/RUNR-08 (replay preserves interrupt outcomes + resume[] — empirically verified), §Pitfall 7 (replay must not invalidate the interrupt terminal), §Open Question 1 (A2 rebuild recommendation — scan the JSONL tail for the last interrupt terminal + correlate the DB waiting_user row; ~30 lines reusing JsonlStore)
    - src/bun/copilotkit/railyin-runner.ts — the cold-replay pipeline (lines 149-190: truncate → finalizeRunEvents → completeOpenToolCalls → compactEvents)
    - src/bun/copilotkit/railyin-runner.test.ts — ev() (lines 27-29), appendCompletedRun (lines 73-79), per-run boundary tests (test 3 series)
    - src/bun/copilotkit/jsonl-store.ts — read() (lines 63-78) for the log scan
    - src/bun/copilotkit/interrupt-registry.ts — the module singleton from 03-01 (register/get/hasOpen/clear/updateExecutionId/reset, seq counters)
    - src/bun/index.ts — the composition root: agent (line 270), jsonlStore + runner (lines 291-292), runtime (lines 293-296)
    - e2e/api/copilotkit/railyin.test.ts — the restart-replay two-server pattern (test 10, lines 279-311)
    - src/bun/copilotkit/railyin-agent.ts — the 03-02 resume branch (inserted after the conversation-exists check, before extractUserText): the `const open = interruptRegistry.get(threadId)` gate + the `!open → INVALID_INTERRUPT` path to extend with the ensureOpen fallback; the agent owns `this.db` (same field the advisory-lock query uses, lines 162-168)
  </read_first>
  <behavior>
    - Replay A (interrupt terminal shape): log = [RUN_STARTED, TEXT_MESSAGE_START/CONTENT/END, RUN_FINISHED with outcome { type: "interrupt", interrupts: [...] }] → connect() re-emits the RUN_FINISHED with outcome.type "interrupt" verbatim (finalizeRunEvents early-returns; compactEvents preserves — no INCOMPLETE_STREAM RUN_ERROR)
    - Replay B (two-run resume shape): log = [interrupt run, then a resume run (RUN_STARTED with input.resume[] + text block + RUN_FINISHED)] → connect() replays both runs with per-run boundaries; the second RUN_STARTED.input.resume[0].interruptId survives compaction
    - Rebuild C (post-restart resume): registry empty + store configured + JSONL tail carries an interrupt terminal + DB has a `waiting_user` executions row for the conversation → ensureOpen(threadId, db) returns the entry with the SAME interruptId and the executionId from the row; the next register on that thread mints seq+1 (counter continuity)
    - Rebuild D (nothing to rebuild): no log / no waiting_user row → ensureOpen returns null
    - e2e E (restart-replay with pending decision): server A runs __SCRIPT_DECISION__ → shutdown; server B over the SAME durable dataDir: connect replays the interrupt card (RUN_FINISHED outcome.interrupt present); a resume run on server B succeeds with continuation frames — post-restart resume works (A2). E passes ONLY because the resume branch falls back `get() → ensureOpen(threadId, this.db)` on the fresh process (the registry is empty at boot; without the fallback the branch rejects INVALID_INTERRUPT and E cannot pass)
  </behavior>
  <action>
    1. `src/bun/copilotkit/interrupt-registry.ts` — add `configure(options: { store?: JsonlStore }): void` (module-level; called once at the composition root; inert until set) and `ensureOpen(threadId: string, db: Database): PendingInterrupt | null` — returns the existing entry, else lazily rebuilds: when a store is configured, `store.read(threadId)` and scan BACKWARDS for the last RUN_FINISHED with `outcome?.type === "interrupt"`; extract `interruptId` and `metadata` (payload = JSON.stringify(metadata) ?? ""); parse the trailing seq from the id (`/-(\d+)$/`) and bump the thread counter to `max(counter, seq)`; correlate the execution row via `SELECT id FROM executions WHERE conversation_id = ? AND status = 'waiting_user'` → executionId; re-register the entry with the SAME interruptId (never mint a new id — the client resumes the persisted id) and return it. No log terminal / no row → null. Import `JsonlStore` type and `Database` from bun:sqlite.
    2. `src/bun/copilotkit/railyin-agent.ts` — wire the rebuild into the run path (key_links: this is what makes e2e test 17 and the post-restart-resume truth pass): in the 03-02 resume branch, change the bare `const open = interruptRegistry.get(threadId)` gate to a cold-path fallback — `let open = interruptRegistry.get(threadId); if (!open) open = interruptRegistry.ensureOpen(threadId, this.db);` (the agent owns `this.db` — the same field the advisory-lock query uses at lines 162-168). When `ensureOpen` also returns null → keep the existing INVALID_INTERRUPT emit + return (nothing to rebuild, nothing to resume). The D-04 hasOpen/block path and the hot path stay on `get()` — no registry-contract change, no other call-site changes. The 03-02 R-tests still pass unchanged: they seed the registry directly (hot path behavior untouched) and the unit harness never calls configure, so `ensureOpen` returns null exactly where the old `!open → INVALID_INTERRUPT` rejection applied; the fallback is only reachable on a fresh process, covered by e2e E and rebuild tests C/D.
    3. `src/bun/index.ts` — after the jsonlStore creation (line 291-292) and gated to the non-probe path: `if (!copilotProbeEnabled) interruptRegistry.configure({ store: jsonlStore });` — the probe path stays untouched (probe threadIds like "t1" are non-numeric and must never reach the store). Import the registry from `./copilotkit/interrupt-registry.ts`.
    4. `src/bun/copilotkit/railyin-runner.test.ts` — add the two replay shapes (Replay A/B) using the existing ev()/appendCompletedRun-style builders; assert connect() re-emits both verbatim (per-run boundary assertions like test 3c).
    5. `src/bun/copilotkit/interrupt-registry.test.ts` — add the rebuild tests (C/D): temp-dir JsonlStore with a crafted interrupt-terminal log + a real in-memory DB (initDb helper) with a seeded waiting_user executions row; configure + ensureOpen assertions; reset() in beforeEach.
    6. `e2e/api/copilotkit/railyin.test.ts` — add test 17 per behavior E: reuse the mkdtempSync + two-server dataDir pattern from test 10; server A runs __SCRIPT_DECISION__ and shuts down; server B (fresh process — empty registry) connects (assert the interrupt card replays) then resumes with the persisted interruptId (assert continuation frames + RUN_FINISHED).
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit --timeout 20000 && bun test e2e/api/copilotkit/railyin.test.ts --timeout 30000 && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Replay A/B tests pass — the interrupt terminal and the resume run's input.resume survive the cold-replay pipeline verbatim
    - Rebuild C/D tests pass — same interruptId restored, executionId correlated from the DB row, seq counter continues
    - e2e test 17 passes — post-restart resume succeeds on a fresh server over the durable dataDir (the resume branch's `get() → ensureOpen(threadId, this.db)` fallback is what makes the resume reach the rebuild)
    - `bun test e2e/api/copilotkit/copilotkit.test.ts --timeout 30000` still green (probe path untouched); `bun run typecheck` clean
  </acceptance_criteria>
  <done>Interrupted runs and resume runs replay verbatim, and a decision paused before a server restart can still be resumed on a fresh process — the registry rebuild restores the interruptId, payload, and execution row correlation, and the agent's resume branch falls back to ensureOpen when the registry is empty, matching old-stack parity.</done>
  <reversibility rating="reversible">Additive registry capability + composition-root call; removing configure() restores the reject-only fallback (documented limitation).</reversibility>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite, 03-COVERAGE.md (no-external-API + UI-03 split), 03-VALIDATION.md sign-off</name>
  <files>
    .planning/phases/03-decision-interrupts-resume/03-COVERAGE.md,
    .planning/phases/03-decision-interrupts-resume/03-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md — the exact structure to mirror (detector record, decision, rationale table, outcome)
    - .planning/phases/03-decision-interrupts-resume/03-VALIDATION.md — the draft contract to complete (per-task verification map, Wave 0 checklist, open-question resolutions, sign-off)
    - .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-VALIDATION.md — the completed example (nyquist_compliant: true, wave_0_complete: true)
  </read_first>
  <behavior>
    - Full-suite gate: `bun test src/bun --timeout 20000` (backend) + `bun test e2e/api --timeout 30000` (e2e incl. Phase 1 probe regression) + `bun run typecheck` all green in one pass
    - 03-COVERAGE.md exists with the no-external-API decision record (in-process translation — no network API) and the UI-03 coverage split (Phase 3 delivers the event + payload contract; card rendering deferred to Phase 5 per CONTEXT deferred list)
    - 03-VALIDATION.md complete: per-task verification map all green with test counts, Wave 0 checklist fully checked (mock-engine __SCRIPT_DECISION__ + interrupt fixtures exist), open-question resolutions recorded (Q1 → A2 rebuild in 03-03 Task 2; Q2 → payload contract documented in translateResumeToSubmission; Q3 → A6 opts seam in 03-02 Task 2), nyquist_compliant: true, sign-off approved
  </behavior>
  <action>
    1. Run the full-suite gate command (behavior 1) and record the exact pass counts.
    2. Create `03-COVERAGE.md` mirroring 02-COVERAGE.md: detector record `{"detected":false,"signals":[]}` (the phase translates in-process EngineEvent ⇄ AG-UI interrupt shapes via installed SDKs — no external API is called, no remote service contacted, no credentials; the only HTTP surface is the app's own loopback origin), plus a "UI-03 coverage split" section: Phase 3 delivers the interrupt event contract (metadata = parsed DecisionRequestPayload) and the resume payload contract (documented in translateResumeToSubmission); the #interrupt slot rendering lands in Phase 5 per CONTEXT deferred list — the REQUIREMENTS.md UI-03 checkbox closes at Phase 5, and this record is the traceability note.
    3. Complete `03-VALIDATION.md`: fill the per-task verification map (all 9 tasks across 03-01..03-03 with test counts), check the Wave 0 checklist (mock-engine decision script + interrupt contract fixtures now exist as phase deliverables), record the open-question resolutions and the assumption-deltas (THREAD_BUSY precise message; INVALID_PAYLOAD code), set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, approve the sign-off.
  </action>
  <verify>
    <automated>bun test src/bun --timeout 20000 && bun test e2e/api --timeout 30000 && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The full-suite command passes in one run (backend + e2e + typecheck green — record the counts in the SUMMARY)
    - 03-COVERAGE.md exists with the no-external-API decision AND the UI-03 coverage split
    - 03-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`; the per-task map lists all tasks with passing commands
  </acceptance_criteria>
  <done>The phase gate closes: the full suite is green on the new interrupt stack, the API-coverage decision and the UI-03 contract split are recorded, and the validation strategy is signed off for /gsd-verify-work.</done>
  <reversibility rating="reversible">Documentation records — no production code touched.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loopback client → /api/copilotkit/* (run + connect) | untrusted RunAgentInput JSON incl. resume[] crossing into a FRESH process after restart (the registry rebuild path); the durable JSONL log is re-read on cold connect |
| fresh process → durable JSONL + executions rows | the rebuild reads the thread log and the DB; both are local, process-owned files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Tampering | JSONL log corruption during rebuild | medium | mitigate | Tolerant reader (JsonlStore.read skips malformed lines — jsonl-store.ts:63-78); the rebuild scans for a structurally valid RUN_FINISHED; no terminal found → ensureOpen returns null → resume rejected cleanly (INVALID_INTERRUPT), never a crash or a 500 |
| T-03-13 | (correctness) | replay corrupts the interrupt terminal | high | mitigate | Runner pipeline (finalizeRunEvents early-return + compactEvents) empirically preserves outcome.interrupt and resume[] — pinned by Replay A/B unit shapes and e2e test 17 (Pitfall 7) |
| T-03-14 | Tampering | threadId traversal through connect/rebuild on a fresh process | high | mitigate | Existing layered defenses re-asserted in e2e: agent `/^\d+$/` + JsonlStore assertThreadId containment before ANY fs use; the rebuild only ever reads via the store's validated read() |
| T-03-15 | Spoofing | forged resume against a rebuilt registry entry | medium | mitigate | ensureOpen re-registers ONLY the id that was persisted in the log (never mints a new one); D-05 all-or-nothing validation still applies on the resumed run |
| T-03-16 | Spoofing/DoS | cross-origin abuse of the mount across restarts | medium | mitigate | Origin guard unchanged (e2e tests g/h keep passing); resume runs use the same mount — no new surface |

Zero package installs this phase — RESEARCH.md §Package Legitimacy Audit: no `[ASSUMED]`/`[SUS]` names, no supply-chain checkpoint needed (T-03-SC row not applicable).
</threat_model>

<verification>
- `bun test src/bun --timeout 20000` — full backend suite (incl. the new interrupt/registry/runner/seam tests)
- `bun test e2e/api --timeout 30000` — full e2e suite (decision-cycle tests 11-17 + Phase 1 probe regression)
- `bun run typecheck` — clean
</verification>

<success_criteria>
- ROADMAP SC1/SC2/SC3 proven on the real wire (interrupt card terminal, translated resume, block-while-pending)
- ROADMAP SC4: contract tests with a fake engine prove the full decision cycle — events, interrupt outcome, resume, and replay — end to end (D-09, VERF-01)
- D-08: interrupted runs persist and replay; post-restart resume works (A2)
- Phase records complete: 03-COVERAGE.md (no-external-API + UI-03 split), 03-VALIDATION.md signed off
</success_criteria>

## Artifacts this plan produces

- **e2e/api/copilotkit/railyin.test.ts** — tests 11-17: interrupt run (D-03), block-while-pending (D-04), translated resume + JSONL assertion (CHAT-09 SC2), INVALID_INTERRUPT, cancel (A4), forwardedProps inert (Pitfall 6), restart-replay-with-pending-decision (A2)
- **src/bun/copilotkit/interrupt-registry.ts** — `configure({ store })` + `ensureOpen(threadId, db)`: lazy rebuild from the last interrupt terminal in the thread log + the `waiting_user` executions row; seq counter continuity; same-id re-registration
- **src/bun/copilotkit/railyin-agent.ts** — resume branch cold-path fallback: `get(threadId)` → null ⇒ `ensureOpen(threadId, this.db)` (hot path and D-04 block unchanged)
- **src/bun/index.ts** — `interruptRegistry.configure({ store: jsonlStore })` in the non-probe path only
- **src/bun/copilotkit/interrupt-registry.test.ts / railyin-runner.test.ts** — rebuild tests (C/D) and replay-shape pins (A/B)
- **03-COVERAGE.md** — no-external-API decision record + UI-03 coverage split (contract this phase, rendering Phase 5)
- **03-VALIDATION.md** — completed verification map, Wave 0 checklist, open-question resolutions, `nyquist_compliant: true`
- **Env/config** — none new

<output>
Create `.planning/phases/03-decision-interrupts-resume/03-03-SUMMARY.md` when done.
</output>
