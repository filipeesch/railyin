---
phase: 03-decision-interrupts-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bun/copilotkit/event-bridge.ts
  - src/bun/copilotkit/event-bridge.test.ts
  - src/bun/copilotkit/interrupt-registry.ts
  - src/bun/copilotkit/interrupt-registry.test.ts
  - src/bun/copilotkit/railyin-agent.ts
  - src/bun/copilotkit/railyin-agent.test.ts
  - src/bun/testing/mock-engine.ts
autonomous: true
requirements:
  - RUNR-08
  - CHAT-09
  - UI-03
  - VERF-01
estimate:
  tokens: 38000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - An engine `decision_request` ends the run with RUN_FINISHED carrying `outcome: { type: "interrupt", interrupts: [{ id: "decision-<conversationId>-<seq>", reason: "decision_request", message, metadata }] }` — a NORMAL terminal (never RUN_ERROR), no events after it (D-01/D-02/D-03/D-06; RUNR-08)
    - The interrupt id is stable per decision batch and NEVER derived from executionId (`decision-${conversationId}-${seq}`, per-thread counter — Pitfall 3)
    - The pending interrupt lives in a module-level per-thread registry that survives agent cloning (Pitfall 4) and is reset between tests
    - While a decision interrupt is pending for a thread, a new run WITHOUT `resume[]` is rejected server-side with RUN_ERROR THREAD_BUSY and a precise message (D-04; CHAT-09 SC3)
    - A `decision_request` without a subsequent `onRunEnd` (non-standard coordinator/pause path) still ends with the interrupt terminal, never a plain RUN_FINISHED (Pitfall 5)
    - Every interrupt event zod-parses via EventSchemas; `metadata` carries the parsed DecisionRequestPayload for the Phase 5 decision card (UI-03 event-contract split)
  artifacts:
    - src/bun/copilotkit/interrupt-registry.ts (+ interrupt-registry.test.ts) — module-level singleton: register/get/hasOpen/clear/updateExecutionId/reset; id scheme `decision-${conversationId}-${seq}`
    - src/bun/copilotkit/event-bridge.ts — `buildInterruptOutcome(threadId, runId, payload, interruptId)` pure shape helper (bridge stays terminal-free — Pitfall 3)
    - src/bun/copilotkit/railyin-agent.ts — decision_request payload capture in onEngineEvent; `onRunEnd("decision")` → registry.register + finishInterrupt; WR-02 guard consults the captured payload; D-04 registry block
    - src/bun/testing/mock-engine.ts — `__SCRIPT_DECISION__` Phase A script (token + decision_request, no done)
    - railyin-agent.test.ts / event-bridge.test.ts / interrupt-registry.test.ts — contract tests for the pause half
  key_links:
    - decision_request payload capture fires in onEngineEvent BEFORE onRunEnd("decision") (stream-processor.ts:494-507 ordering — payload is visible at terminal time)
    - finishInterrupt reuses finish()'s closer/synthesize/complete/clear sequence (railyin-agent.ts:203-226) with buildInterruptOutcome as the terminal
    - Registry id minted at terminal time from conversationId + per-thread seq — run.executionId is null during synchronous fake dispatch (Pitfall 3)
    - D-04 registry block placement: after conversation-exists check, BEFORE extractUserText (railyin-agent.ts:149) — same region the 03-02 resume branch occupies
    - EventSchemas from @ag-ui/core validates the interrupt outcome (RunFinishedInterruptOutcomeSchema — the installed contract, RUNR-08)
---

<objective>
The **pause half** of the decision cycle, as the phase's tracer slice: an engine `decision_request` ends the run with the **canonical AG-UI interrupt outcome** — RUN_FINISHED `{ outcome: { type: "interrupt", interrupts: [{ id: "decision-<conversationId>-<seq>", reason: "decision_request", message, metadata }] } }` — emitted by the agent through a new `finishInterrupt` path, with the pending interrupt registered in a module-level per-thread registry (survives `cloneAgentForRequest`, Pitfall 4), the pure `buildInterruptOutcome` shape helper pinned by EventSchemas-validated unit tests, and D-04 block-while-pending enforced server-side (CHAT-09 SC3). The resume half lands in 03-02.

Purpose: RUNR-08 (canonical interrupt terminal, D-01/D-02/D-03/D-06), CHAT-09 SC3 (pending interrupt blocks new input server-side, D-04), UI-03 (the event contract carries parsed DecisionRequestPayload in `metadata` — the Phase 5 card data; rendering itself is deferred to Phase 5 per CONTEXT, recorded in 03-COVERAGE.md at phase gate), VERF-01 (fake-engine contract tests for the pause path). This is the tracer because it wires the phase's riskiest new surface — the interrupt wire format and the registry — end-to-end on the phase's first task, before resume adds the second half.

MVP deviation (accepted, mirrors Phase 2): the ROADMAP Phase 3 goal is capability-shaped, not a user story; the four success criteria drive planning (per planning_context).

Assumption-delta (recorded, advisory): the D-04 registry block reuses the existing THREAD_BUSY error code with a more precise message (research: the registry "adds precise error messages"; the e2e asserts the code, so the code stays stable).

Output: the interrupt-emission path (bridge helper + registry + agent terminal + mock script), pause enforcement, and their contract tests.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decision-interrupts-resume/03-CONTEXT.md
@.planning/phases/03-decision-interrupts-resume/03-RESEARCH.md
@.planning/phases/03-decision-interrupts-resume/03-PATTERNS.md
@src/bun/copilotkit/railyin-agent.ts
@src/bun/copilotkit/event-bridge.ts
@src/bun/copilotkit/railyin-agent.test.ts
@src/bun/testing/mock-engine.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Emit the interrupt terminal — engine decision_request → RUN_FINISHED with outcome.interrupt through the agent</name>
  <files>
    src/bun/copilotkit/event-bridge.ts,
    src/bun/copilotkit/interrupt-registry.ts,
    src/bun/copilotkit/railyin-agent.ts,
    src/bun/testing/mock-engine.ts,
    src/bun/copilotkit/railyin-agent.test.ts
  </files>
  <read_first>
    - 03-RESEARCH.md §Pattern 1 (terminal from the agent's finish(), payload captured in onEngineEvent — fires immediately BEFORE onRunEnd("decision"), stream-processor.ts:494-507), §Code Examples (buildInterruptOutcome sketch lines 361-387; RunFinishedInterruptOutcomeSchema lines 323-338), §Pitfall 3 (id must not depend on executionId)
    - 03-PATTERNS.md §railyin-agent.ts (finish() shape lines 77-94, onEngineEvent tap 108-121, onRunEnd branch 110-119), §event-bridge.ts (terminalEvent builder lines 149-162)
    - src/bun/copilotkit/railyin-agent.ts — finish() (lines 203-226), onEngineEvent tap (lines 232-259), onRunEnd (lines 260-266), the .then executionId hook (lines 268-283)
    - src/bun/copilotkit/event-bridge.ts — terminalEvent (lines 315-325), the terminal-free module contract (lines 1-15)
    - src/bun/testing/mock-engine.ts — SCRIPT_MARKERS (lines 22-27) and scriptedEvents (lines 29-63)
    - src/bun/copilotkit/railyin-agent.test.ts — the fake-coordinator harness (lines 51-85), collectRun (lines 18-28), runInput (lines 30-39)
  </read_first>
  <behavior>
    - Test 1 (decision cycle): fake executeChatTurn drives onEngineEvent(token) → onEngineEvent(decision_request with a serialized payload) → onRunEnd("decision"); run emits RUN_STARTED FIRST, the TEXT block, then RUN_FINISHED LAST with outcome.type === "interrupt"; interrupt.reason === "decision_request"; interrupt.id matches /^decision-\d+-\d+$/; interrupt.message === "mock context"; NO events after the terminal; exactly one terminal; never a RUN_ERROR
    - Test 2 (wire-valid): the RUN_FINISHED interrupt event zod-parses via EventSchemas from @ag-ui/core (RUNR-08)
    - Test 3 (registry lifecycle): after the cycle, registry.hasOpen(threadId) is true; two decision batches on the same thread (registry.clear between) mint decision-<conv>-1 then decision-<conv>-2; a second thread mints its own seq from 1
    - Test 4 (Pitfall 3): the id is valid even though executionId is null at terminal time (synchronous fake dispatch); registry.updateExecutionId(threadId, 42) runs after executeChatTurn resolves
    - Test 5 (malformed payload): decision_request with a non-JSON payload string → RUN_FINISHED interrupt with metadata undefined and message fallback "A decision is required."
  </behavior>
  <action>
    1. `src/bun/copilotkit/event-bridge.ts` — add pure export `buildInterruptOutcome(threadId: string, runId: string, payload: string, interruptId: string): BaseEvent` per the research sketch (lines 361-387): parse payload defensively (`try { parsed = JSON.parse(payload) } catch { keep null }`), return `{ type: EventType.RUN_FINISHED, threadId, runId, outcome: { type: "interrupt", interrupts: [{ id: interruptId, reason: "decision_request", message: parsed?.context ?? "A decision is required.", metadata: parsed ?? undefined }] } }` — mirroring RunFinishedInterruptOutcomeSchema. Do NOT extend `terminalEvent`'s union and do NOT emit terminals from the bridge (Pitfall 3 — exactly one terminal per run, agent-owned).
    2. `src/bun/copilotkit/interrupt-registry.ts` (NEW) — module-level singleton (survives `cloneAgentForRequest`, Pitfall 4; the runtime's ɵGLOBAL_STORE is the pattern precedent): `interface PendingInterrupt { interruptId: string; conversationId: number; executionId: number | null; payload: string; createdAt: number }`; a module-level `Map<string, PendingInterrupt>` plus a per-thread seq `Map<string, number>`. Exports: `register(conversationId: number, payload: string): string` (mints `decision-${conversationId}-${++seq}`, stores the entry with executionId null, returns the id — NEVER uses executionId, Pitfall 3); `get(threadId)`, `hasOpen(threadId)`, `clear(threadId)`; `updateExecutionId(threadId, executionId)` (no-op when no entry); `reset()` (test hook — Pattern 6). Entry payload is the raw serialized DecisionRequestPayload string (parsing stays in the bridge helper).
    3. `src/bun/copilotkit/railyin-agent.ts` — in `run()`, add a closure `let capturedDecisionPayload: string | null = null`. In the onEngineEvent tap (lines 232-259): `if (event.type === "decision_request") capturedDecisionPayload = event.payload;` (the payload fires BEFORE onRunEnd("decision") — stream-processor.ts:494-507). In onRunEnd (lines 260-266): `if (outcome === "decision") { const id = interruptRegistry.register(conversationId, capturedDecisionPayload ?? ""); finishInterrupt(id); }` else keep the existing mapping. Add `finishInterrupt(interruptId: string)` mirroring `finish()` (lines 203-226) exactly: terminalEmitted guard → closers via `translateEngineEvent({ type: "done" }, state)` → `synthesizeMissingToolResults` tail → `subject.next(buildInterruptOutcome(threadId, runId, capturedDecisionPayload ?? "", interruptId))` → `subject.complete()` → clear activeRun. In the `.then(({ executionId }) => ...)` hook (lines 268-283): when `capturedDecisionPayload != null`, call `interruptRegistry.updateExecutionId(threadId, executionId)` (before the abortRequested handling — the executionId is known by the time the user can resume).
    4. `src/bun/testing/mock-engine.ts` — add `"__SCRIPT_DECISION__"` to SCRIPT_MARKERS and a Phase A branch after the `__SCRIPT_ERROR__` block (before `return null`): `{ events: [ { type: "token", content: "I need your decision." }, { type: "decision_request", payload: JSON.stringify({ context: "mock context", questions: [{ question: "Choose __DECISION_OPTION__", type: "exclusive", options: [{ title: "A", description: "" }, { title: "B", description: "" }] }] }) } ] }` — deliberately NO done event: the run ends at the decision (stream-processor maps decision_request → onRunEnd("decision")). Copy the event-shape syntax verbatim from the existing scripts (lines 31-61). Phase B (resume continuation) lands in 03-02 Task 3.
    5. `src/bun/copilotkit/railyin-agent.test.ts` — add `interruptRegistry.reset()` to beforeEach (module singleton — cross-test leakage, Pattern 6). Add the decision-cycle fake: an executeChatTurn override that captures opts, drives `opts.onEngineEvent({ type: "token", content: "I need your decision." })`, `opts.onEngineEvent({ type: "decision_request", payload: <serialized mock payload> })`, `opts.onRunEnd?.("decision")`, returns `{ message: {}, executionId: 42 }`. Cover behaviors 1-5. For behavior 3, call `interruptRegistry.clear(threadId)` between the two batches; seed a second conversation via seedChatSession for the per-thread seq isolation.
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/railyin-agent.test.ts --timeout 20000 && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/bun/copilotkit/railyin-agent.test.ts` passes the decision-cycle, wire-valid, registry-lifecycle, Pitfall-3, and malformed-payload tests
    - The RUN_FINISHED interrupt event parses via EventSchemas in every test (RUNR-08 — the installed schemas ARE the contract)
    - `bun run typecheck` clean (interrupt-registry.ts compiles with the new imports)
    - grep confirms `buildInterruptOutcome` is exported from event-bridge.ts and `interruptRegistry` is imported in railyin-agent.ts; the bridge has no fs/net imports (pure module contract intact)
  </acceptance_criteria>
  <done>The engine's decision_request now ends the run with the canonical interrupt terminal (RUN_FINISHED outcome.interrupt, normal completion), the pending interrupt is registered module-level with an executionId-independent id, and the fake-engine contract test proves the full pause path with wire-valid events.</done>
  <reversibility rating="costly">D-01: the canonical interrupt wire format is a published AG-UI contract; reversing it later breaks clients (CONTEXT marks D-01 costly) — flagged, not gated.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the interrupt wire shape and registry lifecycle with unit tests</name>
  <files>
    src/bun/copilotkit/event-bridge.test.ts,
    src/bun/copilotkit/interrupt-registry.test.ts
  </files>
  <read_first>
    - 03-PATTERNS.md §event-bridge.test.ts (assertValid harness lines 261-270, terminal-paths describe block lines 318-343), §interrupt-registry.test.ts (fresh-state beforeEach pattern)
    - src/bun/copilotkit/event-bridge.test.ts — the assertValid helper (lines 19-27) and the existing describe structure
    - src/bun/copilotkit/interrupt-registry.ts (from Task 1) — the exact exports to pin
    - 03-RESEARCH.md §Code Examples (RunFinishedInterruptOutcomeSchema lines 323-338 — the shape the tests assert)
  </read_first>
  <behavior>
    - Test 1 (buildInterruptOutcome valid): serialized payload with context + questions → RUN_FINISHED, outcome.type "interrupt", interrupts[0].reason "decision_request", message === payload.context, metadata === parsed payload, EventSchemas-valid
    - Test 2 (buildInterruptOutcome malformed): "not-json{{{" → metadata undefined, message === "A decision is required.", still EventSchemas-valid
    - Test 3 (registry lifecycle): register mints decision-<conv>-<seq> incrementing per thread; independent threads keep independent seqs; get returns the entry; clear removes it (hasOpen false); updateExecutionId sets only an existing entry's executionId; reset empties all threads
  </behavior>
  <action>
    1. `src/bun/copilotkit/event-bridge.test.ts` — add a "interrupt outcome" describe block: call `buildInterruptOutcome(threadId, runId, payload, interruptId)` directly and run every returned event through the existing `assertValid` harness (EventSchemas.safeParse — the installed @ag-ui/core contract). Assert the exact shape per behaviors 1-2 (threadId/runId passthrough, outcome.type, reason, message fallback, metadata parsing).
    2. `src/bun/copilotkit/interrupt-registry.test.ts` (NEW) — module-level state tests with `interruptRegistry.reset()` in beforeEach (Pattern 6). Seed no DB — the registry is pure in-memory state. Cover behavior 3. Import the module functions from `./interrupt-registry.ts` (re-export `reset` explicitly — do NOT rely on side-effect-only imports).
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/event-bridge.test.ts src/bun/copilotkit/interrupt-registry.test.ts --timeout 20000</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass (interrupt-shape + registry-lifecycle suites)
    - Every interrupt event in the tests zod-parses via EventSchemas without errors — including the metadata-undefined case
    - `bun test src/bun/copilotkit/railyin-agent.test.ts --timeout 20000` still passes (no cross-test registry leakage — reset() works)
  </acceptance_criteria>
  <done>The interrupt wire shape and the registry lifecycle are pinned by co-located unit tests — the installed EventSchemas validate every emitted interrupt event, and registry state resets cleanly between tests.</done>
  <reversibility rating="reversible">Test-only additions plus one small pure helper — deletable without architectural change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pause-path hardening — block-while-pending (D-04) + decision-without-onRunEnd guard (Pitfall 5)</name>
  <files>
    src/bun/copilotkit/railyin-agent.ts,
    src/bun/copilotkit/railyin-agent.test.ts
  </files>
  <read_first>
    - 03-RESEARCH.md §Pattern 1 (the WR-02 guard gap: covers done/error/ask_user/shell_approval but NOT decision_request — lines 164-165), §Pitfall 5, §Pattern 2 (resume branch placement rationale — the D-04 block sits in the same region)
    - src/bun/copilotkit/railyin-agent.ts — extractUserText (lines 149-153), advisory lock (lines 162-168), the WR-02 guard list (lines 249-258), emitRunError (lines 129-134)
    - src/bun/copilotkit/railyin-agent.test.ts — test 4 (pause-path completion guard, lines 162-184) and test 10 (advisory lock incl. waiting_user → THREAD_BUSY, lines 384-428)
  </read_first>
  <behavior>
    - Test A (D-04 block): an open registry entry for the thread (registered via the decision-cycle fake or direct register) → a run WITHOUT resume[] → RUN_ERROR THREAD_BUSY with message "A decision interrupt is pending for this thread"; executeChatTurn never called
    - Test B (no block when clean): no open entry → the run proceeds normally (RUN_FINISHED)
    - Test C (Pitfall 5 guard): fake drives onEngineEvent(token) then onEngineEvent(decision_request) but NEVER calls onRunEnd (pause-path coordinator) → the run ends with the interrupt terminal (RUN_FINISHED outcome.interrupt), NOT a plain RUN_FINISHED and never a RUN_ERROR
  </behavior>
  <action>
    1. `src/bun/copilotkit/railyin-agent.ts` — insert the D-04 registry block AFTER the conversation-exists check (line 147) and BEFORE `extractUserText` (line 149): `if (!input.resume?.length && interruptRegistry.hasOpen(threadId)) { emitRunError("A decision interrupt is pending for this thread", "THREAD_BUSY"); return subject.asObservable() as unknown as ReturnType<AbstractAgent["run"]>; }` — the THREAD_BUSY code stays identical to the advisory lock (e2e asserts the code; the registry adds the precise message per research); the resume branch that precedes this region lands in 03-02 Task 3.
    2. WR-02 guard (lines 249-258): add `event.type === "decision_request"` to the guard list; inside the queueMicrotask, when `capturedDecisionPayload != null && !terminalEmitted`, mint the id via `interruptRegistry.register(conversationId, capturedDecisionPayload)` and emit `finishInterrupt(id)` instead of `guardedComplete()` (Pitfall 5 — the decision must not silently vanish when a coordinator returns instead of calling onRunEnd).
    3. `src/bun/copilotkit/railyin-agent.test.ts` — add the three tests. Test A: run the decision-cycle fake first (or `interruptRegistry.register(...)` directly), then `collectRun` a plain run on the same thread; assert the last event is RUN_ERROR with code THREAD_BUSY and the precise message. Test B: plain run on a fresh thread after `interruptRegistry.reset()`. Test C: new fake driving token + decision_request with NO onRunEnd; assert the last event is RUN_FINISHED with outcome.type "interrupt" (event shape from Task 1) and no RUN_ERROR appears.
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/railyin-agent.test.ts --timeout 20000</automated>
  </verify>
  <acceptance_criteria>
    - All three hardening tests pass (block-while-pending, clean-run, decision-without-onRunEnd)
    - The guard test proves no RUN_ERROR and no plain RUN_FINISHED for the pause-path decision (the interrupt terminal wins)
    - `bun test src/bun/copilotkit --timeout 20000` passes as a directory run (Task 1 + Task 2 suites stay green)
  </acceptance_criteria>
  <done>While a decision interrupt is pending, a new non-resume run is rejected server-side with a precise THREAD_BUSY error; a decision_request that never reaches onRunEnd still terminates with the interrupt outcome — the pause path cannot strand or swallow a decision.</done>
  <reversibility rating="reversible">Two guarded agent-side branches — removing them restores Phase 2 behavior exactly.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loopback client → /api/copilotkit/* | untrusted RunAgentInput JSON crossing into the runtime → RailyinAgent (same-origin localhost, no auth — inherited posture) |
| agent → event-bridge / interrupt-registry | internal: the agent passes the raw serialized decision payload into pure shape building and module-level state; payload string originates from the engine, not the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `buildInterruptOutcome` payload parsing | medium | mitigate | Defensive `JSON.parse` in try/catch — malformed engine payload yields `metadata: undefined` + message fallback, never a crash; output always EventSchemas-validated (V5) |
| T-03-02 | (correctness→availability) | agent WR-02 guard | high | mitigate | Guard consults the captured decision payload — a decision_request without onRunEnd still emits the interrupt terminal (Pitfall 5); the thread never wedges on an unregistered decision |
| T-03-03 | Spoofing | interrupt id collisions across threads | low | mitigate | Per-thread seq counters + conversationId embedded in the id (`decision-${conversationId}-${seq}`); independent threads never mint the same id (Pitfall 3) |
| T-03-04 | DoS | module-level registry growth across threads | low | accept | Entries are cleared on resume/cancel (03-02) and `reset()` between tests; single-user local app — bounded, proportionate (RESEARCH §Security Domain posture) |
| T-03-05 | Tampering | package supply chain | low | accept | Zero package installs this phase — RESEARCH.md §Package Legitimacy Audit confirms no legitimacy gate needed; no `[ASSUMED]`/`[SUS]` names |

Existing hardenings re-verified, not re-implemented: threadId `/^\d+$/` + conversation-exists validation (agent, first line) and `JsonlStore` containment (second line); runtime zod `RunAgentInputSchema` parse → 400; origin guard on the mount (e2e tests g/h).
</threat_model>

<verification>
- `bun test src/bun/copilotkit --timeout 20000` — interrupt emission, registry lifecycle, pause hardening (RUNR-08, CHAT-09, VERF-01)
- `bun test e2e/api/copilotkit/copilotkit.test.ts --timeout 30000` — Phase 1 probe regression (probe path untouched — the registry only exists in the non-probe module graph)
- `bun run typecheck` — new module + rewired agent compile
</verification>

<success_criteria>
- ROADMAP SC1 (pause half): engine decision_request genuinely ends the run with the structured interrupt terminal — no further tokens/tool calls, normal completion (D-03)
- ROADMAP SC3 (server side): a pending interrupt blocks new non-resume input with a clear error (D-04)
- RUNR-08 emission contract + UI-03 metadata contract unit-pinned; VERF-01 fake-engine pause-path coverage
</success_criteria>

## Artifacts this plan produces

- **src/bun/copilotkit/interrupt-registry.ts** — module-level singleton: `PendingInterrupt` entry `{ interruptId, conversationId, executionId, payload, createdAt }`; `register(conversationId, payload): string` (mints `decision-${conversationId}-${seq}`), `get`, `hasOpen`, `clear`, `updateExecutionId`, `reset`
- **src/bun/copilotkit/event-bridge.ts** — `buildInterruptOutcome(threadId, runId, payload, interruptId): BaseEvent` (RUN_FINISHED + outcome.interrupt; defensive payload parse; message fallback)
- **src/bun/copilotkit/railyin-agent.ts** — `capturedDecisionPayload` closure; `onRunEnd("decision")` → registry.register + `finishInterrupt(id)` (closer/synthesize/complete sequence); `.then` → registry.updateExecutionId; D-04 registry block before extractUserText; WR-02 guard gains decision_request
- **src/bun/testing/mock-engine.ts** — `__SCRIPT_DECISION__` Phase A script (token + decision_request, no done)
- **src/bun/copilotkit/event-bridge.test.ts / interrupt-registry.test.ts / railyin-agent.test.ts** — contract tests: interrupt shape (EventSchemas-validated), registry lifecycle/id scheme, decision cycle, malformed payload, block-while-pending, Pitfall-5 guard
- **Env/config** — none new

<output>
Create `.planning/phases/03-decision-interrupts-resume/03-01-SUMMARY.md` when done.
</output>
