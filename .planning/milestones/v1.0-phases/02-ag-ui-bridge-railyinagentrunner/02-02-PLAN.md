---
phase: 02-ag-ui-bridge-railyinagentrunner
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/bun/copilotkit/jsonl-store.ts
  - src/bun/copilotkit/jsonl-store.test.ts
  - src/bun/copilotkit/railyin-runner.ts
  - src/bun/copilotkit/railyin-runner.test.ts
  - src/bun/index.ts
  - e2e/api/fixtures/server.ts
  - e2e/api/copilotkit/railyin.test.ts
autonomous: true
requirements:
  - RUNR-02
  - RUNR-03
  - RUNR-04
  - RUNR-05
  - RUNR-06
  - RUNR-07
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Every run's wire events — including the runner-patched RUN_STARTED.input — append verbatim to `data/threads/{threadId}.jsonl` via `super.run().pipe(tap(...))` (RUNR-02; anti-pattern "persist from agent.run()" avoided)
    - `connect()` on a cold thread (fresh process, file exists) replays the JSONL log with per-run boundaries and completed tool calls — `completeOpenToolCalls` → `compactEvents` → emit; a log whose LAST run lacks a terminal is completed; replay truncates at the first RUN_ERROR (RUNR-05, RUNR-07, research Pitfall 4 + Open Question 1)
    - `connect()` on a never-run thread (no file, no store) completes empty — zero frames, 200 (RUNR-06; Phase 1 test 5 contract)
    - A second concurrent run on the same thread throws `Error("Thread already running")` synchronously at the runner level; over SSE it surfaces as HTTP 200 with an EMPTY body — asserted at both levels (RUNR-04; Pitfall 2)
    - threadId = String(conversation.id) drives the file name; standalone sessions are threads without a taskId — the mapping holds on the real wire (RUNR-03; D-06)
    - The store rejects non-numeric threadIds (`^\d+$`) and containment-checks the resolved path BEFORE any filesystem use; traversal attempts are unit-tested (security V5/V8)
  artifacts:
    - src/bun/copilotkit/jsonl-store.ts (+ jsonl-store.test.ts) — append/read/exists/endRun over `data/threads/{threadId}.jsonl`, tolerant reader, sanitization
    - src/bun/copilotkit/railyin-runner.ts (+ railyin-runner.test.ts) — InMemoryAgentRunner subclass: run() pipe-tap persistence, connect() hot/cold/never-run branching, replay shapes
    - src/bun/index.ts — D-12 runner wiring: `new JsonlStore(getDataDir())` + `new RailyinAgentRunner(store)` passed to CopilotRuntime (probe mode keeps base runner)
    - e2e/api/fixtures/server.ts — `dataDir` option (persistent RAILYN_DATA_DIR for restart-replay e2e, shutdown skips deletion)
    - e2e/api/copilotkit/railyin.test.ts — extension: JSONL file assertions, never-run connect, concurrent-run zero frames, replay across server restarts
  key_links:
    - Persist from `super.run()`'s observable (pipe-tap), NEVER from inside agent.run() — the runner patches RUN_STARTED.input after the agent emits, and the tap sees the patched event (research anti-pattern line 257)
    - `connect()` branching uses `this.getThreadEvents(threadId).length > 0` as the store-presence probe (verified in-memory.mjs:496-512: `[]` for unknown threads) → hot path `super.connect()` keeps the live tail; cold path replays the file; never-run → `super.connect()` completes empty
    - ThreadId sanitization in the store is the SECOND defense line (agent validation from 02-01 is the first); both must reject `../` and absolute paths
    - e2e replay needs the SAME RAILYN_DATA_DIR across two spawned servers — the new `dataDir` fixture option (mcpConfig-derived dataDir is deleted at shutdown)
---

<objective>
Make the runner durable: `jsonl-store.ts` persists every wire event append-only per thread, and `RailyinAgentRunner` (an `InMemoryAgentRunner` subclass) overrides `run()` (pipe-tap persistence) and `connect()` (hot-path base machinery / cold-path JSONL replay / never-run empty completion). This is the D-04/D-05 extension pattern: the base runner keeps the concurrency guard, compaction, and live-tail — Railyin only adds the durable log (the #3553 cold-start fix) and the replay shape. D-08 (RUNR-04): the base runner's synchronous "Thread already running" throw IS the lock — preserved, unit-tested, and asserted as HTTP 200 + empty SSE body on the wire. D-11: all new files live under `src/bun/copilotkit/`.

Purpose: RUNR-02 (per-thread JSONL via the custom runner), RUNR-04 (inherited lock, tested at both levels), RUNR-05 (replay from the event log, not snapshots), RUNR-06 (never-run empty snapshot), RUNR-07 (replayed tool calls synthesize TOOL_CALL_RESULT — no stale running cards), RUNR-03 (threadId = conversation.id mapping proven on the wire). Phase 2 scope: basic append-only with a tolerant reader — crash tolerance (buffered writer, atomic index) is deliberately Phase 4.

Output: the store + runner modules with unit tests, the composition-root runner swap, the fixture `dataDir` seam, and e2e proof of persistence/replay/lock/never-run on the real wire.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-CONTEXT.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-RESEARCH.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-PATTERNS.md
@src/bun/oauth/token-store.ts
@src/bun/utils/platform.ts
@src/bun/index.ts
@e2e/api/fixtures/server.ts
@e2e/api/copilotkit/railyin.test.ts
@node_modules/@copilotkit/runtime/dist/v2/runtime/runner/in-memory.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the JSONL store with threadId sanitization and a tolerant reader</name>
  <files>
    src/bun/copilotkit/jsonl-store.ts,
    src/bun/copilotkit/jsonl-store.test.ts
  </files>
  <read_first>
    - 02-RESEARCH.md §Pattern 3 (store API, path/location, line schema, tolerant read, sanitization), §Common Pitfalls 7/8
    - 02-PATTERNS.md §jsonl-store.ts (token-store.ts analog :113-133, THREAD_ID_RE :217-221)
    - src/bun/oauth/token-store.ts — the exact file-module pattern (existsSync/readFileSync/appendFileSync/mkdirSync, :14-45)
    - src/bun/utils/platform.ts — getDataDir() (lines 16-18)
    - src/bun/test/helpers.ts — makeTempDir pattern (lines 265-268: mkdtempSync(join(tmpdir(), "railyn-test-")) + cleanup)
  </read_first>
  <behavior>
    - Test 1 (round-trip): append(threadId, event) then read(threadId) returns the exact events in order (one JSON object per line, verbatim)
    - Test 2 (missing file): read("999") on a fresh temp dir returns null; exists("999") is false
    - Test 3 (sanitization): append/read/exists with "../evil", "a/../../x", and "/absolute/path" threadIds ALL throw before any filesystem access
    - Test 4 (tolerant read): a file whose trailing line is truncated JSON (crash mid-append) still reads the complete lines, skips the partial line without failing
    - Test 5 (dir creation): append on a threadId whose threads/ dir does not yet exist creates it
  </behavior>
  <action>
    Create `src/bun/copilotkit/jsonl-store.ts` following the token-store.ts shape (sync fs ops; Bun/Node single-threaded append discipline):

    - `const THREAD_ID_RE = /^\d+$/;` and a private `assertThreadId(threadId: string): void` that throws `Error(\`Invalid threadId: ${threadId}\`)` when the id fails the regex or when the CONTAINMENT check fails (see below) — called FIRST in every public method (sanitize-before-filesystem-use order, security V5/V8; PITFALLS Security Mistakes).
    - Containment: `const path = threadLogPath(dataDir, threadId)` then `if (!path.resolve(path).startsWith(path.resolve(threadsDir) + path.sep)) throw ...` — resolve once per call, never interpolate an unvalidated id into a path first.
    - `export function threadLogPath(dataDir: string, threadId: string): string` → `join(dataDir, "threads", `${threadId}.jsonl`)` (mirror token-store.ts:18-24; the runner/agent may reuse it for read-only helpers).
    - `export class JsonlStore` constructed with `private readonly dataDir: string` (injected from `getDataDir()` at the composition root — no constructor-visible deps, D-05):
      - `append(threadId: string, event: BaseEvent): void` — mkdirSync(dirname, {recursive:true}) when missing, then `appendFileSync(path, JSON.stringify(event) + "\n", "utf-8")` — ONE event per line, verbatim, per-event append (never run-end batch — replay needs the mid-run tail for reconnect, Pitfall 7)
      - `read(threadId: string): BaseEvent[] | null` — null when the file is absent; tolerant parse: split lines, JSON.parse each, SKIP + console.warn a malformed/partial trailing line instead of failing the file (Phase 4 hardens; Pitfall 7)
      - `exists(threadId: string): boolean` — existsSync after validation
      - `endRun(threadId: string): void` — validates the id; reserved bookkeeping hook for crash-tolerance metadata (Phase 4). Phase 2: no persisted payload (the terminal event in the log IS the run-end marker)
    - Co-locate `jsonl-store.test.ts`: makeTempDir per test (helpers.ts pattern), clean up in afterEach. Cover behaviors 1-5; for the partial-line test, write a file with `writeFileSync` containing one complete line + `{"type":"RUN_STARTED"` (truncated).
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/jsonl-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/bun/copilotkit/jsonl-store.test.ts` passes round-trip, missing-file, sanitization (traversal + absolute paths), tolerant-read, and dir-creation tests
    - No `path.resolve` result ever escapes the threads dir — the containment check is on the resolved path, tested with `../` and absolute attempts
    - grep confirms `THREAD_ID_RE` validation precedes every fs call in jsonl-store.ts
  </acceptance_criteria>
  <done>The append-only JSONL store exists at src/bun/copilotkit/jsonl-store.ts with the four-method API, threadId sanitization + containment before any filesystem use, per-event appends, and a tolerant reader — all unit-tested.</done>
  <reversibility rating="reversible">D-05: the file format is private to the store module — reversible by construction.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build RailyinAgentRunner — pipe-tap persistence, hot/cold connect branching, replay shapes</name>
  <files>
    src/bun/copilotkit/railyin-runner.ts,
    src/bun/copilotkit/railyin-runner.test.ts
  </files>
  <read_first>
    - node_modules/@copilotkit/runtime/dist/v2/runtime/runner/in-memory.mjs — run() (lines 297-432: the "Thread already running" throw at 300-302, RUN_STARTED input patch at 372-398, runSubject.asObservable), connect() (store presence → compactEvents + live-tail dedup; absent → complete), getThreadEvents (lines 496-512: returns [] for unknown threads — the store-presence probe), getThreadState
    - node_modules/@copilotkit/runtime/dist/v2/runtime/runner/agent-runner.d.mts — AgentRunnerRunRequest / AgentRunnerConnectRequest shapes
    - 02-RESEARCH.md §Pattern 1 (runner skeleton :203-221), §Pattern 4 (replay order: completeOpenToolCalls → compactEvents; finalizeRunEvents only when the LAST run lacks a terminal), §Common Pitfalls 2/4
    - 02-PATTERNS.md §railyin-runner.ts (:83-111) and §Shared Patterns (replay-shape checklist)
    - src/bun/copilotkit/jsonl-store.ts (Task 1 output), src/bun/copilotkit/event-bridge.ts (completeOpenToolCalls-style synthesis from 02-01)
  </read_first>
  <behavior>
    - Test 1 (lock): runner.run() on an already-running thread (fake agent whose runAgent never settles) throws `Error("Thread already running")` synchronously on the SECOND call (RUNR-04; Pitfall 2)
    - Test 2 (wire-exact persistence): fake agent emitting RUN_STARTED (WITHOUT input) + TEXT events + RUN_FINISHED via onEvent; the JSONL file contains the SAME events INCLUDING the runner-patched RUN_STARTED.input (RUNR-02; anti-pattern avoided)
    - Test 3 (replay shapes): (a) missing file → connect completes empty; (b) empty file → completes empty; (c) N completed runs → replayed in per-run boundaries (RUN_STARTED..RUN_FINISHED, RUN_STARTED..RUN_FINISHED); (d) interrupted last run (no terminal) → completed with finalizeRunEvents; (e) errored-run-then-run → replay truncated at the first RUN_ERROR (RUNR-05; Pitfall 4, Open Question 1)
    - Test 4 (dangling tools on replay): a log with TOOL_CALL_START but no RESULT → replayed stream contains synthetic TOOL_CALL_RESULT (RUNR-07; D-09)
    - Test 5 (hot path): after a run in the SAME process, connect() delegates to super.connect() (store probe `getThreadEvents().length > 0`) — replayed events present
  </behavior>
  <action>
    Create `src/bun/copilotkit/railyin-runner.ts` — subclass-override shape per executor-test-helpers.ts:71-91 (StubStreamProcessor):

    - Imports VERIFIED from `@copilotkit/runtime/v2` (index.d.mts:13-21): `InMemoryAgentRunner`, `AgentRunnerRunRequest`, `AgentRunnerConnectRequest`, `finalizeRunEvents`; `Observable`/`tap` from rxjs; `compactEvents`/`BaseEvent` from `@ag-ui/client`.
    - `export class RailyinAgentRunner extends InMemoryAgentRunner` with `constructor(private readonly store: JsonlStore) { super(); }` — pass NO options: `onConcurrentRun` stays "throw" (the lock contract, RESEARCH.md:199).
    - `override run(request: AgentRunnerRunRequest): Observable<BaseEvent>`: `return super.run(request).pipe(tap({ next: (event) => this.store.append(request.threadId, event), complete: () => this.store.endRun(request.threadId) }))` — persistence happens on the runner's observable so the log contains EXACTLY what the client received, including the runner's RUN_STARTED.input patch (never persist from inside agent.run()).
    - `override connect(request: AgentRunnerConnectRequest): Observable<BaseEvent>` with THREE branches:
      1. HOT (thread known to this process): `this.getThreadEvents(request.threadId).length > 0` → `return super.connect(request)` — base machinery replays compacted history AND live-tails the in-flight run with messageId dedup (verified base behavior).
      2. COLD (fresh process, durable log exists): `this.store.exists(request.threadId)` → replay: read the file; if the LAST run lacks a terminal event, run `finalizeRunEvents(events)` (import from `@copilotkit/runtime/v2`); then `completeOpenToolCalls(events)` — a small local pass that appends TOOL_CALL_END + TOOL_CALL_RESULT {messageId: `${toolCallId}-result`, toolCallId, content: ""} for dangling TOOL_CALL_STARTs (RUNR-07; do NOT rely on finalizeRunEvents for this — it early-returns when a terminal exists, research A5); truncate the event list at the FIRST RUN_ERROR if one exists (Pitfall 4 safe default — the pinned client cannot hydrate past a RUN_ERROR); then `compactEvents(events)` (from `@ag-ui/client`, keeps per-run boundaries) and emit the result verbatim into a ReplaySubject(Infinity) → asObservable (the shape the pinned client already handles on connect, matching base in-memory connect).
      3. NEVER-RUN (no store, no file): `return super.connect(request)` — base completes empty (RUNR-06; Phase 1 test 5 contract).
      Order matters: branch 1 FIRST (live tail beats replay), branch 2 next, branch 3 last.
    - Do NOT override `stop`/`isRunning`/`listThreads` (base implementations; stop proven in Phase 1).
    - Co-locate `railyin-runner.test.ts`: temp dataDir + JsonlStore per test; for lock + wire-exact tests use a fake agent object `{ runAgent: async (input, { onEvent }) => {...}, abortRun: () => {} }` and a valid `AgentRunnerRunRequest` (`{ threadId, agent, input }` with a minimal RunAgentInput). For replay-shape tests, write JSONL files through the store directly (or hand-written fixtures in the temp dir), then connect with `{ threadId }` and collect events. For Test 5 (hot path), run first then connect — assert replayed events and NO live-tail loss (the store probe drives the branch).
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/railyin-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/bun/copilotkit/railyin-runner.test.ts` passes lock, wire-exact persistence, all five replay shapes, dangling-tool synthesis, and hot-path tests
    - The lock test asserts the THROW (not an SSE status — Pitfall 2); the SSE 200+empty-body assertion lives in e2e (Task 3)
    - JSONL lines match the wire events exactly, including the runner-patched RUN_STARTED.input (Test 2)
  </acceptance_criteria>
  <done>RailyinAgentRunner persists every wire event to JSONL via pipe-tap, replays cold threads with completed tool calls and per-run boundaries (truncated at first RUN_ERROR), completes empty for never-run threads, and keeps the base lock + hot-path live tail — all unit-tested.</done>
  <reversibility rating="costly">D-04: the runner override IS the persistence architecture; changing the connect()/run() shape later touches the runner contract — flagged (research-backed extension pattern, AWS AgentCoreRunner precedent).</reversibility>
</task>

<task type="auto">
  <name>Task 3: Wire the runner into the composition root and prove persistence, replay, lock, and never-run on the real wire</name>
  <files>
    src/bun/index.ts,
    e2e/api/fixtures/server.ts,
    e2e/api/copilotkit/railyin.test.ts
  </files>
  <read_first>
    - src/bun/index.ts — the mount block (lines 245-277 from 02-01 Task 3), `CopilotRuntimeOptions.runner` verified support (node_modules/@copilotkit/runtime/dist/v2/core/runtime.d.mts:152-153)
    - e2e/api/fixtures/server.ts — dataDir + mcpConfig seam (lines 138-150), shutdown cleanup (lines 263-270)
    - e2e/api/copilotkit/railyin.test.ts — the 02-01 Task 3 file being extended (do not restructure; add describes)
    - 02-RESEARCH.md §Common Pitfalls 2 (SSE lock surface: HTTP 200 + EMPTY body, NOT 500) and the replay e2e checklist (Validation Architecture, line 546)
  </read_first>
  <behavior>
    - Test 7 (JSONL on disk): after a real run, `join(dataDir, "threads", `${threadId}.jsonl`)` exists; first line parses to RUN_STARTED carrying input.messages with the user text; last line is RUN_FINISHED (RUNR-02)
    - Test 8 (never-run connect): connect on a fresh threadId → 200 SSE, zero frames (RUNR-06)
    - Test 9 (concurrent run): run "__SCRIPT_SLOW__", fire a SECOND run on the same threadId while the first is active → 200 + empty body (zero frames), NOT 500 (RUNR-04; Pitfall 2)
    - Test 10 (replay across restarts): server A run "__SCRIPT_TOOLS__" → shutdown → server B with the SAME dataDir → connect → replayed frames: RUN_STARTED..RUN_FINISHED with per-run boundary AND a TOOL_CALL_RESULT present (RUNR-05/07, #3553 cold-start fix)
  </behavior>
  <action>
    1. `src/bun/index.ts` (MOD): extend the 02-01 mount block — construct `const jsonlStore = new JsonlStore(getDataDir())` (import getDataDir from "./utils/platform.ts" and JsonlStore from "./copilotkit/jsonl-store.ts") and `const railyinRunner = new RailyinAgentRunner(jsonlStore)`; pass `runner: railyinRunner` to the CopilotRuntime constructor (options key verified in runtime.d.mts). CRITICAL: the runner is used ONLY in the non-probe path — when `RAILYN_COPILOTKIT_PROBE === "1"`, keep the probe registration EXACTLY as Phase 1/02-01 (ScriptedAgent + base InMemoryAgentRunner — probe threadIds like "t1" are non-numeric and the probe wire text must stay byte-identical; construct `new CopilotRuntime({ agents, runner: new InMemoryAgentRunner() })` in probe mode or omit the runner key). Preserve the mount branch, srv.timeout(req, 0), and multi-route handler.
    2. `e2e/api/fixtures/server.ts` (MOD): add `dataDir?: string` to StartServerOptions — when provided, set `RAILYN_DATA_DIR` to it (mkdir recursive) AND skip the `rmSync(runtimeDir)` in shutdown() (the caller owns cleanup — the restart-replay test needs the durable dir to survive between two spawned servers; document this contract on the option). `TestServer.dataDir` already returns the value.
    3. `e2e/api/copilotkit/railyin.test.ts` (MOD): add the four behaviors above:
       - Test 7: the existing server spawn gains `mcpConfig: {}` (populates server.dataDir — the mcpConfig → RAILYN_DATA_DIR seam, server.ts:142-147); after a run completes, read the file from disk and assert first/last lines.
       - Test 9: fire the "__SCRIPT_SLOW__" run (do NOT await the body), wait ~400ms, fire the second run on the same threadId → assert `res.status === 200` AND `parseSseFrames(await res.text())` has length 0 (Pitfall 2 — assert the EMPTY BODY, never a 500); then await the first run's full body to close the stream cleanly.
       - Test 10: create a temp dir via mkdtempSync; `startServer({ mcpConfig: {}, dataDir: tempDir })` → chatSessions.create → run "__SCRIPT_TOOLS__" (await full body) → shutdown → `startServer({ mcpConfig: {}, dataDir: tempDir })` again → POST connect on the same threadId → assert frames: first RUN_STARTED, last RUN_FINISHED, some TOOL_CALL_RESULT present; clean up tempDir in afterAll.
       - Keep the existing 02-01 run-path tests untouched and green (they now run WITH the durable runner — persistence is additive).
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/railyin.test.ts && bun test e2e/api/copilotkit/copilotkit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test e2e/api/copilotkit/railyin.test.ts` passes tests 1-10 (run path + JSONL on disk + never-run + concurrent-run empty body + restart replay)
    - `bun test e2e/api/copilotkit/copilotkit.test.ts` still green (probe mode untouched)
    - The concurrent-run test asserts 200 + zero frames (never a 500 status assertion)
    - The replay test uses the SAME dataDir across two spawned servers via the new fixture option
  </acceptance_criteria>
  <done>The durable runner is live in production: runs persist to data/threads/{conversation.id}.jsonl, connects replay the log across process restarts with completed tool calls, never-run threads connect empty, and concurrent runs are rejected (empty SSE body) — all proven on a real spawned server.</done>
  <reversibility rating="reversible">D-12's runner wiring is additive in the composition root; probe mode untouched; dropping the runner key restores base behavior.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → JSONL store | client-supplied threadId becomes a file name under `data/threads/` — the phase's new filesystem write path (security V5/V8) |
| HTTP client → /api/copilotkit/* | concurrent run requests and connect requests arriving at the runner (DoS/lock surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | threadId path traversal into `data/threads/{threadId}.jsonl` | high | mitigate | `^\d+$` validation + resolved-path containment check in jsonl-store BEFORE any fs call (V5/V8; RESEARCH Pattern 3); unit tests with `../` and absolute attempts; the agent's numeric validation (02-01) is the first line — the store does NOT trust its caller |
| T-02-08 | DoS | concurrent runs on one thread (double execution) | medium | mitigate | Inherited runner lock — "Thread already running" synchronous throw (unit-tested, Task 2) and the 200+empty-body SSE surface (e2e-tested, Task 3); client busy-guard is Phase 5 UX |
| T-02-09 | Tampering | corrupt/partial JSONL (crash mid-append) poisoning replay | low | mitigate | Tolerant reader: partial trailing line skipped + logged, never fails the file (Pitfall 7; unit-tested); full crash tolerance (buffered writer, atomic index) deferred to Phase 4 per CONTEXT.md |
| T-02-10 | DoS | unbounded JSONL growth over many runs | low | accept | Single-user local app; one thread file per conversation; retention/compaction is a Phase 4 concern (research Standard Stack row: write-buffer/retention job exists) |
| T-02-11 | Tampering | replay of an errored run poisoning client hydration (RUN_ERROR followed by later runs) | medium | mitigate | Replay truncates at the first RUN_ERROR (Pitfall 4 safe default; unit-tested shape (e)); per-run boundary order preserved via compactEvents |

Package legitimacy: no package installs in this plan (rxjs pin is 02-03) — no checkpoint needed; the phase's only install (`bun add rxjs@^7.8.2`) is audited in RESEARCH.md §Package Legitimacy Audit with verdict OK/Approved.
</threat_model>

<verification>
- `bun test src/bun/copilotkit/jsonl-store.test.ts` — store round-trip, sanitization, tolerant read
- `bun test src/bun/copilotkit/railyin-runner.test.ts` — lock throw, wire-exact persistence, 5 replay shapes, dangling-tool synthesis, hot path
- `bun test e2e/api/copilotkit/railyin.test.ts` — JSONL on disk, never-run connect, concurrent-run empty body, restart replay (tests 7-10)
- `bun test e2e/api/copilotkit/copilotkit.test.ts` — Phase 1 probe regression
- `bun run typecheck` — new modules + fixture option compile
</verification>

<success_criteria>
- ROADMAP SC2: card conversations persist to `data/threads/{conversation.id}.jsonl` (standalone sessions = threads without a taskId — the e2e uses chatSessions.create)
- ROADMAP SC3: reload/reconnect mid-run replays the full conversation from the JSONL event log with no stale "running" tool cards (TOOL_CALL_RESULT synthesized at replay AND persisted by the bridge at run end)
- ROADMAP SC4: connecting to a never-run thread returns an empty snapshot (zero frames, 200)
- ROADMAP SC5: second concurrent run rejected — runner throw at unit level, 200+empty-body at the wire level
</success_criteria>

## Artifacts this phase produces

- **src/bun/copilotkit/jsonl-store.ts** — `THREAD_ID_RE` sanitization + containment; `threadLogPath(dataDir, threadId)`; `class JsonlStore` with `append(threadId, event)`, `read(threadId): BaseEvent[] | null` (tolerant), `exists(threadId)`, `endRun(threadId)`; per-event append, no envelope
- **src/bun/copilotkit/railyin-runner.ts** — `class RailyinAgentRunner extends InMemoryAgentRunner`: `run()` pipe-tap persistence; `connect()` 3-branch (hot via `getThreadEvents().length > 0` probe → super.connect; cold → completeOpenToolCalls → finalizeRunEvents-if-unterminated → truncate-at-first-RUN_ERROR → compactEvents → replay; never-run → super.connect empty)
- **src/bun/copilotkit/jsonl-store.test.ts / railyin-runner.test.ts** — co-located suites (sanitization, tolerant read, lock throw, 5 replay shapes, wire-exactness)
- **src/bun/index.ts** — `JsonlStore(getDataDir())` + `RailyinAgentRunner(store)` in the non-probe path only; probe mode keeps base InMemoryAgentRunner
- **e2e/api/fixtures/server.ts** — `StartServerOptions.dataDir` (durable RAILYN_DATA_DIR across restarts; shutdown skips deletion when set)
- **e2e/api/copilotkit/railyin.test.ts** — tests 7-10: JSONL file assertions, never-run connect, concurrent-run 200+empty, restart replay with completed tool calls
- **Env/config** — none new (RAILYN_DATA_DIR reuse; getDataDir() from platform.ts)

<output>
Create `.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-02-SUMMARY.md` when done.
</output>
