---
phase: 02-ag-ui-bridge-railyinagentrunner
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/bun/copilotkit/railyin-agent.ts
  - src/bun/copilotkit/railyin-agent.test.ts
  - package.json
  - bun.lock
  - e2e/api/copilotkit/pins.test.ts
  - .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md
  - .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-VALIDATION.md
autonomous: true
requirements:
  - BRDG-01
  - RUNR-03
  - RUNR-04
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The agent resolves the workspace key per conversation: task-linked → task's board workspace_key, else chat_sessions.workspace_key, else default — all three branches unit-tested against a real in-memory DB (RUNR-03; research Open Question 3; mirrors conversations.ts:64-76)
    - A run on a conversation with an active execution (status 'running' or 'waiting_user') is rejected with a clear RUN_ERROR before any executor work — the advisory cross-path lock (RUNR-04 cross-path; research Open Question 2); the runner lock still fires FIRST for same-thread AG-UI concurrency (no behavior regression)
    - A run on an unknown conversation id emits RUN_ERROR (RUNR-03/06 boundary; no executor side effects)
    - rxjs is an explicit direct dependency pinned to ^7.8.2 and the pins test asserts it (HOST-03 continuation; research Installation)
    - Full backend suite, full e2e suite (including Phase 1 probe tests), and `bun run typecheck` are green at phase close
    - 02-COVERAGE.md records the no-external-API decision and 02-VALIDATION.md is marked complete (nyquist_compliant: true)
  artifacts:
    - src/bun/copilotkit/railyin-agent.ts — workspaceKey resolver + advisory lock + unknown-conversation check (expanding 02-01's agent)
    - src/bun/copilotkit/railyin-agent.test.ts — three-branch resolver tests + advisory-lock test + unknown-conversation test
    - package.json + bun.lock — rxjs ^7.8.2 direct dependency
    - e2e/api/copilotkit/pins.test.ts — rxjs pin assertion
    - .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md, 02-VALIDATION.md — phase-gate records
  key_links:
    - Advisory lock checked INSIDE agent.run() BEFORE executeChatTurn — but the runner lock fires FIRST (runner.run() throws before the agent runs), so same-thread AG-UI concurrency still surfaces as 200+empty-body while cross-path concurrency (board transition) surfaces as RUN_ERROR
    - The advisory check inspects `executions` rows with status IN ('running','waiting_user') for the conversation — a PREVIOUS run's 'completed'/'failed' rows never block
    - Workspace resolver SQL mirrors conversations.ts:64-76 (LEFT JOIN tasks → boards, chat_sessions; task_workspace_key ?? session_workspace_key ?? getDefaultWorkspaceKey())
    - `bun add rxjs@^7.8.2` is the phase's ONLY package install — audit table in RESEARCH.md §Package Legitimacy Audit (verdict OK/Approved, no postinstall) — no blocking human checkpoint required
---

<objective>
Production-readiness for `RailyinAgent` + the phase gate: the workspace-key resolver (3 branches, RUNR-03), the advisory cross-path run lock (RUNR-04), the unknown-conversation rejection, the explicit rxjs pin (HOST-03 continuation), and the full-suite + documentation close-out (02-COVERAGE.md no-external-API decision, 02-VALIDATION.md marked complete).

Purpose: close the two open research questions with safe defaults (workspace-key resolver — Open Question 3; advisory executions-row check — Open Question 2), make the rxjs import contract explicit (research Installation), and prove the phase end-to-end green before /gsd-verify-work.

Output: hardened agent with resolver + lock tests, pinned rxjs, and the phase-gate records.
</objective>

<execution_context>
@/Users/filipe.esch/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/filipe.esch/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-CONTEXT.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-RESEARCH.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-PATTERNS.md
@.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-VALIDATION.md
@.planning/phases/01-copilotruntime-hosting-thread-apis-spike/COVERAGE.md
@src/bun/copilotkit/railyin-agent.ts
@src/bun/copilotkit/railyin-agent.test.ts
@src/bun/handlers/conversations.ts
@e2e/api/copilotkit/pins.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden RailyinAgent — workspace-key resolver (3 branches), advisory cross-path lock, unknown-conversation rejection</name>
  <files>
    src/bun/copilotkit/railyin-agent.ts,
    src/bun/copilotkit/railyin-agent.test.ts
  </files>
  <read_first>
    - src/bun/handlers/conversations.ts — the workspace-key resolution SQL (lines 60-76: LEFT JOIN tasks → boards for task_workspace_key, LEFT JOIN chat_sessions for session_workspace_key, `?? getDefaultWorkspaceKey()`)
    - 02-RESEARCH.md §Open Questions 2 (advisory executions-row check, line 491) and 3 (workspace-key resolver, lines 493-495)
    - src/bun/copilotkit/railyin-agent.ts — the 02-01 agent (run() structure; the current `getDefaultWorkspaceKey()` placeholder at the executeChatTurn call)
    - src/bun/copilotkit/railyin-agent.test.ts — the fake-coordinator + real-DB test pattern from 02-01 Task 3
    - src/bun/test/helpers.ts — initDb/seedChatSession (lines 10-61, 409-434) for seeding a task-linked conversation
  </read_first>
  <behavior>
    - Test 1 (task-linked branch): a conversation with task_id → board.workspace_key wins (seed a board + task with a known workspace_key)
    - Test 2 (session branch): a standalone conversation (task_id NULL) with chat_sessions.workspace_key set → session key wins
    - Test 3 (default branch): a conversation with neither → getDefaultWorkspaceKey()
    - Test 4 (advisory lock): an executions row with status 'running' (or 'waiting_user') for the conversation → run() emits RUN_ERROR (code "THREAD_BUSY"-style) WITHOUT calling executeChatTurn; a 'completed' row does NOT block
    - Test 5 (unknown conversation): run on a conversation id with no row → RUN_ERROR, executeChatTurn never called
  </behavior>
  <action>
    Expand `src/bun/copilotkit/railyin-agent.ts` (the 02-01 file — extend, do not restructure; the runner lock still fires first for same-thread AG-UI concurrency, so this advisory check only catches cross-path cases):

    1. Extract a small module-level resolver `export function resolveWorkspaceKey(db: Database, conversationId: number): string | null` that returns the workspace key or null when the conversation does not exist — single SQL mirroring conversations.ts:64-76:
       `SELECT b.workspace_key AS task_workspace_key, cs.workspace_key AS session_workspace_key FROM conversations c LEFT JOIN tasks t ON t.conversation_id = c.id LEFT JOIN boards b ON b.id = t.board_id LEFT JOIN chat_sessions cs ON cs.conversation_id = c.id WHERE c.id = ?`; result → `row?.task_workspace_key ?? row?.session_workspace_key ?? getDefaultWorkspaceKey()` (import getDefaultWorkspaceKey from "../workspace-context"). A null result (no conversations row) distinguishes "unknown conversation" from "known with default key".
    2. In `run()`: replace the `getDefaultWorkspaceKey()` placeholder with the resolver — `resolveWorkspaceKey(db, conversationId)`; null → the RUN_STARTED + RUN_ERROR {code: "THREAD_NOT_FOUND"} path (already implemented in 02-01 — keep it; the resolver now feeds it).
    3. Add the advisory cross-path lock in `run()` BEFORE the executeChatTurn call: `db.query("SELECT 1 FROM executions WHERE conversation_id = ? AND status IN ('running','waiting_user')").get(conversationId)` — a hit → emit RUN_STARTED {threadId, runId, input} then RUN_ERROR {message: "Thread already has an active execution", code: "THREAD_BUSY"} then complete, WITHOUT calling executeChatTurn. NOTE the layering: the runner's "Thread already running" throw happens BEFORE the agent runs for same-thread AG-UI concurrency (e2e asserts 200+empty-body — unchanged); this advisory check only fires for cross-path concurrency (a board transition execution active on the same conversation, research Open Question 2). A 'completed'/'failed' previous run never blocks (status filter). Keep the check cheap — one indexed lookup, no policy machinery (queue-vs-reject policy stays reject for v1).
    4. Extend `railyin-agent.test.ts`: seed a real in-memory DB with (a) a board+task-linked conversation (use helpers seedChatSession + a raw INSERT for the board/task rows, or follow boards.test.ts seeding), (b) a standalone conversation with chat_sessions.workspace_key, (c) a conversation with neither. Drive run() with the fake coordinator; assert the workspaceKey passed to executeChatTurn for each branch. For Test 4, INSERT an executions row {conversation_id, status: 'running'} first; assert the fake coordinator's executeChatTurn was NEVER called and the stream ended in RUN_ERROR. For Test 5, use a conversation id with no row (e.g. 99999).
  </action>
  <verify>
    <automated>bun test src/bun/copilotkit/railyin-agent.test.ts && bun test e2e/api/copilotkit/railyin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/bun/copilotkit/railyin-agent.test.ts` passes the 3 resolver branches + advisory-lock + unknown-conversation tests
    - The e2e suite stays green — the advisory lock does NOT fire for sequential runs on the same conversation ('completed' rows) and the runner lock still yields 200+empty-body for same-thread concurrency
    - run() never calls executeChatTurn when the advisory check fires or the conversation is unknown
  </acceptance_criteria>
  <done>The agent resolves workspace keys per conversation (task → chat_sessions → default), rejects cross-path concurrent runs with a clear RUN_ERROR, and rejects unknown conversations — all unit-tested with no e2e regression.</done>
  <reversibility rating="reversible">The advisory lock is a policy knob — disabling it later is a one-line removal; the resolver is additive.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Pin rxjs as an explicit direct dependency</name>
  <files>
    package.json,
    bun.lock,
    e2e/api/copilotkit/pins.test.ts
  </files>
  <read_first>
    - 02-RESEARCH.md §Installation (lines 119-125: `bun add rxjs@^7.8.2`; hoisted 7.8.2 today, nested 7.8.1 in @ag-ui/client; explicit pin prevents dedupe surprises)
    - 02-RESEARCH.md §Package Legitimacy Audit (rxjs row: verdict OK/Approved, no postinstall script — the gate is satisfied; no blocking human checkpoint needed)
    - e2e/api/copilotkit/pins.test.ts — the exact-pin assertion pattern (lines 26-38)
  </read_first>
  <behavior>
    - Test: package.json dependencies contains `rxjs` at `^7.8.2` (pins.test.ts extension)
    - `bun run typecheck` still passes with the explicit dependency (the probe + agent already import from "rxjs")
  </behavior>
  <action>
    Run `bun add rxjs@^7.8.2` (installs as a direct dependency; already hoisted at 7.8.2 — this makes the pin explicit and prevents dedupe surprises per research). Then extend `e2e/api/copilotkit/pins.test.ts` with a fourth test asserting `pkg.dependencies["rxjs"]` equals `"^7.8.2"` (mirror the exact-string assertions at lines 28-34). Do NOT bump any other package.
  </action>
  <verify>
    <automated>bun test e2e/api/copilotkit/pins.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test e2e/api/copilotkit/pins.test.ts` passes with the new rxjs assertion
    - `bun run typecheck` clean; `bun test src/bun/copilotkit` still green (imports unchanged — explicit pin only)
  </acceptance_criteria>
  <done>rxjs is an explicit `^7.8.2` direct dependency asserted by the pins test — the audit gate (verdict OK/Approved) is recorded and no checkpoint was required.</done>
  <reversibility rating="reversible">Version-pin change — reverting is a one-line package.json edit.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suites green, COVERAGE.md decision, VALIDATION.md close-out</name>
  <files>
    .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md,
    .planning/phases/02-ag-ui-bridge-railyinagentrunner/02-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/01-copilotruntime-hosting-thread-apis-spike/COVERAGE.md — the no-external-API declaration format to mirror
    - 02-VALIDATION.md — the per-task verification map, Wave 0 checklist, and sign-off section to complete
  </read_first>
  <behavior>
    - Full backend suite green: `bun test src/bun --timeout 20000`
    - Full e2e suite green: `bun test e2e/api --timeout 30000` (includes Phase 1 probe + the new railyin.test.ts)
    - `bun run typecheck` clean
  </behavior>
  <action>
    1. Run the full phase verification: `bun test src/bun --timeout 20000 && bun test e2e/api --timeout 30000 && bun run typecheck`. Any failure: fix, re-run, and record in the SUMMARY.
    2. Create `02-COVERAGE.md` (api-coverage contribution): mirror Phase 1's decision record — "No external API integration" declaration with the same rationale table (network API consumed: none; remote credentials: none; third-party endpoints: none — `@ag-ui/*` + `@copilotkit/runtime` are in-process SDKs; the only HTTP surface is the app's own loopback origin serving /api/copilotkit/*, which is the deliverable under test, not an integration). Record the detector outcome `{"detected":false,"signals":[]}`.
     3. Complete `02-VALIDATION.md` (the draft already carries the corrected co-located paths — `src/bun/copilotkit/*.test.ts` + `src/bun/test/execution-seam.test.ts`, no `-x` flags, corrected quick-run dir, per-task plan attribution): fill/tick the per-task verification map (task IDs 02-01 T1-T3 / 02-02 T1-T3 / 02-03 T1-T3, waves 1/2/2, requirement refs, the automated commands from each plan's verify), check every Wave 0 item (all five test files now exist), record the open-question resolutions (RESEARCH.md Open Questions (RESOLVED): Q1 → 02-02 Task 2, Q2 → 02-03 Task 1, Q3 → 02-03 Task 1), set `wave_0_complete: true`, `nyquist_compliant: true`, and tick the sign-off checklist.
  </action>
  <verify>
    <automated>bun test src/bun --timeout 20000 && bun test e2e/api --timeout 30000 && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Full backend + e2e suites and typecheck all green in one pass
    - 02-COVERAGE.md exists with the no-external-API declaration mirroring Phase 1's format
    - 02-VALIDATION.md has `nyquist_compliant: true` and a completed verification map + Wave 0 checklist
  </acceptance_criteria>
  <done>The phase gate passes: full suites green, the COVERAGE.md decision is recorded, and VALIDATION.md is closed out — the phase is ready for /gsd-verify-work.</done>
  <reversibility rating="reversible">Docs-only task.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → executions/conversations tables | advisory lock reads execution status rows derived from client-triggered runs (internal DB boundary) |
| npm registry → project | `bun add rxjs@^7.8.2` — the phase's only package install (supply-chain boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | DoS | cross-path double execution: AG-UI chat run while a board transition is active on the same conversation | medium | mitigate | Advisory executions-row check (`status IN ('running','waiting_user')`) in agent.run() BEFORE executeChatTurn → RUN_ERROR "THREAD_BUSY"; unit-tested; same-thread AG-UI concurrency still handled by the inherited runner lock (200+empty-body e2e unchanged — layering verified in Task 1) |
| T-02-13 | Tampering | config tampering (engines.yaml/workspace YAML) steering engine selection | low | accept | Local single-user app, existing posture — config is developer-owned; the agent routes through EngineRegistry only (D-10), no new config surface in this phase |
| T-02-14 | (command injection) | engine tool calls executing shell commands | low | accept | Preexisting engine surface, UNCHANGED this phase — the bridge treats engine output as data and adds no execution path (research Security Domain: "bridge treats engine output as data, never re-executes it") |
| T-02-15 | Tampering | unknown conversation id reaching the executor | low | mitigate | Resolver returns null → RUN_ERROR THREAD_NOT_FOUND before executeChatTurn; unit-tested (Task 1 behavior 5) |
| T-02-SC | Tampering | rxjs npm install (supply chain) | low | mitigate | RESEARCH.md §Package Legitimacy Audit: rxjs verdict OK/Approved (10y old, ~98M/wk, no postinstall); explicit `^7.8.2` pin + pins.test.ts assertion — no blocking human checkpoint required for an Approved verdict (gate rules: checkpoints only for [ASSUMED]/[SUS]) |
</threat_model>

<verification>
- `bun test src/bun/copilotkit/railyin-agent.test.ts` — resolver branches + advisory lock + unknown conversation
- `bun test e2e/api/copilotkit/pins.test.ts` — rxjs pin
- `bun test src/bun --timeout 20000 && bun test e2e/api --timeout 30000 && bun run typecheck` — full phase gate
- 02-COVERAGE.md + 02-VALIDATION.md — records present and complete
</verification>

<success_criteria>
- RUNR-03 fully closed: thread mapping + workspace resolution hold for card conversations and standalone sessions (3-branch unit tests + e2e mapping)
- RUNR-04 fully closed: runner lock (same-thread) AND advisory lock (cross-path) both reject concurrent runs with clear signals
- The phase's five ROADMAP success criteria are covered by automated tests across plans 02-01..02-03
</success_criteria>

## Artifacts this phase produces

- **src/bun/copilotkit/railyin-agent.ts** — `resolveWorkspaceKey(db, conversationId)` (task → chat_sessions → default, mirroring conversations.ts:64-76); advisory cross-path lock (executions `running`/`waiting_user` → RUN_ERROR THREAD_BUSY); unknown-conversation → RUN_ERROR THREAD_NOT_FOUND
- **src/bun/copilotkit/railyin-agent.test.ts** — 3-branch resolver tests + advisory-lock + unknown-conversation tests (real in-memory DB)
- **package.json / bun.lock** — `rxjs: ^7.8.2` explicit direct dependency
- **e2e/api/copilotkit/pins.test.ts** — fourth pin assertion for rxjs
- **.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-COVERAGE.md** — no-external-API decision record (detector `{"detected":false}`)
- **.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-VALIDATION.md** — completed verification map, Wave 0 checklist, `nyquist_compliant: true`

<output>
Create `.planning/phases/02-ag-ui-bridge-railyinagentrunner/02-03-SUMMARY.md` when done.
</output>
