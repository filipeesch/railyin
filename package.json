{
  "name": "railyin",
  "version": "0.1.0",
  "type": "module",
  "description": "AI-assisted delivery orchestration for software teams",
  "scripts": {
    "dev": "vite build && electrobun dev --watch",
    "dev:debug": "vite build && electrobun dev --watch -- --debug",
    "dev:test": "vite build && electrobun dev --watch -- --debug --memory-db",
    "dev:kill": "pkill -f 'Railyn-dev' || true; pkill -f 'electrobun' || true",
    "dev:watch": "concurrently -n vite,electrobun -c cyan,magenta \"vite build --watch\" \"electrobun dev --watch\"",
    "build:canary": "vite build && electrobun build --env=canary",
    "test": "bun test src/bun/test --timeout 20000",
    "test:ui": "bun test src/ui-tests --timeout 120000",
    "test:ui:review": "bun test src/ui-tests/review-overlay.test.ts --timeout 120000",
    "test:ui:chat": "bun test src/ui-tests/chat.test.ts --timeout 120000",
    "test:ui:chat-ext": "bun test src/ui-tests/extended-chat.test.ts --timeout 120000",
    "test:ui:board": "bun test src/ui-tests/board.test.ts --timeout 120000",
    "refine:mock": "bun refinement/runner.ts --mode mock",
    "refine:local": "bun refinement/runner.ts --mode local",
    "refine:live": "bun refinement/runner.ts --mode live",
    "test:ui:run": "./scripts/run-ui-tests.sh"
  },
  "dependencies": {
    "@github/copilot-sdk": "^0.2.1",
    "@iconify/vue": "^5.0.0",
    "@monaco-editor/loader": "^1.4.0",
    "electrobun": "latest",
    "js-yaml": "^4.1.0",
    "marked": "^17.0.5"
  },
  "devDependencies": {
    "@primevue/themes": "^4.0.0",
    "@types/bun": "latest",
    "@types/js-yaml": "^4.0.9",
    "@types/marked": "^6.0.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "concurrently": "^9.1.2",
    "pinia": "^2.2.0",
    "primeicons": "^7.0.0",
    "primevue": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  }
}
